---
icon: lucide/list-tree
---

# Proposal Catalogue

The raw material behind the *From great to once in a lifetime* review: every proposal that
sixteen independent expert lenses made about this codebase, each grounded in the code with
file references, and each then put to three skeptics — one checking the premise against the
repository, one judging value and fit against the project's stated philosophy, one judging
feasibility against the installed LanceDB, FastMCP and PyArrow. A proposal survives when at
least two of the three did not refute it. The skeptics' corrections and sharpened versions
are folded into the synthesised document; this page is the index.

**Status:** 120 proposals, 63 survived, 2 refuted, 55 still under verification.
This page is regenerated as verification completes.

Effort is S/M/L/XL as the proposing lens estimated it; impact is the lens's own 1–5 rating,
before judging. Neither has been normalised across lenses yet.

## Developer experience and onboarding (devx)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `devx-1` | Corpus-free first search: `make fixture-db` and an in-process `ka-query` CLI that prints exactly what the model sees | S | 4 | survived 3/3, with corrections | The path from clone to 'I searched something and read the model-facing text' requires either network to a demo service or Dagger+Docker. The project's own 18-charter fixture — chosen to cover every documented trap — is … |
| `devx-2` | `make ci-local` and `make smoke-local`: the Dagger pipeline's checks and its MCP smoke test runnable with uv alone, Dagger kept for the … | M | 5 | survived 3/3, with corrections | The deepest test in the repository — 'the only place the index configuration is verified end to end' (ci.yml:34-38, testmcp.go:13-15) — cannot be run by a contributor without Dagger + a container runtime, yet 90% of … |
| `devx-3` | CI wall time from 2 m 09 s to under 75 s: a uv cache volume in the Dagger engine, four parallel jobs, pip-audit moved to the … | S | 4 | refuted 1/3 (Meticulous code reviewer; Maintainer of this exact project) | The lever for 'results in under two minutes' is not the tests (17 s) but the 67 s environment build repeated per call, and the serialisation of four independent calls. Dagger has cache volumes and GitHub Actions has job … |
| `devx-4` | Session-scoped fixture table with a lance-version guard, and `make watch`: the suite from 7.1 s to under 4 s | S | 3 | survived 3/3, with corrections | Half the inner-loop latency is redundant ingest of an immutable table. A 3–4 s suite with a file watcher is a different working style from a 7 s suite run by hand — and this project's tests are the kind you want running … |
| `devx-5` | The LLM-facing surface as a versioned, snapshot-tested artifact with a token budget, and the tool docs generated from it | M | 4 | survived 3/3, with corrections | The most consequential text in the repository — what steers the model toward `bref` not `brev`, `Tallinn` not `Reval`, `fuzzy=1` on a base form — has no diff, no snapshot, no budget and a hand-copied twin that drifts. … |
| `devx-6` | A measured-claims ledger and decision records: every number in prose names the script that produced it, and every 'why' lives in the tree | L | 5 | survived 3/3, with corrections | The project's defining virtue — 'every design decision measured on the corpus and written down next to the code' — has no mechanism: the measurements cannot be re-run, the citations cannot be checked, and the *why* … |
| `devx-7` | Hermetic boot and `make doctor`: switch off FastMCP's PyPI call at every start, pin the tool versions the repo actually runs, and diagnose … | S | 3 | survived 3/3, with corrections | A newcomer's first boot phones home, and the toolchain the project 'runs on' is whatever the day's uv/ruff/ty/inspector resolve to. There is also no single command that answers 'why does my server say no table' — the … |
| `devx-8` | An executable 'add a corpus' tutorial: voudintilit as the worked example, with a 20-row fixture, a stopwatch, and the docs pulling code … | XL | 5 | survived 3/3, with corrections | Everything needed to port a corpus exists as reusable functions plus prose; nothing proves the port is a week, and a second contributor would rediscover the `df` traps (year 0, no end year, unaccented labels, apparatus) … |

## Documentation and knowledge design (docs)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `docs-1` | corpus-facts.json: one measured source for every number, rendered into docs, README and tool descriptions, with a numeral linter that fails … | L | 5 | survived 3/3, with corrections | Two audits corrected numbers by reading; the third drift instance (141,032 vs 141,118) and the fourth (277 in sample output) were found here by arithmetic, not reading. Nothing in the repository knows what the numbers … |
| `docs-2` | Executable examples: every tool call shown in prose runs against the fixture in CI, sample outputs are regenerated not retyped, and the … | M | 4 | survived 3/3, with corrections | Examples are the part of documentation a model and a reader copy verbatim, and they are the one part with zero test coverage. A parameter rename, a changed error sentence or a re-harvest that drops a DF number would … |
| `docs-3` | One structured guidance source rendered into the LLM instructions, the tool descriptions, README 'Tools', the landing page and docs/tools — … | L | 4 | survived 2/3, with corrections | Six renderings of one body of knowledge drift independently (the audits found the fuzzy default wrong in two of them and the compound count differing between two others), and the LLM-facing text has no budget, no … |
| `docs-4` | Decision records with their measurement scripts, and a corpus changelog generated per harvest | L | 4 | survived 3/3, with corrections | A reader in 2036 cannot answer 'why is fuzzy off' or 'why is the Debian base kept' without grep and luck, cannot re-run the experiment that decided it, and cannot tell which snapshot a number was measured on. The 'next … |
| `docs-5` | Pages for the audiences that have none: 'How to cite' (with CITATION.cff and snapshot provenance), a Finnish/Swedish glossary generated … | S | 3 | survived 3/3, with corrections | Three of the four audiences have no path; the model-facing glossary is invisible to humans; the project's most original security argument (text returned to a model is an injection surface for fabricated citations) is … |
| `docs-6` | A one-page writing guide that codifies the voice, plus three mechanical prose rules enforced in `checks` | S | 3 | survived 3/3, with corrections | A contributor — or the sibling projects porting text back — has nothing to follow but taste, and the reviewer has nothing to point at. The mechanical half of the voice (numerals, percentages, dashes, docstring mirrors) … |
| `docs-7` | Serve the knowledge to the model as MCP resources and a prompt — facts, glossary, snapshot provenance, citation — instead of only in the … | M | 3 | survived 2/3, with corrections | A model that wants the full label vocabulary, the snapshot date to cite, or the glossary has nowhere to read it, and the only lever the maintainers have is to lengthen the description — which is what the last three docs … |

## Ecosystem and federation across the Nordic archive MCP family (ecosystem)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `ecosystem-1` | archive-mcp-core: one framework that Kansallisarkisto, Riksarkivet and the next archive instantiate from a declarative CorpusSpec | XL | 5 | pending (0/3 verdicts in) | Every fix is made three times (the comment 'ra-mcp and the sibling likely have it too' is the symptom), voudintilit and tuomiokirjat each need a fresh copy of df_tool.py, and there is no way to say 'this server conforms … |
| `ecosystem-2` | nordic-archives router: one MCP endpoint that searches Diplomatarium Fennicum and SDHK together with provenance-tagged, … | L | 5 | pending (0/3 verdicts in) | No federated search exists at the MCP level; a model must know both servers, translate vocabularies itself, and has no way to learn that two hits are the same charter or that one archive was down. |
| `ecosystem-3` | One query grammar for the family: ra-mcp's inline syntax silently returns zero on this engine today | M | 4 | pending (0/3 verdicts in) | The family has two incompatible keyword dialects and no translation layer, so a federated query cannot send one string to both engines, and cross-training between servers actively harms recall. |
| `ecosystem-4` | Stamp the snapshot's provenance into the table and publish a machine-readable server card at /.well-known/archive-mcp.json | S | 4 | pending (0/3 verdicts in) | Snapshot identity and family contract are documented in prose only; neither a person nor a program can read them from a running server. |
| `ecosystem-5` | A family-wide telemetry attribute namespace and research_context parity with ra-mcp | S | 3 | pending (0/3 verdicts in) | Telemetry is per-server bespoke; the family has no shared vocabulary for spans, metrics or research intent. |
| `ecosystem-6` | A DF↔SDHK concordance: cross-archive identifiers for charters edited in both national editions | L | 4 | pending (0/3 verdicts in) | Nothing links the two editions, so a historian gets two citations for one document, two datings that disagree, and no signal that they disagree. |
| `ecosystem-7` | A shared conformance test kit so every server in the family proves the same search invariants | M | 4 | pending (0/3 verdicts in) | The most valuable tests in the family are trapped in one repo and coupled to one corpus's terms; conformance is asserted by copying prose ('same shape as ra-mcp'), not by running the same tests. |
| `ecosystem-8` | A family error taxonomy, readiness contract and settings convention, extracted from _answer before it is copied a third time | S | 3 | pending (0/3 verdicts in) | Error disclosure, span error classes, readiness shape and settings prefixes are conventions that live in one function and one tuple, are documented as 'same as ra-mcp' by assertion, and will drift on the next port. |

## Information retrieval science and search quality (ir)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `ir-1` | A relevance test collection and regression gate built from simulated known-item topics, variant clusters and facet topics — no historians … | XL | 5 | survived 3/3, with corrections | Every retrieval decision in this project — stop words kept, fuzzy off, stemming on, concatenated field — was measured once by hand and cannot be re-measured automatically. Proposals ir-2, ir-3, ir-6 and ir-7 all change … |
| `ir-2` | Dissolve the stemming-vs-fuzzy exclusivity with a per-term BooleanQuery(SHOULD stemmed, SHOULD fuzzy) — fuzzy=1 stops costing stemming | S | 5 | survived 3/3, with corrections | The exclusivity is a property of a single MatchQuery, not of the engine. lancedb 0.38 exposes BooleanQuery with Occur.SHOULD/MUST (verified: lancedb.query.BooleanQuery, Occur). Measured on the fixture: … |
| `ir-3` | Index-time historical-spelling normalisation (a parallel `searchable_norm` column) plus corpus-derived variant clusters that explain … | L | 5 | survived 3/3, with corrections | Sixty years of historical-IR practice (Koolen et al. 2006 for 17th-century Dutch; Ernst-Gerlach & Fuhr 2006; Pettersson, Megyesi & Nivre 2013 — specifically early-modern Swedish court records, the tuomiokirjat genre; … |
| `ir-4` | Best-passage snippets with highlighted matches and a per-hit 'why it matched' line, computed with the index's own analyser | M | 4 | survived 3/3, with corrections | A language model reading 25 hits must decide which to open with df_get_charter; today it decides on an opening formula that is nearly identical across medieval charters ('Vi Magnus, met Gudhz nadh…', 'Vniuersis … |
| `ir-5` | Count and page on a _rowid+_score ranked pass, hydrate only the page with take_row_ids — then raise the 10,000 cap | S | 4 | survived 3/3, with corrections | Measured on a 54,000-row table built from the fixture: a 10k-hit query costs 100-112 ms and 7-12 MB of Python dicts today; `select(['_score']).with_row_id(True)…to_arrow()` costs 8.6-10.3 ms, and hydrating a 25-row page … |
| `ir-6` | Split the concatenated index into transcript and catalogue fields with boosts, so BM25 length normalisation stops putting the 36% … | M | 4 | survived 3/3, with corrections | Every place-, language- or index-term keyword the project teaches the model to use is answered first by the records that carry no text, because the concatenation makes them the shortest documents in the collection. That … |
| `ir-7` | Per-language analysis for a corpus that is 42% not Swedish: a Latin-stemmed column and a German-stemmed column routed by the `language` … | M | 3 | survived 3/3, with corrections | 'Best single choice' is the wrong frame when the language of every row is known: analysis can be routed. tantivy ships a German stemmer (language='German') but no Latin one; Latin needs an ingest-time stemmer — … |

## LLM tool design and MCP ergonomics (llmtool)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `llmtool-1` | A historian-question evaluation harness that scores tool-call trajectories, not just hits | XL | 5 | survived 3/3, with corrections | Retrieval quality is proven (test_retrieval_quality.py), tool-use quality is asserted. There is no number for: how often a model reaches the right charter, how many calls it wastes, how often it passes a filter value … |
| `llmtool-2` | Return structuredContent and an outputSchema beside the text, and set isError on error replies | M | 4 | survived 3/3, with corrections | A model reading 25 hits has to re-extract DF numbers, years and URLs from markdown, which is where citation errors come from; a client cannot render hits, cannot build a 'open charter' link, and cannot distinguish an … |
| `llmtool-3` | Keyword-in-context snippets with a page-level token budget instead of a 400-character transcript prefix | S | 4 | survived 3/3, with corrections | The model is asked to judge relevance from text that frequently does not contain the word it searched for, so it either calls df_get_charter on every hit (wasted calls, ~500 tokens each) or judges on the wrong evidence. … |
| `llmtool-4` | Facets in the search envelope, computed from the match set the engine already materialises | M | 4 | survived 3/3, with corrections | A historian's first question of a result set is its shape ('when and where are these, in which language, how many are transcribed'), and the model cannot answer it without paging through up to 10,000 hits at ~120 tokens … |
| `llmtool-5` | A df_spellings tool backed by an ingest-time vocabulary table, so spelling variation becomes a lookup instead of a guess | L | 4 | survived 3/3, with corrections | The model is asked to solve a lexicographic problem (what forms does this word take in this corpus) with a probabilistic hammer (edit distance on the whole query) and a warning. A historian would consult a glossary; the … |
| `llmtool-6` | Put every number in the tool descriptions under test, move the essay into a readable resource, and measure the diet with the harness | M | 3 | survived 3/3, with corrections | The descriptions are the project's most valuable prose and also its least tested; a re-harvest that moves any count silently makes the model's guidance wrong. And their length is a cost paid on every session with no … |
| `llmtool-7` | A df://charter/{n} resource template, a cite_charter prompt, and a batch df_get_charters so a client can attach a charter instead of a tool … | S | 3 | survived 3/3, with corrections | A charter is a document, not an action; MCP models documents as resources so a client can attach 'DF 1031' to the context by reference (Claude Code's @-mention, Claude Desktop's resource picker) without the model … |
| `llmtool-8` | Model-facing contract tests: executable examples, error messages that name the next action, and a missing-table message written for the … | S | 3 | survived 3/3, with corrections | The project proves its prose to a human reader (audits, docs) and its code to pytest, but nothing proves the sentences the model reads are executable, actionable and addressed to the right party. That is the last … |

## Observability and operations (ops)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `ops-1` | Make the RED histograms able to answer a latency SLI, and count the error strings the spans hide | S | 4 | pending (0/3 verdicts in) | The project's own claim (dataset.py:76-78: 'an error-rate panel and a latency percentile both work') is false for latency under the SDK it ships with, and half-true for errors: the class of failure this server was … |
| `ops-2` | SLOs, dashboards and burn-rate alerts shipped as code, with a test that every metric they name exists | M | 4 | pending (0/3 verdicts in) | An operator can see spans and four series but has no statement of what 'good' is for this service, no page to look at, nothing that pages, and no way to know a metric rename in dataset.py broke a dashboard until someone … |
| `ops-3` | 'Degraded' as a first-class state: a query deadline, an in-flight gate, and a readiness body that carries table identity and evicts a dead … | L | 5 | pending (0/3 verdicts in) | The server has exactly two states, up and missing-table. Slow object storage, a saturated worker pool, a table swapped underneath it, and a remount are all reported as either 'ready' or an internal error, to the … |
| `ops-4` | Trace ids on every stderr line, one canonical log line per tools/call, and an instance id that gives stdio sessions an identity | S | 3 | pending (0/3 verdicts in) | A log line and a span about the same failure cannot be joined by an operator reading container logs; there is no per-call record that says tool, outcome, duration, hit count and page in one greppable line; and stdio — … |
| `ops-5` | A Helm chart derived from settings.py, with probe timings, a PDB, and memory limits computed from the measured per-search cost | XL | 4 | pending (0/3 verdicts in) | The stated bar is 'an operator runs it without reading source'. Today the operator has to derive probe timings from prose, guess memory, and rediscover the uid trap on a PVC. |
| `ops-6` | Runbooks with an automated `doctor`: turn lance's one error message into six distinguishable verdicts | M | 4 | pending (0/3 verdicts in) | There are no runbooks, and the one automated diagnosis the server has is wrong for the second-most-likely failure. Nothing measures how many versions a re-ingest leaves behind on a 19 GB table, and nothing tells an … |
| `ops-7` | A capacity model and a load-test gate in Dagger, so a lancedb bump that Dependabot auto-merges cannot silently double the cost of a search | M | 3 | pending (0/3 verdicts in) | The project 'scales to 7.8M pages without changing shape' is asserted; the per-search cost that decides pod memory and concurrency has never been written down, and a regression in the engine would reach main unnoticed. |

## Open-source craft and community (oss)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `oss-1` | The server advertises fastmcp's version as its own: wire serverInfo.version to the package version | S | 4 | pending (0/3 verdicts in) | When v0.1.0 is released, every MCP client, registry listing and bug report will identify the server as 3.4.4; a dependabot bump of fastmcp will silently 'release' a new server version to every client while the tool … |
| `oss-2` | Snapshot the wire-level tool contract and make 'breaking' a tested, documented property of an MCP release | M | 5 | pending (0/3 verdicts in) | For an MCP server the tool schema and the text format are the API. A rename of `issuingplace`, a change of `le=MAX_LIMIT` to 50, a flipped `match_all` default, or a new result header line breaks every client prompt and … |
| `oss-3` | Release the data too: a versioned, manifest-carrying LanceDB table published beside the image, with the server reporting code and data … | L | 5 | pending (0/3 verdicts in) | A user who runs `docker run riksarkivet/kansallisarkisto-mcp:v0.1.0` has a signed, attested, SBOM'd image and nothing to search. 'Scales without changing shape' and 'the release is verifiable end to end' both stop at … |
| `oss-4` | Design the first release as a verifiable event: in-tree CHANGELOG on the site, OCI labels, a post-publish verifier, and badges that link to … | M | 4 | pending (0/3 verdicts in) | The pipeline is more elaborate than most released projects', and nothing has ever exercised it end to end — the first tag push is the first run of the provenance, release-asset and signing steps against a real registry. … |
| `oss-5` | Community files shaped by this corpus: an 'unfindable document' issue template that feeds test_retrieval_quality.py, a CONTRIBUTING that … | S | 3 | pending (0/3 verdicts in) | The project's best mechanism — a researcher notices a charter they cannot find, and it becomes a pinned test — has no intake. A historian who searches `Hundæbæth` and gets nothing has nowhere to report it that would … |
| `oss-6` | Make Kansallisarkisto a co-owner by structure: CODEOWNERS over the material-facing files, a vocabulary file only the archive can bless, a … | XL | 5 | pending (0/3 verdicts in) | Everything about ownership is Riksarkivet's and everything about authority over the content is Kansallisarkisto's, and the repository has no structure through which the second can act: no reviewable artefact for the … |
| `oss-7` | Adoption paths that fit 'stdio on a laptop': PyPI with trusted publishing so `uvx kansallisarkisto-mcp` works, a registry server.json, and … | M | 4 | pending (0/3 verdicts in) | The cheapest path — `claude mcp add kansallisarkisto -- uvx kansallisarkisto-mcp` — is impossible without a PyPI release, and would fail confusingly with one because of the `/data` resolution. There is no registry entry … |

## Performance engineering (perf)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `perf-1` | One ranking, twenty-five rows: rank on _rowid+_score, page with take_row_ids | M | 5 | pending (0/3 verdicts in) | A capped query costs ~100 ms and 5 MB of Arrow plus ~50 MB of transient Python objects to learn a total of '10000+' and return 25 rows; 2/3 of the time is GIL-held dict building that also serialises every concurrent … |
| `perf-2` | Answer `initialize` before lancedb is imported: lazy engine imports and a background boot probe, pinned by a test | S | 4 | pending (0/3 verdicts in) | A laptop stdio client waits ~1.7 s for the handshake, and the design comment 'stdio on a laptop must cost nothing' is not measured anywhere. The lazy connection in `get_lancedb` is a good idea that a later import … |
| `perf-3` | A benchmark suite CI gates on: replicated fixture at a scale knob, p50/p99 per tool through the in-memory client, RSS, import time, ingest … | L | 5 | pending (0/3 verdicts in) | The project's method is 'measure, then write the number next to the code' — but the numbers are frozen prose. A regression in any of them (a lancedb bump changing `to_list` cost, a projection change, a new eager import) … |
| `perf-4` | The FTS index build is the one non-streaming step: bound it, measure it per row, and ship pre-built tables so operators never build | L | 4 | pending (0/3 verdicts in) | The project promises the three corpora share one shape; the ingest path is fine for df and voudintilit but has an unmeasured, unbounded memory step that would fail on the corpus that is 99% of the pages, and nothing … |
| `perf-5` | Measure the threadpool ceiling and the GIL ping-pong: bound workers, shrink the switch interval, and record why the async API is not the … | S | 3 | pending (0/3 verdicts in) | The sync-in-threadpool decision is right but unmeasured, the per-process ceiling (~13 q/s today, ~110 after perf-1 on 4 cores) is unknown to operators, 40 in-flight queries can each hold 5-25 MB of Arrow, and any busy … |
| `perf-6` | Ingest leaves every previous version and index on disk: cleanup and compaction at the end of ingest_df | S | 2 | pending (0/3 verdicts in) | The harvest is designed to be re-run (scripts/harvest.py is resumable; audit finding 6 assumes re-ingest), but every re-run silently keeps the previous corpus on disk, and the docs never say to clean up. |
| `perf-7` | A corpus-shaped synthetic generator so the 7.8M-row shape is measured nightly rather than asserted | XL | 4 | pending (0/3 verdicts in) | The project's central scaling claim rests on extrapolation from a corpus 1,000x smaller, with the cost curves that matter most (fuzzy, LIKE, common Swedish function words that the stop-word decision keeps searchable) … |

## Resilience and failure modes (resilience)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `resilience-1` | A real per-call deadline: lance's native query timeout, because FastMCP's timeout= is a silent no-op for these tools | M | 5 | pending (0/3 verdicts in) | No query can be bounded in time. A model retrying fuzzy=2 on a long keyword against 7.8M tuomiokirjat pages holds a worker thread for as long as lance takes; a wedged S3/NFS read holds it forever; the boot probe … |
| `resilience-2` | Two-phase search: rank 10k row ids, fetch only the page — a bytes budget per call that removes the OOM and the GIL serialisation | L | 5 | pending (0/3 verdicts in) | The 10k materialisation is the single scaling hazard: per-call bytes and GIL time are proportional to row width, not page size, so tuomiokirjat (2.5 KB pages) is ~10× df's cost per call and 40 concurrent calls can … |
| `resilience-3` | A failure taxonomy and a degraded-state machine: classify lance errors, tell the model whether to retry, flip /ready, and fast-fail through … | L | 5 | pending (0/3 verdicts in) | Neither the model nor an orchestrator can distinguish 'retry' from 'stop, the archive is down' from 'narrow your query'. A model that gets `internal RuntimeError` on a permanently unreadable table will retry with … |
| `resilience-4` | Admission control: a small in-process search gate with a queue wait, so a hammering model gets 'busy' text instead of taking the server down | S | 4 | pending (0/3 verdicts in) | Under load the server degrades for everyone at once (latency, then OOM), and a well-behaved second user pays for a badly-behaved first. There is no signal — to the model, to /ready, or to a metric — that the server is … |
| `resilience-5` | A bulletproof harvest: pin the plan, verify every slice against its own filter, write atomic parts, and make coverage arithmetic honest | XL | 4 | pending (0/3 verdicts in) | Resume is unsafe against the one thing that always happens (the index moving), the report can claim completeness it does not have, and the only defence — `--verify` on objectID uniqueness — is blind to a short or … |
| `resilience-6` | Prove the swap: pin lance's atomic commit under disk-full, ship a blue/green table swap, and stop unbounded version retention on re-ingest | S | 3 | pending (0/3 verdicts in) | The safe deployment sequence exists by accident and is undocumented, and the unsafe one (`make ingest-df` into the served directory, repeatedly) grows disk without bound. Nothing tests either. |
| `resilience-7` | Keep the telemetry stack's own failures out of the application's error signal | S | 3 | pending (0/3 verdicts in) | With the collector down, the error-rate panel shows ~0.5 errors/s from a healthy server, indistinguishable from real faults, and the log bridge amplifies its own failure. The one time an operator most needs the error … |
| `resilience-8` | Fault injection as a test discipline: a `faults` fixture family that builds each failure from the 18-row fixture and asserts the documented … | M | 4 | pending (0/3 verdicts in) | Resilience claims in this repository are prose (deployment.md's ownership paragraph, tools.py's 'never raises' docstrings) verified by hand once; the retrieval-quality suite showed what pinning a class of failure buys, … |

## Scale engineering for 7.8M pages (key: scale)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `scale-1` | Rank by row id, take only the page: same ranked set, 1/15th of the cost | S | 5 | survived 3/3, with corrections | Every search pays for 9,975 rows it throws away. At 7.8M court pages (~2.5 KB each) a common term means ~25 MB of transcript through Arrow->dict per call, ~120 MB RSS per in-flight query, and on object storage 60+ range … |
| `scale-2` | Honest totals above 10,000: a counting phase that only runs when the ranked window saturates | M | 4 | survived 3/3, with corrections | On 7.8M court pages 'och', 'kyrkia', 'ting' each hit hundreds of thousands to millions of pages; 'sochn' hits tens of thousands. All of them will read `10000+` to the model, which cannot tell a 12,000-hit term from a … |
| `scale-3` | A resumable, staged, validated ingest for a 19 GB export | L | 5 | survived 3/3, with corrections | Extrapolated to tuomiokirjat (~2.75B tokens): ~9 min parse, tens of minutes of lance write, 20-40 min FTS build on 4 cores at unknown memory, ~6 GB of index with positions. A crash at minute 50 restarts from zero. … |
| `scale-4` | Release a table like a build: staged name, validated, tagged, pinned — no silent zero-hit window | M | 5 | survived 3/3, with corrections | A re-harvest of tuomiokirjat re-ingested in place is a 30+ minute outage in which every search says 'No results' — the one failure the project's 'boots without data, tools never raise' philosophy cannot see, because … |
| `scale-5` | Make `s3://` true: one table handle, a sized Session, prewarmed indexes, and a request budget per query | M | 4 | survived 3/3, with corrections | On S3 at ~20-40 ms per request, today's query floor is ~3-4 sequential metadata round trips plus 60+ data requests before scoring — 0.5-2 s per search warm, and each cold term also fetches its posting block from a ~6 GB … |
| `scale-6` | A capacity model the repository keeps true: synthetic corpus generator, benchmark harness, nightly numbers | L | 4 | survived 3/3, with corrections | The honest capacity model today is 'unknown above 6,876 rows'. The 7.8M-page claim in the module docstring (dataset.py:1-6, SearchResult comment :101-103) is architectural intent with no number behind it, and any … |
| `scale-7` | Replace the LIKE prefilter with an indexed vocabulary lookup before the corpus makes it a full scan | S | 3 | survived 3/3, with corrections | The column scan is linear in rows: ~3.7 s per query at 7.8M pages for the equivalent filter (court, parish, archive collection in tuomiokirjat), on every page of every filtered search, and on S3 it downloads the whole … |

## Software architecture and the multi-corpus abstraction (arch)

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `arch-1` | A corpus is a declarative CorpusSpec; ingest, search, indexes, tool signatures and the invariant suite are derived from it | XL | 5 | survived 2/2, with corrections | The next corpus is not 'a week's work'; it is a re-implementation of df with different nouns, and every one of the four copies of the filter list can drift independently (the index plan and the predicate builder already … |
| `arch-2` | Corpus facts are computed at ingest into a sibling `<table>__facts` table, interpolated into tool descriptions at boot, and every number in … | L | 5 | survived 2/2, with corrections | The project's best property (measured numbers beside the code) is also its rot vector: the numbers cannot be checked, drift has already happened (27/28, 16/18 in the first audit), and the MCP layer cannot say what is … |
| `arch-3` | One error taxonomy in the lib (CorpusInput, CorpusUnavailable, everything else) and one answer() envelope; the missing-table concept moves … | M | 4 | survived 2/2, with corrections | Every new corpus and every new caller-fixable condition must be threaded through three files and two channels, and the lib's public contract for a missing table is a leaked lancedb message. |
| `arch-4` | The result envelope carries corpus identity and a per-record Citation, and tools return structured content beside the text | S | 4 | survived 2/2, with corrections | A historian's trust rests on citations, and the citation is the one thing the server does not return in machine-checkable form; a client with structuredContent support gets nothing to verify against. |
| `arch-5` | Package layout for three corpora and two sibling archives: an archive-agnostic `archive-mcp-kit`, a `kansallisarkisto-corpora` package of … | XL | 5 | refuted 1/2 (Project maintainer) | Sharing happens by copy-and-annotate. A fix in one archive reaches the others only if someone remembers the docstring, and the SBOM/pip-audit/Trivy machinery attests three divergent copies of the same 700 lines. |
| `arch-6` | Settings own URI resolution once, and one Catalog answers 'present / readable / rows' for boot log, readiness and the facades | S | 3 | survived 2/2, with corrections | The seam between settings and resolution is a property that re-runs a directory walk, and the 'is the corpus there' question has three implementations with three answers. |
| `arch-7` | Keep sync tools in the threadpool, but bound per-corpus search concurrency below the 40-wide anyio limiter using facts-derived row cost | M | 4 | pending (0/3 verdicts in) | The right per-call design (sync in a worker) has no per-process budget, so the first hosted deployment of tuomiokirjat turns 40 concurrent saturated searches into a multi-GB spike and an 'internal MemoryError' text … |
| `arch-8` | The invariant and retrieval-quality suites run for every corpus from its spec and fixture, so the second corpus is tested on day one | S | 4 | pending (0/3 verdicts in) | The project's stated bar is 'retrieval quality proven rather than asserted'; that proof is currently a df-only artefact and would not transfer. |

## philology

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `philology-1` | The ordinal sign º (U+00BA) is a letter to the tokeniser: every Roman-numeral date is an unfindable token, and the test that guards it … | S | 4 | survived 3/3, with corrections | A historian who types the year the way the charter writes it (`mcccxl`, `anno mccccº`) gets zero. Dating-by-Roman-numeral is the single most common way a medieval Swedish/Latin charter states its own date, and it is … |
| `philology-2` | Tell the model what kind of text it is reading: DF transcripts are edition text, not HTR, and `language` is the language of the original, … | S | 3 | survived 3/3, with corrections | Both statements steer a historian wrong in opposite directions. Calling an edition 'HTR' makes the model over-hedge quotations from a text that is, for scholarly purposes, citable as FMU; and a user asking 'give me the … |
| `philology-3` | A graphemic-normalisation index column, so orthographic variance is handled at index time without giving up stemming | L | 5 | survived 3/3, with corrections | The corpus's largest recall problem (bref/breff overlap 71 of 2,104) is today solved only by a mechanism that breaks the second-largest one (inflection), so the model has to choose which half of the material to lose on … |
| `philology-4` | An onomasticon and a Finnish gazetteer, applied as BooleanQuery variant expansion — Reval finds Reffla, Revel and Tallinn; Karl finds Karulj | XL | 5 | survived 3/3, with corrections | A historian's first query is a person or a place, and in this corpus both appear in three onomastic systems at once — Latin (Karolus/Karulj/Karoli, Aboensis, Revalia), vernacular Swedish/Low German (Karl/Karll, … |
| `philology-5` | A chronology layer: parse the charter's own dating clause, resolve feast days and weekdays by computus, and flag where it disagrees with … | L | 4 | survived 2/3, with corrections | For a medievalist the day matters — itineraries, sequences of letters in a negotiation, which came first — and 'by 1447'/'1443–1445' style intervals are only the cataloguer's floor. The corpus contains the information; … |
| `philology-6` | Make every answer citable: snapshot identity in the table, a provenance footer on every charter, and a clean separation of edition text … | M | 4 | survived 3/3, with corrections | A scholar citing from this server needs: the edition (DF n = FMU n / REA n), the text status (edition, philology-2), the snapshot date of the derived copy, and a statement that the quoted text is verbatim edition text … |
| `philology-7` | A DF↔SDHK concordance column: the Swedish sibling already holds the same charters with day-precise dates and modern regests | M | 4 | survived 2/3, with corrections | Two national editions of one body of material, each cited by its own community, with no machine-readable concordance; a historian working across the Gulf of Bothnia does the matching by hand. And the regest is the … |
| `philology-8` | Model the next two corpora as declared CorpusSpecs whose domain traps generate the tool descriptions — so voudintilit and tuomiokirjat … | XL | 5 | survived 3/3, with corrections | The bailiff accounts are tabular HTR: each page is a list of farms/persons with sums in mark/öre/penning under fiscal years, with the abbreviations `mk`, `öre`, `pn`, `t:na` (tunna), `sp:r`, and the unit of citation is … |

## repro — Reproducibility and data provenance

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `repro-1` | Dataset identity: a snapshot manifest born at harvest, carried in the LanceDB schema metadata, surfaced in /ready, in every tool reply and … | L | 5 | survived 3/3, with corrections | A running server cannot say which harvest it serves. A historian citing 'DF 2457 via kansallisarkisto-mcp' cannot say when the snapshot was taken or whether it covered 100% or 98.25%; an operator with two /data … |
| `repro-2` | An order-independent corpus digest and `harvest.py --diff`: make a re-harvest a first-class event with a changelog of records added, … | S | 4 | survived 3/3, with corrections | The project knows the index moves and cannot say how. 'Re-harvesting df reproduces the stored snapshot record for record' (data-sources.md:21) is a claim with no mechanism behind it. Kansallisarkisto re-recognising 500 … |
| `repro-3` | `verify-table`: prove the built table is the harvest — semantic determinism checked, the ingest's own history pruned, the storage format … | M | 4 | survived 3/3, with corrections | 'Same JSONL → same table' is believed, not proven, and there is no command an operator can run against /data to learn whether the mounted table is complete, indexed as the code assumes, and derived from the manifest it … |
| `repro-4` | A snapshot facts ledger: stop burning July-2026 corpus statistics into tool descriptions, docs and the formatter | S | 3 | survived 3/3, with corrections | The descriptions are the project's best asset — the model relies on them to choose filters — and they are a hand-copied snapshot of a snapshot. The moment the data is re-harvested, the code lies to the model with … |
| `repro-5` | Surface the 1.75% at query time: a shortfall table beside the corpus, and a coverage note only when a search overlaps a cell the harvest … | M | 4 | survived 3/3, with corrections | A user searching tuomiokirjat for Viborg 1890s would get a confident total from a corpus that silently lacks up to 128,297 Vyborg town-court pages. The project's own philosophy — say what is missing rather than return a … |
| `repro-6` | Versioned data releases: a signed OCI data artifact the image can pin by digest, a DOI'd snapshot descriptor, and a licence statement that … | XL | 5 | survived 3/3, with corrections | An operator who pulls `riksarkivet/kansallisarkisto-mcp:v0.2.0` must still harvest and ingest themselves, and two operators will serve two different snapshots under the same image tag. A reviewer of a paper cannot … |
| `repro-7` | A drift sentinel: harvest df weekly in CI, compare its digest with the released descriptor, and turn 'the index moves' into a time series … | M | 3 | survived 3/3, with corrections | The most consequential external dependency of this server — the archive's own edits — is monitored by nobody. A model quoting a transcript that Kansallisarkisto corrected last month is the failure the tool descriptions … |

## scholar

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `scholar-1` | A provenance envelope on every reply: snapshot manifest in the table, effective query and edition URL in the text | M | 5 | survived 3/3, with corrections | A historian cannot cite an answer built on this server: there is no snapshot identity, no record of what was searched, and the link to the archive's own page is not in the reply. The text is 'verbatim edition text as … |
| `scholar-2` | Keyword-in-context snippets, 'matched in' attribution, and an explicit text-status per hit | M | 5 | survived 3/3, with corrections | A quarter of transcribed hits show the model a passage that does not contain what it searched for, so it either over-claims from the opening lines or calls df_get_charter for everything. Untranscribed hits invite the … |
| `scholar-3` | df_facets and a computed corpus profile: histograms per decade/place/language over the already-materialised match set, and descriptions … | L | 5 | survived 3/3, with corrections | Researchers ask distributional questions (per decade, per place, per issuer type, transcribed vs not) and the server can only answer with page after page of hits. And the numbers the model is taught are typed, not … |
| `scholar-4` | Reading order: order='year'\|'df'\|'relevance' on df_search, plus df_browse over a DF-number range | S | 4 | survived 3/3, with corrections | A historian reads a result set chronologically and reads a charter with its neighbours; the model can do neither without hundreds of calls. Because the full ranked set is already materialised, chronological paging is a … |
| `scholar-5` | Snapshot diff: keep the previous harvest and report what changed since it | S | 3 | survived 2/3, with corrections | 'What changed since the last harvest' — added, removed, re-transcribed, re-dated records — is unanswerable, and the previous file is gone. |
| `scholar-6` | A cross-corpus citable-record contract (parent, sequence, text_status, citation_url) settled now, before voudintilit and tuomiokirjat are … | XL | 5 | survived 2/3, with corrections | Without a shared citable-record shape the next two ingests will each reinvent citation, dating, neighbours, facets and provenance, and a court case cut at a page boundary will be unreadable. |
| `scholar-7` | A reproducible, published retrieval evaluation, a dataset card for the snapshot, CITATION.cff and a DOI — so the archive and the DH … | L | 4 | survived 3/3, with corrections | 'Retrieval quality proven rather than asserted' is the project's stated bar, and the proof is a hand-measured number in prose. Nothing lets Kansallisarkisto, a reviewer or a historian cite the software, the snapshot or … |

## security

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `security-1` | A threat model that is a test: three deployment profiles, a control matrix, and a test that keeps the matrix honest | M | 4 | pending (0/3 verdicts in) | Controls cannot be classified as sound, missing or performative because there is no statement of what they defend, against whom, in which deployment. Concretely, the same code is (P1) a stdio child of a desktop MCP host … |
| `security-2` | A hosted-endpoint profile in settings: token/JWT auth, host-origin protection, trusted proxies, stateless sessions and a per-client rate … | L | 5 | pending (0/3 verdicts in) | For P3 (hosted) the server has no way to say who may call it, which Host it answers to, which proxies it believes, or how much one client may spend. All of it is one env-gated block away, and the project's philosophy … |
| `security-3` | Make the worst query cheap: two-phase search that counts by row id and fetches only the page, plus keyword and term bounds | M | 4 | pending (0/3 verdicts in) | An unauthenticated caller — or an honest model paging with match_all=false on a stop-word-like term — makes the server materialise ~23 MB per call (10k x 2.3 KB) per worker thread, and on tuomiokirjat (7.8M pages, … |
| `security-4` | Close the transcript hole in the anti-forgery measure and strip format characters that survive _oneline | S | 4 | pending (0/3 verdicts in) | The field that IS the OCR — the transcript, the largest and least controlled input — is the one field the structural protection does not cover, in the view a model reads when it wants the full text. And the protection … |
| `security-5` | Earn the badges: least-privilege release jobs, a verify job that runs cosign and slsa-verifier as a consumer would, reproducible rebuild … | XL | 5 | pending (0/3 verdicts in) | Badges: Tests/Security/Docs/Secret-Leaks/Scorecard are earned (they report real runs). SLSA L3, Sigstore and SBOM are aspirational by the README's own admission and, more importantly, will remain *unverified* after the … |
| `security-6` | Dependency cooldown and a hash-locked docs build: sit out the malicious-release window and stop floating packages from holding a Pages … | S | 3 | pending (0/3 verdicts in) | Both auto-merge and the docs build accept the newest artefact on the day. Most malicious PyPI/npm releases are detected and yanked within days; a short cooldown removes most of that exposure at zero engineering cost, … |
| `security-7` | Property-based fuzzing of the predicate builders, the query parser boundary and the record ingest — the fuzzing Scorecard looks for, with … | M | 3 | pending (0/3 verdicts in) | The injection and robustness properties are among the project's strongest claims and are pinned by a dozen hand-picked strings. They are properties over *all* strings, and the fixture plus a Python oracle makes them … |
| `security-8` | A signed data manifest: make the mounted table — the half of the deployment the image does not contain — provenanced, citable and … | M | 4 | pending (0/3 verdicts in) | The corpus is the attack surface and the trust anchor at once, and it is the one artefact with no provenance: an operator cannot tell which harvest a server is serving, a historian cannot cite the snapshot a quotation … |

## testing

| id | proposal | effort | impact | verdict | the gap |
|---|---|:-:|:-:|---|---|
| `testing-1` | Fault-injection suite for the mounted-table deployment: the silent-empty-corpus and split-brain modes the probes cannot see | M | 5 | pending (0/3 verdicts in) | Three real deployment states — FTS index never built, rows appended after indexing, index files missing/unreadable — are invisible to /ready, to the boot probe, and to every test; two of them produce a server that … |
| `testing-2` | Nightly corpus conformance without shipping data: harvest df live (3 s), regenerate a measurements registry, and make every quoted number a … | L | 5 | pending (0/3 verdicts in) | 238 measured numbers are the product this server hands to a language model, and none of them is recomputed by anything; the 7 tests that would check the corpus never execute; drift between harvests (voudintilit moved … |
| `testing-3` | Prove the declared dependency floor and the table-format contract between the ingest machine and the image | S | 4 | pending (0/3 verdicts in) | The declared floor is false by at least ten minor releases, and nothing tests that a table built with lancedb N is served correctly by lancedb N+k — the exact situation the mount-based deployment creates. Dependabot … |
| `testing-4` | A brute-force reference search as an oracle, and hypothesis properties over the predicate builders and formatter | M | 4 | pending (0/3 verdicts in) | The properties the project reasons about — filters are literal, the year filter is overlap, AND⊆OR, phrase⊆AND, fuzzy(base)⊇exact, pages tile — are asserted on two or three chosen inputs; the injection resistance the … |
| `testing-5` | Golden contract of the model-facing surface: tools/list schema, formatted responses, executable description examples, and a real stdio … | M | 4 | pending (0/3 verdicts in) | The text a model reads — schema, descriptions, instructions, formatted results — is the product, and a FastMCP bump or a wording edit can change it without any test noticing; the default transport is never exercised; … |
| `testing-6` | Mutation testing as a scheduled, scored gate — and the sub-2-second inner loop that makes it affordable | S | 3 | pending (0/3 verdicts in) | Mutation testing found real gaps once and left no instrument behind; the formatter — the only code whose output a model reads verbatim — has a 70% kill rate on operator mutants; and the whole suite re-indexes the … |
| `testing-7` | Contract tests for harvest.py against a fake and a recorded Sisältöhaku | M | 3 | pending (0/3 verdicts in) | The pipeline that decides what 98.25% means, and the encoding details that fail silently, are verified only by a memory of a probing session; a refactor that switched the range to the string form would produce a … |
| `testing-8` | Scale conformance: a synthetic tuomiokirjat-shaped corpus and a budget test for the 10k materialisation pattern | L | 4 | pending (0/3 verdicts in) | The claim that the shape survives 7.8M rows rests on a pattern measured at 6,876 rows; at 100k rows a common-term search already spends 0.4 s and 50 MB counting rows it discards, and four concurrent model calls multiply … |

## Refuted, and why

**`arch-5` — Package layout for three corpora and two sibling archives: an archive-agnostic `archive-mcp-kit`, a `kansallisarkisto-corpora` package of …**

- *Project maintainer:* The premise of copy-and-annotate sharing is documented (dataset.py:8, lib/telemetry.py:13-14, mcp/telemetry.py:18, routes.py:17; audit finding 13's 'ra-mcp likely has it too'). But a maintainer says 'not now', and not primarily for the reason the proposal gives. (1) The only step that can be done in this repo — 'split the lib into kit-shaped modules with df in corpora/df.py' — is arch-1's PR 1 restated; every other measurable outcome (ra-mcp deleting modules, three SBOMs listing the kit, the OTel test existing once) depends on ra-mcp's maintainers and on ape-mcp, which this repo does not control. (2) It does not weigh its own supply-chain cost: a kit on PyPI is a second signed, …

**`devx-3` — CI wall time from 2 m 09 s to under 75 s: a uv cache volume in the Dagger engine, four parallel jobs, pip-audit moved to the …**

- *Meticulous code reviewer:* Pulled the job log for run 34360630378 (job 102496380161). Step times match (67/17/29/9 s), but the breakdown contradicts the diagnosis. Inside the 67 s 'checks' step: dagger CLI download + `docker pull registry.dagger.io/engine:v0.21.9` 9.4 s, connect 12.5 s total, `loading type definitions` (Go module compile) 20.1 s, then `KansallisarkistoMcp.checks` 33.1 s — of which `uv sync --frozen --no-cache --all-packages` was 'Prepared 97 packages in 1.86s, Installed 97 packages in 146ms' (~5.7 s including image pulls) and pip-audit ('Installed 28 packages', 'No known vulnerabilities found') took 15.3 s. In the `test` step, uv sync was 'Prepared 97 packages in 1.62s' (2.1 s). In the `test-mcp` …
- *Maintainer of this exact project:* Checked: ci.yml:18-56 is one job with four sequential dagger calls; .dagger/main.go:268 and test.go:307 use `uv sync --frozen --no-cache --all-packages`; main.go:247/251 and test.go:296-297 pull `ghcr.io/astral-sh/uv:latest`; pip-audit runs inside `Checks` (checks.go:343-350). The uv pin is the second audit's finding 7 verbatim — do not re-report it. The wall-time baseline (67/17/29/9 s) cannot be verified offline and the proposal's headline number (<75 s) rests on Dagger Cloud's remote cache, which is only available when `DAGGER_CLOUD_TOKEN` is present — never on fork PRs, which are exactly the contributor case the 'people push small commits' argument is about. The 'test-mcp calls …

