---
icon: lucide/file-search
---

# Open-source craft and community

Every proposal from this lens in full, followed by the three skeptics' verdicts on it.
Effort and impact are the lens's own estimates; the verdict tally is the skeptics'.

## `oss-1` — The server advertises fastmcp's version as its own: wire serverInfo.version to the package version

**survived 3/3, with corrections · effort S · impact 4/5**

**What the code does today.** packages/kansallisarkisto-mcp/src/ra_mcp_kansallisarkisto_mcp/tools.py:17-19 constructs `FastMCP(name="ra-kansallisarkisto-mcp", instructions=...)` with no `version=`. fastmcp 3.4.4's server.py:326 accepts `version`, and server.py:417 falls back to `fastmcp.__version__` when it is None. Measured with an in-memory client (scratchpad/spike_contract.py): `initialize` returns `serverInfo: ra-kansallisarkisto-mcp 3.4.4 protocol 2025-11-25` — a client logs 3.4.4 for a server whose pyproject says 0.1.0 (packages/kansallisarkisto-mcp/pyproject.toml:3). Meanwhile telemetry.py:56-66 already resolves the real version via `importlib.metadata.version("ra-mcp-kansallisarkisto-mcp")` for the OTel `service.version` resource attribute, so the project has two notions of its version and the one on the wire is wrong. publish.yml:44-56 validates the tag against both pyprojects, so the tag is already the source of truth for the third notion.

**The gap.** When v0.1.0 is released, every MCP client, registry listing and bug report will identify the server as 3.4.4; a dependabot bump of fastmcp will silently 'release' a new server version to every client while the tool contract has not changed. A reference MCP server must report its own version in `initialize`.

**The proposal.** Add `__version__` to `ra_mcp_kansallisarkisto_mcp/__init__.py` (currently a one-line docstring) via `importlib.metadata.version("ra-mcp-kansallisarkisto-mcp")` with the same `PackageNotFoundError -> "0.0.0+unknown"` fallback telemetry.py uses; pass `version=__version__` at tools.py:17; make telemetry.py:66 import it instead of re-resolving. Add `test_server_reports_its_own_version` to packages/kansallisarkisto-mcp/tests/test_tools.py: `client.initialize_result.serverInfo.version == importlib.metadata.version(...)` and `!= fastmcp.__version__`. Add the same assertion to scripts/mcp_smoke.py (a 7th check) so the published image is verified to report the tag it was built from — publish.yml already runs `test-published`.

**Why it is once-in-a-lifetime.** It is the difference between a server that can be cited ('kansallisarkisto-mcp 0.3.1 returned DF 2457') and one that cannot. Every other proposal in this list — contract versioning, data versioning, registry entries — hangs a version on the wire, and today that hook is the framework's number.

**How we would know.** `initialize` returns `serverInfo.version == "0.1.0"` on the v0.1.0 image (mcp_smoke.py check 7 green in publish.yml); a fastmcp bump PR leaves serverInfo.version unchanged; the OTel `service.version` and the MCP `serverInfo.version` are the same string in test_telemetry.py.

**Risks.** `fastmcp inspect --format mcp` (used by proposal oss-7's manifest) reads serverInfo too, so it must be regenerated. None otherwise; the change is ~10 lines.

**First step.** One PR: `__version__` in `__init__.py`, `version=__version__` at tools.py:17, telemetry.py:66 imports it, one test in test_tools.py, one check in mcp_smoke.py.

**Spike.** Run `uv run python scratchpad/spike_contract.py` (in-memory fastmcp Client against tools.kansallisarkisto_mcp): prints `serverInfo: ra-kansallisarkisto-mcp 3.4.4 protocol: 2025-11-25`. After the fix the same script must print 0.1.0.

### Skeptics

**Meticulous code review of batch D-1 (oss-1..oss-7) against the repository at HEAD 69b2afe: every cited file:line re-read, the two spike scripts re-run, the wheel unpacked and imported outside the tree, lancedb 0.20/0.22/0.24 installed and probed, the MCP registry validator source and the Scorecard check list fetched.** — *not refuted*, confidence 0.97

Reproduced: an in-memory fastmcp Client against tools.kansallisarkisto_mcp returns initialize_result.serverInfo name='ra-kansallisarkisto-mcp' version='3.4.4' protocolVersion 2025-11-25. tools.py:17-19 constructs FastMCP(name=..., instructions=...) with no version=. fastmcp 3.4.4 FastMCP.__init__ has `version: str | int | float | None = None` and line ~96 of that __init__ does `version=_coerce_version(version) or fastmcp.__version__` — the fallback is exactly as described. packages/kansallisarkisto-mcp/pyproject.toml:3 says 0.1.0. telemetry.py:56-66 resolves `version("ra-mcp-kansallisarkisto-mcp")` for service.version. publish.yml:49-61 validates the tag against both pyprojects. __init__.py is a one-line docstring. test_tools.py:26-28 asserts only tool names; mcp_smoke.py has 6 checks and none touches serverInfo. No test in test_telemetry.py mentions service.version today (grep: nothing).

*Corrections:* telemetry.py:66's fallback is the string "unknown", not "0.0.0+unknown" — the proposal invents the fallback it says it copies. There is no existing test_telemetry assertion on service.version, so 'the same string in test_telemetry.py' is a new assertion, not a strengthened one.

*Sharpened:* Add `__version__` to ra_mcp_kansallisarkisto_mcp/__init__.py via importlib.metadata with the existing "unknown" fallback, pass `version=__version__` at tools.py:17, and while touching the constructor also pass `website_url="https://ai-riksarkivet.github.io/kansallisarkisto-mcp/"` — FastMCP 3.4.4's __init__ accepts `version`, `website_url` and `icons`, and today serverInfo ships websiteUrl=None. Make telemetry.py import `__version__` so OTel service.version and MCP serverInfo.version cannot diverge, assert equality in test_telemetry.py, assert `!= fastmcp.__version__` in test_tools.py, and add a 7th mcp_smoke.py check `client.initialize_result.serverInfo.version == os.environ['EXPECTED_VERSION']` fed from the tag in publish.yml's test-published step (which today only curls /health, .dagger/serve.go:166).

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.95

Reproduced the evidence, not just re-read it. tools.py:17-19 builds FastMCP(name=..., instructions=...) with no version=. fastmcp 3.4.4 server.py:326 takes `version`, server.py:417 falls back to `fastmcp.__version__`. An in-memory `Client(kansallisarkisto_mcp)` printed `serverInfo: ra-kansallisarkisto-mcp 3.4.4 proto 2025-11-25 fastmcp 3.4.4`; packages/kansallisarkisto-mcp/pyproject.toml:3 says 0.1.0. telemetry.py:66-69 resolves the real version with importlib.metadata for OTel and falls back to the string "unknown" (not "0.0.0+unknown" as the proposal says). __init__.py is a one-line docstring. scripts/mcp_smoke.py:36 checks only tool names and ends with 'passed (6 checks)'. publish.yml:44-56 validates the tag against both pyprojects. Fit: this is a wire-level correctness bug, ~10 lines, zero runtime cost on stdio, no new dependency; a server that reports the framework's version cannot be cited and a Dependabot fastmcp bump would silently 'release' it. A maintainer who wrote 'cite the DF number — the only stable handle' would want the server to have a stable handle too.

*Corrections:* telemetry.py's fallback is "unknown", not "0.0.0+unknown"; whichever string is chosen, use one. Note that `version=` in FastMCP is coerced via `_coerce_version` (server.py:417), so pass a str.

*Sharpened:* One source of truth for the running version: `ra_mcp_kansallisarkisto_mcp.__version__` from importlib.metadata (fallback "0.0.0+unknown"), passed as `version=__version__` at tools.py:17, imported by telemetry.py:66 for `service.version`, logged in server.py's boot line next to the LanceDB URI, and returned in the `/health` and `/ready` JSON — so the MCP client, the OTel resource, the operator's log and the probe all read the same string. Test: `client.initialize_result.serverInfo.version == importlib.metadata.version(...) != fastmcp.__version__`, plus a three-way equality assertion in test_telemetry.py; mcp_smoke.py check 7 over `/mcp` so publish.yml's `test-published` proves the pushed image reports the tag it was built from. This is the hook oss-2, oss-3 and oss-7 all hang on; do it first.

**Feasibility and honesty of effort (LanceDB / PyArrow / FastMCP / OTel / Dagger / GitHub Actions engineer)** — *not refuted*, confidence 0.97

Read fastmcp 3.4.4 server/server.py: `version` is a constructor parameter at line 326 and line 417 does `version=_coerce_version(version) or fastmcp.__version__` — the fallback is exactly as claimed. `Client.initialize_result` exists (client/client.py:387). Ran scratchpad/spike_contract.py: `serverInfo: ra-kansallisarkisto-mcp 3.4.4 protocol: 2025-11-25`. Ran my own judge_d1/v1.py: `FastMCP(name='x', version='0.1.0')` -> serverInfo.version '0.1.0'; `FastMCP(name='y')` -> '3.4.4'; `importlib.metadata.version('ra-mcp-kansallisarkisto-mcp')` returns '0.1.0' in this venv (editable install has dist-info, so the container, which installs editable too, will resolve it). tools.py:17-19 has no `version=` (verified). telemetry.py:64-67 resolves the version separately for OTel (verified). `fastmcp inspect ... --format mcp -o manifest.json` runs here and today writes `serverInfo.version: '3.4.4'` (inspect.py:364 reads `mcp._mcp_server.version`), confirming the manifest risk. mcp_smoke.py uses the fastmcp Client over HTTP, so `client.initialize_result.serverInfo.version` is available for a 7th check. Effort S is honest: ~10 lines plus two tests.

*Corrections:* telemetry.py's fallback string is `"unknown"` (line 67), not `"0.0.0+unknown"`; pick one string and use it in both places. `fastmcp inspect` output must be regenerated, as noted, but that only matters once oss-2's manifest asset exists.

*Sharpened:* Pass `version=__version__` AND `website_url="https://ai-riksarkivet.github.io/kansallisarkisto-mcp/"` at tools.py:17 — FastMCP 3.4.4 accepts `website_url` (server.py:327) and it lands in `serverInfo.websiteUrl`, which `fastmcp inspect` and registry clients already read. Add one assertion to test_telemetry.py (it already has an in-memory exporter) that the exported Resource's `service.version` equals `client.initialize_result.serverInfo.version`, so the two notions of version cannot drift again.

---

## `oss-2` — Snapshot the wire-level tool contract and make 'breaking' a tested, documented property of an MCP release

**survived 3/3, with corrections · effort M · impact 5/5**

**What the code does today.** The tool contract exists only as Python annotations in df_tool.py:87-170 (df_search: 10 params with `ge`/`le` bounds, defaults `limit=25`, `fuzzy=0`, `match_all=True`) and df_tool.py:213-215 (`df_number` ge=1). test_tools.py:26-28 asserts only the set of tool names; scripts/mcp_smoke.py:36 likewise. cliff.toml:15 marks `(**BREAKING**)` only from `commit.breaking`, i.e. a `!` or BREAKING footer — and `git log` over all 51 commits contains zero such markers, although `a99bbf0 fix(lib): stop discarding fuzzy on a phrase` turned a silently-ignored parameter into an error reply and `f84c229 docs(mcp): sharpen the tool descriptions` rewrote what the model is told. formatter.py:67 and dataset.py:469 fix the output shapes `**DF N** — dating — place` and `... search results for '...': showing X of Y records (offset N)` that a downstream agent or eval parses. Measured (scratchpad/spike_contract.py): the full `tools/list` dump is 7,956 bytes; the structural subset (names, param names, types, bounds, defaults, required) is 868 bytes; descriptions alone are 1,638 + 567 chars.

**The gap.** For an MCP server the tool schema and the text format are the API. A rename of `issuingplace`, a change of `le=MAX_LIMIT` to 50, a flipped `match_all` default, or a new result header line breaks every client prompt and eval built on it, and nothing in CI or in the release notes would say so. 'Tool-contract changes are breaking changes' is not encoded anywhere.

**The proposal.** (1) Commit `packages/kansallisarkisto-mcp/contract/tools.json` — the sorted `tools/list` dump — and `contract/output.txt` — the fixture output of two canonical calls (`df_search(keyword="Åbo", limit=2)`, `df_get_charter(526)`). (2) `tests/test_contract.py`: compute the structural hash (drop `description` keys) and compare to the file; a mismatch fails with the diff and the instruction `make contract` — and `make contract` refuses unless `git log -1` carries `!` or `BREAKING CHANGE:`. Description-only drift is a separate, non-failing test that prints the diff (descriptions are tuned often — f84c229 — and must stay cheap). (3) `docs/tools/contract.md`: the policy — breaking = tool renamed/removed, parameter renamed/removed/retyped, a bound tightened, a default changed, the header/record/footer lines in format_results and _render_charter changed; non-breaking = new optional parameter with a default, description text, snippet length (SNIPPET_CHARS). Semver: breaking -> minor while 0.x, major after 1.0. (4) cliff.toml: add a first `commit_parsers` entry `{ message = "^(feat|fix|refactor)\\(tools\\)!", group = "<!-- 0 -->Tool contract (BREAKING)" }` so those commits lead the release note. (5) publish.yml: `fastmcp inspect ... --format mcp -o manifest.json` and attach `manifest.json` + `contract/tools.json` as release assets beside the SBOMs (lines 150-157), so a consumer can diff two releases' contracts without running either.

**Why it is once-in-a-lifetime.** No archival MCP server publishes a machine-diffable tool contract per release. Doing it here, with the output format included, turns 'a historian trusts and cites it' into something an eval can pin: the string that reaches the model at v0.2.0 is the string that was reviewed.

**How we would know.** A PR changing `le=MAX_LIMIT` at df_tool.py:106 to 50 without `!` fails CI at test_contract.py; the v0.2.0 release page carries `manifest.json` and `contract/tools.json` and a 'Tool contract (BREAKING)' section whenever the structural hash changed since the previous tag; `diff <(jq . v0.1.0/contract.json) <(jq . v0.2.0/contract.json)` is the documented way to read a release.

**Risks.** Two snapshot files to keep current; the description-drift test must stay informational or it will be resented and deleted. The `!` check in `make contract` is local policy, not a hard gate, until branch protection exists (audit finding 11: `main` has none).

**First step.** PR 1: generate `contract/tools.json` from the spike script, add `test_contract.py` with the structural-hash comparison and `make contract`; PR 2: `docs/tools/contract.md` and the cliff.toml parser.

**Spike.** `uv run python scratchpad/spike_contract.py` already produces both hashes (full 287c5a16…, structural 35941af6…) and prints the `fuzzy` and `limit` schema fragments `{default:0,min:0,max:2}` / `{default:25,min:1,max:100}` that the snapshot would pin. Change `le=MAX_LIMIT` to `le=50` in a scratch copy and rerun: the structural hash changes, the description-only hash does not.

### Skeptics

**Meticulous code review of batch D-1 (oss-1..oss-7) against the repository at HEAD 69b2afe: every cited file:line re-read, the two spike scripts re-run, the wheel unpacked and imported outside the tree, lancedb 0.20/0.22/0.24 installed and probed, the MCP registry validator source and the Scorecard check list fetched.** — *not refuted*, confidence 0.90

Re-ran scratchpad/spike_contract.py: full dump 7,956 bytes sha 287c5a16…, structural 868 bytes sha 35941af6…, fuzzy {default:0,min:0,max:2}, limit {default:25,min:1,max:100} — all numbers match. df_tool.py:87-169 has the 10 params with ge/le bounds and defaults exactly as stated; df_tool.py:214 has df_number ge=1. test_tools.py:26-28 asserts only the name set; mcp_smoke.py:36 the same. `git log --format='%s%n%b' | grep -cE '!:|BREAKING CHANGE'` = 0 over 51 commits; a99bbf0's message confirms fuzzy on a quoted keyword went from silently dropped to 'It now raises with a sentence'. formatter.py:67 renders `**DF N** — dating — place`; dataset.py:469 renders `{label} search results for '...': showing X of Y records (offset N)`. `fastmcp inspect --format mcp -o` exists in the installed CLI. docs/tools/*.md and deployment.md contain no 'breaking' or 'contract' policy (grep).

*Corrections:* cliff.toml's `(**BREAKING**)` marker is on line 19, not 15. The proposed gate '`make contract` refuses unless `git log -1` carries `!`' is backwards: the snapshot is regenerated before the commit that will carry the `!` exists, so `git log -1` inspects the previous commit. Gate it in the test instead: when the structural hash differs from the snapshot at the last tag (`git describe --tags --abbrev=0`), require a `!`/BREAKING commit in `<last-tag>..HEAD` — and until a tag exists, against the committed snapshot file.

*Sharpened:* Commit `contract/tools.json` (sorted tools/list) and `contract/output.txt` (df_search(keyword="Åbo", limit=2) and df_get_charter(526) on the fixture, so the three line shapes at formatter.py:67 and dataset.py:462-476 are pinned too). test_contract.py: structural hash (descriptions dropped) must equal the snapshot; a change is allowed only when the commit range since the last v-tag contains a `!` commit; description drift is a separate informational test. Add to cliff.toml a leading parser for `^(feat|fix|refactor)\(tools\)!` and let publish.yml attach `manifest.json` from `fastmcp inspect packages/kansallisarkisto-mcp/src/ra_mcp_kansallisarkisto_mcp/tools.py:kansallisarkisto_mcp --format mcp` beside the SBOMs (publish.yml:150-157). Note this manifest will contain serverInfo.version, so it depends on oss-1 landing first.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.75

Checked: df_tool.py:87-170 is the only place the schema lives (10 params, `ge`/`le` bounds from config.MAX_LIMIT); test_tools.py:26-28 and mcp_smoke.py:36 assert only the set of tool names; cliff.toml marks BREAKING only from `commit.breaking`; `git log --format=%s | grep -c '!:'` = 0 across 51 commits though a99bbf0 (fuzzy on a phrase becomes an error reply) and f84c229 (descriptions rewritten) are both in the log; formatter.py:67 and dataset.format_results fix the header/record lines an agent parses. Ran scratchpad/spike_contract.py: 7,956-byte dump, structural subset 868 bytes, fuzzy `{default 0, 0..2}` and limit `{default 25, 1..100}`. Also dumped tools/list myself: each tool carries `annotations` ({readOnlyHint: True, openWorldHint: False}), `outputSchema`, and `meta.fastmcp.tags` as a sorted list — none of which the proposal's 'structural subset' names, and a flipped readOnlyHint is exactly the kind of silent break a client that auto-approves read-only tools would feel. `fastmcp inspect --format mcp -o` exists in 3.4.4. Fit: for an MCP server the schema plus the text shape IS the API; the project already treats prose as load-bearing, and a snapshot is the cheapest way to make 'breaking' a tested property. It scales to voudintilit/tuomiokirjat unchanged (new tools are additive). One design flaw: `make contract` refusing unless `git log -1` carries `!` checks the previous commit, not the one being made — you regenerate before you commit, so the gate fires on the wrong commit.

*Corrections:* (1) The structural subset must include `annotations`, `outputSchema` and `_meta.fastmcp.tags` — not only names/params/bounds/defaults. (2) `make contract` cannot gate on `git log -1`; the regenerating commit does not exist yet. Gate instead with an explicit `CONTRACT_BREAK=1 make contract` that stamps the reason into contract/CHANGES.md, and a CI step that fails when `contract/tools.json` changed in the PR diff without a `!`/BREAKING commit in the PR range (`git log origin/main..HEAD`). (3) Description-only drift is worth recording, not just printing: keep `contract/descriptions.sha` as a second snapshot updated freely by `make contract-docs`, so a release note can say 'descriptions changed' without anyone remembering.

*Sharpened:* Commit `contract/tools.json` (sorted `tools/list` incl. annotations, outputSchema, tags) and `contract/output.txt` (fixture output of `df_search(keyword='Åbo', limit=2)` and `df_get_charter(526)`, covering the header, record, untranscribed marker and footer lines); `tests/test_contract.py` compares the structural hash and the golden output and fails with the diff and the regeneration instruction; a separate informational test reports description drift. `docs/tools/contract.md` states the policy in the project's voice with examples from its own history (a99bbf0 was breaking; f84c229 was not). cliff.toml gains a leading 'Tool contract (BREAKING)' parser; publish.yml attaches `manifest.json` (fastmcp inspect --format mcp) and `contract/tools.json` beside the SBOMs so `diff` between two release pages is the documented way to read a release. Order: after oss-1 (serverInfo.version is part of the manifest), before oss-7 (adoption).

**Feasibility and honesty of effort (LanceDB / PyArrow / FastMCP / OTel / Dagger / GitHub Actions engineer)** — *not refuted*, confidence 0.90

Verified df_tool.py:87-170 (10 params, `ge`/`le` bounds, `limit` at line 106 with `le=MAX_LIMIT`), df_tool.py:213-215 (`df_number` ge=1), formatter.py:67 (`**DF N** — dating — place`), dataset.py:469 (results header line), test_tools.py:26-28 and mcp_smoke.py:36 assert only tool names. `git log --format=%s | grep -cE '!:|BREAKING'` = 0 over 51 commits; a99bbf0 and f84c229 exist as described. Reran spike_contract.py: full 7,956 bytes / 287c5a16…, structural 868 bytes / 35941af6…. Ran my own judge_d1/c1.py that patches `config.MAX_LIMIT` before importing df_tool (df_tool.py:22 imports the name, so this reproduces a real `le=50` edit): structural hash 35941af6… -> 18b3885a… (limit.maximum 100 -> 50) — the gate fires. `fastmcp inspect --format mcp -o manifest.json` works in this venv and carries serverInfo, environment {fastmcp 3.4.4, mcp 1.30.0} and full tool schemas — usable as the release asset. Tool annotations (`readOnlyHint=True, openWorldHint=False`) are present on the wire and belong in the structural snapshot. git-cliff 2.13.1 runs here via uvx (~20 s); its `commit_parsers` are first-match so a leading `!` parser works. Effort M is honest.

*Corrections:* The spike's claim that changing `le=MAX_LIMIT` leaves the description-only hash unchanged is wrong: `limit`'s description is the f-string `f"Max results (1–{MAX_LIMIT})"` (df_tool.py:106), so my run shows the description hash also moved (fb1714c1… -> 13fb53d2…). The structural gate still catches the change, which is what matters, but the description test must not be sold as independent of bounds. Also, pytest has no 'print a diff but pass' mode visible without `-s`; make the description-drift test emit `warnings.warn(diff)` so it shows in the summary. And `make contract` checking `git log -1` for `!` is order-inverted (you regenerate before committing); check the message of the commit that changes the snapshot in CI instead.

*Sharpened:* Commit `contract/tools.json` generated from `fastmcp inspect --format mcp` with `serverInfo.version` and `environment` stripped (they change every release), keep annotations in the structural hash, and add a CI step on tag push that diffs the snapshot against the previous tag's and fails unless the release's git-cliff output contains a `(**BREAKING**)` entry. Pin the two output shapes with the fixture (`df_search(keyword='Åbo', limit=2)`, `df_get_charter(526)`) as `contract/output.txt` exactly as proposed; that is the part no other MCP server does.

---

## `oss-3` — Release the data too: a versioned, manifest-carrying LanceDB table published beside the image, with the server reporting code and data versions together

**survived 3/3, with corrections · effort L · impact 5/5**

**What the code does today.** README.md:112-114 and .gitignore: 'No corpus ships with this repository … the data is re-harvested rather than versioned'. deployment.md 'Release' versions only code. ingest.py:26-48 declares DF_SCHEMA; ingest_df builds the FTS index (dataset.py:136-192) and scalar indexes at ingest; server.py:24-50 logs table names at boot but no data identity; routes.py `/ready` reports only `df`. data-sources.md records that the live index moves (voudintilit 99,031 -> 99,125 between harvests) and that df harvest is 100% (README:32) — 3 MB, ~3 s. Measured on the fixture (scratchpad/spike_manifest.py): ingest is 0.04 s; `table.replace_field_metadata("object_id", {"data_manifest": json})` round-trips through `db.open_table().schema.field().metadata` (table-level `schema.metadata` is None, so field metadata is the available slot); two independent ingests of the same file give the identical ranked order for `Åbo` (7 hits) — the build is deterministic; the FTS config is readable back from `list_indices()[..].index_details`. README:40 gives the df table as ~69 MB.

**The gap.** A user who runs `docker run riksarkivet/kansallisarkisto-mcp:v0.1.0` has a signed, attested, SBOM'd image and nothing to search. 'Scales without changing shape' and 'the release is verifiable end to end' both stop at the data boundary: there is no way to say which harvest a result came from, no way to know a mounted table matches the schema the code expects, and a 69 MB artefact that takes 3 s to build is not shipped because of a philosophy written for the 19 GB corpus.

**The proposal.** (1) `ingest.py`: `DF_SCHEMA_VERSION = 1`; `ingest_df` writes a manifest — `{schema_version, table, source_path, source_sha256, source_rows, kept, skipped, harvested_at (from .data/df/report.json), fts: {language, stem, remove_stop_words, with_position, ascii_folding, max_token_length}, lib_version, lancedb, pyarrow, fingerprint}` — into field metadata on `object_id` AND as a sidecar `data/df.manifest.json` (readable by an operator with `cat`). `fingerprint` = sha256 of the ranked DF-number lists for 10 fixed queries (`konung`, `Åbo`, `ecclesia`, `"de ecclesia"`, `bref` fuzzy=1, …) so two ingests can be compared without comparing files. (2) `dataset.py`: `read_manifest(db, table)`; `tools.get_search()` refuses with a caller-fixable message when `schema_version != DF_SCHEMA_VERSION` ('table built for schema 1, this server needs 2 — run make ingest-df'); server.py:49 logs `data: df-2026.07.01 schema 1 rows 6876`; `/ready` JSON gains `data_version`; an MCP resource `kansallisarkisto://df/manifest` (a resource, not a tool, so oss-2's contract is untouched). (3) `.github/workflows/publish-data.yml` (Dagger `PublishData`): harvest df (3 s), ingest, verify fingerprint against the previous release, `tar` `data/df.lance` + manifest, push with `oras push docker.io/riksarkivet/kansallisarkisto-data:df-2026.09.09` with the manifest as OCI annotations, cosign-sign it keylessly exactly as the image is (publish.yml:160-166), attach the tarball as a release asset, and add a `data:` line to the release note template. (4) `docker-compose.yml` and deployment.md: a one-line `oras pull` (or `curl` of the release asset) before `up`. (5) Data version scheme `df-YYYY.MM.DD` (harvest date) with `+schemaN`; compatibility rule in `docs/how-it-works/data-sources.md`: a code release states the schema versions it accepts.

**Why it is once-in-a-lifetime.** An archive server whose every answer can be traced to (code version, data version, source hash, index config) is what lets a historian cite a result and a second researcher reproduce it a year later. The determinism is already there — it was measured today — it is just not captured.

**How we would know.** `docker run` + `oras pull` from the v0.1.0 release page yields `/ready` -> 200 with `data_version: df-2026.09.09`; two people ingesting the same harvest report the same fingerprint; a table built with schema 1 mounted under code expecting schema 2 answers `/ready` 503 with the re-ingest sentence rather than a lance error; the release note lists the data version and the 10-query fingerprint.

**Risks.** This argues against the stated philosophy ('re-harvested, not versioned') — and against README:195-201, which says the records are Kansallisarkisto's and reuse terms must be consulted. Publishing the table publicly needs written consent from Kansallisarkisto (see oss-6); until then ship only the manifest + fingerprint and keep the tarball as an artefact of a private workflow run. For tuomiokirjat the tarball path does not apply (19 GB); the manifest and schema-version handshake do, which is the part that 'scales without changing shape'. Field metadata on `object_id` is a slot, not a design; if lancedb adds table-level metadata, move it.

**First step.** PR: `DF_SCHEMA_VERSION`, manifest written by `ingest_df` (field metadata + sidecar), `read_manifest`, boot log line, `/ready` field, and the fingerprint over the 18-row fixture pinned in test_ingest.py. No publishing yet.

**Spike.** `uv run python scratchpad/spike_manifest.py` — already run: manifest round-trips via field metadata, FTS config reads back, two ingests give identical ranked order, 0.04 s / 0.2 MB for 18 rows. Extend it to compute the 10-query fingerprint and assert it is stable across 5 ingests.

### Skeptics

**Meticulous code review of batch D-1 (oss-1..oss-7) against the repository at HEAD 69b2afe: every cited file:line re-read, the two spike scripts re-run, the wheel unpacked and imported outside the tree, lancedb 0.20/0.22/0.24 installed and probed, the MCP registry validator source and the Scorecard check list fetched.** — *not refuted*, confidence 0.85

Re-ran scratchpad/spike_manifest.py on lancedb 0.38.0: field metadata on object_id round-trips through db.open_table().schema.field().metadata; table-level schema.metadata is None; list_indices()[..].index_details reads back language=Swedish, stem=True, remove_stop_words=False, with_position=True, ascii_folding=True, max_token_length=64; two ingests give the identical ranked order for 'Åbo' (7 hits); 0.2 MB. README.md:110-111 says 're-harvested rather than versioned'; README:39-40 gives 100% and ~69 MB; data-sources.md:68-69 records 99,031 -> 99,125; ingest.py:30-48 declares DF_SCHEMA; dataset.py:136-192 is build_fts_index; server.py:24-50 logs table names only; routes.py:55 /ready returns only {status, table}. deployment.md 'Release' (lines 74-85) versions code only. The philosophy conflict and the consent question are acknowledged in the proposal's own risks.

*Corrections:* (1) `harvested_at (from .data/df/report.json)` cannot be read from report.json: harvest.py:322-334 writes index, mode, complete, index_total, documents_written, documents_unreachable, slices_remaining, coverage_pct, http_requests, elapsed_seconds, shortfalls — no timestamp. The harvester must start recording one (or the manifest takes the JSONL mtime). (2) `table.replace_field_metadata` is deprecated as of lancedb 0.33.1 (warning printed on this run); use `update_field_metadata({"object_id": {...}})`. (3) The 141,032 figure is not in this proposal but the same lens uses it in oss-6; here the numbers check out.

*Sharpened:* Ship the identity first, the tarball never until DATA_TERMS exists: `DF_SCHEMA_VERSION = 1` in ingest.py; `ingest_df` writes a manifest via `update_field_metadata` on object_id and as `data/df.manifest.json` with schema_version, source_sha256, source_rows/kept/skipped, harvest `report.json` fields (index_total, coverage_pct, complete) plus a harvest timestamp the harvester must start writing, the FTS config read back from list_indices() rather than re-stated, lib/lancedb/pyarrow versions, and a fingerprint = sha256 of the ranked DF lists for 10 fixed queries. `read_manifest()` in dataset.py; get_search() refuses on schema mismatch with a caller-fixable sentence; boot log and /ready carry data_version; an MCP resource `kansallisarkisto://df/manifest` keeps oss-2's tool contract unchanged. Pin the fixture fingerprint in test_ingest.py across two ingests (determinism measured today). The OCI/oras publishing step is gated on oss-6's DATA_TERMS.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.70

Checked README.md:112-114 ('re-harvested rather than versioned'), data-sources.md:18-21 (same, and that re-harvesting df reproduces the July 2026 snapshot), ingest.py:26-48 (DF_SCHEMA declared, no metadata), server.py:24-50 (boot logs table names only), routes.py:47-52 (`/ready` returns only `table: df`). Re-ran the manifest experiment: the spike's claim that 'table-level schema.metadata is None, so field metadata is the available slot' is an artefact of DF_SCHEMA carrying no metadata. Passing `pa.schema(..., metadata={...})` to `create_table` round-trips through `open_table().schema.metadata` on lancedb 0.38.0, 0.33.0 and 0.25.0 (measured in scratch venvs). `replace_field_metadata` is deprecated at 0.33.1 and absent before 0.24, so the proposed slot is the fragile one. Fit: the manifest + schema-version handshake is the part that turns 'boots without data' into 'boots and says which data', keeps 'a message, not a crash', lives in the lib (no MCP dep), and is exactly the shape tuomiokirjat needs (per-table manifest, 19 GB never shipped). The tarball/oras/cosign publication half argues against README:195-201 and the proposal says so itself: it is blocked on Kansallisarkisto's reuse terms (oss-6's DATA_TERMS). A maintainer says yes to the handshake now and 'not until consent' to the publish, which the proposal already sequences. The 10-query fingerprint is the genuinely new idea: it lets two operators compare tables without comparing files, and lets a release note state retrieval identity.

*Corrections:* Write the manifest as schema-level metadata on the schema passed to `create_table` (`DF_SCHEMA.with_metadata({'ka.manifest': json})`), not as field metadata on `object_id`: it round-trips on every lancedb version tested (0.25.0, 0.33.0, 0.38.0) and avoids `replace_field_metadata`, which is deprecated in 0.33.1 and missing below 0.24. `.data/df/report.json` as the source of `harvested_at` must be optional — the fixture ingest has no report. Effort for the first PR is M, not L; L is the publish half, which is gated on DATA_TERMS.

*Sharpened:* Per-table data identity as a lib concept: `DF_SCHEMA_VERSION = 1`, `ingest_df` writes `{schema_version, source_sha256, rows, harvested_at?, fts config as read back from list_indices, lib_version, lancedb, pyarrow, fingerprint}` into schema metadata and a `<table>.manifest.json` sidecar; `dataset.read_manifest(db, table)`; `get_search()` refuses a schema-version mismatch with a caller-fixable sentence (text, not exception); server.py logs `data: df schema 1 rows 6876 source <sha8> harvested 2026-07-01`; `/ready` and an MCP resource `kansallisarkisto://df/manifest` expose it. Fingerprint = sha256 of the ranked DF lists for a fixed query set kept next to test_retrieval_quality.py (so voudintilit/tuomiokirjat each declare their own set), asserted stable across 5 ingests on the fixture. The oras/cosign data release and `docker-compose` pull follow only once DATA_TERMS.md (oss-6) says the table may be redistributed; until then the release note carries the manifest and fingerprint, which already lets a second researcher check they have the same data.

**Feasibility and honesty of effort (LanceDB / PyArrow / FastMCP / OTel / Dagger / GitHub Actions engineer)** — *not refuted*, confidence 0.85

Reran spike_manifest.py: ingest 0.03 s, field metadata round-trips, FTS config readable from `list_indices()[..].index_details` ({'language': 'Swedish', 'stem': True, 'remove_stop_words': False, 'with_position': True, 'ascii_folding': True, 'max_token_length': 64}), two ingests give identical `Åbo` order, `schema.metadata` is None. Read lancedb 0.38 table.py: `replace_field_metadata` (line 4416) is deprecated since 0.33.1 in favour of `update_field_metadata(*updates)` (lines 2299-2330, dict form `{"path":..., "metadata":{...}}`); there is no table-level config/metadata API in the Python package (grep for update_config/def config: none), so field metadata is genuinely the only slot. Ran judge_d1/fp.py: a 10-query fingerprint (konung, Åbo, ecclesia, "de ecclesia", bref fuzzy=1, breff fuzzy=2, Raseborg year 1444, `latina ruotsi` match_all=False, Piispat, df) is identical across 5 independent ingests (ebefe022f391e78f). Ran judge_d1/fp2.py: metadata written with `update_field_metadata` survives `optimize()` + `build_fts_index(db, DF_TABLE)` + `optimize()` (table version 9, metadata intact, FTS still hits). Ran judge_d1/res.py: `@mcp.resource("kansallisarkisto://df/manifest")` registers a resource with zero effect on tools/list. `get_search()` raising a `MissingTableError` subclass keeps df_tool's `_answer` untouched. The oras/cosign publish path cannot be exercised here (no registry, no Dagger CLI) but `oras push --annotation` and `cosign sign <oci-ref>` are standard. Effort L is honest for the whole; the manifest/handshake PR alone is M.

*Corrections:* Use `table.update_field_metadata({"path": "object_id", "metadata": {"data_manifest": json}})`, not the deprecated `replace_field_metadata`. `.data/df/report.json` carries no `harvested_at`: harvest.py:321-333 writes index, mode, complete, index_total, documents_written, documents_unreachable, coverage_pct, http_requests, elapsed_seconds, shortfalls — add a UTC timestamp to the report in harvest.py (one line) or fall back to the export file's mtime. Line references drift slightly (README ~69 MB is at lines 17/42, 100% at 16/41).

*Sharpened:* Ship the schema-version handshake and manifest first (ingest.py `DF_SCHEMA_VERSION`, sidecar `df.manifest.json`, `read_manifest`, `/ready` field, boot log, resource), computing the fingerprint through `DfSearch` so it pins analyser + ranking, and pin the 18-row fingerprint in test_ingest.py so an FTS config change fails a test until the manifest's fingerprint is updated alongside a release-note line. Treat a fingerprint change as a 'Retrieval behaviour' release-note entry (oss-4). Hold the OCI/oras publication behind DATA_TERMS (oss-6), but wire `harvest.py` to stamp `harvested_at` now so the data version `df-YYYY.MM.DD` has a source.

---

## `oss-4` — Design the first release as a verifiable event: in-tree CHANGELOG on the site, OCI labels, a post-publish verifier, and badges that link to proof

**survived 3/3, with corrections · effort M · impact 4/5**

**What the code does today.** `git tag` is empty; 51 commits, every subject conventional (checked against cliff.toml's parsers — none would be dropped by `filter_unconventional = true`), so git-cliff can produce a complete v0.1.0 note. publish.yml:132-140 runs `git cliff --latest` only inside CI and only into the GitHub release body; cliff.toml:1-9 promises Keep-a-Changelog but no CHANGELOG.md exists and zensical.toml's nav has no changelog page. README.md:210-214 admits the three supply-chain badges 'state how this project releases, not yet something you can download and verify'; the SLSA/Sigstore/SBOM badges are static shields.io images linking to docs sections. publish.yml:97-109 (`docker/build-push-action`) sets no `labels:` and the Dockerfile has no `LABEL` (grep: none), so the pushed image carries no `org.opencontainers.image.version/revision/source`. `test-published` (publish.yml:170-177) only checks `/health` per deployment.md. zensical.toml:99 links a Docker Hub repo that does not exist yet; .docker/docker-compose.yml:2 pulls `riksarkivet/kansallisarkisto-mcp:latest`. Audit 2 finding 4 (dispatch can overwrite a version tag) and 'publish.yml runs test and scan-ci but not checks' are still open.

**The gap.** The pipeline is more elaborate than most released projects', and nothing has ever exercised it end to end — the first tag push is the first run of the provenance, release-asset and signing steps against a real registry. There is no cadence, no statement of what a release note must contain for an MCP server, and the badges are promises.

**The proposal.** (1) `make release VERSION=0.1.0`: sed both pyprojects, `uv lock`, `git cliff --tag v0.1.0 -o docs/changelog.md`, commit `chore(release): v0.1.0`, `git tag -s`. Add `docs/changelog.md` to zensical.toml nav ('Changelog'); docs.yml already rebuilds on push to main. (2) publish.yml: add `uv lock --check` and `dagger call checks` before `test` (closes the pip-audit gap); `docker/metadata-action` to emit OCI labels (`version`, `revision`, `source`, `licenses=Apache-2.0`, `title`); dispatch runs push `sha-<short>` only (closes audit finding 4). (3) A `verify-release` Dagger function run as the final step: `cosign verify --certificate-identity-regexp '^https://github.com/AI-Riksarkivet/kansallisarkisto-mcp/.github/workflows/publish.yml@refs/tags/v' --certificate-oidc-issuer https://token.actions.githubusercontent.com riksarkivet/kansallisarkisto-mcp@<digest>` and `slsa-verifier verify-image … --source-uri github.com/AI-Riksarkivet/kansallisarkisto-mcp --source-tag v0.1.0`, plus `test-published` extended to call `tools/list` over `/mcp` and check `serverInfo.version` (oss-1). (4) cliff.toml `footer`: a 'Verify this release' block with those two commands and `## Data` / `## Tool contract` / `## Retrieval behaviour` headings — the last for index/analyser changes that move counts (data-sources.md records konung 277 -> 279 after the apparatus fix; that is release-note material for a search server). (5) Badges: replace the three static shields with links to the verify-release job of the latest run (`actions/workflows/publish.yml/badge.svg` + `?event=push` on tags) and a 'Latest release' shield (`img.shields.io/github/v/release/...`) that is red-or-absent until a release exists; keep the earned ones (Tests, Security, CodeQL, Scorecard). (6) Cadence policy in `docs/development/deployment.md`: release immediately on any tool-contract change (minor while 0.x); a patch on the first Monday after Dependabot bumps land (Trivy and Dependabot both run Mondays — security.yml:18, dependabot.yml:7); a data release per harvest (oss-3).

**Why it is once-in-a-lifetime.** A release page from which a stranger can, with two copy-pasted commands, prove the image was built from this tag by GitHub's builder — and read in the same note which tool parameters and which search counts changed — is the standard the badges already claim. The first release is the only chance to set that shape before consumers exist.

**How we would know.** From a clean machine, the two commands in the v0.1.0 release body succeed; `docker inspect` shows `org.opencontainers.image.version=0.1.0` and `.revision=<sha>`; the site's Changelog page equals `git cliff`; OpenSSF Scorecard's Signed-Releases check moves from N/A to scored; every README badge resolves to a URL whose content is the evidence for the badge.

**Risks.** Signed tags require a maintainer GPG/SSH signing key — a small ceremony for a single-maintainer project (git shortlog: one author). `docker/metadata-action` is one more SHA-pinned action. The first real run of slsa-github-generator and cosign against Docker Hub may need secrets adjustments; do a `v0.1.0-rc.1` prerelease first and let `verify-release` fail there rather than on 0.1.0.

**First step.** PR: `make release`, `docs/changelog.md` generated from the current history (it is complete), nav entry, OCI labels, `uv lock --check` + `checks` in publish.yml, `sha-` tag for dispatch. Then cut `v0.1.0-rc.1`.

**Spike.** `git log --format=%s | grep -vE '^(feat|fix|docs|refactor|test|chore|ci|build|perf|style|revert)(\([^)]*\))?!?: '` returns nothing — the whole history is changelog-ready. (`uvx git-cliff` itself timed out downloading through the proxy here, so the rendered note was not previewed.)

### Skeptics

**Meticulous code review of batch D-1 (oss-1..oss-7) against the repository at HEAD 69b2afe: every cited file:line re-read, the two spike scripts re-run, the wheel unpacked and imported outside the tree, lancedb 0.20/0.22/0.24 installed and probed, the MCP registry validator source and the Scorecard check list fetched.** — *not refuted*, confidence 0.90

`git tag` is empty; `git log --oneline | wc -l` = 51; the proposal's unconventional-subject grep returns nothing. cliff.toml:1-9 is the Keep-a-Changelog header; no CHANGELOG.md; zensical.toml nav (13-44) has no changelog page. publish.yml:137-140 runs `git cliff --latest` into RELEASE_NOTES.md only. publish.yml:91-103 build-push-action has no `labels:`; `grep LABEL` on the Dockerfile returns nothing. TestPublished (.dagger/serve.go:149-173) only runs healthCheckCmd. docker-compose.yml:3 pulls :latest. Audit 2 finding 4 (dispatch overwrites version tag) and finding 11's 'publish.yml runs test and scan-ci but not checks' and 'main has no branch protection' are open. `uv lock --check` runs nowhere in CI today (grep of ci.yml and .dagger/*.go: only a file-inclusion mention in test.go:31). security.yml:17-18 and dependabot.yml schedule Mondays. Scorecard's check list (fetched from ossf/scorecard docs/checks.md) includes Signed-Releases, which scores .intoto.jsonl release assets once a release exists.

*Corrections:* zensical.toml's Docker Hub link is on line 93, not 99; the git-cliff step is at publish.yml:137-140, not 132-140. `git shortlog` shows two authors (carpelan 50, one Claude co-authored commit), not one — the single-maintainer point stands. `uv lock --check` is absent from ci.yml/checks as well, so adding it only to publish.yml would leave PRs unguarded; put it in .dagger/checks.go.

*Sharpened:* `make release VERSION=` (sed both pyprojects, `uv lock`, `git cliff --tag vX -o docs/changelog.md`, commit, `git tag -s`), docs/changelog.md in the zensical nav; publish.yml gains `uv lock --check` (also in checks.go) and `dagger call checks` before `test`, docker/metadata-action OCI labels (version/revision/source/licenses), and `sha-<short>` tags for dispatch. A final `verify-release` Dagger function runs `cosign verify --certificate-identity-regexp '^https://github.com/AI-Riksarkivet/kansallisarkisto-mcp/.github/workflows/publish.yml@refs/tags/v'` and `slsa-verifier verify-image --source-uri github.com/AI-Riksarkivet/kansallisarkisto-mcp --source-tag` against the pushed digest, and extends TestPublished to an MCP `initialize` + `tools/list` check of serverInfo.version (oss-1). cliff.toml footer carries the two verify commands and `## Data` / `## Tool contract` / `## Retrieval behaviour` headings. Cut v0.1.0-rc.1 first so the untested cosign/SLSA/Docker Hub path fails on a prerelease.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.65

Verified: `git tag` empty; 51 commits; cliff.toml header promises Keep-a-Changelog, `footer = ""`, no CHANGELOG.md in the tree, no changelog entry in zensical.toml nav; publish.yml:97-109 build-push has no `labels:` and the Dockerfile has no LABEL; README.md:19-20 SLSA/Sigstore badges are static shields linking to docs; `test-published` only hits `/health`; security.yml and dependabot.yml both run Mondays; audit 2 finding 4 (dispatch overwrites a version tag) and finding 11 (publish runs test and scan-ci but not checks) are open. Fit: the project's own bar is 'the release is verifiable end to end' and the README admits nothing has been released. Most of this is release engineering that any project could do, but here the badges already make the claim, so verifying the claim is honesty rather than ceremony. The strongest piece is the post-publish verifier; the weakest are signed tags (a single-maintainer key ceremony with no stated consumer) and badge rearrangement. One structural error: the proposal puts `verify-release` as 'the final step' of the publish job, but SLSA provenance is generated in the separate `provenance` job that `needs: [publish]` (publish.yml:180-200), so `slsa-verifier verify-image` inside the publish job would always fail — the verifier must be a third job `needs: [provenance]`. The 'Retrieval behaviour' release-note heading is the one idea specific to a search server and worth keeping.

*Corrections:* `verify-release` must be a third job with `needs: [publish, provenance]`; inside the publish job the SLSA attestation does not exist yet. `git tag -s` should be optional — GitHub's verified-tag path or an unsigned tag plus the SLSA source-tag check gives the same verifiability without a personal key. A 'Retrieval behaviour' section needs a commit-scope convention to feed it (e.g. `feat(index)`, `fix(index)`, `fix(search)` parsers in cliff.toml) — git-cliff cannot infer it from prose.

*Sharpened:* Cut `v0.1.0-rc.1` as the first end-to-end run. Before it: `make release VERSION=` (bump both pyprojects, `uv lock`, `git cliff -o docs/changelog.md`, commit, tag) with `docs/changelog.md` in the nav; publish.yml gains `uv lock --check` and `dagger call checks` (pip-audit finally gates a release), `docker/metadata-action` OCI labels (version/revision/source/licenses), `sha-<short>` tags for dispatch (closes audit finding 4), and a third job `verify-release` needing `provenance` that runs `cosign verify` with the workflow-identity regexp, `slsa-verifier verify-image --source-tag`, and mcp_smoke.py over `/mcp` asserting `serverInfo.version == tag` (oss-1). cliff.toml footer = the two verify commands; parsers add 'Tool contract (BREAKING)' (oss-2) and 'Retrieval behaviour' fed by `(index|search)` scopes, so a count change like konung 277→279 is release-note material. Replace only the two static SLSA/Sigstore shields with links to the latest verify-release run; keep the earned ones. Cadence: release on any contract change; patch after Monday's Dependabot/Trivy cycle when anything landed; data manifest per harvest.

**Feasibility and honesty of effort (LanceDB / PyArrow / FastMCP / OTel / Dagger / GitHub Actions engineer)** — *not refuted*, confidence 0.85

`git tag` empty, 51 commits, the grep for non-conventional subjects returns 0 (verified). Dockerfile has no LABEL; build-push-action (publish.yml:91-103) sets no `labels:`; `test-published` (serve.go:149-173) only runs a curl health check; ci.yml runs `checks` (line 22) but publish.yml does not; `uv lock --check` appears nowhere (grep); docker-compose.yml:2 pulls `:latest`; zensical.toml:93 links a Docker Hub page; security.yml and dependabot.yml both run Mondays — all verified. Contrary to the spike note, `uvx git-cliff` (2.13.1) works here in ~20 s and `git-cliff --unreleased --tag v0.1.0 --strip header` renders all 51 commits into a grouped 0.1.0 note — the changelog page is a single command away. cliff.toml has `footer = ""` and `--strip header` keeps the footer, so the 'Verify this release' block fits. docs.yml pins zensical 0.0.60 and rebuilds on push to main. mcp_smoke.py already takes `MCP_URL`, so extending `test-published` to a real tools/list + serverInfo check is a small Dagger change reusing testmcp.go's Python container. Effort M honest.

*Corrections:* The spike's 'git-cliff timed out' is not reproducible — `uvx git-cliff --version` returns 2.13.1 here; the rendered note exists and is complete. The cosign command in the footer cannot contain the digest at template time; give the tag-ref form (`cosign verify ... riksarkivet/kansallisarkisto-mcp:v0.1.0`, cosign resolves the digest) or template `<digest>` for the reader. Scorecard's Signed-Releases already counts the attached `provenance.intoto.jsonl` as a signed release asset, so that check will score after the first tag with or without this proposal.

*Sharpened:* Cut `v0.1.0-rc.1` first as proposed, but make `verify-release` a Dagger function that runs `cosign verify` + `slsa-verifier verify-image` + mcp_smoke.py (with the oss-1 serverInfo check) against the pushed tag, and run the same function from `make verify-release IMAGE=...` so a stranger's two commands and CI's are literally the same code. Generate docs/changelog.md with `git cliff --tag vX.Y.Z -o docs/changelog.md` inside `make release`, and add the `## Retrieval behaviour` heading with the oss-3 fingerprint line.

---

## `oss-5` — Community files shaped by this corpus: an 'unfindable document' issue template that feeds test_retrieval_quality.py, a CONTRIBUTING that encodes 'measure it and write it beside the code', and a CITATION.cff that cites the archive

**survived 3/3, with corrections · effort S · impact 3/5**

**What the code does today.** None of CONTRIBUTING.md, CODE_OF_CONDUCT.md, CITATION.cff, CODEOWNERS, .github/ISSUE_TEMPLATE, PULL_REQUEST_TEMPLATE.md exist (checked). README.md:203-205 instructs 'Cite the archive, not this snapshot'. data-sources.md 'Can everything be found?' and test_retrieval_quality.py:1-20 describe the sweep that found three classes of silently-unfindable documents, each now pinned against the 18-row fixture; conftest.py:11-15 documents why each fixture charter is there. dataset.py:44-60 and models.py:14-22 show the house rule: every default carries its measured count. The audits call the retrieval-quality suite 'the most valuable thing in the repository'. Audit 2 finding 9 shows the numbers drift (27 vs 28 compound-language charters).

**The gap.** The project's best mechanism — a researcher notices a charter they cannot find, and it becomes a pinned test — has no intake. A historian who searches `Hundæbæth` and gets nothing has nowhere to report it that would turn it into a fixture row; a contributor has no statement of the rules the code visibly follows; a researcher citing the software has no metadata to cite.

**The proposal.** (1) `.github/ISSUE_TEMPLATE/unfindable.yml` (form): DF number; the word searched; the form in the transcript at df.kansallisarkisto.fi/document/N (required checkbox 'I checked the archive page'); filters used; `fuzzy` tried (0/1/2); expected vs got. Label `retrieval`. (2) `.github/ISSUE_TEMPLATE/tool-contract.yml` for a parameter/description problem — the audit's `fuzzy`-on-phrase class. (3) `CONTRIBUTING.md`, short, with the three rules the code already obeys and the audits enforce: a default that changes a count carries the count in a comment and, where the fixture can show it, a test; a change to df_tool.py's schema or formatter's line shapes follows `docs/tools/contract.md` (oss-2) and a `!` commit; a fixture addition to df_sample.jsonl states which trap the charter covers (conftest.py style) and updates the fixture count everywhere via `wc -l` (audit 1 finding 6). Plus: `make check`, `make test`, `make test-mcp` before a PR; corpus-only tests are skipped without `.data/df` and how to harvest it in 3 s. (4) `CITATION.cff`: `type: software`, authors = Riksarkivet AI lab and (per oss-6) Kansallisarkisto, `repository-code`, `license: Apache-2.0`, and a `references:` entry for Diplomatarium Fennicum with `url: https://df.kansallisarkisto.fi` so the archive citation the README demands is machine-readable; GitHub then shows 'Cite this repository'. (5) `CODE_OF_CONDUCT.md` = Contributor Covenant 2.1 with the SECURITY.md contact. (6) `.github/PULL_REQUEST_TEMPLATE.md` with two checkboxes: 'tool contract unchanged, or commit carries `!`' and 'every new number in a comment or description was measured on the corpus (say which harvest)'.

**Why it is once-in-a-lifetime.** The issue template is a retrieval-quality pipeline with humans as the sweep. Done right, the fixture grows from 18 to the charters real researchers could not find, and the test suite becomes the documented history of the corpus's traps.

**How we would know.** GitHub community-profile checklist at 100%; Scorecard 'Contributing' signals present; the first `retrieval`-labelled issue lands as a fixture row + parametrised case in test_retrieval_quality.py within one release, with the fixture count updated in the seven places audit 1 listed.

**Risks.** Templates nobody triages become noise; the single-maintainer situation (oss-6) is the real constraint. Adding charters to the fixture slows the suite negligibly (ingest 0.04 s for 18 rows).

**First step.** One PR adding the six files; CITATION.cff validated with `cffconvert --validate` in CI's `checks` (a `uvx cffconvert` line in .dagger/checks.go).

### Skeptics

**Meticulous code review of batch D-1 (oss-1..oss-7) against the repository at HEAD 69b2afe: every cited file:line re-read, the two spike scripts re-run, the wheel unpacked and imported outside the tree, lancedb 0.20/0.22/0.24 installed and probed, the MCP registry validator source and the Scorecard check list fetched.** — *not refuted*, confidence 0.85

Confirmed absent: CONTRIBUTING.md, CODE_OF_CONDUCT.md, CITATION.cff, CODEOWNERS (root and .github/), .github/ISSUE_TEMPLATE/, .github/PULL_REQUEST_TEMPLATE.md. README.md:199-201 says 'Cite the archive, not this snapshot'. test_retrieval_quality.py:1-20 describes the three classes; conftest.py:11-15 documents the fixture choice; dataset.py:44-60 and models.py:14-22 carry measured counts beside defaults. audit.md:287 says 'The retrieval-quality suite is the most valuable thing in the repository'. 27 vs 28 is in audit 2 finding 9 (audit.md:177-178). Audit 1 finding 6 is 'seven places say 16'. cffconvert 2.0.0 exists on PyPI. The unfindable example 'Hundæbæth' is the models.py:16 example.

*Corrections:* OpenSSF Scorecard has no 'Contributing' check — its check list (fetched: Binary-Artifacts … Contributors … Security-Policy … Signed-Releases) scores 'Contributors' by company diversity of committers, not by a CONTRIBUTING.md. The measurable outcome should cite GitHub's community-profile checklist only. README line refs are 199-201, not 203-205.

*Sharpened:* Six files in one PR, with the issue form as the load-bearing piece: `.github/ISSUE_TEMPLATE/unfindable.yml` requiring DF number, the searched word, the form on df.kansallisarkisto.fi/document/N, filters and fuzzy tried, labelled `retrieval`; CONTRIBUTING.md stating the three rules the code visibly obeys (a count-changing default carries its measured count and, where the fixture can show it, a test; schema/format changes follow docs/tools/contract.md with a `!` commit; a fixture row names its trap and the fixture count is updated in every place — with a test that greps the docs for the `wc -l` figure so audit 1 finding 6 cannot recur); CITATION.cff with a `references:` entry for Diplomatarium Fennicum validated by `uvx cffconvert --validate` in .dagger/checks.go; Contributor Covenant 2.1; a PR template with the two checkboxes. Outcome: GitHub community profile 100% and the first `retrieval` issue becomes a fixture row plus a parametrised case in test_retrieval_quality.py.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.60

Confirmed by `ls`: no CONTRIBUTING.md, CODE_OF_CONDUCT.md, CITATION.cff, CODEOWNERS, .github/ISSUE_TEMPLATE or PR template (.github holds only dependabot.yml and workflows/). test_retrieval_quality.py:1-20 documents the three classes of silently-unfindable documents; conftest.py:11-15 explains each fixture charter; audit 1 finding 6 lists the seven places the fixture count lives. README:203-205 says 'cite the archive, not this snapshot'. Fit: most of this list is boilerplate the brief warns about (CoC, PR template, community-profile 100%), and there are no users yet — the first release (oss-4) has to exist before an intake can receive anything. But the 'unfindable document' template is not generic: it is the retrieval-quality sweep with a historian as the sweeper, and it feeds the file the audits call the most valuable thing in the repository. CONTRIBUTING encoding the three house rules the code visibly follows (measured counts beside the code, contract changes carry `!`, fixture additions state their trap) is short and specific. CITATION.cff with a `references` entry for DF is compatible with 'cite the archive' — it makes that instruction machine-readable rather than contradicting it. Single-maintainer triage is the honest risk. A maintainer says yes to three of the six files, after oss-4.

*Corrections:* Scope: drop CODE_OF_CONDUCT and the PR template from the 'once in a lifetime' claim — they are profile checklist items; add them only if the community profile matters for Scorecard. `cffconvert --validate` in .dagger/checks.go is one more tool to pin by version, consistent with the project's posture. The template must not require the corpus: ask the reporter to paste the transcript passage from df.kansallisarkisto.fi/document/N verbatim, so a fixture row can be authored offline (CI has no network).

*Sharpened:* An issue form `unfindable.yml` whose fields map 1:1 onto a parametrised row in test_retrieval_quality.py — corpus (df / voudintilit / tuomiokirjat), record id, the word searched, fuzzy tried, filters, the passage as it appears on the archive page, expected vs got — so triage is 'copy the form into the table, add the record to df_sample.jsonl with a conftest-style note, update the count via wc -l'. A one-page CONTRIBUTING.md with exactly the three rules and the three make targets. CITATION.cff (type: software, license Apache-2.0, `references:` Diplomatarium Fennicum at df.kansallisarkisto.fi, preferred-citation pointing at the archive) validated in `checks`. Ship after the first release; measure success by the first fixture row that came from a historian rather than a sweep.

**Feasibility and honesty of effort (LanceDB / PyArrow / FastMCP / OTel / Dagger / GitHub Actions engineer)** — *not refuted*, confidence 0.85

Verified none of CONTRIBUTING.md, CODE_OF_CONDUCT.md, CITATION.cff, CHANGELOG.md, .github/ISSUE_TEMPLATE exist. test_retrieval_quality.py:1-20 documents the three unfindable classes and uses `@pytest.mark.parametrize` (lines 35, 149, 198), so an issue -> fixture row -> parametrised case path is real. conftest.py:11-15 explains fixture selection; audit 1 finding 6 lists the seven fixture-count sites (README:140, docs/development/index.md:56, conftest.py:12, .dagger/serve.go:18, .dagger/testmcp.go:10, ...); audit 2 line 188 records 27 vs 28. `uvx --from cffconvert cffconvert --version` runs here under Python 3.14 (2.0.0), so a `uvx cffconvert --validate` line in .dagger/checks.go is runnable. LICENSE and SECURITY.md exist, so the community-profile checklist can reach 100% with these files. Effort S is honest (six files, one Go line).

*Corrections:* The issue form cannot 'feed' the test file by itself — it is an intake, and the conversion to a fixture row is manual. cffconvert 2.0.0 is a 2021 release; it validates CFF 1.2.0 fine but is effectively unmaintained, so pin it and expect to swap it for `cff-validator` later. CITATION.cff `references:` entries need `type`, `title` and `authors` (an entity `- name: Kansallisarkisto`) to validate.

*Sharpened:* Make the issue form's fields exactly the parametrize tuple test_retrieval_quality.py needs — `(df_number, keyword, fuzzy, language, issuingplace, year_min, year_max, expected)` — and add `scripts/add_fixture.py <objectID>` that pulls the record from Sisältöhaku's /api/search by objectID, appends it to df_sample.jsonl, and bumps `DF_FIXTURE_ROWS` and the seven prose sites from `wc -l`; then 'lands as a fixture row within one release' is a 10-minute maintainer task, not an aspiration.

---

## `oss-6` — Make Kansallisarkisto a co-owner by structure: CODEOWNERS over the material-facing files, a vocabulary file only the archive can bless, a data-terms document, and a Finnish/Swedish front door

**survived 3/3, with corrections · effort XL · impact 5/5**

**What the code does today.** zensical.toml:5-8 `site_author = "Riksarkivet"`, `Copyright 2026 Riksarkivet`; package names carry Riksarkivet's prefix on Finland's data (`ra-mcp-kansallisarkisto-lib`, packages/kansallisarkisto-lib/pyproject.toml:2); the image namespace is `riksarkivet/` (publish.yml:15); SECURITY.md:14-18 positions Kansallisarkisto as a third party ('belong to Kansallisarkisto, not here'). `git shortlog -sne`: one human author. There is no Finnish- or Swedish-language page anywhere in docs/. The material knowledge lives as prose only Kansallisarkisto's cataloguers can authoritatively confirm: tools.py:36-44 (the 75-value index-term vocabulary with counts), df_tool.py:107-136 (15 language labels, 499 places, the Reval/Tallinn rule) — 29 + 61 hard-coded numbers in those two files (counted), and audit 2 found them already drifting (27 vs 28). data-sources.md 'Provenance': the 1.75% shortfall (141,032 documents) is retrievable only via the ES API at es.demo.kansallisarkisto.fi 'with an API key from Kansallisarkisto'. README:195-201 defers reuse terms to Kansallisarkisto without a document saying what they are.

**The gap.** Everything about ownership is Riksarkivet's and everything about authority over the content is Kansallisarkisto's, and the repository has no structure through which the second can act: no reviewable artefact for the vocabulary, no place to sign off on data terms, no path that needs a Finnish account to merge, no language the Finnish archive's own staff would land in.

**The proposal.** (1) `.github/CODEOWNERS`: `docs/how-it-works/data-sources.md`, `docs/how-it-works/search-tips.md`, `packages/kansallisarkisto-lib/src/ra_mcp_kansallisarkisto_lib/vocab/`, `packages/kansallisarkisto-lib/tests/fixtures/`, and `packages/kansallisarkisto-mcp/src/ra_mcp_kansallisarkisto_mcp/df_tool.py` owned by `@AI-Riksarkivet/ka-material` (a team with Kansallisarkisto accounts) in addition to the code team; enable branch protection with 'require review from code owners' — which also closes the Scorecard Branch-Protection and Code-Review zeros (audit 2 finding 11). (2) `scripts/vocab.py` regenerates `vocab/df.json` from the harvest: index terms, language labels, countries, top-50 places, each with its count and the harvest date; a test asserts every number quoted in df_tool.py and tools.py appears in vocab/df.json (or the descriptions are rendered from it with `{counts[...]}` placeholders) — the drift audit 2 found becomes a failing test, and a Kansallisarkisto reviewer sees vocabulary changes as a JSON diff, not as prose. (3) `DATA_TERMS.md` (linked from README 'Credit and licence' and SECURITY 'Scope') co-authored with Kansallisarkisto: what may be redistributed (the built table? the fixture's 18 real charters, which are already in the repo?), attribution string, and the API-key route for the 1.75% — this is the document that unblocks oss-3 and legitimises the fixture. (4) `GOVERNANCE.md`: two maintainers, one per archive, both required on contract (`!`) and data-terms changes; anything else one review. (5) `docs/index.fi.md` and `docs/index.sv.md` — a one-page landing in Finnish and Swedish (the material is Swedish; Finland is bilingual), linked from the site header; the tool `instructions` already tell the model to answer in the user's language (tools.py:57-59), the docs should meet the same bar. (6) Naming: register the neutral PyPI names `kansallisarkisto-mcp` / `kansallisarkisto-lib` (oss-7) and keep the `ra_mcp_` import path; move the image to a shared org namespace only when both archives agree — say so in GOVERNANCE.md rather than silently keeping `riksarkivet/`.

**Why it is once-in-a-lifetime.** A cross-border national-archives project where the code owner and the record owner both hold a key is rare enough to be cited as a model. Concretely it is what turns 98.25% coverage into 100%: only a co-owner hands over the API key. The vocabulary file is the piece that makes the archive's knowledge a reviewable artefact instead of a paragraph one developer typed.

**How we would know.** A PR touching search-tips.md cannot merge without a review from a Kansallisarkisto account; `vocab/df.json` exists and the description-number test passes; DATA_TERMS.md is signed off and the fixture's status under it is stated; coverage in data-sources.md reads 100% after a harvest with the ES API key; Finnish and Swedish landing pages are live on the site.

**Risks.** The effort is institutional, not technical, and the repository can only prepare the structure. CODEOWNERS with no Finnish accounts blocks merges — add the entries only when the team exists. The vocab-driven descriptions must not make the LLM-facing text worse: keep the prose hand-written and interpolate counts only. Renaming packages is churn; propose it, do not do it unilaterally.

**First step.** Repository-side, now: `scripts/vocab.py` + `vocab/df.json` regenerated from the harvest + the number-consistency test (fails today on 27 vs 28 — fix that in the same PR); a draft `GOVERNANCE.md` and `DATA_TERMS.md` with the open questions listed for Kansallisarkisto. CODEOWNERS and the languages follow the first meeting.

**Spike.** Count the surface: `grep -oE '[0-9][0-9,]{1,}' df_tool.py tools.py | wc -l` gives 61 and 29 — ninety corpus numbers held in prose. On the fixture, generate `vocab/df_sample.json` from df_sample.jsonl with the same script and show the test shape: every `(label, count)` pair in a description must exist in the vocab file.

### Skeptics

**Meticulous code review of batch D-1 (oss-1..oss-7) against the repository at HEAD 69b2afe: every cited file:line re-read, the two spike scripts re-run, the wheel unpacked and imported outside the tree, lancedb 0.20/0.22/0.24 installed and probed, the MCP registry validator source and the Scorecard check list fetched.** — *not refuted*, confidence 0.80

zensical.toml:4-8: site_author Riksarkivet, copyright Riksarkivet. Package names ra-mcp-kansallisarkisto-{lib,mcp}; image riksarkivet/kansallisarkisto-mcp (publish.yml:18,40). SECURITY.md:14-18 positions Kansallisarkisto as the data owner elsewhere. `git log` authors: carpelan (50) plus one co-authored commit. No *.fi.md/*.sv.md under docs/ (find) and no Finnish/Swedish page text (grep). tools.py:36-44 holds the 75-value vocabulary with counts; df_tool.py:107-136 the language/place lists with counts. `grep -oE '[0-9][0-9,]{1,}' … | wc -l` = 61 (df_tool.py) and 29 (tools.py), exactly as claimed. Audit 2 finding 9: 27 vs 28. data-sources.md:61-66: 98.25%, ES API with an API key. README:195-201 defers reuse terms. CODEOWNERS with required code-owner review does need branch protection, which audit 2 finding 11 says is absent — the proposal says so.

*Corrections:* The shortfall is 141,118 documents (8,082,496 − 7,941,378 per data-sources.md:61), not 141,032; the figure 141,032 appears nowhere in the repository. Making `df_tool.py` code-owned by a material team means every parameter/description change needs a Kansallisarkisto review, which conflicts with 'descriptions are tuned often' (oss-2) — own the vocab file and the docs pages, and render counts from the vocab file so df_tool.py itself need not be in CODEOWNERS.

*Sharpened:* Do the repository-side part now: `scripts/vocab.py` regenerating `vocab/df.json` (index terms, languages, countries, top-50 places, each with count and harvest date) from the harvest, a `vocab/df_sample.json` from the 18-row fixture so the shape is testable offline, and a test asserting every `(label, count)` pair quoted in df_tool.py and tools.py exists in vocab/df.json — it fails today on 27 vs 28, fix that in the same PR. Draft GOVERNANCE.md and DATA_TERMS.md with the open questions (redistribution of the built table, the fixture's 18 real charters already in the repo, attribution string, the API-key route to the 141,118 unreachable documents). CODEOWNERS over docs/how-it-works/*.md, vocab/ and tests/fixtures/ only, added once a `ka-material` team exists; Finnish and Swedish landing pages after the first meeting.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.60

Verified the ownership asymmetry: zensical.toml:4-7 site_author/copyright Riksarkivet; package names `ra-mcp-kansallisarkisto-*`; image namespace `riksarkivet/` (publish.yml:15); SECURITY.md:14-18 puts the records with Kansallisarkisto; `git shortlog -sne` one author; no non-English doc. Verified the number surface: `grep -oE '[0-9][0-9,]{1,}' | wc -l` gives 61 (df_tool.py) and 29 (tools.py); the drift is real — search-tips.md:88 says 27 compound-language charters, df_tool.py:116 says 28 (audit 2 finding 9). data-sources.md:61-66 confirms the 1.75% gap is retrievable only via the ES API 'with an API key'. tools.py:57-59 tells the model to reply in the user's language. Fit: the institutional items — CODEOWNERS over a team that does not exist, a two-maintainer GOVERNANCE.md for a one-author repo, fi/sv landing pages, renaming packages — are 'not now', and the proposal says so itself, so they are not a reason to refute. The repository-side core is exactly what the philosophy asks for: 'every default carries its measured count' currently means ninety hand-retyped numbers in LLM-facing prose that a Finnish cataloguer cannot review as anything but a paragraph. `vocab/<corpus>.json` regenerated from the harvest plus a test that every (label, count) in a description exists in it makes drift a failing test and makes the next corpus's vocabulary a generated file instead of a week of prose — that is the '7.8M pages without changing shape' argument. DATA_TERMS.md is the document that unblocks oss-3 and states the status of the 18 real charters already in the repo, which nobody has written down. A maintainer says yes to those two now and prepares the rest as a draft for the first meeting with Kansallisarkisto.

*Corrections:* The vocab regeneration needs the harvest and CI has no network, so `vocab/df.json` is a committed artefact regenerated by `make vocab` at harvest time with `harvested_at` inside it; the test compares prose to the committed file, not to the live index. Interpolating counts into the descriptions must keep the hand-written prose (f84c229 was tuned against the live service) — format placeholders only. CODEOWNERS with no Finnish accounts blocks every merge; do not add it before the team exists.

*Sharpened:* Now, repository-side: `scripts/vocab.py` → `packages/kansallisarkisto-lib/.../vocab/df.json` (index terms with counts, language labels, countries, top-50 places, compound-language count, harvest date), df_tool.py and tools.py render their counts from it via placeholders, and `test_vocab_numbers.py` asserts every number quoted in the two files and in search-tips.md/data-sources.md appears in vocab/df.json — failing today on 27 vs 28 and fixed in the same PR. A `DATA_TERMS.md` draft listing the open questions for Kansallisarkisto (redistribution of the built table, status of the 18 fixture charters, attribution string, API-key route for the 1.75%). Contingent on a Kansallisarkisto counterpart: CODEOWNERS over data-sources.md, search-tips.md, vocab/, fixtures/ and df_tool.py; GOVERNANCE.md naming two maintainers; fi/sv landing pages; the neutral PyPI names. The measurable win that only a co-owner can deliver is coverage 98.25% → 100%.

**Feasibility and honesty of effort (LanceDB / PyArrow / FastMCP / OTel / Dagger / GitHub Actions engineer)** — *not refuted*, confidence 0.80

`grep -oE '[0-9][0-9,]{1,}' ... | wc -l` gives exactly 61 (df_tool.py) and 29 (tools.py) — verified. zensical.toml:5-8 `site_author = "Riksarkivet"`, copyright Riksarkivet; SECURITY.md:14-18 as quoted; one author in `git shortlog -sne`; audit 2 line 188 '27 vs 28'. DF_SCHEMA (ingest.py:36-39) carries indexterm, issuingplace, issuingplacecountry, language, so `vocab/df.json` can be computed from the ingested table with `pyarrow.compute.value_counts` and does not need the raw harvest. A description-number test is feasible: the `(label, count)` pairs are literal strings in df_tool.py and appear verbatim in the tools/list dump. Checked zensical 0.0.60 (the pinned docs build): no i18n plugin, but `templates/partials/alternate.html` iterates `config.extra.alternate` and `theme.language` defaults to 'en' (config.py:517), so a language switcher between separately-authored Finnish/Swedish pages is supported. CODEOWNERS + 'require review from code owners' is a GitHub setting, no code. Effort XL is honest for the institutional part; the repository-side first step is about M.

*Corrections:* `docs/index.fi.md` / `docs/index.sv.md` is the mkdocs-static-i18n naming convention, which zensical does not run; author `docs/fi/index.md` and `docs/sv/index.md` and register them under `[extra] alternate = [{name='Suomi', link='/fi/', lang='fi'}, ...]` in zensical.toml. CODEOWNERS on `packages/kansallisarkisto-lib/tests/fixtures/` will also block every fixture PR from oss-5 until a Finnish reviewer exists — scope it to the vocab and docs first. The 27-vs-28 drift is between search-tips.md and df_tool.py; a vocab test on df_tool.py catches only the code side unless search-tips.md is also checked.

*Sharpened:* Do the repository-side part now as one PR: `scripts/vocab.py` computes `vocab/df.json` from the LanceDB table (value_counts over the four catalogue columns, plus the harvest date from oss-3's manifest), and a test asserts every `'label' (…, N)` pair in the tools/list dump and every count in docs/how-it-works/search-tips.md exists in vocab/df.json. Put GOVERNANCE.md and DATA_TERMS.md in as drafts with the open questions. Add CODEOWNERS entries only for `vocab/`, data-sources.md and search-tips.md, and only once the `ka-material` team has a member.

---

## `oss-7` — Adoption paths that fit 'stdio on a laptop': PyPI with trusted publishing so `uvx kansallisarkisto-mcp` works, a registry server.json, and a compatibility matrix — with the installed-wheel data-path trap fixed first

**survived 3/3, with corrections · effort M · impact 4/5**

**What the code does today.** getting-started/index.md:13-18 requires a clone and the repo as cwd. config.py:36-43 `_project_root` walks up from `__file__` looking for `pyproject.toml` + `packages/`; from an installed wheel that never matches, so config.py:56-62 returns `/data` even when it does not exist. Verified by unpacking the wheel outside the repo (scratchpad/site): `_project_root() -> None`, `resolve_lancedb_uri() -> /data`, `/data` absent — the missing-table sentence (errors.py) would then tell a `uvx` user about `make ingest-df` in a clone they do not have. security.md 'Dependency audit' says the packages are 'never published to PyPI and always installed editable'. `uv build --package ra-mcp-kansallisarkisto-lib` succeeds; the wheel's METADATA has no Description/readme, no Project-URL, no classifiers, no LICENSE file in dist-info (checked) — the lib pyproject.toml:1-13 declares none. lib pyproject.toml:11 pins `lancedb>=0.20,<1` while dataset.py:187-188 passes `ascii_folding` and `max_token_length`, FTS kwargs the lower bound may not accept; the locked engine is lancedb 0.38.0, pyarrow 24.0.0, mcp 1.30.0, fastmcp ==3.4.4 (mcp pyproject.toml:12), protocol 2025-11-25 (spike). Makefile:30 already uses `npx @modelcontextprotocol/inspector`. Audit 2 finding 11 lists what a hosted endpoint would need (auth, host-origin protection, keyword length).

**The gap.** The cheapest path — `claude mcp add kansallisarkisto -- uvx kansallisarkisto-mcp` — is impossible without a PyPI release, and would fail confusingly with one because of the `/data` resolution. There is no registry entry for clients to discover the server and no statement of which engine/framework/client versions the tests actually cover.

**The proposal.** (1) Fix the trap: in config.py, when `_project_root()` is None and `/data` is absent, return `~/.local/share/kansallisarkisto-mcp/data` (XDG) and have errors.py's MISSING_TABLE name the resolved path and the two ways to fill it (`KA_LANCEDB_URI=…` or oss-3's data download); `scripts/ingest_df.py` becomes a console script `kansallisarkisto-ingest` in the lib package so a wheel user can build the table without a clone. (2) pyproject metadata for both packages: `readme`, `authors`, `classifiers` (Typing :: Typed, Topic :: Sociology :: History), `[project.urls]` (Homepage, Documentation, Changelog, Source), `license-files`; narrow `lancedb>=` to the first release accepting the FTS kwargs (find it with `uv pip install lancedb==0.2x` + `FTS(ascii_folding=True, max_token_length=64)`), and add that lower bound to a CI matrix job. (3) publish.yml: a `pypi` job with `pypa/gh-action-pypi-publish` via OIDC trusted publishing and PEP 740 attestations (`attestations: true`) — no token, same keyless posture as cosign; runs after `verify-release` (oss-4). (4) `server.json` at the repo root for the official MCP registry (`name: io.github.AI-Riksarkivet/kansallisarkisto-mcp`, `packages: [{registry_type: pypi, identifier: ra-mcp-kansallisarkisto-mcp, transport: stdio, environment_variables: [KA_LANCEDB_URI]}]`), published with `mcp-publisher` in the same job; the `mcpName` goes into pyproject `[project.urls]` as the registry requires. (5) `docs/compatibility.md`: protocol 2025-11-25; fastmcp 3.4.4 (exact pin — say why); mcp 1.30.0; lancedb 0.38.0 tested / lower bound X; pyarrow 24; Python 3.14.7; clients exercised: fastmcp Client in tests, Claude Code stdio + streamable HTTP (getting-started), MCP Inspector (`make inspect`); untested: Claude Desktop, Cursor, Windsurf — listed as untested rather than implied. (6) Defer MCPB/Claude Desktop bundle until oss-3 ships a downloadable table (a bundle with no data is a worse first impression than a clone); defer a hosted demo until the audit's list is settled, and say so in the matrix page.

**Why it is once-in-a-lifetime.** 'An operator runs it without reading source' starts with `uvx`. A signed, attested PyPI release plus a registry entry makes the server discoverable by every MCP client without a git clone — and the compatibility page turns 'works with MCP' into a list of things someone actually ran.

**How we would know.** `uvx --from ra-mcp-kansallisarkisto-mcp kansallisarkisto-mcp` boots on a clean machine and its first tool call names the resolved data directory and how to fill it; the PyPI page renders the README with links; `pip download` of the wheel shows a PEP 740 attestation; the server appears in the MCP registry; the CI matrix runs the suite at the lancedb lower bound and at the lock.

**Risks.** PyPI project names `ra-mcp-kansallisarkisto-*` are Riksarkivet-branded (oss-6); publishing them makes renaming later a deprecation. The registry's schema moves; pin `mcp-publisher` by version. Trusted publishing needs a one-time PyPI-side configuration by the account owner. Widening adoption before oss-2's contract snapshot exists invites the breaking changes the snapshot is meant to catch — order them oss-1, oss-2, then this.

**First step.** PR: the config.py XDG fallback + MISSING_TABLE wording + `kansallisarkisto-ingest` console script + pyproject metadata; verify with `uv build` and `uv pip install dist/*.whl` in a clean venv (lancedb download permitting) that `resolve_lancedb_uri()` names a writable path.

**Spike.** Done: `uv build --package ra-mcp-kansallisarkisto-lib -o scratchpad/dist` (22 KB wheel, METADATA lacks Description/URLs/classifiers); unpack the wheel outside the repo and import `config.py` alone: `_project_root() -> None`, `resolve_lancedb_uri() -> '/data'` with `/data` absent. Next: `uv pip install 'lancedb==0.24'` … in a scratch venv and call `FTS(ascii_folding=True, max_token_length=64)` to find the true lower bound (the clean-venv lancedb download timed out through this proxy; retry with `UV_HTTP_TIMEOUT=600`).

### Skeptics

**Meticulous code review of batch D-1 (oss-1..oss-7) against the repository at HEAD 69b2afe: every cited file:line re-read, the two spike scripts re-run, the wheel unpacked and imported outside the tree, lancedb 0.20/0.22/0.24 installed and probed, the MCP registry validator source and the Scorecard check list fetched.** — *not refuted*, confidence 0.85

Reproduced the trap: unpacked scratchpad/dist/ra_mcp_kansallisarkisto_lib-0.1.0-py3-none-any.whl outside the repo, imported config with the project venv's lancedb and KA_LANCEDB_URI unset: `_project_root() -> None`, `resolve_lancedb_uri() -> '/data'`, /data absent — config.py:36-43 and :56-62 as described; errors.py:10-12 tells that user about `make ingest-df`. Wheel METADATA (checked) has Summary, License-Expression, Requires-* and nothing else — no Description, Project-URL or classifiers; zero license files in the wheel. docs/development/security.md:216-217 says 'never published to PyPI and always installed editable'. getting-started/index.md:13-18 needs a clone. Makefile:30 uses npx inspector. lancedb 0.38.0, pyarrow 24.0.0, mcp 1.30.0, fastmcp==3.4.4 (mcp pyproject:13). Fetched the registry's internal/validators/registries/pypi.go: PyPI ownership is proven by an `mcp-name: <server-name>` token in the package's PyPI description (README), not by a pyproject URL. Installed lancedb 0.20.0, 0.22.0 and 0.24.0 under Python 3.14 in scratch envs: `inspect.signature(FTS)` already has both `ascii_folding` and `max_token_length` at 0.20.0.

*Corrections:* (1) The lower bound `lancedb>=0.20` DOES accept `ascii_folding` and `max_token_length` (measured on 0.20.0, 0.22.0, 0.24.0), so there is no FTS-kwarg reason to narrow it; the matrix job is still worth having but should test behaviour (stop-word/stemming defaults that dataset.py:152-156 says 'flipped between releases') rather than kwargs. (2) The registry does not read `mcpName` from `[project.urls]` for PyPI packages: its validator requires the literal line `mcp-name: io.github.AI-Riksarkivet/kansallisarkisto-mcp` in the PyPI README/description — which the wheel does not carry at all today, so `readme=` in pyproject is a precondition for registry publication, not just cosmetics. (3) `uvx kansallisarkisto-mcp` (title) cannot work under the current package name; the outcome's `uvx --from ra-mcp-kansallisarkisto-mcp kansallisarkisto-mcp` is the honest form.

*Sharpened:* Fix the path first: when `_project_root()` is None and /data is absent, resolve to `~/.local/share/kansallisarkisto-mcp/data` (XDG) and make MISSING_TABLE name the resolved path and the two ways to fill it; ship `scripts/ingest_df.py` as a `kansallisarkisto-ingest` console script in the lib package. Then pyproject metadata for both packages — `readme` (with the registry's `mcp-name:` line in the mcp package README), `authors`, `classifiers`, `[project.urls]`, `license-files` — verified by building and installing the wheel in a clean venv and checking METADATA carries Description and Project-URL. publish.yml: a `pypi` job with pypa/gh-action-pypi-publish via OIDC trusted publishing (PEP 740 attestations on by default) after verify-release (oss-4); `server.json` at the root published with a version-pinned `mcp-publisher`. `docs/compatibility.md` lists protocol 2025-11-25, fastmcp 3.4.4 exact pin with the reason, mcp 1.30.0, lancedb 0.38.0 tested with a CI matrix leg at 0.20 (kwargs accepted; behaviour to be confirmed by test_search_invariants' index-config assertion), pyarrow 24, Python 3.14.7, and names Claude Desktop/Cursor/Windsurf as untested. Order: oss-1, oss-2, then this.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.75

Confirmed the data-path trap by reading config.py:36-62: `_project_root()` needs pyproject.toml + packages/, which an installed wheel never has, so `resolve_lancedb_uri()` returns `/data` and errors.py's MISSING_TABLE then tells a `uvx` user about `make ingest-df` in a clone they do not have. Built both wheels: the lib METADATA has no readme/URLs/classifiers; the mcp wheel's `Requires-Dist: ra-mcp-kansallisarkisto-lib` is UNPINNED, so a published pair could resolve to mismatched versions — publish.yml only checks the two pyprojects agree with the tag, not that the dependency spec does. security.md:216-217 confirms 'never published to PyPI'. The lancedb lower-bound evidence is wrong but the conclusion is stronger than proposed: I installed lancedb 0.20.0, 0.24.0, 0.25.0 and 0.33.0 into scratch venvs on the project's Python 3.14.7. `FTS(ascii_folding=True, max_token_length=64)` is accepted at 0.20.0 (signature has both), so the FTS kwargs are not the problem. What breaks: 0.20.0 segfaults on a one-row `create_table`; 0.20.0, 0.24.0 and 0.25.0 have no `DBConnection.list_tables`, which dataset.py:133 `table_names()` calls on every boot check and `get_search()`; 0.33.0 has it. So `lancedb>=0.20,<1` in lib pyproject.toml:11 admits versions on which the server cannot boot — the true lower bound is in (0.25, 0.33]. `[project.scripts] kansallisarkisto-mcp` exists, so `uvx --from` would work once published. scripts/harvest.py imports stdlib only (urllib, gzip, json), so it can be a console script with no new dependency — which matters because a wheel user without a clone cannot ingest what they cannot harvest. Fit: 'stdio on a laptop costs nothing' and 'an operator runs it without reading source' both start with `uvx`; trusted publishing with PEP 740 attestations matches the keyless posture exactly. Correctly sequenced after oss-1 and oss-2.

*Corrections:* (1) The lancedb lower bound is broken, but not because of the FTS kwargs: `list_tables()` (dataset.py:133) is absent through at least 0.25.0 and 0.20.0 segfaults on Python 3.14; bisect between 0.25 and 0.33 and set that as the bound, with a CI job at the bound. (2) Pin the inter-package dependency: the mcp wheel must require `ra-mcp-kansallisarkisto-lib==<same version>` (or publish.yml validates the spec alongside the two pyprojects), or a PyPI user can get lib 0.1.0 with mcp 0.2.0. (3) `kansallisarkisto-ingest` alone leaves a wheel user with nothing to ingest; harvest.py is stdlib-only, so ship `kansallisarkisto-harvest` too, or make the `uvx` path wait for oss-3's downloadable table. (4) The XDG fallback should only be taken when `_project_root()` is None; the four-step order in config.py's docstring must gain a fifth step and docs/api/index.md (already drifting per audit 2) must follow.

*Sharpened:* PR 1 (lib): fix the lower bound to the first lancedb with `list_tables` that runs on 3.14 (bisect 0.26–0.33), add a CI matrix leg at that bound and at the lock; XDG fallback as step 5 in config.py with MISSING_TABLE naming the resolved path and the two ways to fill it; `kansallisarkisto-harvest` and `kansallisarkisto-ingest` console scripts in the lib (stdlib-only harvest, so no new deps); pyproject metadata (readme, urls, classifiers, license-files) and `ra-mcp-kansallisarkisto-lib==<version>` in the mcp package. PR 2: `pypi` job in publish.yml via OIDC trusted publishing with `attestations: true`, after oss-4's verify-release; `server.json` for the MCP registry with `mcp-publisher` pinned; `docs/compatibility.md` stating protocol 2025-11-25, fastmcp ==3.4.4 and why, mcp 1.30.0, lancedb tested range, Python 3.14.7, and which clients were actually exercised versus listed as untested. Success: `uvx --from ra-mcp-kansallisarkisto-mcp kansallisarkisto-mcp` boots on a clean machine and its first tool call names `~/.local/share/kansallisarkisto-mcp/data` and the harvest+ingest commands to fill it.

**Feasibility and honesty of effort (LanceDB / PyArrow / FastMCP / OTel / Dagger / GitHub Actions engineer)** — *not refuted*, confidence 0.85

Reproduced the wheel trap: with PYTHONPATH pointing at scratchpad/site (the unpacked lib wheel) `_project_root() -> None`, `resolve_lancedb_uri() -> /data`, `/data` absent — matches config.py:36-62. The lib wheel METADATA has no Description, Project-URL or classifiers (verified); the mcp wheel (scratchpad/dist_mcp) has `Requires-Dist: ra-mcp-kansallisarkisto-lib` unpinned and a `kansallisarkisto-mcp` console script, so `uvx --from ra-mcp-kansallisarkisto-mcp kansallisarkisto-mcp` would work once both packages are on PyPI. Fetched the official registry docs via the proxy: generic-server-json.md's Python example uses `registryType: pypi`, `registryBaseUrl: https://pypi.org`, `runtimeHint: uvx`, `transport: {type: stdio}`, `environmentVariables`; quickstart.mdx line 290 states the PyPI ownership marker is an `mcp-name: <server-name>` line (or HTML comment) in the package README on PyPI. pypa/gh-action-pypi-publish with OIDC and `attestations: true` is standard. Effort M is honest for the listed pieces (about 6 small changes plus one workflow job).

*Corrections:* Two evidence errors. (1) The lancedb lower-bound claim is wrong: `inspect.signature(lancedb.index.FTS)` in the scratch venvs for lancedb 0.20.0, 0.24.0, 0.25.0 and 0.33.0 all accept `ascii_folding` and `max_token_length`, so `lancedb>=0.20` is not contradicted by dataset.py:187-188; if a bound must move it will be for `update_field_metadata` (0.33.1+, if oss-3 lands) or `list_indices().index_details`, not the FTS kwargs. (2) The registry does not read `mcpName` from `[project.urls]`: for PyPI the marker is `mcp-name: io.github.AI-Riksarkivet/kansallisarkisto-mcp` in the README rendered on PyPI — and the mcp pyproject declares no `readme`, so there is no PyPI README today; adding `readme` fixes both. Also pin the workspace dependency (`ra-mcp-kansallisarkisto-lib==0.1.0` or `>=0.1,<0.2`) in the mcp pyproject before publishing, or `uvx` can pair mismatched wheels; publish lib before mcp in the same job. The XDG fallback needs only `os.environ.get('XDG_DATA_HOME')` — no platformdirs dependency. Packaging `scripts/ingest_df.py` as a console script is trivial (it imports only the lib), but harvest.py (stdlib urllib only, 410 lines) would also have to ship for a wheel user to obtain the export without a clone.

*Sharpened:* Order stays oss-1, oss-2, then this. First PR: `readme` + `[project.urls]` + classifiers + `license-files` in both pyprojects, the `mcp-name:` line in the mcp package README, pinned lib dependency, XDG fallback with MISSING_TABLE naming the resolved path, and `kansallisarkisto-ingest` + `kansallisarkisto-harvest` console scripts in the lib (harvest.py is stdlib-only, so it ships at no dependency cost). Compat matrix: state lancedb 0.20 as the tested lower bound for FTS kwargs (measured), and run the CI matrix at 0.20 and at the lock. `server.json` per the fetched 2025-12-11 schema with `runtimeHint: uvx`; publish with `mcp-publisher login github` under `io.github.AI-Riksarkivet/`, which requires the org-level GitHub auth the docs describe.

---

