---
icon: lucide/file-search
---

# Performance engineering

Every proposal from this lens in full, followed by the three skeptics' verdicts on it.
Effort and impact are the lens's own estimates; the verdict tally is the skeptics'.

## `perf-1` — One ranking, twenty-five rows: rank on _rowid+_score, page with take_row_ids

**survived 3/3, with corrections · effort M · impact 5/5**

**What the code does today.** dataset.py:290 opens the table per call; :307-309 projects every column except FTS_COLUMN plus _score; :326 `matches = query.limit(MAX_TOTAL_COUNT).to_list()`; :336-337 counts and slices the page in Python. The docstring at :249-255 justifies dropping the FTS column (6.1 MB -> 3.3 MB) but still materialises up to 10,000 full rows as Python dicts. Measured on the 18-row fixture replicated x3000 (54,000 rows; 'Åbo' capped at 10,000 hits, warm cache, 4 CPUs): open_table 0.48 ms, schema.names 0.24 ms, `to_list` full projection 105 ms p50 / 117 p90; `to_arrow` of the same 35 ms (so ~70 ms is Arrow->dict conversion holding the GIL); projection without transcript 80 ms; `select(['_score']).with_row_id(True).to_arrow()` 10.6 ms and 120 KB; `limit(25)` full 6.8 ms; Python slice 0.00 ms; format_search_results 0.04 ms. The 10k Arrow page-set is 5.0 MB, 72% transcript. RSS over 200 capped queries through DfSearch.search: 145 -> 233 MB after one query, 451 after 25, 715 after 150, 632 after gc (allocator retention of 10k x 15-key dict pages); the narrow variant stays flat at 194 MB. Same table at x30000 (540,000 rows): full 110 ms, ranking-only 16.9 ms. Also verified: on this tie-heavy table a separate `limit(25)` does NOT equal the first 25 of `limit(10000)` and `offset(25).limit(25)` does not equal slice[25:50] — the docstring's tie-reorder concern (:244-247) is real — while re-running the same 10k query is stable, and `Table.take_row_ids` exists but returns rows in storage order, not request order.

**The gap.** A capped query costs ~100 ms and 5 MB of Arrow plus ~50 MB of transient Python objects to learn a total of '10000+' and return 25 rows; 2/3 of the time is GIL-held dict building that also serialises every concurrent query (see perf-5). At tuomiokirjat page sizes (19 GB / 7.8M ≈ 2.4 KB of text per row) the same code materialises ~25 MB of Arrow and hundreds of MB of dicts per query per worker thread.

**The proposal.** In `lancedb_fts_search` (dataset.py:290-352) keep exactly one ranked query but make it narrow, then fetch the page by row id: ```python ranked = query.select(["_score"]).with_row_id(True).fast_search().limit(MAX_TOTAL_COUNT).to_arrow() # ~120 KB total = ranked.num_rows page_ids = ranked["_rowid"][offset:offset + limit].to_pylist() page_scores = ranked["_score"][offset:offset + limit].to_pylist() rows = table.take_row_ids(page_ids).select(columns).with_row_id().to_list() if page_ids else [] by_id = {r.pop("_rowid"): r for r in rows} page = [{**by_id[i], "_score": s} for i, s in zip(page_ids, page_scores)] # restores ranking order; take_row_ids does not ``` `where` stays on the ranking query so total and page remain over the filtered set. Record shape is unchanged (formatter.py untouched). Cache the `Table` handle and column list on `DfSearch` (search_operations.py:26-28, :129-130) — open_table+schema is 0.7 ms, i.e. 5% of the new budget, and one manifest read per query on object storage. Do the same in `get_charter` (:129-131). Add `test_page_rows_come_back_in_ranking_order` beside `test_pages_tile_the_result_set_without_gaps_or_repeats` (test_search_invariants.py:133) because take_row_ids reorders. Measured two-step cost: 13.5 ms p50 at 54k rows, 20.0 ms at 540k (deep offset 9,975: 16.0 / 21.6 ms).

**Why it is once-in-a-lifetime.** It turns the engine's missing count API from a 100 ms / 5 MB tax into a 10 ms / 120 KB one without giving up the single-ranking invariant the project argued for, and it is the change that makes 'scales to 7.8M pages without changing shape' true: the per-query working set becomes independent of transcript size. It also removes the biggest GIL holder in the process, which is why perf-5's numbers move.

**How we would know.** On the x3000 replica: capped df_search p50 <= 20 ms (from 105); VmRSS after 200 capped queries <= 220 MB (from ~630); in-memory FastMCP Client throughput at concurrency 4 >= 60 calls/s (from 10.6); every existing invariant test green, plus the new ranking-order test. The docstring's '6.1 MB to 3.3 MB' sentence is replaced by a number the benchmark suite re-derives.

**Risks.** `_rowid` is stable only while no compaction/update happens between ranking and take; the tables are build-once (dataset.py:312-314) but perf-6's cleanup/optimize must run in the ingest path only. `take_row_ids` on a row deleted between the two calls raises — wrap and treat as a transient. The sync take builder takes `with_row_id()` with no argument while the FTS builder takes `with_row_id(True)`; pin both against the lancedb version. lancedb warns that `_score` auto-projection will stop; selecting it explicitly (as here) is the future-proof form.

**First step.** One PR: implement the two-step in `lancedb_fts_search`, cache the table handle in `DfSearch`, add the ranking-order test, and update the projection docstring with the measured before/after. ~60 lines changed.

**Spike.** Ingest the fixture into a temp LanceDB with `ingest_df`, replicate it x3000 by rewriting `df`/`objectID` per copy, then time (15 reps, p50) `tbl.search(MatchQuery('Åbo', column='searchable_text', operator=AND), query_type='fts').select(cols+['_score']).fast_search().limit(10000).to_list()` against `select(['_score']).with_row_id(True)...to_arrow()` + `tbl.take_row_ids(ids[:25]).select(cols).to_list()`, and sample /proc/self/status VmRSS every 25 queries for 200 queries in both modes. Expected: ~105 vs ~13 ms; ~630 vs ~194 MB.

### Skeptics

**Meticulous code reviewer — every premise re-checked against the repository and re-run on replicated fixtures (x3000 = 54,000 rows, x30000 = 540,000 rows) with lancedb 0.38.0 / fastmcp 3.4.4 / anyio 4.15.1 on Python 3.14.7** — *not refuted*, confidence 0.92

Premise verified line by line: dataset.py:290 `table = db.open_table(table_name)`, :307-309 projects every column except FTS_COLUMN plus `_score`, :326 `matches = query.limit(MAX_TOTAL_COUNT).to_list()`, :336-337 `total = len(matches)` / Python slice; docstring :243-255 (tie reorder + 6.1 MB -> 3.3 MB). API surface verified with inspect on lancedb 0.38.0: `LanceTable.take_row_ids(row_ids: list[int]) -> LanceTakeQueryBuilder` exists; `LanceFtsQueryBuilder.with_row_id(self, with_row_id: bool)` takes an argument while `LanceTakeQueryBuilder.with_row_id(self)` takes none — exactly the pin the proposal warns about. Reproduced on the x3000 replica (scratchpad/perf1.py): today's full `to_list` 131.5 ms p50, the same `to_arrow` 38.4 ms (so ~93 ms is Arrow->dict under the GIL), `select(['_score']).with_row_id(True)...to_arrow()` 11.7 ms and exactly 120,000 bytes for 10,000 rows vs 5,015,246 bytes for the full projection; two-step (ranking + `take_row_ids(page).select(cols).with_row_id().to_list()`) 16.7 ms, deep offset 9,975 16.5 ms. `take_row_ids` returns storage order, not request order (checked: `[r['_rowid'] for r in rows] == ids` is False) so the by-id reorder is necessary; the reordered page equals today's slice in both `df` ids and `_score`. `where` on the ranking query works (`language = 'latina'` -> 6,000 filtered rows). `limit(25)` != first 25 of `limit(10000)` (False) — the tie-reorder concern is real. RSS through DfSearch.search grew 369 -> 384 -> 410 -> 430 -> 501 MB over 1/25/50/100 capped queries. At 540k rows: full 159 ms, ranking-only 32.9 ms. Not covered by either audit (audit 2 finding 11 only notes 'each search materialises up to 10,000 rows' as a hosted-endpoint concern).

*Corrections:* search_operations.py citations are right (:26-28 __init__, :129-131 get_charter's open_table+schema+to_list). Absolute numbers on this box are ~25% slower than the proposal's (131 vs 105 ms today, 16.7 vs 13.5 ms two-step) — the ratios hold. One detail the snippet handles but the prose should state: `take_row_ids(...).with_row_id().to_list()` leaves a `_rowid` key in every record, so the `pop` is load-bearing for 'record shape unchanged'. Caching the Table handle on DfSearch interacts with audit-2 finding 1 (readiness should drop the cached facade on failure): the cached handle must be dropped in the same place.

*Sharpened:* Replace the single full-projection fetch in `lancedb_fts_search` with one ranked `select(['_score']).with_row_id(True).fast_search().limit(MAX_TOTAL_COUNT).to_arrow()` (120 KB flat, independent of transcript size) plus `table.take_row_ids(page_ids)` reordered by id, keeping `where` on the ranking query so total and page stay filtered; measured here 131 -> 17 ms at 54k rows and 159 -> ~35 ms at 540k, with per-query Arrow shrinking 5.0 MB -> 120 KB. Add `test_page_rows_come_back_in_ranking_order` (take_row_ids demonstrably returns storage order) and a benchmark assertion `capped_query_ms / ranking_only_ms < 2`. Rewrite the docstring paragraph at dataset.py:249-255 so the retained claim is 'the ranked set is two 8-byte columns; rows are fetched only for the page' with the re-derived numbers. Cache the Table handle and column list on DfSearch but invalidate it wherever the readiness fix drops the facade.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/OTel-SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in mind. Every claim below was re-run on this machine (4 CPUs, Python 3.14.7, lancedb 0.38.0, fastmcp 3.4.4) against x3000 (54k-row) and x30000 (540k-row) replicas of the 18-row fixture.** — *not refuted*, confidence 0.90

All cited lines are right (dataset.py:290 open_table, :307-309 projection, :326 to_list, :336-337 slice, :243-255 docstring). Reproduced the spike on a 54k-row replica (scratchpad/spike1.py): full-projection `to_list` p50 161 ms, `to_arrow` 50 ms (so ~110 ms is GIL-held dict building), ranking-only `select(['_score']).with_row_id(True).fast_search().limit(10000).to_arrow()` 11.9 ms and exactly 120,000 bytes, two-step (ranking + `take_row_ids(ids[:25]).select(cols).with_row_id().to_list()`) 17.9 ms p50; with a `where` prefilter the ranking-only query is 6.3 ms. `take_row_ids` returns storage order, not request order (confirmed: rows came back 6074, 6346, 6506 for a request of 17344, 8110, 6506), so the re-ordering dict is necessary, and the re-ordered page's first five `df` values match today's `lancedb_fts_search` exactly. RSS over 100 capped queries: today's path 344 -> 469 MB (467 after gc), two-step flat. `Table.take_row_ids(row_ids) -> LanceTakeQueryBuilder` and `LanceFtsQueryBuilder.with_row_id(bool)` exist in 0.38.0 (inspected). This is the change that makes the working set independent of transcript size, which is the whole 7.8M-page argument, and it keeps the single-ranking invariant the docstring argues for. One real problem in the design, see corrections: caching the Table handle on DfSearch is not free of consequences.

*Corrections:* Caching the `Table` handle on `DfSearch` (search_operations.py:26-28 in the proposal's numbering; actually :178-180 and :281) pins the dataset version. Measured: a handle opened at version 7 (18 rows) still reports 18 rows and 7 FTS hits after `ingest_df` re-created the table with 36 rows, and raises `RuntimeError: lance error: Not found` after `optimize(cleanup_older_than=0)` deletes the old version. Today's per-query `open_table` (0.5 ms locally) is what makes a re-ingest or late mount visible without a restart — the property audit finding 1 (readiness picking up a late table) relies on. Either keep `open_table` per query (it is 3% of the new 18 ms budget) or cache the handle and call `checkout_latest()` per query, and say so in a comment. Also: lancedb's sync `take_row_ids` builder takes `with_row_id()` with no argument while the FTS builder takes `with_row_id(True)` — both verified on 0.38.0, worth a version-pinned test.

*Sharpened:* In `lancedb_fts_search` replace the single full-projection `to_list` with one narrow ranked fetch (`select(['_score']).with_row_id(True)`, ~120 KB at the 10k cap regardless of corpus) plus a `take_row_ids` page fetch re-ordered by the ranking; measured on a 54k-row replica: capped query 161 -> 18 ms p50, RSS growth over 100 queries 125 MB -> 0. Keep `open_table` per query (or `checkout_latest()` on a cached handle) so a re-ingested or late-mounted table stays visible, which the readiness fix from audit finding 1 depends on. Add `test_page_rows_come_back_in_ranking_order` and a test that the cached path sees a re-ingest. Then, because the per-hit cost is now 12 bytes rather than a row, re-measure what raising MAX_TOTAL_COUNT (10k -> 100k) costs on the 540k replica: an honest total for tuomiokirjat ('10000+' today) may become affordable, which is the historian-facing payoff. Replace the '6.1 MB -> 3.3 MB' sentence with the ranking-only/full ratio the benchmark re-derives.

**Feasibility and honesty of effort (LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / anyio / Dagger / GitHub Actions), verified against library source and experiments on the replicated 18-row fixture** — *not refuted*, confidence 0.92

Code citations are exact: dataset.py:290 open_table, :309 select([*columns,'_score']), :315 fast_search, :326 query.limit(MAX_TOTAL_COUNT).to_list(), :337 Python slice. API claims checked in lancedb 0.38 source: LanceTable.take_row_ids(row_ids)->LanceTakeQueryBuilder at lancedb/table.py:2660; LanceFtsQueryBuilder inherits with_row_id(self, with_row_id: bool) at lancedb/query.py:1284; BaseQueryBuilder (the take builder) has the no-arg with_row_id() at query.py:2868; fast_search on the FTS builder at query.py:2047. Reproduced the spike at x3000 (54,000 rows, scratchpad/rep.py): full to_list p50 134.9 ms; select(['_score']).with_row_id(True).fast_search().limit(10000).to_arrow() 13.0 ms, 10,000 rows, columns ['_score','_rowid'], 120,000 bytes exactly; two-step page 18.4 ms; deep offset 9,975: 16.8 ms; the two-step page-0 df order equals the full path's. take_row_ids returned the 25 ids in storage (sorted) order, not request order — the proposal's reorder claim and the by_id re-sort are correct. At x30000: full 160.7 ms vs two-step 41.1 ms. RSS over 100 queries: full path 284->369 MB, two-step 369->347 MB (flat). Thread throughput (spin.py): two-step 65 q/s at 1 thread, 105 at 4; full 7.6/8.9. Effort M for ~60 lines + one test + docstring is honest.

*Corrections:* take_row_ids on a non-existent row id does NOT raise in 0.38: tbl.take_row_ids([10**12]).select(cols).to_list() returned zero rows silently. So the 'wrap and treat as transient' guidance is wrong — the failure mode is a KeyError in by_id[i]; handle missing ids explicitly (drop them from the page, or re-run the ranking once). The <=20 ms p50 target is borderline on this machine (18.4 ms at x3000, 41 ms at x30000); state it as a ratio. RSS growth I measured is ~85 MB/100 full queries, smaller than the 145->715 MB claimed (their run went through DfSearch.search+formatter for 200 queries), so quote a per-100-query slope rather than an absolute.

*Sharpened:* Replace the full-projection to_list at dataset.py:326 with: one ranking query projected to ['_score'] plus _rowid (with_row_id(True)) fetched as Arrow (exactly 16 bytes/hit, 120 KB at the cap), then table.take_row_ids(page_ids).select(columns).with_row_id() for the <=100-row page, re-sorted to ranking order because take_row_ids returns storage order; tolerate ids that come back missing rather than catching an exception. Gates as ratios so they survive machines: two-step p50 <= 0.25x the full-projection p50 (measured 0.14x at 54k rows, 0.26x at 540k) and Arrow bytes per capped query <= 16 x MAX_TOTAL_COUNT; RSS slope <= 10 MB/100 queries (measured full path +85, two-step flat). Cache the Table handle in DfSearch (search_operations.py:26-28 and get_charter :129). Keep the test test_pages_tile_the_result_set_without_gaps_or_repeats (test_search_invariants.py:133) and add the ranking-order test.

---

## `perf-2` — Answer `initialize` before lancedb is imported: lazy engine imports and a background boot probe, pinned by a test

**survived 3/3, with corrections · effort S · impact 4/5**

**What the code does today.** dataset.py:20-21 import `lancedb.index` and `lancedb.query` at module top, which defeats the deliberate lazy `import lancedb` inside `get_lancedb` (dataset.py:118): `import ra_mcp_kansallisarkisto_mcp.tools` puts `lancedb` and `lance_namespace_urllib3_client` in sys.modules (verified). server.py:218 runs `log_table_status()` (and the one-row probe, :191) before `run()` at :224/:231, so nothing is read from stdin until the whole import tree and the probe are done. Measured with `-X importtime`: server import tree 2.3 s; `lancedb` 1.0-1.15 s, of which 831 ms self-time is `lance_namespace_urllib3_client` (a 41,232-line generated REST client with 148 model modules, imported unconditionally by lancedb/db.py:35 and never used by a local table); fastmcp 455 ms + mcp 267 ms (the server imports `mcp.client.session`, 158 ms). Floor without lancedb (fastmcp + pydantic_settings + otel API + pyarrow): 540-610 ms. Real stdio session via `fastmcp.Client(StdioTransport)`: spawn -> initialize 1.58-1.87 s; tools/list 5 ms; first df_search 22-37 ms on 18 rows, 201 ms on 540k rows; connect 3 ms; list_tables 1 ms. So ~90% of time-to-first-answer is imports, and half of those are for a feature this server cannot use.

**The gap.** A laptop stdio client waits ~1.7 s for the handshake, and the design comment 'stdio on a laptop must cost nothing' is not measured anywhere. The lazy connection in `get_lancedb` is a good idea that a later import silently undid — exactly the kind of drift this codebase pins elsewhere (test_search_invariants.py:42-56 asserts the index config against the built index).

**The proposal.** (1) Move `from lancedb.index import FTS, Bitmap, BTree` into `build_fts_index`/`build_scalar_indexes` and `from lancedb.query import FullTextOperator, MatchQuery` into `lancedb_fts_search` (function-local imports are a dict lookup after the first call). (2) In `server.main()` start `log_table_status()` on a daemon thread after logging/telemetry setup so FastMCP starts reading stdin immediately; `get_search()` already serialises first use with a lock (tools.py:293), so a tool call arriving mid-import simply waits. (3) Pin it: `test_importing_the_server_does_not_import_lancedb` runs `python -c "import ra_mcp_kansallisarkisto_mcp.tools, sys; assert 'lancedb' not in sys.modules"` in a subprocess, and `bench_coldstart` (perf-3) records spawn->initialize. (4) File the upstream lancedb issue with the number: eager `lance_namespace` import costs 830 ms on every local-only process; ask for a lazy import in `lancedb/db.py`. (5) Record in df_tool.py's module docstring that `-X importtime` inflates by ~30% and the real handshake floor is fastmcp's ~0.6 s.

**Why it is once-in-a-lifetime.** It makes the stated laptop promise true and measured: the handshake drops to fastmcp's floor and the engine loads during the seconds-to-minutes a human spends before the first question. It is also the first time a project of this kind will have quantified where a stdio MCP server's cold start actually goes and sent the fix upstream rather than absorbing it.

**How we would know.** spawn -> initialize <= 0.8 s (from 1.6-1.9 s) on the same machine; `lancedb` absent from sys.modules after importing `tools`; first df_search after a cold spawn unchanged (<= 40 ms on the fixture); an upstream lancedb issue number recorded in docs/development/index.md.

**Risks.** The background import competes for the GIL with the handshake — but the handshake is milliseconds of Python and the import is 1 s, so the overlap is with client latency, not a free lunch. A tool call that arrives during the import pays the full second once (same as today, just later). The test must run in a subprocess because pytest's own fixtures import lancedb. If a future FastMCP reads stdin before `run()`, step 2 becomes redundant, not harmful.

**First step.** One PR: the two import moves, the daemon-thread boot probe, the subprocess test, and a sentence in the df_tool docstring; then the upstream issue.

**Spike.** `python -X importtime -c 'import ra_mcp_kansallisarkisto_mcp.server' 2>imp.txt` then sort column 2 — lance_namespace_urllib3_client ~830 ms self, fastmcp ~455 ms. `python -c 'import sys, ra_mcp_kansallisarkisto_mcp.tools; print("lancedb" in sys.modules)'` -> True today. Time `fastmcp.Client(StdioTransport(command='uv', args=['run','--no-sync','kansallisarkisto-mcp'], env={'KA_LANCEDB_URI': <fixture db>}))` from construction to `__aenter__` return: ~1.7 s.

### Skeptics

**Meticulous code reviewer — every premise re-checked against the repository and re-run on replicated fixtures (x3000 = 54,000 rows, x30000 = 540,000 rows) with lancedb 0.38.0 / fastmcp 3.4.4 / anyio 4.15.1 on Python 3.14.7** — *not refuted*, confidence 0.90

dataset.py:20-21 `from lancedb.index import FTS, Bitmap, BTree` / `from lancedb.query import FullTextOperator, MatchQuery` at module top — confirmed; the lazy `import lancedb` at :118 is therefore dead as a deferral. `python -c 'import sys, ra_mcp_kansallisarkisto_mcp.tools; print("lancedb" in sys.modules, "lance_namespace_urllib3_client" in sys.modules)'` -> True True. `-X importtime` of the server: 2.093 s total; `lancedb` subtree 1,199,960 us; `lance_namespace` 884,712 us of which `lance_namespace_urllib3_client` 883,283 us; `lancedb/db.py:35` is `from lance_namespace import (...)`, unconditional. Floor without lancedb (fastmcp, pydantic_settings, opentelemetry.trace, pyarrow, pydantic) measured 0.62 s wall. Real stdio session via `fastmcp.Client(StdioTransport(...))` against the x3000 table: spawn -> initialize 2.46 / 3.07 / 2.58 s on this box, tools/list 6-9 ms, first df_search 306-509 ms — same shape as the proposal's 1.6-1.9 s. `get_search()` double-checked lock exists (tools.py:75, :85-93) and `get_lancedb` imports lancedb inside `_connections_lock` (dataset.py:115-121), so a tool call arriving mid-import blocks on the lock as claimed. Nothing else in packages/*/src imports lancedb eagerly (grep: only dataset.py:20-21; ingest.py and search_operations.py are TYPE_CHECKING-only). No audit finding covers cold start.

*Corrections:* Line numbers are off: `log_table_status()` is called at server.py:209 (not :218), `run()` at :215/:222 (not :224/:231), the probe at :182 (not :191); the first-use lock is tools.py:72-75/:85-93 (not :290-293). Because `lancedb/__init__.py` imports `._lancedb`, `.db` and friends, moving the two imports into functions defers the whole 1.2 s to the first search or index build — correct, but the first df_search after a cold spawn then pays it unless the daemon-thread probe has already run, so the 'first df_search unchanged' outcome depends on step 2 landing with step 1. `test_server.py` tests the boot probe synchronously (e.g. `test_boot_probe_runs_a_real_query_on_a_healthy_table`); a daemon thread needs a join/handle for those tests.

*Sharpened:* Make the handshake independent of the engine: function-local `from lancedb.index import ...` in build_fts_index/build_scalar_indexes and `from lancedb.query import ...` in lancedb_fts_search; start `log_table_status()` on a named daemon thread returned by `main()` (so tests can join it) before `kansallisarkisto_mcp.run()`; pin with a subprocess test that `lancedb` is absent from sys.modules after importing `tools` AND with a cold-start benchmark of spawn->initialize (measured here 2.5-3.1 s; target the ~0.6 s fastmcp+pyarrow floor plus process spawn). Also record the split in docs/development/observability or deployment: lancedb 1.20 s of which lance_namespace_urllib3_client 0.88 s (a REST client a local table never uses), fastmcp+mcp ~0.45 s, and file the upstream lancedb issue against db.py:35 with that number.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/OTel-SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in mind. Every claim below was re-run on this machine (4 CPUs, Python 3.14.7, lancedb 0.38.0, fastmcp 3.4.4) against x3000 (54k-row) and x30000 (540k-row) replicas of the 18-row fixture.** — *not refuted*, confidence 0.70

Confirmed: dataset.py:20-21 import `lancedb.index`/`lancedb.query` at module top and `import ra_mcp_kansallisarkisto_mcp.tools` puts `lancedb` and `lance_namespace_urllib3_client` in sys.modules. `-X importtime` here: server 2.05 s total, lancedb subtree 1.20 s of which `lance_namespace` 0.88 s (lancedb/db.py:35 and :44 import it unconditionally). Plain `import lancedb` 1.22 s; fastmcp+pydantic_settings+otel-api+pyarrow floor 0.53 s. Real handshake via `fastmcp.Client(StdioTransport)`: spawn -> initialize 2.60 s on this machine. I applied the proposal to a scratch copy (function-local imports in build_fts_index/build_scalar_indexes/lancedb_fts_search, `log_table_status` on a daemon thread): `lancedb` is absent from sys.modules after importing tools, and spawn -> initialize fell to 1.08-1.41 s. So the mechanism works and the subprocess test would pin a real drift. GitHub search of lancedb/lancedb issues for lazy/slow import finds nothing on `lance_namespace`, so the upstream issue is genuinely new. It is a small, in-style change (a measured decision next to the code) and the lib stays MCP-free.

*Corrections:* (1) The measurable outcome 'first df_search after a cold spawn unchanged (<= 40 ms)' is false when the call arrives immediately: on the patched tree the first search took 1.27-1.80 s (from ~350 ms) because it waits for the background import/probe; the cost moves, it does not vanish — the risks section admits this, the outcome line must too. (2) 'stdio on a laptop must cost nothing' is the OpenTelemetry promise (observability.md:35, lib telemetry.py:6, README env table), not a cold-start promise; the proposal should state a new promise rather than cite that one. (3) Line numbers: server.py has 114 lines — `log_table_status()` is called at :94, the probe is :67, `run()` is :100-107; the `get_search` lock is tools.py:189 and :200-201, not :293. (4) `get_search()` lists tables but the boot probe also runs a real search; a daemon-thread probe that is still running when the process exits (a client that connects and disconnects) will be killed mid-query — harmless but must be noted. (5) `-X importtime` inflation is ~25% here (2.05 s vs ~1.6 s real), not 30%.

*Sharpened:* Two function-local imports in dataset.py and a subprocess test `test_importing_the_server_does_not_import_lancedb` pin the drift now, at zero risk; the daemon-thread boot probe is a second, separate decision to be recorded honestly: it moves ~1.2 s from before `initialize` to the first tool call if that call arrives within a second, which is a good trade for a human client and a neutral one for a scripted client (mcp_smoke.py). State the new promise where it lives — `spawn -> initialize <= fastmcp floor + ~0.3 s`, measured by bench_coldstart — and file the lancedb issue with the number (`lance_namespace` = 72% of `import lancedb`, unused by local tables); none exists today.

**Feasibility and honesty of effort (LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / anyio / Dagger / GitHub Actions), verified against library source and experiments on the replicated 18-row fixture** — *not refuted*, confidence 0.90

Verified: `import ra_mcp_kansallisarkisto_mcp.tools` leaves both 'lancedb' and 'lance_namespace_urllib3_client' in sys.modules. The only eager lancedb imports in src are dataset.py:20-21 (ingest.py:20 and search_operations.py:12 are under TYPE_CHECKING), so the two proposed moves are sufficient. lancedb/db.py:35 does `from lance_namespace import (...)` unconditionally; the generated client is 58,217 lines of Python in this venv. -X importtime on `import ra_mcp_kansallisarkisto_mcp.server` (3 runs): 2.04 s cumulative; lancedb 1.19 s of which lance_namespace 0.88 s cumulative (largest self-times: lance_namespace_urllib3_client.api.metadata_api 222 ms, table_api 222 ms); fastmcp 0.41 s. Wall clock: server import 1.95 s, `import lancedb` 1.19 s, `import lance_namespace_urllib3_client` alone 0.98 s, floor (fastmcp+pydantic_settings+otel api+pyarrow+mcp.client.session) 0.52 s. Real stdio session via fastmcp.Client(StdioTransport(uv run --no-sync kansallisarkisto-mcp)) on the x300 db: spawn->initialize 1.99/2.07/2.08 s, tools/list 4-5 ms, first df_search ~50 ms (cold.py). uv run overhead is 32 ms. FastMCP dispatches sync tools through anyio worker threads and get_search() is lock-guarded (tools.py:75, :87), so a background boot probe and a mid-import tool call serialise correctly; Python's per-module import lock makes the worker thread wait on the daemon thread's import rather than double-importing. Effort S is honest for the code; the upstream issue is extra but small.

*Corrections:* Line numbers are off: the lock is tools.py:75/:87 (not :293); log_table_status() is called at server.py:96 and run() at :103/:110 (not :218/:224/:231). The '-X importtime inflates by ~30%' claim did not reproduce: 2.04 s under importtime vs 1.95 s wall, ~5%. The <=0.8 s handshake target is tight: the measured floor is 0.52 s of imports + 32 ms uv + interpreter start + FastMCP construction and its stderr banner; commit to <=0.9 s, or better to 'within 150 ms of the fastmcp-only floor measured in the same bench run'.

*Sharpened:* Move the two lancedb imports at dataset.py:20-21 into build_fts_index/build_scalar_indexes and lancedb_fts_search; run log_table_status() on a daemon thread after init_telemetry() in server.main() so FastMCP reads stdin at once. Pin with a subprocess test asserting 'lancedb' not in sys.modules after importing tools, and with bench_coldstart recording spawn->initialize both today (2.0 s measured here) and after (target: fastmcp floor + <=150 ms). File the upstream lancedb issue quoting 0.88 s / 58k lines of lance_namespace_urllib3_client imported by lancedb/db.py:35 for local-only use, and record the issue number in docs. Also measure and state the one-time cost a tool call pays if it arrives during the background import (~1.2 s).

---

## `perf-3` — A benchmark suite CI gates on: replicated fixture at a scale knob, p50/p99 per tool through the in-memory client, RSS, import time, ingest rows/s and index build

**survived 3/3, with corrections · effort L · impact 5/5**

**What the code does today.** There is no performance measurement anywhere in the repository: .dagger/test.go runs `pytest -q` only; the Makefile has no bench target; ci.yml runs checks/test/test-mcp/test-server. Every number in the code comments (6.1 MB -> 3.3 MB at dataset.py:252, 'cheap after the first call' at tools.py:319-321, 'a few milliseconds on a mounted table' in audit finding 1) was measured once by hand and is unverified since. The only in-repo data is 18 rows (conftest.py:249-254); corpus-only tests are gated on `.data/df/df.jsonl.gz` (test_search_invariants.py:215-216) and skip in CI. conftest.py:276-278 makes `df_table` function-scoped, so each of the ~100 search tests re-ingests and rebuilds five indexes (the 10 s suite is mostly ingest). Measured facts the suite would have caught: capped query 105 ms with 70 ms of dict building; RSS 145 -> 715 MB over 150 queries; throughput 13 q/s flat from 1 to 8 threads; import 2.3 s; index-build peak RSS 991 MB at 540k rows; ingest 27.6-34.7k rows/s. pytest-benchmark 5.3.0 installs on Python 3.14.7 (verified).

**The gap.** The project's method is 'measure, then write the number next to the code' — but the numbers are frozen prose. A regression in any of them (a lancedb bump changing `to_list` cost, a projection change, a new eager import) is invisible until an operator notices.

**The proposal.** Add `packages/kansallisarkisto-lib/benchmarks/` (excluded from the default pytest run via `--ignore`, selected by `make bench`): - `conftest.py`: session-scoped `scaled_db` built by `replicate_fixture(n)` — a helper in `ra_mcp_kansallisarkisto_lib.testing` that rewrites `df` to 1..18n and suffixes `objectID`, writes JSONL, and runs the real `ingest_df`. `KA_BENCH_SCALE` default 3000 (54k rows, ~4 s to build; every fixture term is capped at 10k except single-row terms at 3,000 hits, so both branches of `total_is_capped` are exercised). - `bench_search.py`: scenarios {capped 'Åbo', uncapped single-row term, every-doc 'df', AND vs OR 'konung Åbo', phrase '"de ecclesia"', fuzzy=1 'Åbo' and 'bref', filters: bitmap equals, btree year overlap, LIKE issuingplace, deep offset 9975, get_charter}, each direct via `DfSearch` and via `fastmcp.Client(kansallisarkisto_mcp)` in-memory (includes pydantic validation, threadpool hop, formatter; measured dispatch overhead 5-6 ms). Report p50/p99 over 200 calls. - `bench_ingest.py`: rows/s for parse+create_table, FTS build seconds, scalar seconds, `ru_maxrss` delta, on-disk bytes per row (data vs _indices), at scale. - `bench_memory.py`: VmRSS delta after 100 capped queries (tracemalloc cannot see Arrow). - `bench_coldstart.py`: subprocess `-X importtime` total and lancedb subtree; spawn->initialize via StdioTransport; first query after `os.posix_fadvise(fd,0,0,POSIX_FADV_DONTNEED)` over the table files (measured 215 vs 99 ms warm at 540k). - `bench_contention.py`: q/s at 1/4 threads and ms/query beside a CPU-bound Python thread (perf-5). Numbers: pytest-benchmark `--benchmark-json` -> `benchmarks/results/<sha>.json` as a workflow artifact; a committed `benchmarks/baseline.json` regenerated by `make bench-baseline`. Gates that survive noisy runners: pytest-benchmark `--benchmark-compare-fail=median:50%` on absolutes (loose, min-of-3-rounds), plus hard machine-independent ratios asserted in code: RSS growth < 50 MB per 100 queries; capped-query time / ranking-only time < 2; `lancedb` share of import < 60% until perf-2 lands then `lancedb not in sys.modules`; index-build peak RSS per MB of text < a stated constant. Run in a Dagger `Bench` function (`.dagger/bench.go`) on PRs touching dataset.py, ingest.py, search_operations.py, df_tool.py, formatter.py or uv.lock (paths filter), and nightly on main appending to a `benchmarks` branch that `docs/development/performance.md` renders — the same measured-numbers-next-to-code style, now regenerated. Side benefit: make `df_table` session-scoped for read-only tests and the unit suite roughly halves.

**Why it is once-in-a-lifetime.** Archival MCP servers do not ship benchmark suites; this one would carry its measured claims as executable assertions, so 'every design decision measured on the corpus' becomes a property of CI rather than of the author's memory. The next corpus gets its numbers for free by changing KA_BENCH_SCALE and the fixture.

**How we would know.** `make bench` produces a JSON with >= 20 named scenarios in < 3 min at scale 3000; CI fails a PR that reintroduces the full-projection `to_list` (median +400%, RSS +400 MB) or an eager lancedb import; `docs/development/performance.md` shows a per-commit history with at least import time, capped/uncapped p50/p99, ingest rows/s, index build s, peak RSS.

**Risks.** Runner noise: GitHub-hosted runners vary 2x, so absolute gates must be loose and the sharp gates must be ratios; Dagger adds container start time. The replicated fixture has a tiny vocabulary, so posting-list and term-dictionary costs are understated (perf-7 addresses fidelity). pytest-benchmark's stats machinery is heavier than needed — a 100-line harness writing the same JSON is a valid alternative and avoids a dev dependency.

**First step.** PR 1: `replicate_fixture(n)` helper + `bench_search.py` with five scenarios via DfSearch and the in-memory client, `make bench`, JSON output, no gate. PR 2: baseline + Dagger function + ratio gates. PR 3: ingest/memory/coldstart/contention files and the docs page.

**Spike.** Already runnable: replicate the fixture x3000 (writes 57.7 MB JSONL, ingests in ~3.5 s incl. FTS), then a loop timing `DfSearch.search` and `Client(kansallisarkisto_mcp).call_tool('df_search', ...)` for the scenario list, printing p50/p99 and VmRSS. Compare against x300 to see which scenarios scale with rows (every-doc 12.6 -> 31 ms, fuzzy 14.8 -> 65.7 ms, LIKE 16 -> 57 ms for x10 rows) and which do not (capped rank ~10 -> 17 ms).

### Skeptics

**Meticulous code reviewer — every premise re-checked against the repository and re-run on replicated fixtures (x3000 = 54,000 rows, x30000 = 540,000 rows) with lancedb 0.38.0 / fastmcp 3.4.4 / anyio 4.15.1 on Python 3.14.7** — *not refuted*, confidence 0.85

No benchmark exists: `grep -rni bench` over .github/workflows, Makefile, .dagger and pyproject files returns nothing; .dagger/test.go runs only `uv run pytest --tb=short -q`; ci.yml runs `checks`, `test`, `test-mcp`, `test-server` (lines 22/30/43/54). The prose numbers cited are real and unverified by any test: dataset.py:252 '6.1 MB to 3.3 MB', tools.py:101 'Cheap after the first call', audit.md finding 1 'a few milliseconds on a mounted table'. `df_table` is a plain `@pytest.fixture` (function scope) at conftest.py:39-41 and `search` at :44-46. Suite: 194 passed, 7 skipped in 9.48 s; fixture ingest measured 36-55 ms, so ~100 re-ingests are roughly half the wall time. `uv pip install --dry-run pytest-benchmark==5.3.0` resolves on 3.14.7 (+py-cpuinfo2). Scaling measurements in the proposal are reproduced in direction on my replicas (see perf-1/perf-7 verdicts). The two audits contain no performance measurement.

*Corrections:* conftest.py is 46 lines, so ':249-254' and ':276-278' are wrong — the fixture comment is :12-16 and `df_table` is :39-41 (function-scoped by default). 'the 10 s suite is mostly ingest' overstates: the slowest single setup is 0.23 s and fixture ingest is ~40-55 ms, so session-scoping saves on the order of half, not most. The '145 -> 715 MB over 150 queries' figure was not reproducible in isolation here (369 -> 501 MB over 100 queries in a process that had already done other work); the growth is real, the absolute is machine/allocator-dependent, which is exactly why the proposal's own advice to gate on ratios and deltas rather than absolutes is right.

*Sharpened:* Add `packages/kansallisarkisto-lib/benchmarks/` with a session-scoped `replicate_fixture(n)` (KA_BENCH_SCALE default 3000; 57.7 MB JSONL, ~4.5 s ingest measured) and scenarios wired to the numbers this review reproduced so the first baseline is not hand-typed: capped 'Åbo' full-projection 131 ms vs ranking-only 11.7 ms; fuzzy=1 164 ms; LIKE 146 ms; phrase 58 ms; get_charter 5.6 ms; server import 2.09 s with lancedb 1.20 s; FTS build 1.3-1.5 s / +165 MB at 54k and 14.5 s / +830 MB at 540k. Gate on machine-independent ratios (RSS delta per 100 queries, capped/ranking-only ratio, lancedb share of import) in a Dagger `Bench` function with a paths filter, and make `df_table` session-scoped for read-only tests as the side benefit.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/OTel-SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in mind. Every claim below was re-run on this machine (4 CPUs, Python 3.14.7, lancedb 0.38.0, fastmcp 3.4.4) against x3000 (54k-row) and x30000 (540k-row) replicas of the 18-row fixture.** — *not refuted*, confidence 0.70

Confirmed there is no performance measurement anywhere: Makefile has only `test`/`test-mcp`, .dagger/test.go:39 runs `pytest --tb=short -q`, no bench directory. The suite here runs in 7.6 s and `--durations` shows per-test setup of 0.05-0.07 s (the fixture ingest plus five index builds) across ~150 tests — so 'mostly ingest' is right, and `df_table` is function-scoped (lib tests/conftest.py:38-40; mcp tests/conftest.py:22-26 likewise). The numbers the proposal says a suite would have caught are the ones I reproduced for perf-1/2/4/5/6, so the value is real: this project's method is 'measure, then write the number next to the code', and today every such number is frozen prose that a lancedb bump silently invalidates. What I would push back on is the shape: a committed baseline.json with `--benchmark-compare-fail` on GitHub-hosted runners is the classic flaky gate, pytest-benchmark is a new dev dependency in a project that treats every dependency as supply-chain surface, and `ra_mcp_kansallisarkisto_lib.testing` would ship a benchmark helper inside the library users install. The proposal itself concedes the 100-line harness alternative; that is the version a maintainer merges.

*Corrections:* Line references: 'cheap after the first call' is tools.py:101-103, not :319-321; the fixture is lib tests/conftest.py:38-40 (and mcp tests/conftest.py:22-26), not conftest.py:249-254/:276-278; the suite is ~7.6-10 s, of which setup is the majority. Session-scoping `df_table` needs care: test_ingest.py and any test that rebuilds indexes must keep their own db (they mutate), so it is a 'read-only fixture' split rather than a scope flip.

*Sharpened:* A `benchmarks/` directory (outside both packages, ignored by default pytest) with a ~100-line harness and no new dependency: `replicate_fixture(n)` writing JSONL and running the real `ingest_df`, `KA_BENCH_SCALE` defaulting to 3000, the scenario list via `DfSearch` and the in-memory `fastmcp.Client`, JSON output per commit as a CI artifact and a rendered `docs/development/performance.md`. Gate in CI only on machine-independent ratios expressed as ordinary tests: `lancedb not in sys.modules` after importing tools, capped-query time / ranking-only time < 2, VmRSS growth over 100 capped queries < 50 MB, FTS-build peak RSS per MB of text below the constant perf-4 measures. Absolute p50/p99 go to the history page, never to a fail gate. Do it in that order: harness + JSON first, ratio tests second, the nightly page third; the read-only-fixture split is a separate small PR that halves the unit suite.

**Feasibility and honesty of effort (LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / anyio / Dagger / GitHub Actions), verified against library source and experiments on the replicated 18-row fixture** — *not refuted*, confidence 0.85

Confirmed there is no performance measurement anywhere: Makefile has no bench target, .dagger/test.go runs `uv run pytest --tb=short -q`, .github/workflows/ci.yml has no path filters. conftest.py df_table (packages/kansallisarkisto-lib/tests/conftest.py:39) is function-scoped and re-ingests per test; full suite runs in 7.5 s (194 passed, 7 skipped). `uv pip install --dry-run pytest-benchmark` resolves 5.3.0 (+py-cpuinfo2) on Python 3.14.7. The in-memory `Client(tools.kansallisarkisto_mcp)` pattern already exists at packages/kansallisarkisto-mcp/tests/test_tools.py:21, so the through-the-server scenarios reuse it. Replicating the fixture is runnable here: x300 ingests in 0.47 s, x3000 in a few seconds, x30000 (540k rows) in tens of seconds with a 14 s FTS build. The proposed ratio gates are discriminating on my numbers: full-projection RSS slope +85 MB/100 queries vs flat for the two-step; capped/ranking-only ratio 1.4x at x3000. FastMCP dispatch overhead and posix_fadvise cold-cache trick are standard. Dagger part is not runnable here (no CLI) but a Go function mirroring test.go is routine. Effort L across three PRs is honest.

*Corrections:* conftest.py is 46 lines; the cited lines 249-254 and 276-278 do not exist (fixture at :39, db at :34). The suite is 7.5 s, not 10 s. The 'capped-query time / ranking-only time < 2' gate is noisy at 540k rows (I measured ranking-only 61 ms vs two-step 41 ms in one run because the first scenario warmed the cache); gate on bytes per query and on two-step vs full-projection instead. A nightly workflow that pushes to a `benchmarks` branch needs `contents: write` scoped to that one job to keep the Scorecard token-permissions check green.

*Sharpened:* benchmarks/ with a session-scoped replicate_fixture(n) helper (KA_BENCH_SCALE=3000, ~4 s to build), scenarios both via DfSearch and via the existing in-memory fastmcp.Client, JSON output per commit, and machine-independent ratio gates: Arrow bytes per capped query <= 16 x MAX_TOTAL_COUNT (perf-1), RSS slope <= 10 MB per 100 queries, 'lancedb' not in sys.modules after importing tools (perf-2), FTS build peak-RSS per MB of data reported (perf-4). Run in a Dagger Bench function on PRs touching the spine files and nightly on main; render the history into docs/development/performance.md. Making df_table session-scoped for read-only tests is a separate, safe PR.

---

## `perf-4` — The FTS index build is the one non-streaming step: bound it, measure it per row, and ship pre-built tables so operators never build

**survived 3/3, with corrections · effort L · impact 4/5**

**What the code does today.** ingest.py:271-279 streams the table write and its docstring (:276-277) says 'peak memory is one batch rather than the whole corpus' — true of the write only. ingest.py:281 then calls `build_fts_index` once over the whole table; dataset.py:169-191 configures `FTS(language, stem, remove_stop_words=False, with_position=True, ascii_folding, max_token_length=64)` — there is no memory, partition or worker knob on that config, and lance is bundled inside `lancedb._lancedb` with no pylance surface to reach one. Measured `ru_maxrss` for `build_fts_index` alone: 138 -> 298 MB at 54k rows (41 MB table), 138 -> 991 MB at 540k rows (274 MB data); the ~850 MB delta is ~2.3x the data bytes, with positions on. Build time 1.3 s and 10.9-12.9 s (linear so far). df's 6,876 rows are trivial; tuomiokirjat is 7,835,557 pages and 19 GB of text, so a linear extrapolation is ~40 GB of peak RSS on the ingest machine — more than the 15 GB laptop this was measured on and any GitHub runner. `mode='overwrite'` (:279) plus four `replace=True` index builds also mean a re-ingest leaves the previous data and index files on disk (see perf-6).

**The gap.** The project promises the three corpora share one shape; the ingest path is fine for df and voudintilit but has an unmeasured, unbounded memory step that would fail on the corpus that is 99% of the pages, and nothing says so.

**The proposal.** (1) Measure the slope: `bench_ingest` (perf-3) records peak RSS per MB of `searchable_text` at scales 300/3000/30000 and the docs state the formula and the machine class needed. (2) Find the lever, in order: check whether lancedb's FTS builder is per-fragment by ingesting with smaller fragments (lance `max_rows_per_file`, if reachable through `create_table` kwargs) and re-measuring peak; if not, try building the index per year-partitioned table and searching N tables from the spine merging by `_score` (argues against the single-table shape — record the trade-off); if neither, open the upstream issue with the measured slope and ask for a bounded/spilling inverted-index build. (3) Independently of the lever: publish the built table as a release artifact — `dagger call build-table` produces `df.lance` as a tarball (or OCI artifact) with its digest recorded in the release next to the SBOM, and `docs/development/deployment.md` documents mounting it, so the 'empty image + mounted table' story does not require every operator to own an ingest machine. (4) Turn the ingest docstring's memory claim into two sentences: write is one batch; index build is proportional to text, with the measured constant.

**Why it is once-in-a-lifetime.** It is the difference between 'the shape scales' as a belief and as a measured property with a published constant — and shipping the table as a verifiable artifact extends the project's supply-chain rigour (cosign, SLSA, SBOM) to the data, which no comparable archive server does.

**How we would know.** A published number 'peak RSS = a + b x text MB' with b measured at three scales within 20% of each other; a documented ingest for voudintilit (98,945 rows) that fits in 2 GB; either a bounded build path for 7.8M pages demonstrated at 1M synthetic rows under 4 GB, or an upstream issue plus a documented partition strategy; a `df.lance` artifact with a digest in the v0.2 release.

**Risks.** The constant comes from replicated text with a tiny vocabulary; real OCR text has a far larger term dictionary and more positions per document, so the true slope is probably worse, not better. Partitioning changes ranking (BM25 statistics per partition) and pagination semantics — it must not be adopted without the invariant tests running across partitions. Artifact size: ~4-6 GB for tuomiokirjat is fine for a release asset but not for a git-lfs habit.

**First step.** PR: `bench_ingest` peak-RSS-per-MB at three scales, the corrected ingest docstring, and a table in deployment.md; then the fragment-size experiment.

**Spike.** Rebuild the FTS index on the x300, x3000 and x30000 replicas with `build_fts_index(db, 'df')` and read `resource.getrusage(RUSAGE_SELF).ru_maxrss` before/after each (fresh process per scale): expect ~+160 MB, ~+850 MB and a linear fit; then re-create the x30000 table with smaller fragments if `create_table` accepts a rows-per-file option and see whether the peak moves.

### Skeptics

**Meticulous code reviewer — every premise re-checked against the repository and re-run on replicated fixtures (x3000 = 54,000 rows, x30000 = 540,000 rows) with lancedb 0.38.0 / fastmcp 3.4.4 / anyio 4.15.1 on Python 3.14.7** — *not refuted*, confidence 0.70

ingest.py streams the write (RecordBatchReader at :143, docstring :141-142 'peak memory is one batch') and then calls `build_fts_index` once over the whole table (:146); dataset.py:169-191 configures FTS with no memory argument — true of THIS code. Peak RSS reproduced in fresh processes (scratchpad/perf4b.py, `ru_maxrss`): build_fts_index alone 138 -> 309 MB at 54k rows (proposal: 298) and 144 -> 973 MB at 540k (proposal: 991), build 1.3 s and 14.5 s; whole ingest at 540k peaks at 1,130 MB. `create_table` signature has no rows-per-file knob (verified: name, data, schema, mode, exist_ok, on_bad_vectors, fill_value, embedding_functions, namespace_path, storage_options, data_storage_version, enable_v2_manifest_paths). Neither audit covers ingest memory. BUT the claim 'there is no memory, partition or worker knob on that config, and ... no pylance surface to reach one' is wrong for the pinned lancedb 0.38.0, see corrections.

*Corrections:* `lancedb.index.FTS` has `memory_limit: int | None` ('total memory limit in MiB for the local FTS build stage ... divided evenly among indexing workers ... build-only') and `num_workers` (lancedb/index.py:172-180, :208-209), and `strings` on `lancedb/_lancedb.abi3.so` shows build-time env knobs LANCE_FTS_NUM_SHARDS, LANCE_FTS_PARTITION_SIZE, LANCE_FTS_WRITE_QUEUE_SIZE, LANCE_FTS_POSTING_BATCH_ROWS, LANCE_FTS_POSTING_GROUP_MAX_TOKENS, plus LANCE_MEM_POOL_SIZE / LANCE_BYPASS_SPILLING for the datafusion pool. Measured at 540k rows in fresh processes: default 973 MB peak; memory_limit=64 -> 746 MB (10.7 s); memory_limit=16 -> 706 MB (10.0 s); memory_limit=64 + num_workers=1 -> 783 MB. So a lever exists and is reachable from build_fts_index today, but it is not a bound: a 16 MiB limit still peaked ~560 MB above baseline on 274 MB of data, so the 'find the lever' step must start by sweeping these knobs rather than by guessing at fragment sizes. Line references are off (create_table is ingest.py:144, the index builds :146-152, the docstring :141-142; the file is 152 lines).

*Sharpened:* Measure `peak RSS = a + b x text MB` for build_fts_index at scales 300/3000/30000 under four settings — default, FTS(memory_limit=16), FTS(memory_limit=64, num_workers=1), and LANCE_FTS_NUM_SHARDS/LANCE_FTS_PARTITION_SIZE — and expose `memory_limit`/`num_workers` as build_fts_index keyword arguments with the measured trade-off (here: -25% peak, -30% build time at 540k) written next to them; if none bounds the build for a 7.8M-row synthetic, file the upstream issue with the slope. Independently, ship the built `df.lance` as a digest-recorded release artifact so operators never build, and rewrite the ingest docstring to say 'write: one batch; index: proportional to text, constant b measured'.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/OTel-SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in mind. Every claim below was re-run on this machine (4 CPUs, Python 3.14.7, lancedb 0.38.0, fastmcp 3.4.4) against x3000 (54k-row) and x30000 (540k-row) replicas of the 18-row fixture.** — *not refuted*, confidence 0.75

The memory claim is real and reproduced: ingesting the 540k-row replica took `ru_maxrss` 138 -> 1185 MB (35.8 s), and `build_fts_index` alone in a fresh process 144 -> 294 MB at 54k rows and 144 -> 900 MB at 540k rows (data 547 MB, `_indices` 904 MB with positions on). Linear extrapolation to 7.8M pages is indeed far beyond a laptop, and ingest.py:141-143's docstring ('peak memory is one batch') is true of the write only. But the proposal's central factual claim — 'there is no memory, partition or worker knob on that config, and no pylance surface to reach one' — is wrong, and it drives the proposal toward the wrong lever (per-year partitioned tables, which would change BM25 statistics and pagination, as its own risk section admits). See corrections. The measured-slope-and-published-constant part, and the corrected docstring, are exactly the project's style. The release-artifact part (3) collides with a decision the project deliberately deferred: data-sources.md:160-165 says 'Consult Kansallisarkisto for terms of reuse and redistribution' — publishing a derived `df.lance` as a GitHub release asset is a licensing act the maintainers have so far declined to make, and 'image ships empty, data is mounted' is the stated design partly for that reason.

*Corrections:* `lancedb.index.FTS.__init__` in 0.38.0 takes `memory_limit: int | None` ('total memory limit in MiB for the local FTS build stage, divided evenly among indexing workers') and `num_workers: int | None` (default ~half the cores); the bundled lance also reads `LANCE_FTS_NUM_SHARDS`, `LANCE_FTS_PARTITION_SIZE`, `LANCE_FTS_WRITE_QUEUE_SIZE` and `LANCE_FTS_POSTING_BATCH_ROWS` (strings in `_lancedb.abi3.so`). Measured on the 540k replica: default 900 MB peak / 14.0 s; `memory_limit=256` 772 MB / 10.3 s; `memory_limit=128, num_workers=2` 823 MB / 10.4 s; `memory_limit=64, num_workers=1` 707 MB / 18.9 s. So the knob exists, trims ~20% and, at this scale, leaves a residual ~560 MB that still scales with the text — that residual, not the absence of a knob, is the upstream question. Line refs: the create_table/index calls are ingest.py:143-152, not :271-287. Partitioning by year is not a lever to try 'next'; it changes ranking and must be ruled out unless the invariant tests are run across partitions.

*Sharpened:* Pass `memory_limit` and `num_workers` through `build_fts_index` (and `scripts/ingest_df.py`) with the measured table next to them, and measure peak RSS per MB of `searchable_text` at three scales with and without the limit; state the constant and the residual in the ingest docstring and deployment.md ('write is one batch; FTS build peaks at a + b x text MB, b = ... with memory_limit, ... without'). Take the residual to upstream lance with the numbers: 'memory_limit bounds the build stage but a text-proportional component remains'. Drop table partitioning. Treat shipping a built table as a licensing question to put to Kansallisarkisto first; if the answer is yes, the cosign/SLSA/SBOM pipeline already covers an OCI artifact and the deployment page documents mounting it — but not before.

**Feasibility and honesty of effort (LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / anyio / Dagger / GitHub Actions), verified against library source and experiments on the replicated 18-row fixture** — *not refuted*, confidence 0.80

Streaming write and one-shot FTS build confirmed at ingest.py:141-147 (create_table mode='overwrite' at :144, build_fts_index :146). Measured FTS build alone (fts_rss.py, fresh process, ru_maxrss): x3000 (54k rows) 144->301 MB in 1.5 s; x30000 (540k rows, data dir 547 MB, _indices 904 MB) 144->840 MB in 14.2 s — linear-ish and reproducing the proposal's shape. create_table's signature in lancedb/db.py:1728-1740 has no rows-per-file/fragment kwarg, and pylance is not installed (to_lance() at table.py:2599 raises ImportError), so the 'fragment size' experiment as written is not reachable. Shipping df.lance as a release asset via Dagger export + cosign sign-blob is routine. Effort L is honest.

*Corrections:* The central factual claim 'there is no memory, partition or worker knob on that config' is false for lancedb 0.38: the FTS dataclass (lancedb/index.py:112) has `memory_limit: Optional[int]` (MiB, 'total memory limit for the local FTS build stage, divided among workers', index.py:172-175) and `num_workers` (:176-180). Measured with memory_limit=128 at x30000: peak 144->755 MB (vs 840) and build 10.9 s (vs 14.2) — so the knob exists but bounds only ~85 MB of a ~700 MB delta at this scale; the rest of the peak is outside the stage it governs. Also my peak/data slope is ~1.3x of the data directory (696 MB / 547 MB), not 2.3x, so the constant must be re-measured rather than quoted. A fragment-size lever without pylance exists: create the table from the first batch and `table.add()` subsequent batches (each add is a new fragment).

*Sharpened:* Expose memory_limit/num_workers through build_fts_index (dataset.py:168) as ingest-time parameters, then measure peak RSS per MB of text at x300/x3000/x30000 with memory_limit unset, 128 and 512 MiB, and report how much of the peak the knob governs (measured here: ~10%). If the ungoverned remainder still scales linearly, that is the number for the upstream issue ('memory_limit bounds the build stage but process peak is still ~1.3x data bytes with positions on'). Independently ship df.lance from `dagger call build-table` as a signed release asset (cosign sign-blob, digest beside the SBOM), and rewrite the ingest docstring at ingest.py:141-142 to say the write is one batch and the index build is proportional to text with the measured constant.

---

## `perf-5` — Measure the threadpool ceiling and the GIL ping-pong: bound workers, shrink the switch interval, and record why the async API is not the answer

**survived 2/3, with corrections · effort S · impact 3/5**

**What the code does today.** df_tool.py:6-10 makes both tools sync so FastMCP runs them in a worker thread; FastMCP dispatches through `anyio.to_thread.run_sync` (fastmcp/utilities/async_utils.py:34) with anyio's default limiter of 40 concurrent threads; tools.py:290-293 already observed 64 simultaneous first calls. Measured on the x3000 replica: today's path 9.9 q/s at 1 thread and 13-14 q/s at 2/4/8 threads; through the in-memory FastMCP client 9.8-10.6 calls/s at concurrency 1-16; lancedb's native async API (`connect_async`, gathered coroutines) 10.4-12.7 q/s at concurrency 1-16 — no gain. Beside one CPU-bound Python thread (a spinner) the picture is dramatic: today's search 100 -> ~916 ms/query; the perf-1 two-step 11 -> 736 ms (65x); with `sys.setswitchinterval(0.0002)` 59 ms; async two-step 12 -> 571 ms beside the spinner and 4,364 ms/query beside an event loop running 2 ms Python slices. Mechanism: lancedb's sync API bounces every Rust future through its background event-loop thread, and every hop re-acquires the GIL, waiting up to the 5 ms switch interval whenever any Python thread is busy — and today's `to_list` (70 ms of pure Python per query) is exactly such a thread for every concurrent query, which is why throughput does not scale with threads. After perf-1 the two-step path scales to 76 q/s at 1 thread and 111 q/s at 4-8 threads (8.5x today).

**The gap.** The sync-in-threadpool decision is right but unmeasured, the per-process ceiling (~13 q/s today, ~110 after perf-1 on 4 cores) is unknown to operators, 40 in-flight queries can each hold 5-25 MB of Arrow, and any busy Python thread in the process (OTel BatchSpanProcessor export, another query's formatting) degrades every search 10-60x.

**The proposal.** (1) In `server.main()`: `sys.setswitchinterval(0.0005)` with the measured justification (736 -> ~59 ms under contention; no cost when idle — verify FastMCP throughput is unchanged in `bench_contention`). (2) Cap the worker pool: `anyio.to_thread.current_default_thread_limiter().total_tokens = min(8, os.cpu_count())` inside the running loop (a FastMCP lifespan hook), and document the memory bound per in-flight query (120 KB ranking + one page after perf-1). (3) Add `bench_contention` (perf-3) with three lines: q/s at 1/4 threads, ms/query beside a spinner, ms/query via the async API — so the df_tool docstring's decision is pinned by numbers, not prose. (4) Record in df_tool.py that the async API was measured and rejected: equal throughput, worse under a busy loop. (5) HTTP only, later and XL if a hosted endpoint ever exists: a query worker process (one lancedb connection per process, `multiprocessing` with Arrow IPC) removes the GIL from the search path entirely — argue against enabling it for stdio, where a second process contradicts 'costs nothing on a laptop'.

**Why it is once-in-a-lifetime.** It replaces a plausible docstring with the measured concurrency model of a Rust-engine-under-CPython MCP server — including the counter-intuitive result that the vendor's async API does not help — and gives operators a real capacity number per core.

**How we would know.** bench_contention shows two-step search <= 80 ms/query beside a spinner (from 736) after the switch-interval change and >= 100 q/s at 4 threads on the replica; in-flight worker threads never exceed the cap under a 64-way `asyncio.gather` through the in-memory client; the docstring in df_tool.py cites the numbers.

**Risks.** A smaller switch interval raises context-switch overhead for genuinely CPU-bound Python; the server has almost none, but measure fastmcp's own JSON path before and after. Changing anyio's limiter is a global for the process (fine here, it owns the process). The worker-process idea doubles memory for the table's page cache and changes the failure model; it is an HTTP-only option and must stay off for stdio.

**First step.** One PR: switch interval + limiter cap in `main()` with comments quoting the measurements, plus the bench_contention scenario.

**Spike.** On the x3000 replica, run 10 two-step searches alone (~11 ms each), then with `threading.Thread(target=lambda: busy-loop)` alive (~700 ms each), then again after `sys.setswitchinterval(0.0002)` (~60 ms); repeat via `lancedb.connect_async` inside `asyncio.run` with a coroutine doing 2 ms busy slices between `await asyncio.sleep(0)` (seconds per query).

### Skeptics

**Meticulous code reviewer — every premise re-checked against the repository and re-run on replicated fixtures (x3000 = 54,000 rows, x30000 = 540,000 rows) with lancedb 0.38.0 / fastmcp 3.4.4 / anyio 4.15.1 on Python 3.14.7** — *not refuted*, confidence 0.85

df_tool.py:6-10 states the sync-in-threadpool decision; fastmcp/utilities/async_utils.py:9 imports `anyio.to_thread.run_sync` and :34 calls it, and fastmcp/tools/function_tool.py:457 dispatches sync tools through `call_sync_fn_in_threadpool`; anyio's default limiter is `CapacityLimiter(40)` (anyio/_backends/_asyncio.py:3162). lancedb's sync builders go through the background loop (`LOOP.run(self._inner.to_arrow(timeout))` at lancedb/query.py:4187; table.py:2508/2646), which is the ping-pong mechanism described. Reproduced on the x3000 replica (scratchpad/perf5.py): DfSearch.search alone 140 ms; beside one CPU-bound Python thread 1,078 ms; after `sys.setswitchinterval(0.0005)` 241 ms; at 0.0002 196 ms. Throughput 7.3 / 9.4 / 8.8 q/s at 1/2/4 threads — flat, as claimed. The async-API comparison and the post-perf-1 76/111 q/s figures were not re-run here. Neither audit measures concurrency (audit 2 finding 11 only lists the 10k materialisation as a hosted-endpoint concern).

*Corrections:* tools.py:290-293 should read :72-75 (the '64 simultaneous first calls' comment). The '59 ms beside a spinner' figure is for the two-step path of perf-1; on today's path a 0.2 ms switch interval still leaves a spinner costing 1.4x (196 vs 140 ms), so the switch-interval change is worth much less until perf-1 lands. FastMCP 3.4.4 exposes `lifespan` (fastmcp/server/server.py:272 default_lifespan) so the limiter cap has a natural home.

*Sharpened:* Land it as a measured concurrency model: `sys.setswitchinterval(0.0005)` in `main()` with the reproduced numbers (1,078 -> 241 ms beside a busy Python thread on today's path), the anyio limiter capped at min(8, cpu_count) in a FastMCP lifespan so at most 8 x (120 KB ranking + one page) is in flight after perf-1, and a `bench_contention` scenario that records q/s at 1/4 threads and ms/query beside a spinner. State the per-core ceiling in deployment.md as a number the benchmark regenerates, and record in df_tool.py that the vendor async API was measured and rejected (that measurement still needs to be committed as code, not prose).

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/OTel-SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in mind. Every claim below was re-run on this machine (4 CPUs, Python 3.14.7, lancedb 0.38.0, fastmcp 3.4.4) against x3000 (54k-row) and x30000 (540k-row) replicas of the 18-row fixture.** — *refuted*, confidence 0.60

The measurements are honest and I reproduced their shape (scratchpad/spike5.py, 54k replica): FastMCP dispatches sync tools through `anyio.to_thread.run_sync` (fastmcp/utilities/async_utils.py:34) under anyio's default `CapacityLimiter(40)` (anyio/_backends/_asyncio.py:3162); lancedb's sync API bounces through `background_loop.LOOP.run`; beside a pure-Python spinner today's search went 146 -> 1100 ms and the two-step 19 -> 902 ms, and `sys.setswitchinterval(0.0005)` brought those to 232 and 78 ms. But the realistic case did not move: two-step throughput at 4 threads was 74.7 q/s with the default interval and 80.7 with the short one (noise), and today's path 8.3 vs 8.2 q/s. The 65x number needs a thread that spins in pure Python for the whole query, and nothing in this process does that once perf-1 removes the 100 ms `to_list`: the formatter is 0.04 ms, pydantic and JSON are milliseconds, the OTel exporter is off by default and I/O-bound when on. So the switch-interval change is a global process tweak justified by a scenario the server does not have — 'not for the reason given'. The limiter cap's memory argument (40 in-flight x 5-25 MB) is real today and void after perf-1 (40 x 120 KB); on its own it is generic. What is genuinely valuable — the measured async-API rejection and the q/s-per-core number written next to the df_tool.py:6-10 docstring — belongs in perf-3's bench_contention and a docstring edit, not in a PR whose first step is two knobs in main().

*Corrections:* The df_tool.py:6-10 docstring is right about sync-in-threadpool and the proposal confirms it; the missing thing is the number, not a fix. If perf-3's in-memory FastMCP benchmark at concurrency 4-16 ever shows the switch interval moving throughput (mine did not), that measurement — not the spinner — is the justification to record.

**Feasibility and honesty of effort (LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / anyio / Dagger / GitHub Actions), verified against library source and experiments on the replicated 18-row fixture** — *not refuted*, confidence 0.90

FastMCP 3.4.4 dispatches sync tools via call_sync_fn_in_threadpool -> run_sync_in_threadpool (fastmcp/utilities/async_utils.py:34-42) on anyio; anyio 4.15.1's default limiter is CapacityLimiter(40) (anyio/_backends/_asyncio.py:3162) and `current_default_thread_limiter().total_tokens = 4` inside a running loop works (verified). FastMCP(lifespan=...) exists (fastmcp/server/server.py:333). Reproduced the contention experiment at x3000 (spin.py): two-step 17.3 ms alone -> 910 ms beside a busy Python thread at the default 5 ms switch interval -> 92.5 ms at 0.5 ms -> 63.4 ms at 0.2 ms; full path 136 -> 932 -> 210 ms. Throughput: two-step 65 q/s at 1 thread, 105.5 at 4; full path 7.6/8.9 (does not scale with threads). connect_async + `await table.search(...)` then .select(['_score']).with_row_id().fast_search().limit(10000).to_arrow() works (8.1 ms). All numbers match the proposal within noise. Effort S is honest.

*Corrections:* tools.py '64 simultaneous first calls' comment is at :73, not :290-293. The measurable outcome '<= 80 ms beside a spinner with sys.setswitchinterval(0.0005)' did not hold here: 92.5 ms at 0.5 ms, 63 ms at 0.2 ms. Either set 0.0002 as the spike itself used, or gate at <= 100 ms / <= 10x the uncontended p50. Note the async API's builder is obtained by awaiting table.search() (AsyncFTSQuery), which the spike text glosses over.

*Sharpened:* In server.main(): sys.setswitchinterval(0.0002) with the measured justification (910 -> 63 ms/query beside a busy thread; 5 ms is the GIL hand-off latency lancedb's sync API pays on every Rust->Python hop via its background loop), and a FastMCP lifespan hook setting anyio's default thread limiter to min(8, os.cpu_count()) (default 40). Pin it in bench_contention with three lines: q/s at 1/4 threads (target >= 60 / >= 100 on the x3000 replica after perf-1), ms/query beside a spinner (<= 10x uncontended), and the async API's throughput, so the df_tool.py module docstring's sync-in-threadpool decision cites numbers.

---

## `perf-6` — Ingest leaves every previous version and index on disk: cleanup and compaction at the end of ingest_df

**survived 3/3, with corrections · effort S · impact 2/5**

**What the code does today.** ingest.py:279 `create_table(..., mode='overwrite')` creates a new dataset version rather than deleting the old files; :281-287 then run five `create_index(..., replace=True)` calls, each another version; nothing in `ingest_df` or scripts/ingest_df.py:36-42 calls `Table.cleanup_old_versions` or `Table.optimize` (both exist on the sync `LanceTable`: `optimize(cleanup_older_than=, delete_unverified=, retrain=)`). Measured: re-running the fixture ingest into the same database twice grows `df.lance` from 224 KB to 624 KB with 21 versions in `_versions/`; rebuilding the FTS index on the 540k replica doubled `_indices/` from 83 MB to 166 MB. At df scale this is invisible; at tuomiokirjat (274 MB data + 83 MB index per 540k replicated rows, so several GB per version) a re-harvest triples the mounted volume, and `open_table` walks the version list on every query (dataset.py:290).

**The gap.** The harvest is designed to be re-run (scripts/harvest.py is resumable; audit finding 6 assumes re-ingest), but every re-run silently keeps the previous corpus on disk, and the docs never say to clean up.

**The proposal.** At the end of `ingest_df`, after the last index build: `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` and return the re-opened handle; log the `CleanupStats` (bytes removed, versions removed). Guard it with the comment already at dataset.py:214-216: builds mutate the dataset and belong in the ingest path, never against live data — cleanup in particular deletes files a running server may have open. Test in test_ingest.py: ingest the fixture twice into one db; assert on-disk bytes after the second run <= 1.05x after the first, and `test_nothing_is_left_unindexed` stays green. Add `--keep-versions` to scripts/ingest_df.py for the debugging case.

**Why it is once-in-a-lifetime.** Small, but it is the difference between an operator re-harvesting monthly and an operator discovering a full disk; it also makes perf-4's shipped table artifact minimal by construction.

**How we would know.** Directory size after two consecutive ingests within 5% of one ingest; `len(table.list_versions())` small and constant across re-runs; the deployment page states the on-disk size of a built table for each corpus.

**Risks.** `delete_unverified=True` is needed because the files are seconds old; it must never run against a table another process is reading (a server pointed at the same URI during ingest could see a version vanish). `optimize` may also compact fragments, which changes `_rowid`s — harmless here because perf-1's ranking and take happen within one query on a table that is only ever read after ingest completes.

**First step.** One PR: the optimize call, the log line, the double-ingest size test, and a sentence in docs/development/deployment.md.

**Spike.** `ingest_df(db, fixture)` twice into one temp db; `du -s` the table directory and `len(table.list_versions())` after each; then repeat with `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` after each ingest.

### Skeptics

**Meticulous code reviewer — every premise re-checked against the repository and re-run on replicated fixtures (x3000 = 54,000 rows, x30000 = 540,000 rows) with lancedb 0.38.0 / fastmcp 3.4.4 / anyio 4.15.1 on Python 3.14.7** — *not refuted*, confidence 0.90

ingest.py:144 `db.create_table(..., mode="overwrite")` then five `create_index(..., replace=True)` via build_fts_index/build_scalar_indexes (:146-152); nothing in ingest_df or scripts/ingest_df.py:36-42 calls `cleanup_old_versions` or `optimize` (grep). Both exist on the sync LanceTable in 0.38.0 with the cited signatures (`optimize(*, cleanup_older_than, delete_unverified, retrain)`, `cleanup_old_versions(older_than, *, delete_unverified) -> CleanupStats`). Reproduced (scratchpad/perf6.py): fixture ingested 1/2/3 times into one db -> df.lance 224 / 424 / 624 KB, 7 / 14 / 21 versions, `_indices/` entries 6 / 12 / 18. With `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` after each ingest: 176 KB and 1 version every time, all six indexes report 0 unindexed rows and an FTS search still returns rows. strace confirms `open_table` opens `df.lance/_versions` on every open, but it costs 0.3 ms at 8 and at 22 versions. Not in either audit (audit 2 finding 6 assumes re-ingest but says nothing about versions).

*Corrections:* The measured sequence is three runs, not two: two ingests give 424 KB / 14 versions; 624 KB / 21 versions is after the third. `LanceTable.optimize()` returns None in lancedb 0.38.0, so 'log the CleanupStats' must call `table.cleanup_old_versions(timedelta(0), delete_unverified=True)` (which returns CleanupStats with bytes/old-versions removed) — either instead of, or after, `optimize`. The 'open_table walks the version list on every query' cost is negligible (0.3 ms regardless of count); the disk argument is the real one. Line references: ingest.py:144 and :146-152, not :279-287.

*Sharpened:* At the end of `ingest_df`, after the last index build, run `table.cleanup_old_versions(timedelta(0), delete_unverified=True)` (log its CleanupStats) and `table.optimize()` for compaction, then return `db.open_table(table_name)`; measured result on the fixture is a constant 176 KB / 1 version across repeated ingests instead of +200 KB and +7 versions per run. Pin it with a double-ingest test asserting on-disk bytes after run 2 <= 1.05x run 1 and `len(list_versions()) == 1`, and add `--keep-versions` to scripts/ingest_df.py. Keep the guard comment: this deletes files a concurrently running server could have open, so it belongs only in the ingest path.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/OTel-SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in mind. Every claim below was re-run on this machine (4 CPUs, Python 3.14.7, lancedb 0.38.0, fastmcp 3.4.4) against x3000 (54k-row) and x30000 (540k-row) replicas of the 18-row fixture.** — *not refuted*, confidence 0.90

Reproduced (scratchpad/spike6.py): three consecutive `ingest_df` runs into one database grow `df.lance` 224 -> 424 -> 624 KB with 7 -> 14 -> 21 versions (one `create_table(mode='overwrite')` at ingest.py:144 plus one version per `create_index(replace=True)` at dataset.py:169/:220/:222). My 54k replica, after three FTS rebuilds, held 15 versions and 14 index directories at 138 MB; `optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` took 10 ms and left 1 version, 6 index dirs, 41 MB, with search intact (10000 capped hits). With optimize after each fixture ingest the directory stays at 176 KB and 1 version. Nothing in `ingest_df` or scripts/ingest_df.py calls it and no doc mentions cleanup (grep over docs/, packages/, scripts/). At df scale invisible, at tuomiokirjat a re-harvest triples the volume — a small, in-style operational fix. The danger the proposal names is real and I measured it: a handle opened before cleanup raises `lance error: Not found` afterwards, so this must stay in the ingest path and the deployment page must say 'never against a table a server is reading'.

*Corrections:* `Table.cleanup_old_versions` is deprecated since 0.21 and on 0.38.0 goes through `to_lance()`, which raises `ImportError: The lance library is required ... pip install pylance` — pylance is not installed and adding it would be a new supply-chain dependency; `optimize()` is the only working call. `optimize()` returns None in the sync API, so there are no `CleanupStats` to log — log `len(table.list_versions())` and the directory size before/after instead. Line refs: create_table is ingest.py:144 and the index calls :146-152; the CLI is scripts/ingest_df.py:39-42. `optimize` also compacts and re-optimises indexes; on the fresh single-fragment tables here it rewrote nothing (data file count 1 -> 1, 10 ms), but on a 19 GB multi-fragment tuomiokirjat build measure that it does not double the ingest I/O before enabling it unconditionally. Drop `--keep-versions`: nobody asked, and the debugging case is `optimize` not being called.

*Sharpened:* At the end of `ingest_df`, after the last index build: `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)`, return the re-opened handle, log versions and bytes before/after; the comment quotes dataset.py:214-216 and adds the measured consequence (a handle open across cleanup raises Not found), so the rule 'ingest path only, never under a live reader' is stated with its reason. Test: ingest the fixture twice into one db and assert bytes <= 1.05x and `len(list_versions()) == 1`; deployment.md states the on-disk size of a built table per corpus (df 176 KB fixture-scale; 540k replica: 547 MB data + ~450 MB index after cleanup).

**Feasibility and honesty of effort (LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / anyio / Dagger / GitHub Actions), verified against library source and experiments on the replicated 18-row fixture** — *not refuted*, confidence 0.92

ingest.py:144 create_table(mode='overwrite') and :146-147 index builds with replace=True confirmed; neither ingest_df nor scripts/ingest_df.py:36-42 calls optimize. Reproduced (ing2.py): first ingest 224 KB / 7 versions, second 424 KB / 14 versions; `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` (lancedb/table.py:4198) brought it to 176 KB / 1 version with all 6 indices intact and DfSearch.search('Åbo') still returning 7 hits; a third ingest+optimize stayed at 176 KB / 1 version. At 540k replicated rows the on-disk cost per un-cleaned re-run is 547 MB data + 904 MB _indices here, so the operational point is stronger than stated. Effort S is honest.

*Corrections:* The sync LanceTable.optimize discards its result (table.py:4249-4255 is `LOOP.run(...)` with no return) and returned None in my run, so there is no CleanupStats to log from it; only AsyncTable.optimize (table.py:6645-6651) returns OptimizeStats(compaction, prune: RemovalStats{bytes_removed, old_versions_removed}). LanceTable.cleanup_old_versions (table.py:4148-4176) goes through to_lance() and raises ImportError here because pylance is not installed. 'open_table walks the version list on every query' overstates it — it reads the latest manifest. The cited '83 MB _indices at 540k' is far below what I measured (904 MB) for the same replication.

*Sharpened:* At the end of ingest_df call table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True) and log bytes freed from table.stats() (table.py:3378, TableStatistics) or a directory-size delta, since the sync optimize returns None; keep the 'ingest path only, never against live data' comment and add --keep-versions to scripts/ingest_df.py. Test: double ingest into one db, assert on-disk bytes <= 1.05x single ingest and len(list_versions()) == 1; deployment.md states the built-table size per corpus (fixture: 176 KB; 540k replicated rows: ~1.45 GB incl. positions).

---

## `perf-7` — A corpus-shaped synthetic generator so the 7.8M-row shape is measured nightly rather than asserted

**survived 2/3, with corrections · effort XL · impact 4/5**

**What the code does today.** The comment at dataset.py:101-103 says the 10k cap is 'routine for tuomiokirjat (7.8M)'; nothing in the repository ever runs at more than 18 rows in CI (conftest.py:249-254) or 6,876 rows on a maintainer's machine (test_search_invariants.py:215-262). Replicating the fixture shows that scaling is term-dependent: from 54k to 540k rows the ranking-only cost of an every-document term went 12.6 -> 31.1 ms, `fuzzy=1` on 'Åbo' 14.8 -> 65.7 ms, the LIKE place filter 16.1 -> 57.4 ms, a phrase 5.2 -> 15.8 ms, the year-range filter 10.7 -> 22.5 ms, while a capped rare-term ranking stayed ~10 -> 17 ms. The fastest-growing paths are exactly the ones the tool tells the model to reach for: `fuzzy=1` as 'the right second attempt' (df_tool.py:158-164) and the substring filters that are deliberately unindexed (dataset.py:210-212, `text_contains` at :379-386). Replicated text under-represents this: the term dictionary does not grow with copies, so fuzzy's dictionary scan and the LIKE scan are both optimistic. The corpus itself is unreachable from CI (no network to Sisältöhaku) and cannot be committed.

**The gap.** The project's central scaling claim rests on extrapolation from a corpus 1,000x smaller, with the cost curves that matter most (fuzzy, LIKE, common Swedish function words that the stop-word decision keeps searchable) being the ones replication hides.

**The proposal.** `scripts/synth_corpus.py`: fit a shape model on the real df export when present (maintainers have it) and commit only the parameters as `benchmarks/corpus_shape.json` (~1 KB, no text): per-language vocabulary size and Zipf exponent, document-length distribution, 36% empty transcripts, place/language/year marginals, the spelling-variant rate (fit so that 'bref' 257 / 'breff' 1,918 / overlap 71 is reproduced within 20%), and the frequency of 'de'/'den'/'om'. Generate N rows deterministically (seeded) in the JSONL shape `ingest_df` reads, with a tuomiokirjat profile (2.4 KB pages, court-record vocabulary drawn from the same Zipf model). Nightly Dagger `bench-scale` at 1M rows on a standard runner, weekly at 7.8M on a 32 GB self-hosted or large runner: ingest rows/s, index build seconds and peak RSS, bytes per row, and p50/p99 for the perf-3 scenario list plus every-doc terms, fuzzy=1, LIKE, phrase and cold-cache first query. Gates written as product decisions: `fuzzy=1` p99 at 7.8M < 2 s and LIKE filter p99 < 2 s, else the tool description and the filter design must change (e.g. an exact/bitmap `issuingplace` plus a prefix index) before tuomiokirjat is announced. Publish to the same performance history page.

**Why it is once-in-a-lifetime.** It is the only way to prove 'scales to 7.8M pages' without the corpus in CI, and it would make this the reference for how to benchmark an archival search server whose data cannot be shipped — a shape file instead of a dataset. It also stress-tests the project's own corpus-specific decisions (stop words kept, fuzzy as the second attempt, unindexed substring filters) at the scale where they bite.

**How we would know.** The synthetic df at 6,876 rows reproduces five measured corpus numbers within 20% (bref/breff overlap, 'de' 1,735, 'den' 846, 'om' 1,133, 'konung' 279); a weekly run at 7.8M reports ingest rows/s, index build time and peak RSS, and p50/p99 for >= 15 scenarios; a documented go/no-go table for tuomiokirjat with the thresholds above.

**Risks.** A Zipf model does not reproduce real posting-list skew or OCR noise, so absolute numbers are indicative; the fit-validation against df's measured numbers is what keeps it honest. A 7.8M-row weekly run costs machine hours and needs a runner with tens of GB of RAM until perf-4 bounds the index build. The generator must not be mistaken for test data for retrieval quality — it is for cost curves only, and the docs must say so.

**First step.** PR 1: `synth_corpus.py` with the shape file fitted on df and a test that the synthetic df reproduces the five numbers; PR 2: Dagger `bench-scale` at 1M with the perf-3 scenarios; PR 3: the weekly 7.8M run and the go/no-go page.

**Spike.** Fit the shape on the 18-row fixture as a stand-in (vocabulary, doc lengths, empty-transcript rate), generate 540k rows with a seeded Zipf sampler over a 50k-term dictionary, ingest via `ingest_df`, and compare fuzzy=1 and LIKE costs against the x30000 replica: a real dictionary should make fuzzy visibly worse than the 65.7 ms the replica shows.

### Skeptics

**Meticulous code reviewer — every premise re-checked against the repository and re-run on replicated fixtures (x3000 = 54,000 rows, x30000 = 540,000 rows) with lancedb 0.38.0 / fastmcp 3.4.4 / anyio 4.15.1 on Python 3.14.7** — *not refuted*, confidence 0.60

Citations check out: dataset.py:101-103 ('routine for tuomiokirjat (7.8M)'), df_tool.py:157-164 (fuzzy=1 as 'the right second attempt'), dataset.py:210-212 and text_contains :379-386 (LIKE deliberately unindexed), test_search_invariants.py:215-216 (corpus gate on .data/df/df.jsonl.gz). Nothing in CI runs above 18 rows (conftest.py:12-16, fixtures/df_sample.jsonl) and the brief confirms the corpus is unreachable from here. Term-dependent scaling reproduced on today's full path from 54k to 540k rows (scratchpad/perf7.py, p50): fuzzy=1 'Åbo' 164 -> 301 ms, LIKE issuingplace 146 -> 198 ms, phrase '"de ecclesia"' 58 -> 216 ms, every-doc 'df' 143 -> 176 ms, year range 137 -> 172 ms, while capped 'Åbo' stayed 140 -> 148 ms and get_charter 5.6 -> 5.3 ms. The replica's vocabulary is 18 documents' worth, so every posting list is 3,000-30,000x long and the term dictionary never grows — the opposite skew to a real corpus, which supports the proposal's fidelity argument. What I could not verify: that a Zipf-shaped generator fitted on the real export reproduces the five corpus numbers within 20% — that needs the export maintainers have.

*Corrections:* conftest.py:249-254 does not exist (the file is 46 lines; the fixture comment is :12-16). The scaling figures quoted in the proposal are ranking-only; on today's full-projection path the phrase query is the fastest-growing of all (3.7x for 10x rows), which the proposal does not list and which matters because the tool descriptions steer models toward quoted phrases as much as toward fuzzy=1.

*Sharpened:* Ship `scripts/synth_corpus.py` that fits per-language vocabulary size, Zipf exponent, document-length distribution, 36% empty transcripts, place/language/year marginals and a spelling-variant rate on the real df export and commits only `benchmarks/corpus_shape.json`; validate the 6,876-row synthetic against bref 257 / breff 1,918 / overlap 71, 'de' 1,735, 'den' 846, 'om' 1,133 and 'konung' 279 within 20%. Then run the perf-3 scenarios at 1M nightly and 7.8M weekly with go/no-go thresholds for fuzzy=1, LIKE AND phrase p99 (measured here: phrase grows fastest on the current path), so 'scales to 7.8M pages without changing shape' is a measured property rather than the comment at dataset.py:101-103.

**Project maintainer — value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/OTel-SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in mind. Every claim below was re-run on this machine (4 CPUs, Python 3.14.7, lancedb 0.38.0, fastmcp 3.4.4) against x3000 (54k-row) and x30000 (540k-row) replicas of the 18-row fixture.** — *refuted*, confidence 0.70

The observation behind it is sound and I reproduced its direction: on replicas, fuzzy and LIKE costs grow with rows while capped ranking does not, and replication cannot grow the term dictionary, so the paths the tool description steers the model to (df_tool.py:158-164 fuzzy=1; dataset.py:210-212 and :379-386 unindexed LIKE) are the ones a replica understates. But the instrument proposed is the wrong one for this project. The maintainers already have the real voudintilit export harvested and scripts/harvest.py:6 and :113 harvest tuomiokirjat; the project's established pattern for 'numbers CI cannot see' is corpus-only tests gated on `.data/` (test_search_invariants.py:215-262), run on a maintainer's machine and written next to the code. A Zipf generator fitted to 6,876 charters cannot produce the one parameter that decides fuzzy and LIKE cost at 7.8M pages — the dictionary size and posting skew of OCR'd court records — and the proposal concedes the numbers would be 'indicative'. An indicative number the code then cites as a scaling proof is the opposite of 'measured on the corpus'. Weekly 7.8M-row runs on a 32 GB self-hosted runner add machine hours and a self-hosted-runner surface to a project that has kept CI to SHA-pinned hosted actions and Scorecard. The go/no-go thresholds (fuzzy p99 < 2 s, LIKE p99 < 2 s) are the right kind of product decision, but they should be taken on the real export, once, before tuomiokirjat is announced — a week of maintainer time, not an XL generator plus recurring infrastructure.

*Corrections:* What survives goes elsewhere: the scale-ratio scenarios (every-doc term, fuzzy=1, LIKE, phrase, year range at x300 vs x3000) belong in perf-3's harness as ratios; the real-corpus cost curve belongs in a corpus-only `bench_corpus.py` gated on `.data/tuomiokirjat/` and run by a maintainer, its numbers recorded in docs/development/performance.md with the go/no-go table. Note also that 'court-record vocabulary drawn from the same Zipf model' is fabricated data by construction.

**Feasibility and honesty of effort (LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / anyio / Dagger / GitHub Actions), verified against library source and experiments on the replicated 18-row fixture** — *not refuted*, confidence 0.75

Citations hold: dataset.py:103 'routine for tuomiokirjat (7.8M)'; df_tool.py:153-163 sells fuzzy=1 as 'the right second attempt'; corpus-only tests at test_search_invariants.py:215+ skip because .data/df is empty here. The mechanism argument is sound — a fuzzy term is a Levenshtein-automaton walk over the term dictionary and LIKE is a column scan, and replication multiplies postings without growing the dictionary — but I could not confirm the term-dependent scaling at the scales runnable here: x300 -> x3000 gave capped 'Åbo' 33 -> 136 ms, fuzzy=1 39.5 -> 148, LIKE issuingplace 33 -> 136, phrase 10.6 -> 53, every-doc 75 -> 126, i.e. all paths scaled alike (the x300 'Åbo' run is uncapped at 3,000 hits, a different regime). That is exactly the proposal's point — the replica cannot show the curve either way — so it is not a refutation. Feasibility of the big run: 7.8M x 2.4 KB = 19 GB JSONL, ingest at ~30k rows/s is ~5 min, FTS build extrapolates from 14 s/540k to ~4 min but peak RSS at my measured ~1.3x of data bytes is ~25 GB (memory_limit trimmed ~10%), so the 32 GB runner claim is right and it needs ~25 GB of disk for data+positions index (standard runners have ~14 GB free). Shape fitting requires the real export, which maintainers have and CI does not; the five validation constants already live in the docstrings. XL is honest.

*Corrections:* The spike's promise that 'a real dictionary should make fuzzy visibly worse than the replica' is untested; at x300->x3000 fuzzy scaled identically to the exact query, so the first PR must include the replica-vs-synthetic comparison as its own acceptance test rather than assume it. Add a disk budget (~25 GB at 7.8M with positions) to the runner requirement, and use lancedb 0.38's FTS(memory_limit=, num_workers=) (index.py:208-209) to size the build.

*Sharpened:* scripts/synth_corpus.py driven by a committed benchmarks/corpus_shape.json fitted on the real df export (vocabulary size and Zipf exponent per language, document-length distribution, 36% empty transcripts, place/language/year marginals, spelling-variant rate tuned to bref 257 / breff 1,918 / overlap 71), validated by a test that the 6,876-row synthetic df reproduces those and 'de' 1,735 / 'den' 846 / 'om' 1,133 / 'konung' 279 within 20%. PR 1's second acceptance test: at 540k rows, fuzzy=1 and LIKE cost on the synthetic corpus vs the replicated fixture (expected: synthetic worse, because the dictionary is ~50k terms rather than the fixture's few hundred). Then a nightly 1M and weekly 7.8M Dagger run on a 32 GB / 30+ GB-disk runner, using FTS(memory_limit=..., num_workers=...) from perf-4 and the perf-3 scenario list, with product go/no-go thresholds (fuzzy=1 and LIKE p99 < 2 s at 7.8M) recorded on the performance page.

---

