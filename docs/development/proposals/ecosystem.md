---
icon: lucide/file-search
---

# Ecosystem and federation across the Nordic archive MCP family

Every proposal from this lens in full, followed by the three skeptics' verdicts on it.
Effort and impact are the lens's own estimates; the verdict tally is the skeptics'.

## `ecosystem-1` — archive-mcp-core: one framework that Kansallisarkisto, Riksarkivet and the next archive instantiate from a declarative CorpusSpec

**survived 3/3, with corrections · effort XL · impact 5/5**

**What the code does today.** The spine announces itself as a copy: dataset.py:8 'Ported from ra-mcp's ra_mcp_dataset_lib'; lib telemetry.py:13-14 'Ported from ra-mcp's ra_mcp_common.telemetry, which the sibling Kansallisarkisto server also carries'; mcp telemetry.py:18 'Ported from ra-mcp's ra_mcp_server.telemetry'; routes.py:17 and observability.md:7 'same shape as ra-mcp'. The two workspace packages are 'never published to PyPI' (security.md:216), so nothing can be shared except by copy. The per-corpus tool is 218 lines (df_tool.py) of which the mechanism — _answer (df_tool.py:31), require_keyword/require_ordered_range (dataset.py:422,433), combine/equals/text_contains predicates, format_results (dataset.py:446) — is generic and only the prose, the renderer (_render_charter, formatter.py:66) and the filter->column map (search_operations.py:97-103) are df-specific. The result envelope is already identical across the family: ra-mcp's diplomatics_search_sdhk answered this session with "SDHK results for 'X': showing 10 of 194 (offset 0) ... More results available. Use offset=10 to see the next page." — the same strings as dataset.py:446-477. Spike (scratchpad/spikes/spec_parity.py): a df_search generated from a 12-line CorpusSpec via inspect.Signature + Tool.from_function produced a byte-identical inputSchema property list and byte-identical replies to the hand-written tool on 10/10 cases including the error paths (blank keyword, inverted range, fuzzy+phrase, out-of-int32 year).

**The gap.** Every fix is made three times (the comment 'ra-mcp and the sibling likely have it too' is the symptom), voudintilit and tuomiokirjat each need a fresh copy of df_tool.py, and there is no way to say 'this server conforms to the family contract' because there is no artefact that is the contract.

**The proposal.** Create a published package `archive-mcp-core` (PyPI, semver, owned jointly by AI-Riksarkivet) with: `core.spine` (dataset.py as is, renamed attributes per ecosystem-5), `core.spec` — `CorpusSpec(prefix, table, label, archive, id: IdSpec(column, citation_prefix='DF', url_template='https://df.kansallisarkisto.fi/document/{id}'), fts: FtsSpec(language='Swedish', stem=True, remove_stop_words=False, with_position=True, ascii_folding=True, max_token_length=64), filters: list[FilterSpec(name, column, kind: equals|contains|overlap_min|overlap_max, description)], keyword_description, search_description, instructions, render_record, snippet_chars=400)`; `core.tools.register_corpus(mcp, spec, get_db)` generating `<prefix>_search` and `<prefix>_get` exactly as the spike does; `core.ingest.build_table(db, spec, batches, provenance)` applying FtsSpec + scalar indexes derived from FilterSpec kinds (equals->Bitmap, overlap->BTree, contains->none, as build_scalar_indexes documents); `core.readiness`, `core.errors` (ecosystem-8), `core.testkit` (ecosystem-7). What stays local per archive: the record model and its corpus-specific cleaning (models.py strip_editorial_apparatus, UNKNOWN_YEAR), the harvester, the fixture, and all measured prose — the spec is where the numbers live, not a generic template. In this repo `packages/kansallisarkisto-lib` becomes `df.py` (~120 lines: DfRecord + df_spec + _render_charter) plus voudintilit.py and tuomiokirjat.py when ingested; `packages/kansallisarkisto-mcp` becomes tools.py = three register_corpus calls plus instructions. Rule enforced by the testkit: every FilterSpec.description and search_description must contain at least one digit — a description without a measured number is rejected, so 'declarative' cannot decay into 'generic'.

**Why it is once-in-a-lifetime.** It is the difference between 'a good archival MCP server' and 'the way archival MCP servers are built': the next corpus (voudintilit, 98,945 rows, already harvested) becomes a spec file and a renderer, and ra-mcp's 40+ dataset tools get the same spine, the same error semantics and the same tests without a port. The brief's own bar — 'the next corpus is a week's work' — becomes a day.

**How we would know.** (1) voudintilit served with a spec + renderer of ≤120 lines and zero edits to core; (2) the existing 201 tests pass unchanged against the generated df tools (they are the parity oracle — the spike already shows byte-identity on 10 cases); (3) ra-mcp's diplomatics module re-hosted on core passes the core testkit; (4) both repos pin the same core version and `pip-audit`/Trivy find it once, not twice.

**Risks.** Argues against the project's stated philosophy that every description is hand-written next to the code with measured numbers: the mitigation is that the spec holds the prose as data and the testkit refuses digit-free descriptions. Three repos and one core create version skew — the server card (ecosystem-4) must report core_version so a router can refuse mismatched servers. ra-mcp's non-search tools (viewer, IIIF, PDF) must not be forced into the spec; core covers the search/get pair and the conventions only. Dynamic signatures (inspect.Signature + __annotations__) are a FastMCP/pydantic implementation detail that could break on upgrade — pin fastmcp as the repo already does (==3.4.4) and keep the parity test.

**First step.** Inside this repo, no new package yet: create `ra_mcp_kansallisarkisto_lib/spec.py` with CorpusSpec/FilterSpec and `ra_mcp_kansallisarkisto_mcp/corpus_tools.py` with register_corpus (port the spike), define `df_spec` carrying the current df_tool.py prose verbatim, register df from it, delete the hand-written df_search/df_get_charter, and let the existing test suite (test_tools.py, test_telemetry.py, mcp_smoke.py) prove parity. One PR, ~300 lines net negative.

**Spike.** scratchpad/spikes/spec_parity.py (already run): builds df_search from a CorpusSpec over the 18-row fixture and diffs schema and replies against tools.kansallisarkisto_mcp — 10/10 byte-identical. Extend it by writing a voudintilit spec against a 20-line synthetic JSONL to show the second corpus needs no new code path.

### Skeptics

**Meticulous code reviewer — premise-checking every cited file, line, library call and spike against the repository and by re-running the spikes and calling ra-mcp** — *not refuted*, confidence 0.70

Verified every citation: dataset.py:8 'Ported from ra-mcp's ra_mcp_dataset_lib'; lib telemetry.py:13-14; mcp telemetry.py:18; routes.py:17 'Same split as ra-mcp'; observability.md:7; security.md ≈216 'never published to PyPI'. df_tool.py is 218 lines; _answer at :31; require_keyword/require_ordered_range at dataset.py:422/433; format_results at :446; _render_charter at formatter.py:66; filter→column map at search_operations.py:97-103. fastmcp==3.4.4 pinned (kansallisarkisto-mcp/pyproject.toml:13); lancedb is '>=0.20,<1', not exact. Called ra-mcp diplomatics_search_sdhk in this session: envelope is "SDHK results for 'X': showing 5 of 128 (offset 0)" … "More results available. Use offset=5 to see the next page." — identical strings to dataset.py:469/477. Re-ran scratchpad/spikes/spec_parity.py: 'param names identical: True' and 10/10 replies byte-identical, including the blank keyword, inverted range, fuzzy+phrase and 10**20-year cases.

*Corrections:* The 'ra-mcp and the sibling likely have it too' sentence is in docs/development/audit.md:648-649 (first-audit prose about the shutdown hang), not a code comment. The parity spike uses placeholder descriptions ('<keyword prose>'), so byte-identity is proven for the property-name list and the reply text, not for the inputSchema as a whole (descriptions, ge/le on offset/limit differ); the first PR must carry the df_tool.py prose verbatim and assert full-schema equality, not just names. dataset.py:8's 'telemetry layer left out' is already reported as audit-2 finding 8 and would be rewritten by this move anyway.

*Sharpened:* First PR stays in-repo and net-negative: spec.py (CorpusSpec/FilterSpec with kinds equals|contains|overlap_min|overlap_max mapped to Bitmap|none|BTree|BTree exactly as ingest_df's btree=('df_number','year_from','year_to') / bitmap=('language','issuingplacecountry') already implies) plus corpus_tools.register_corpus; df_spec carries df_tool.py's descriptions verbatim; the acceptance test is a golden diff of the full tools/list JSON (not just property names) plus the existing 201 tests unchanged. Extract to a published archive-mcp-core only after voudintilit is served from a ≤120-line spec+renderer against a synthetic 20-row JSONL — that second corpus is the proof the abstraction is right, and it can be built here without the network.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.70

Evidence checked: dataset.py:8, lib telemetry.py:13-14, mcp telemetry.py:18, routes.py:17 and observability.md:7 all say 'ported from / same shape as ra-mcp'; security.md:216 says the workspace packages are never published to PyPI; _answer is at df_tool.py:31, require_keyword/require_ordered_range at dataset.py:422/433, format_results at dataset.py:446, the filter→column map at search_operations.py:97-103 — all as cited. I re-ran scratchpad/spikes/spec_parity.py on lancedb 0.38.0 / fastmcp 3.4.4: 'param names identical: True' and 10/10 byte-identical replies including the blank keyword, inverted range, fuzzy+phrase and out-of-int32 year cases. So the mechanism/prose split is real and the in-repo first step is proven. The fit question is the XL half: a published, jointly-owned `archive-mcp-core` contradicts the current posture (nothing published; supply chain audited per-repo), adds a PyPI release pipeline, and asks ra-mcp — a 20-module server whose non-search tools cannot fit the spec — to re-host. dataset.py:1-6 already states the intent that df, voudintilit and tuomiokirjat share one implementation, and today the only way to add voudintilit is a second 218-line df_tool.py, so the spec/register_corpus step is what the project's own docstring asks for. Two cautions from the corpora: tuomiokirjat records are pages, not documents, so `IdSpec(column)` must allow a composite id (file_id + page) or the `<prefix>_get` half of the pair does not fit; and stdio.out shows fastmcp 4.0.3 already released against the ==3.4.4 pin, so the dynamic-signature trick needs the parity test as its guard on every bump. The 'every description must contain a digit' rule is gameable and is better a lint than a hard rejection.

*Corrections:* The proposal is two proposals. The first step (spec.py + corpus_tools.py inside this repo, df registered from a spec, hand-written tools deleted, parity proven by the existing suite) is S/M and consistent with dataset.py's stated intent. The published package, core.testkit, core.errors, core.readiness and ra-mcp re-hosting are an organisational decision that contradicts security.md:216's 'never published to PyPI' and needs its own release/signing pipeline; do not bundle them. IdSpec must support composite ids (tuomiokirjat file_id + page). The digit rule should be a test-time warning or ruff-style lint, not a refusal.

*Sharpened:* Land `CorpusSpec`/`FilterSpec` in ra_mcp_kansallisarkisto_lib/spec.py and `register_corpus(mcp, spec, get_db)` in the mcp package now, with df_spec carrying df_tool.py's prose verbatim and the hand-written df_search/df_get_charter deleted; the acceptance test is that test_tools.py, test_telemetry.py and mcp_smoke.py pass unchanged and the tools/list JSON is byte-identical to today's (extend spec_parity.py's 10 cases into a permanent golden test). Then prove the abstraction on voudintilit — the already-harvested 98,945-row corpus — as a ≤120-line spec plus renderer with zero edits to the mechanism, and make the FilterSpec kinds drive build_scalar_indexes (equals→Bitmap, overlap→BTree, contains→none) so ingest and tools cannot disagree. Only after two corpora share the spec, and only if ra-mcp wants it, extract a package; until then the 'family contract' is this repo's spec module plus its parity test.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel / Dagger+GHA engineer** — *not refuted*, confidence 0.70

Re-ran scratchpad/spikes/spec_parity.py: 'param names identical: True' and 10/10 outputs byte-identical, including the out-of-int32 year case (ValueError from lance-datafusion) and the fuzzy+phrase SearchInputError. FunctionTool.from_function exists (fastmcp/tools/function_tool.py:211) and accepts name/description/annotations; the inspect.Signature + __annotations__ trick works on fastmcp 3.4.4 as the spike shows. ra-mcp's live diplomatics_search_sdhk reply in this session ('SDHK results for 'X': showing 3 of 128 (offset 0) ... More results available. Use offset=3 to see the next page.') matches dataset.py:469 and :477 word for word, so the shared-envelope claim is verified. However the spike's generated handler calls lancedb_fts_search directly with get_db() (spec_parity.py:47) and never goes through DfSearch.search (search_operations.py:30-104), so: test_tools.py:92-104 (monkeypatches df_search.search on tools._search) would no longer intercept anything, and test_telemetry.py:119-146 asserts a 'DfSearch.search' span that the generated tool would not emit. The claim that the existing 201 tests pass unchanged against the generated tools is therefore false as spiked; it needs register_corpus to route through an ops facade with the ops span. The proposed testkit rule 'every FilterSpec.description must contain a digit' would reject the current df spec: year_min/year_max descriptions at df_tool.py:141-142 contain no digit. Effort XL is honest for a jointly-owned PyPI package across three repos; the in-repo first step is M, not the implied S, because of the facade/telemetry seam.

*Corrections:* (1) register_corpus must call a CorpusSearch/DfSearch-style facade that opens the ops span ('<Corpus>.search') and is the object tests monkeypatch — otherwise test_tools.py:92 and test_telemetry.py:119 fail; the spike bypasses that layer. (2) The digit rule as stated rejects today's year_min/year_max descriptions (df_tool.py:141-142); either exempt range filters or scope the rule to search_description + keyword_description + equals/contains filters. (3) Parity as measured is text + schema property order only; add the in-memory exporter's span names and error.type values to the oracle. (4) First-step effort is M (facade seam, telemetry, tests), not a 300-line negative one-afternoon PR.

*Sharpened:* Do the in-repo step first and make it falsifiable: add ra_mcp_kansallisarkisto_lib/spec.py (CorpusSpec/FilterSpec/IdSpec) and a generic CorpusSearch facade in search_operations.py that DfSearch becomes a thin subclass of (same 'DfSearch.search' span name kept via a spec.label), then corpus_tools.register_corpus(mcp, spec, get_search) generating df_search/df_get_charter. Oracle = the full 201-test suite plus test_telemetry's span names plus mcp_smoke.py, byte-identical tool descriptions (inputSchema deep-equal, not just property order). Only when voudintilit's synthetic spec serves with zero core edits does extracting archive-mcp-core to PyPI (XL, cross-org) become justified.

---

## `ecosystem-2` — nordic-archives router: one MCP endpoint that searches Diplomatarium Fennicum and SDHK together with provenance-tagged, partial-failure-tolerant merging

**survived 2/3, with corrections · effort L · impact 5/5**

**What the code does today.** This server exposes df_search/df_get_charter (df_tool.py:61,199); ra-mcp exposes diplomatics_search_sdhk with (keyword, place, language, author, limit, offset, research_context). A Baltic historian today opens two servers with two vocabularies: `issuingplace` here vs `place` there, exact-label `language` here (df_tool.py:107) vs substring there, `year_min/year_max` here and no year filter on SDHK. The same charters sit in both: DF 1086 = SDHK 15325 (identical incipit 'Jak Abram Brodherson, riddare, høwitzman pa Abo'), DF 526 = SDHK 5478 (Kung Magnus, Åbo, 1347, bishop Hemming, Kumo), and the README's own example DF 2457 = SDHK 24100 (Karl Knutsson, Raumo, 1442) — verified by calling ra-mcp in this session. FastMCP 3.4.4 composes servers in-process: `FastMCP.mount(server, namespace=...)` (spike scratchpad/spikes/mount.py) produced tools fi_df_search, fi_df_get_charter, se_diplomatics_search_sdhk and routed calls correctly. The spine's design already makes fan-out safe: handlers are sync and threadpooled (df_tool.py module docstring), and tools never raise (_answer, df_tool.py:31).

**The gap.** No federated search exists at the MCP level; a model must know both servers, translate vocabularies itself, and has no way to learn that two hits are the same charter or that one archive was down.

**The proposal.** A third package `nordic-archives-mcp` (FastMCP name 'nordic-archives'): mounts kansallisarkisto-mcp in-process (namespace 'fi') and ra-mcp via `FastMCP.as_proxy(url)` (namespace 'se'), discovered from each server's card (ecosystem-4). One composite tool `charters_search(keyword, year_min=None, year_max=None, place=None, archives=('fi','se'), limit=10, fuzzy=0)` that: parses keyword once with the family grammar (ecosystem-3) and serialises per backend; fans out with asyncio.gather and a 10 s per-archive timeout; never merges BM25 scores (incommensurable engines — and SDHK's 194 hits for a three-word query show it is not even AND); instead reports per-archive totals, then interleaves hits by year (both carry one), each line prefixed with a provenance tag `[FI DF 1086]` / `[SE SDHK 15325]` followed by the id URL from the card; applies the year filter client-side to SDHK rows (it has none server-side) and says so; collapses known twins from the concordance (ecosystem-6) into one block with both citations and both datings; renders a failed archive as one line 'SE: unavailable (TimeoutError) — results are Finland only' rather than failing the call. Exposes ≤6 tools (charters_search, fi_df_search, fi_df_get_charter, se_diplomatics_search_sdhk, se_search_transcribed, se_browse_document) via mount tool_names; its instructions carry the cross-archive vocabulary (Åbo/Turku, DF text is source language while SDHK 'Summary' is modern Swedish regest).

**Why it is once-in-a-lifetime.** The Swedish realm's medieval record is split by a modern border; nobody has put the two national editions behind one query with honest provenance. The hard part — two servers that share an envelope and never raise — is already built; the router is the payoff of that discipline.

**How we would know.** A gold set of 20 queries built from the twins found today (start: 'Raumo 1442', 'Abram Brodherson', 'Hemming Kumo') returns both citations in one reply for ≥18; with the se proxy pointed at a dead port, fi results arrive in <10 s with the one-line notice; the router's tools/list is ≤6 tools; a test asserts every hit line begins with a `[FI …]` or `[SE …]` tag.

**Risks.** Depends on ra-mcp being reachable over streamable HTTP with a stable tool contract this repo does not control; SDHK has no year filter, so year bounds are post-filtered and totals for SE become approximate — must be labelled. Interleaving by year hides relevance; the alternative (two blocks) is safer and should be the default until the gold set says otherwise. Adds a third deployable and a network dependency to a family whose stated philosophy is 'stdio on a laptop costs nothing' — the router must also run stdio with fi in-process and se optional.

**First step.** Add `packages/nordic-router` with the mount of kansallisarkisto_mcp (namespace 'fi') and an `se` stub server carrying ra-mcp's exact diplomatics_search_sdhk signature; implement charters_search over the two with two-block output and the failure line; test in-memory with the 18-row fixture and a stub that raises/sleeps. No network needed.

**Spike.** scratchpad/spikes/mount.py (already run): FastMCP 3.4.4 mount(namespace=) of the real kansallisarkisto server plus an sdhk-shaped stub, calls routed through the router. Next: add charters_search with asyncio.gather + wait_for(10 s) and a stub that sleeps 30 s, and time the reply.

### Skeptics

**Meticulous code reviewer — premise-checking every cited file, line, library call and spike against the repository and by re-running the spikes and calling ra-mcp** — *not refuted*, confidence 0.75

df_tool.py:61 and :199 are the two tool names; :107-120 documents language as an exact label. Loaded ra-mcp's diplomatics_search_sdhk schema: (keyword, place, language, author, limit, offset, research_context), no year filter, place/language/author are substring — as claimed. Verified all three twins by calling ra-mcp: SDHK 15325 (Åbo, '14000110; omdat. från 13990111', 'Jak Abram Brodherson riddare, høwitzman pa Abo…') matches fixture DF 1086 (Åbo, 1399, same incipit); SDHK 5478 (13470901, Åbo, Kung Magnus, 'Wy. magnus med gudz nadhom…') matches fixture DF 526 (1347, Åbo, 'Vi Magnus, met Gudhz nampn…'); SDHK 24100 (14420415, Åbo slott, Karl Knutsson, Raumo privileges) matches README.md:59-68 DF 2457 (1442, Åbo, Raumo). Re-ran scratchpad/spikes/mount.py on fastmcp 3.4.4: FastMCP.mount(server, namespace=None, as_proxy=None, tool_names=None, prefix=None) exists; router listed fi_df_get_charter, fi_df_search, se_diplomatics_search_sdhk and routed both calls; FastMCP.as_proxy(backend: Client|ClientTransport|FastMCP|AnyUrl|Path|MCPConfig|dict|str) exists. df_tool.py module docstring confirms sync, threadpooled handlers.

*Corrections:* The '194 hits for a three-word query' figure could not be reproduced (query unknown), but the OR-like behaviour it illustrates is confirmed: 'Hemming Kumo' → 1,212 hits, 'Abram Brodherson' → 128, 'Brodherson' alone → 1. SDHK's research_context is described as 'Used for logging only.', not 'telemetry'.

*Sharpened:* Build packages/nordic-router with fi mounted in-process and an se stub that carries the real diplomatics_search_sdhk signature; charters_search defaults to two labelled blocks (FI then SE) with per-archive totals, a client-side year post-filter on SE marked '(approximate: SDHK has no year filter)', asyncio.gather with wait_for(10 s), and a one-line 'SE: unavailable (TimeoutError)' on failure. Seed the gold set from the three verified pairs (DF 1086/SDHK 15325, DF 526/SDHK 5478, DF 2457/SDHK 24100) so the test asserts both citations appear in one reply; use mount(tool_names=…) to cap the surface at six tools.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *refuted*, confidence 0.60

Evidence is sound: I re-ran scratchpad/spikes/mount.py — fastmcp 3.4.4 `FastMCP.mount(server, namespace=…)` yields fi_df_search, fi_df_get_charter, se_diplomatics_search_sdhk and routes calls; `FastMCP.as_proxy` exists; df_tool.py:61/199/107 are as cited; handlers are sync (df_tool.py:6-10). I verified the twin claim myself by calling ra-mcp: SDHK 15325 ('Jak Abram Brodherson riddare, høwitzman pa Abo, helsar idher alla som i Masko sokn ok i Pyke…', dated '14000110; omdat. från 13990111') is DF 1086 in the fixture ('Jak Abram Brodherson, riddare, høwitzman pa Abo, helsar idher alla, som i Masko sokn ok j…', 1399). ra-mcp's diplomatics_search_sdhk schema has place/language/author/limit/offset/research_context and no year filter — also as stated. The refutation is fit and sequencing, not evidence. (1) What the router uniquely adds over an MCP client that simply connects to both servers is twin-collapsing (depends on ecosystem-6, which I refute), vocabulary translation (ecosystem-3's package half) and cards (ecosystem-4's `.well-known`) — strip those and the two-block, no-merge output the proposal itself recommends as the safe default is what the model already gets from two servers. (2) It is a third deployable with a runtime dependency on ra-mcp's HTTP availability and an uncontrolled tool contract, in a project whose bar is 'scales to 7.8M pages without changing shape' — voudintilit and tuomiokirjat are the next corpora and they are inside this archive. (3) The natural home is ra-mcp (already the multi-module server with mount) or a separate repo, not this one. What survives cheaply and belongs here: one sentence in tools.py's instructions telling the model that charters issued in Sweden (1,207) and many royal acts are also edited in SDHK, reachable via ra-mcp's diplomatics_search_sdhk, that DF text is source language while SDHK 'Summary' is a modern-Swedish regest, and that the two editions can date the same act differently (DF 1086 = SDHK 15325: 1399 vs 1400-01-10).

*Corrections:* The federation payoff depends on three other proposals (3, 4, 6); on its own the router reduces to a namespace mount and a two-block fan-out that any MCP client already performs. Not a code-correctness issue — the spike and the twins are real.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel / Dagger+GHA engineer** — *not refuted*, confidence 0.80

Re-ran scratchpad/spikes/mount.py: fastmcp 3.4.4 mount(server, namespace=, as_proxy=, tool_names=, prefix=) produced ['fi_df_get_charter','fi_df_search','se_diplomatics_search_sdhk'] and routed calls. Wrote and ran scratchpad/judge_d3/deadproxy.py: with se = create_proxy('http://127.0.0.1:1/mcp') mounted under namespace 'se', the router's list_tools returns only the fi tools in 0.1 s (ProxyProvider logs 'Client failed to connect'), fi_df_search answers normally, and a call to se_diplomatics_search_sdhk returns is_error=True 'Unknown tool' — the dead backend degrades the router, it does not break it. asyncio.gather over fi + wait_for(2 s) on a 30 s stub returned ['CallToolResult','TimeoutError'] in 2.0 s, so the per-archive timeout design works with sync threadpooled handlers. Verified the live ra-mcp tool schema: diplomatics_search_sdhk(keyword, author, language, limit, offset, place, research_context) — no year filter, so client-side year filtering is necessary as claimed; 'Abram Brodherson' returns 128 hits (OR-like), consistent with the SDHK 194-hit observation. Verified the twin DF 1086 = SDHK 15325 by calling ra-mcp with 'Brodherson Masko Pyke' (SDHK 15325, Åbo, '14000110; omdat. från 13990111', identical incipit). Effort L is honest for the stub-backed first step; the network-facing router adds a deployable, as the risks say.

*Corrections:* (1) FastMCP.as_proxy is deprecated in 3.4.4 (server.py:2436-2470 emits FastMCPDeprecationWarning); use fastmcp.server.create_proxy(url). (2) mount's tool_names is a rename map (server.py:2218-2226), not an allowlist — capping the router at ≤6 tools needs server.disable(...)/enable(tags=..., only=True) (server.py:162-163) or a visibility transform on the mounted providers. (3) When the se backend is down its tools disappear from tools/list rather than erroring, so charters_search must own its own Client to se (with wait_for) instead of calling through the mounted se_ tool, or the 'SE: unavailable' line can never be produced. (4) SDHK replies carry an 'Edition:' source-language text block as well as the modern-Swedish Summary (seen in this session's ra-mcp output), so the vocabulary note should say SDHK offers both.

*Sharpened:* packages/nordic-router with router.mount(kansallisarkisto_mcp, namespace='fi') in-process and an optional se backend via create_proxy(url) guarded by a settings flag (stdio-on-a-laptop keeps fi only). charters_search holds a dedicated fastmcp Client to se, fans out with asyncio.gather + wait_for(10 s), returns two labelled blocks by default (year-interleave behind a flag until the 20-query gold set decides), prefixes every hit with [FI DF n] / [SE SDHK n], applies year bounds client-side to SE rows and labels SE totals as pre-filter counts. Tests: stub that sleeps 30 s -> reply in ≤10.5 s with the one-line notice; stub that raises -> fi-only reply; a test that tools/list of the router is exactly the six names after the disable() filter; a dead-port se at boot -> tools/list still succeeds (proved in judge_d3/deadproxy.py).

---

## `ecosystem-3` — One query grammar for the family: ra-mcp's inline syntax silently returns zero on this engine today

**survived 3/3, with corrections · effort M · impact 4/5**

**What the code does today.** ra-mcp's search_transcribed tells the model 'always use fuzzy search (~)', 'wildcards (troll*, st?ckholm)', 'Quoted phrases return 0 results — never quote'; df_search says the opposite: 'quote a phrase' (df_tool.py:84-86) and fuzziness is a parameter (df_tool.py:154). dataset.py:295 routes anything containing '"' to the parser and everything else to MatchQuery (dataset.py:305), whose tokenizer splits `Åbo~1` into a required token '1'. Measured on the fixture (scratchpad/spikes/syntax.py): `Åbo` 7 hits, `Åbo` fuzzy=1 8 hits, `Åbo~1` 0, `konung~1` 0, `Åb*` 0, `Åb?` 0, `ecclesi*` 3 (only because stemming happens to cover it), `Åbo 1` 0 — i.e. every ra-mcp-syntax query returns a clean 'No results' with no error, so a model that learned Riksarkivet's syntax concludes the Finnish archive has nothing. The reverse also holds: SDHK answered a 3-word keyword with 194 hits (OR-like) while df requires all words (dataset.py:305 FullTextOperator.AND). The project already refuses the analogous silent drop for fuzzy+phrase (dataset.py:295-300).

**The gap.** The family has two incompatible keyword dialects and no translation layer, so a federated query cannot send one string to both engines, and cross-training between servers actively harms recall.

**The proposal.** `archive_mcp_core.query`: `parse(keyword) -> ArchiveQuery(terms=[Term(text, fuzzy: int|None, prefix: bool)], phrases=[str], mode='all'|'any')` — one grammar: bare terms, `"phrase"`, `term~N` (N≤2), `term*`. Two serialisers: `to_lancedb(q)` (MatchQuery with fuzziness=max(term.fuzzy) or the phrase parser; prefix terms rejected) and `to_elastic(q)` for ra-mcp's engine (inline ~N and *, phrases dropped with a notice). On this server, `lancedb_fts_search` accepts `~N` inline as a synonym for fuzzy=N and answers `*`/`?` with SearchInputError: 'prefix wildcards are not supported on this archive; stemming already matches inflections (konung finds konungen), use fuzzy=1 for spelling variants'. Publish the grammar × engine conformance table as a pytest parametrisation both repos run.

**Why it is once-in-a-lifetime.** It turns 'four ways to get a silent zero' into a contract a model can learn once for every Nordic archive; without it, the router in ecosystem-2 cannot exist.

**How we would know.** Zero silent zeros: a fixture test asserts that every keyword containing `~`, `*` or `?` either returns ≥ the exact-match hit count (`Åbo~1` ≥ 7) or starts with 'Error:'; the conformance table has one row per grammar feature per engine and is green in both repos.

**Risks.** LanceDB's MatchQuery has one fuzziness for the whole query, not per term, so `bref~1 Åbo` widens Åbo too — document it or split into two MatchQueries and intersect ids in Python (bounded by MAX_TOTAL_COUNT, dataset.py:37). Accepting `~N` inline contradicts the current descriptions and must be added to them. ra-mcp's side of the conformance table cannot be run here.

**First step.** In lancedb_fts_search, detect `[~*?]` before routing: map `term~N` to fuzzy=N, raise SearchInputError for `*`/`?` with the sentence above; add the four measured cases to test_search_invariants.py. ~40 lines.

**Spike.** scratchpad/spikes/syntax.py (already run) — prints the hit counts above; re-run after the change to see `Åbo~1` become 8 and `Åb*` become an 'Error:' string.

### Skeptics

**Meticulous code reviewer — premise-checking every cited file, line, library call and spike against the repository and by re-running the spikes and calling ra-mcp** — *not refuted*, confidence 0.85

Loaded ra-mcp's search_transcribed description: 'always use fuzzy search (~)', 'wildcards (troll*, st?ckholm) and fuzzy (stockholm~1) work', 'Quoted phrases return 0 results … never quote' — verbatim as claimed. df_tool.py:80-81 says 'quote a phrase'; :154-169 makes fuzzy a parameter. dataset.py:295 routes '"' to the parser, :304-305 builds MatchQuery with FullTextOperator.AND, :300-301 refuses fuzzy+phrase. Re-ran scratchpad/spikes/syntax.py on the fixture: Åbo 7, Åbo fuzzy=1 8, Åbo~1 0, Åbo~1 match_all=False 8, Åb* 0, Åb? 0, konung 3, konung~1 0, konung* 3, ecclesia 3, ecclesi* 3, bare '1' 1, 'Åbo 1' 0 — every number matches, and konung* == konung shows the tokenizer strips '*' while '~1' becomes a required token '1'. SDHK is OR-like (see ecosystem-2). Inspected lancedb.query: MatchQuery fields are (query, column, boost, fuzziness, max_expansions, operator, prefix_length, document_granularity) — one fuzziness per query, as the risk says.

*Corrections:* Line cite: the 'quote a phrase' prose is df_tool.py:80-81, not 84-86. The risk's fallback ('split into two MatchQueries and intersect ids in Python') is unnecessary: lancedb.query exports BooleanQuery(queries) and Occur, and I verified on the fixture that BooleanQuery([(Occur.MUST, MatchQuery('bref', fuzziness=1)), (Occur.MUST, MatchQuery('Åbo', fuzziness=0))]) executes and returns 2 — per-term fuzziness is available engine-side.

*Sharpened:* parse(keyword) → ArchiveQuery(terms=[Term(text, fuzzy, prefix)], phrases, mode); to_lancedb serialises per-term fuzziness as a BooleanQuery of MUST/SHOULD MatchQuery per term (no whole-query widening, no Python intersection), phrases as PhraseQuery, and raises SearchInputError for '*'/'?' with the stemming sentence. First step in lancedb_fts_search: regex for [~*?] before the '"' branch, map term~N to per-term fuzziness, refuse wildcards; add the measured rows (Åbo~1 ≥ 7, Åb* → 'Error:', konung* → 'Error:', 'Åbo 1' unchanged) to test_search_invariants.py and update the df_search description, which today says nothing about '~'.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.80

I re-ran scratchpad/spikes/syntax.py against the 18-row fixture and reproduced every number: Åbo 7, Åbo fuzzy=1 8, Åbo~1 0, Åb* 0, Åb? 0, konung~1 0, ecclesi* 3, 'Åbo 1' 0, bare '1' 1 — so `~1` is tokenised into a required token '1' on the MatchQuery path (dataset.py:305) and `*`/`?` are silently stripped by the tokenizer (konung* = konung = 3), exactly the silent-drop the project already refuses for fuzzy+phrase at dataset.py:295-301. ra-mcp's schema (loaded this session) confirms the other dialect. Then I probed what the proposal did not: lancedb 0.38.0's *string* parser (the path dataset.py:302 already uses for quoted phrases) understands `term~N` per term — Åbo~1 → 8, kirkia~1 → 1, konung~1 → 4, 'bref~1 Åbo' → 8 vs 'bref Åbo' → 7 — but it is OR-only ('konung Åbo' → 7; 'konung AND Åbo' → 7, AND is a literal), and it does not do wildcards (Åb* → 0). And `BooleanQuery([(Occur.MUST, MatchQuery('konung', col, fuzziness=1)), (Occur.MUST, MatchQuery('Åbo', col))])` → 5 while plain AND 'konung Åbo' → 3: per-term fuzziness with match_all honoured, inside the engine. So the proposal's stated risk ('one fuzziness for the whole query… split into two MatchQueries and intersect ids in Python') is wrong — the engine already has the primitive. Fit: the first step is exactly the project's existing rule (dataset.py:295-300: never drop a caller's intent silently), it is ~40 lines in the lib with no new dependency, and it protects the historian who learned the Riksarkivet syntax. The cross-repo package/conformance-table half is ecosystem-1's scope and cannot be run here.

*Corrections:* LanceDB's query parser already accepts inline `term~N` with per-term distance (verified on 0.38.0), so a keyword containing `~` can be routed like a quoted phrase — but that parser is OR-only, so `match_all=True` must instead be built as `BooleanQuery` of `Occur.MUST` `MatchQuery(term, fuzziness=n)` clauses, which works (konung~1 + Åbo MUST → 5). No Python-side id intersection is needed and MAX_TOTAL_COUNT is not involved. `*` and `?` are stripped by the tokenizer rather than parsed, which is why `konung*` looks like it works (it is just `konung`) and `Åb*` returns 0; they must be rejected, not translated. The measured-outcome bound should be `Åbo~1` == the fuzzy=1 count (8), not merely ≥ 7. Drop 'archive_mcp_core.query' as a package; the grammar lives in dataset.py until ecosystem-1's spec exists.

*Sharpened:* In lancedb_fts_search, before routing: parse the keyword into terms with an optional `~N` (N≤2) suffix; if any `*` or `?` remains raise SearchInputError('prefix wildcards are not supported on this archive; stemming already matches inflections (konung finds konungen) — use fuzzy=1 for spelling variants'); if any term carries `~N`, build the request as BooleanQuery(MUST/SHOULD per match_all) of MatchQuery(term, fuzziness=N or the fuzzy= argument) so per-term distance and match_all are both honoured in-engine; an inline `~N` combined with a quoted phrase raises the same SearchInputError as fuzzy+phrase does today. Add the eight measured cases to test_search_invariants.py as a parametrised 'no silent zero' property: every keyword containing ~, * or ? returns either exactly the fuzzy-parameter count or a string starting with 'Error:'. Update the df_search keyword description (df_tool.py:92-101) with one sentence: 'term~1 is accepted as a per-word spelling tolerance; * and ? are refused'. Record the conformance rows (grammar feature × engine) in docs/how-it-works/search-tips.md so ra-mcp can copy the table when it wants parity.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel / Dagger+GHA engineer** — *not refuted*, confidence 0.85

Re-ran scratchpad/spikes/syntax.py on the 18-row fixture: Åbo 7, Åbo fuzzy=1 8, Åbo~1 0, Åb* 0, Åb? 0, konung 3, konung~1 0, konung* 3, ecclesi* 3, Åbo 1 0 — every number in the proposal reproduces. Then ran scratchpad/judge_d3/parser.py to find the mechanism: passing the string straight to table.search('Åbo~1', query_type='fts') (lance's query parser, the path dataset.py:295 uses only for quoted keywords) returns 8, and 'konung~1' returns 4 — the engine's parser DOES understand ~N. The silent zero comes solely from dataset.py:303-305 wrapping unquoted keywords in MatchQuery, whose tokeniser splits '~1' into a required token '1'. '*' and '?' return 0 on both paths, so prefix wildcards are genuinely unsupported; MatchQuery.prefix_length (lancedb/query.py:491-493, 507) is a fuzzy-prefix anchor, not a wildcard (MatchQuery('Åb', fuzziness=2, prefix_length=2) -> 0). MatchQuery.fuzziness is a single per-query int (query.py:503), confirming the stated risk that 'bref~1 Åbo' widens Åbo too. The parser is also OR-ish with wildcards ('Åb* "de ecclesia"' -> 9), so routing wildcards to the parser would be a worse silent failure than rejecting them. The ~40-line first step is an honest estimate; the ra-mcp half of the conformance table cannot run here, as stated.

*Corrections:* The mechanism in evidence_today is half right: lance's string parser already accepts term~N (Åbo~1 -> 8 via the quoted-keyword path); the zero is caused only by dataset.py:295-305 sending unquoted keywords to MatchQuery. So the fix is a pre-parse in lancedb_fts_search that strips ~N into fuzziness=max(N) before building MatchQuery, not a new engine feature. Prefix wildcards are unsupported on both paths (measured 0), and MatchQuery.prefix_length does not implement them, so the SearchInputError for */? is the correct answer, not a workaround.

*Sharpened:* In lancedb_fts_search (dataset.py:279-305), before routing: regex r'(\S+)~(\d)' -> collect per-term fuzziness, rewrite the keyword without the suffix, set fuzziness=max(found, fuzzy) and document that MatchQuery fuzziness is query-wide (or split into one MatchQuery per fuzzy term joined with BooleanQuery MUST — lancedb/query.py exposes __and__ -> BooleanQuery, which keeps AND semantics without a Python intersection); r'[*?]' -> SearchInputError('prefix wildcards are not supported on this archive; stemming already matches inflections (konung finds konungen), use fuzzy=1 for spelling variants'). Four fixture cases into test_search_invariants.py: Åbo~1 == Åbo fuzzy=1 == 8, konung~1 >= 3, Åb* and Åb? start with 'Error:', plus a property test that no keyword containing ~, * or ? ever returns 'No ... results' silently. Publish the grammar x engine table as a parametrised test both repos can run; ra-mcp's rows stay skipped here.

---

## `ecosystem-4` — Stamp the snapshot's provenance into the table and publish a machine-readable server card at /.well-known/archive-mcp.json

**survived 3/3, with corrections · effort S · impact 4/5**

**What the code does today.** The harvester writes report.json with coverage_pct per corpus (harvest.py:13,329,334) and the docs insist 'A harvest is a snapshot, and the index moves — voudintilit went from 99,031 to 99,125' (data-sources.md), but nothing downstream carries that: DF_SCHEMA has no metadata (ingest.py:30), create_table drops it (ingest.py:144), /ready answers only {"status":"ready","table":"df"} (routes.py:55), the OTel resource carries service.version alone (telemetry.py:71), and format_results (dataset.py:446) has no snapshot line. A historian citing 'DF 1086 via kansallisarkisto-mcp' cannot say which harvest, and a router cannot discover what a server serves, which id scheme it uses, or which query grammar it speaks. Spike (scratchpad/spikes/metadata.py): nine `archive.*` keys set via DF_SCHEMA.with_metadata survive create_table, build_fts_index and a reopen from a fresh connection, with FTS intact.

**The gap.** Snapshot identity and family contract are documented in prose only; neither a person nor a program can read them from a running server.

**The proposal.** (1) `ingest_df(db, path, provenance=read_report('.data/df/report.json'))` writes `archive.archive=kansallisarkisto`, `archive.corpus=df`, `archive.source=<url>`, `archive.harvested_at`, `archive.documents`, `archive.coverage_pct`, `archive.report_sha256`, `archive.id_url_template`, `archive.core_version` as schema metadata; `dataset.table_provenance(db, table) -> Provenance`. (2) /ready becomes `{"status":"ready","corpora":{"df":{"documents":6876,"harvested_at":"2026-07-14","coverage_pct":100.0}}}`. (3) New route `GET /.well-known/archive-mcp.json` and an MCP resource `archive://card` with `{family:'archive-mcp', core_version, archive, corpora:[...provenance...], id_schemes:[{prefix:'DF', url_template}], query_grammar:'archive-query/1', tools:[...]}`. (4) One footer line in format_results: 'Snapshot harvested 2026-07-14 from Sisältöhaku (6,876 charters, 100% of the live index)'. (5) `archive.snapshot.harvested_at` on the OTel resource.

**Why it is once-in-a-lifetime.** It makes a citation reproducible ('DF 1086, kansallisarkisto-mcp snapshot 2026-07-14') and makes the family discoverable by machines — the router reads cards instead of a config file, and a mismatched core_version or grammar is refused rather than silently merged.

**How we would know.** test_ready_reports_snapshot passes; a server built from an ingest without report.json still boots and reports provenance 'unknown' rather than failing; the router (ecosystem-2) refuses a card whose query_grammar is absent; every search reply ends with the snapshot line.

**Risks.** Changing provenance requires re-ingest (it is table metadata) — acceptable, that is what a snapshot is. The footer adds ~90 characters per reply; keep it one line. `.well-known` is convention, not a standard for MCP; the MCP resource is the portable half.

**First step.** Add `provenance` to ingest_df and `table_provenance` to dataset.py, wire /ready to it, add the footer line; ~60 lines plus three tests.

**Spike.** scratchpad/spikes/metadata.py (already run): metadata keys survive reopen; extend by asserting `db.open_table('df').schema.metadata[b'archive.harvested_at']` from a second lancedb.connect.

### Skeptics

**Meticulous code reviewer — premise-checking every cited file, line, library call and spike against the repository and by re-running the spikes and calling ra-mcp** — *not refuted*, confidence 0.85

harvest.py:13 documents report.json as 'coverage summary' and :333 writes coverage_pct; data-sources.md:68-69 has the 99,031→99,125 sentence. ingest.py:30 DF_SCHEMA has no metadata; create_table at :144 passes that bare schema; routes.py:55 returns {'status':'ready','table':detail}; mcp telemetry.py:71 Resource.create has service.name/version only; format_results (dataset.py:446-479) has no snapshot line. Re-ran scratchpad/spikes/metadata.py: nine archive.* keys survive create_table + build_fts_index + a fresh lancedb.connect, FTS still answers. Extended it myself: metadata also survives build_scalar_indexes (BTree×3 + Bitmap×2) and reopen, and even appears on the result schema of a projected FTS query (to_arrow().schema.metadata == {b'archive.harvested_at': b'2026-07-14'}). FastMCP 3.4.4 supports resources, so archive://card is feasible.

*Corrections:* 'create_table drops it' is inaccurate — DF_SCHEMA never carries metadata, so nothing is dropped; the fix is to attach it. .data/df/ exists but is empty in this checkout (no report.json), so the 'provenance unknown' path is the one exercised here and must be first-class, not a fallback. Coverage 100% for df is a plausible but unverified example figure; data-sources.md:61 gives 98.25% for the whole harvest.

*Sharpened:* ingest_df(db, path, provenance: Provenance | None) writes archive.* schema metadata (read from report.json when present, else archive.harvested_at='unknown'); dataset.table_provenance(db, table) reads table.schema.metadata; /ready returns per-corpus documents/harvested_at/coverage_pct; format_results gains one footer line and format_charter one line after the header; the OTel resource gains archive.snapshot.harvested_at. Because the metadata rides on the query result schema, the footer can be rendered from the result itself without a second open_table — zero extra I/O per search. Add test_ready_reports_snapshot and a test that an ingest without report.json still boots.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.80

Evidence checked: harvest.py writes report.json with coverage_pct (lines ~322-334); data-sources.md:68-69 states 'voudintilit went from 99,031 to 99,125' and 'nothing in a harvested file signals that drift'; DF_SCHEMA (ingest.py:30-47) carries no metadata and create_table (ingest.py:144) receives it bare; /ready (routes.py:55) answers only status+table; the OTel resource (mcp telemetry.py:71) is service.name+service.version; format_results and format_charter carry no snapshot line. I re-ran scratchpad/spikes/metadata.py on lancedb 0.38.0: nine `archive.*` keys set via DF_SCHEMA.with_metadata survive create_table, build_fts_index (replace=True) and a reopen from a second connection, and FTS still answers (DF 670). Note .data/df is empty in this checkout, so the ingest must accept a missing report.json — the proposal already says so. Fit is strong: the project's own docs insist a harvest is a snapshot and that the historian should cite the archive; making the snapshot identity ride with the table is the cheapest way to make 'DF 1086 via kansallisarkisto-mcp, snapshot 2026-07-14' reproducible, and it is lib-only (pyarrow metadata), costs nothing on stdio, and needs no new dependency. The weak parts: `archive.core_version` and `query_grammar:'archive-query/1'` presuppose ecosystem-1/3; `.well-known/archive-mcp.json` is a convention for a router that does not exist; and a footer on every search page is 90 characters of repetition the model reads 25 times per session. The strongest unstated value is for the lancedb-upgrade question the resilience lenses keep raising: the FTS config and library versions that built the index are not recorded anywhere on disk today.

*Corrections:* Drop `archive.core_version` and `query_grammar` (no core, no grammar exist); record instead `archive.lib_version` (ra-mcp-kansallisarkisto-lib), `archive.lancedb_version` and the FTS config actually passed at build_fts_index (language, stem, remove_stop_words, with_position, ascii_folding, max_token_length) so a table can say how it was built. Put the snapshot line in df_get_charter (the citation moment), /ready, the boot table-status log and the OTel resource; in search results emit it once in the header or not at all rather than as a per-page footer. The `.well-known` route is optional; the MCP resource `archive://card` (or a plain `df_snapshot` tool) is the portable half.

*Sharpened:* `ingest_df(db, path, provenance: Provenance | None)` writes `archive.{archive,corpus,source,harvested_at,documents,coverage_pct,report_sha256,id_url_template,lib_version,lancedb_version,fts.*}` as schema metadata read from .data/df/report.json when present and 'unknown' otherwise; `dataset.table_provenance(db, table) -> Provenance` reads it back. Surface it in four places: server.py's boot log line ('df: 6,876 charters, harvested 2026-07-14, 100% of the live index, built by lib 0.1.0 / lancedb 0.38.0'), /ready's JSON, the OTel resource (`archive.snapshot.harvested_at`), and the first line of df_get_charter so a citation carries its snapshot. Add a boot warning when the on-disk lancedb_version or fts.* differs from what the running code would build — the upgrade canary test_search_invariants.py:42 can only check for a table built in-process today. Three tests: metadata survives reopen; a table ingested without report.json boots and reports 'unknown'; /ready and df_get_charter show the same harvested_at.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel / Dagger+GHA engineer** — *not refuted*, confidence 0.85

Re-ran scratchpad/spikes/metadata.py: nine archive.* keys set via DF_SCHEMA.with_metadata survive create_table(mode='overwrite'), build_fts_index and a reopen from a second lancedb.connect (harvested_at b'2026-07-14T09:12:00Z', FTS still finds DF 670, table version 2). Read the library path: lancedb/table.py:754-756 (sanitize_create_table takes metadata from schema.metadata when none is passed) and :396-398 (_merge_metadata into the target schema), so schema-metadata persistence is a supported code path in 0.38, not an accident. Confirmed the gaps cited: ingest.py DF_SCHEMA (lines 30-48 of the file) has no metadata, routes.py /ready returns {'status':'ready','table':detail}, telemetry.py:71 Resource has service.name/service.version only, dataset.py:446-479 has no footer. @mcp.resource exists (fastmcp/server/server.py:1862) and custom_route is already used (routes.py), so the card and archive://card are trivial. Effort S is honest for parts 1, 2, 4 (~60 lines + 3 tests); the card and OTel resource attribute add ~50 more.

*Corrections:* (1) scripts/harvest.py report.json (lines 329-340) has no harvested_at: its fields are index, mode, complete, index_total, documents_written, documents_unreachable, slices_remaining, coverage_pct, http_requests, elapsed_seconds, shortfalls — the timestamp must be added to the harvester's report (one line) or taken from the file mtime with a lower-confidence label. (2) The example 'coverage_pct 100.0 / 100% of the live index' contradicts the brief's measured 98.25% for df; the footer must print the report's number, whatever it is. (3) 'archive.snapshot.harvested_at' on the OTel Resource: init_telemetry() runs at server.py:92 before the table is opened (log_table_status at :94, lazy get_search later), so the Resource is frozen before the metadata is readable — either read table_provenance before init_telemetry (cheap: open_table + schema) or put harvested_at on spans/ready only.

*Sharpened:* ingest_df(db, path, provenance: Provenance | None) writes archive.* keys via DF_SCHEMA.with_metadata (proved to survive), with harvested_at sourced from a new report.json field written by harvest.py; dataset.table_provenance(db, table) returns a frozen dataclass with 'unknown' defaults so an ingest without report.json still boots. /ready returns corpora:{df:{documents, harvested_at, coverage_pct}} from that; format_results grows one footer line rendered from provenance ('Snapshot harvested 2026-07-14 from Sisältöhaku (6,876 charters, 98.25% of the live index)'); GET /.well-known/archive-mcp.json and @mcp.resource('archive://card') serve the same dict, including query_grammar and id_schemes. Tests: metadata round-trip through a fresh connect (extend the spike), /ready shape, the footer present in every non-error search reply, and the unknown-provenance path. ~110 lines total, S.

---

## `ecosystem-5` — A family-wide telemetry attribute namespace and research_context parity with ra-mcp

**survived 3/3, with corrections · effort S · impact 3/5**

**What the code does today.** The operations span names its attributes after the corpus — 'df.keyword', 'df.limit', 'df.language' (search_operations.py:84-95), 'df.number'/'df.found' (search_operations.py:116) — and the metrics after the server: kansallisarkisto.lancedb.queries/errors/query.duration/results (dataset.py:81-87); the LanceDB span uses semconv db.* (dataset.py:320). voudintilit would presumably get 'voudintilit.keyword', and ra-mcp its own names, so one dashboard cannot union searches across corpora or archives. Every ra-mcp tool takes `research_context: 'Brief summary of the user's research goal. Used for telemetry only.'`; df_search has no such parameter, so the family cannot answer 'what are historians asking for' consistently — the thing the .results zero bucket (dataset.py:87) was designed to approximate.

**The gap.** Telemetry is per-server bespoke; the family has no shared vocabulary for spans, metrics or research intent.

**The proposal.** Freeze in core: ops-span attributes `archive.archive`, `archive.corpus`, `archive.query.text`, `archive.query.match_all`, `archive.query.fuzzy`, `archive.filter.<name>` (only when set, as today), `archive.result.total`, `archive.result.capped`, `archive.result.returned`, `archive.lookup.id`, `archive.lookup.found`, `archive.research_context`; metrics `archive.search.queries|errors|duration|results` with attribute set {archive.archive, archive.corpus, error.type} only; keep db.* on the engine span. Add `research_context: str | None = None` to df_search/df_get_charter with ra-mcp's exact description; record it on the ops span only, never on a metric, never in logs. Span names: `<Corpus>.search` and `search <table>` stay.

**Why it is once-in-a-lifetime.** One Grafana panel across Finland and Sweden: query volume, zero-result rate and p95 by corpus, plus a corpus of research intents — the first cross-archive signal of what historians actually ask and what the archives cannot answer.

**How we would know.** test_telemetry asserts every ops-span attribute key starts with `archive.`; metric names are byte-identical in both repos; a research_context passed by the client appears on the ops span and on no metric.

**Risks.** research_context is free text about a person's research and is PII-adjacent — document retention and keep it off logs; it slightly enlarges the tool schema the model reads. Renaming attributes breaks any existing dashboard (none exist yet — nothing has been released).

**First step.** Rename the attribute dict in DfSearch.search/get_charter and the four metric names; add research_context to both tools; update observability.md's table. ~40 lines, one PR.

**Spike.** Run test_telemetry.py's in-memory exporter and print the attribute keys of the DfSearch.search span before and after; assert the `archive.` prefix.

### Skeptics

**Meticulous code reviewer — premise-checking every cited file, line, library call and spike against the repository and by re-running the spikes and calling ra-mcp** — *not refuted*, confidence 0.80

search_operations.py:84-95 sets df.keyword/df.limit/df.offset/df.match_all/df.fuzzy plus df.language/issuingplace/country/year_min/year_max only when set; :116/:120/:134 set df.number/df.found. dataset.py:81-87 create kansallisarkisto.lancedb.queries/errors/query.duration/results, with the comment at :84-86 describing the zero bucket exactly as the proposal says; :319-320 uses db.system/db.collection.name/db.operation.name/db.query.text on the engine span. observability.md:77-82 documents these names, so the page would need the rename. GitHub API for AI-Riksarkivet/kansallisarkisto-mcp returns [] for both /releases and /tags and git tag is empty — nothing released, so no dashboard exists to break.

*Corrections:* 'Every ra-mcp tool takes research_context' is false: view_document, view_manifest, viewer_navigate and search_guides (schemas loaded this session) have no such parameter; only the search/browse tools do. And ra-mcp does not have one 'exact description' to copy — diplomatics_search_sdhk and search_sbl say 'Used for logging only.' while search_transcribed, search_metadata and browse_document say 'Used for telemetry only.' The family would have to pick one wording; 'telemetry' matches how this repo records it.

*Sharpened:* Rename the ops-span attribute dict to archive.archive/archive.corpus/archive.query.text/archive.query.match_all/archive.query.fuzzy/archive.filter.<name>/archive.result.{total,capped,returned}/archive.lookup.{id,found}, the four metrics to archive.search.{queries,errors,duration,results} with {archive.archive, archive.corpus, error.type}, keep db.* on 'search df'. Add research_context: str | None = None to df_search and df_get_charter with the sentence 'Brief summary of the user's research goal. Used for telemetry only.' (the wording ra-mcp's search tools use), recorded as archive.research_context on the ops span only; test_telemetry asserts every ops-span key starts with 'archive.' and that no metric data point carries it; update observability.md:77-82.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.60

Evidence checked: search_operations.py:84-95 names attributes df.keyword/df.limit/df.offset/df.match_all/df.fuzzy plus df.language etc. only when set; :116-134 df.number/df.found; dataset.py:81-87 metrics kansallisarkisto.lancedb.queries/errors/query.duration/results; dataset.py:320 uses semconv db.* on the engine span; observability.md:77-79 documents the df.* names; test_telemetry.py asserts only db.* keys and error.type, never df.*, so a rename breaks no test. ra-mcp's diplomatics_search_sdhk schema (loaded this session) does carry research_context, but its description reads 'Brief summary of the user's research goal. Used for logging only.' — not 'telemetry only' as quoted. The rename earns its place on this repo's own roadmap without any family argument: dataset.py:1-6 promises one spine for df, voudintilit and tuomiokirjat, and the current scheme would produce voudintilit.keyword and tuomiokirjat.keyword, making the one dashboard the observability page describes impossible across the three corpora this server will host. research_context is the weaker half: it enlarges the schema of both tools with a parameter that does nothing for the answer, asks the model to write free text about a person's research into a span store, and the project already chose to approximate intent through the .results zero bucket (dataset.py:84-87) precisely to keep text off metrics — nobody here asked for it, and 'ra-mcp has it' is the only reason given. Nothing has been released, so the rename is free now and expensive later.

*Corrections:* ra-mcp's description is 'Used for logging only.', not 'Used for telemetry only.' — if adopted, it must be recorded on the span and kept out of logs, which contradicts the text the proposal says to copy verbatim. The metric rename `kansallisarkisto.lancedb.*` → `archive.search.*` should keep `db.system=lancedb` as an attribute so the engine stays visible. 'Freeze in core' presumes ecosystem-1; freeze it in observability.md and a test instead.

*Sharpened:* Rename the operations-span attributes to a corpus-agnostic set now — `archive.corpus` ('df'), `archive.query.text`, `archive.query.match_all`, `archive.query.fuzzy`, `archive.filter.<name>` only when set, `archive.lookup.id`, `archive.lookup.found` — and add `archive.corpus` as a metric attribute on the four dataset.py counters/histograms (renamed `archive.search.queries|errors|duration|results`), so one Grafana panel splits by corpus when voudintilit and tuomiokirjat land; add a test in test_telemetry.py that every attribute on the DfSearch spans starts with `archive.` and that no metric attribute set contains a query string. Defer research_context: adopt it only if ra-mcp can show its logged contexts produced a decision, and if adopted record it on the ops span only with a retention note in observability.md.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel / Dagger+GHA engineer** — *not refuted*, confidence 0.85

Verified the citations: search_operations.py:84-95 sets df.keyword/df.limit/df.offset/df.match_all/df.fuzzy and conditional df.language/issuingplace/country/year_min/year_max; :116 and :120/:126/:134 set df.number/df.found; dataset.py:81-87 names the four metrics kansallisarkisto.lancedb.queries/errors/query.duration/results with attrs db.system + db.collection.name (dataset.py:319, :330); the LanceDB span uses db.* semconv (:320). observability.md:77 is the table that would need the rename. test_telemetry.py:149-161 asserts only db.* attributes on the 'search df' span and :119-146 the span name 'DfSearch.search' — both kept by the proposal, so the rename is test-compatible. The live ra-mcp schema confirms research_context on every tool ('Brief summary of the user's research goal. Used for logging only.' on diplomatics_search_sdhk, 'Used for telemetry only.' on search_transcribed). Adding an optional str param to df_search/df_get_charter is ~10 lines; recording it on the ops span only is a one-line attribute. ~40 lines, S, is honest. The measurable outcome (every ops-span attribute key starts with 'archive.') is directly assertable with the in-memory exporter already in test_telemetry.py:91-116.

*Corrections:* ra-mcp does not have one 'exact description' for research_context — diplomatics_search_sdhk says 'Used for logging only.' and search_transcribed says 'Used for telemetry only.'; pick 'Used for telemetry only' and say so in both repos. Nothing else to correct.

*Sharpened:* Rename in one PR: ops-span attributes archive.archive='kansallisarkisto', archive.corpus='df', archive.query.text/match_all/fuzzy, archive.filter.<name> (only when set), archive.result.total/capped/returned, archive.lookup.id/found, archive.research_context (span only; never on metrics or logs, and the log bridge in telemetry.py:401-421 must not receive it — it does not today because it is never logged). Metrics become archive.search.queries|errors|duration|results with attrs {archive.archive, archive.corpus} (+error.type on errors); keep db.* on the 'search df' engine span. Tests: (a) every key on the DfSearch.search span starts with 'archive.'; (b) research_context='x' appears on the ops span and on no metric data point (use an InMemoryMetricReader); (c) observability.md:77 regenerated from the attribute list. Impact 3 is fair; do it before anything is released so no dashboard breaks.

---

## `ecosystem-6` — A DF↔SDHK concordance: cross-archive identifiers for charters edited in both national editions

**survived 2/3, with corrections · effort L · impact 4/5**

**What the code does today.** The harvested df export has no cross-reference field (data-sources.md field table: objectID, df, transcript, indexterm, issuingplace, issuingplacecountry, language, dating, _geoloc), yet 1,207 charters are issued in Sweden (df_tool.py:132 'Ruotsi 1,207') and many Finland-issued royal acts are in SDHK too. Verified in this session by calling ra-mcp: DF 1086 = SDHK 15325 — same incipit, but DF dates it 1399 and SDHK 1400-01-10 'omdat. från 13990111'; DF 526 = SDHK 5478 — same charter, different edition text ('Vi Magnus, met Gudhz nampn' vs 'Wy. magnus med gudz nadhom'); DF 2457 = SDHK 24100. Fingerprint spike (scratchpad/spikes/concordance): after strip_editorial_apparatus + NFKD fold, DF 1086/SDHK 15325 have 12-token incipit Jaccard 1.00; DF 526/SDHK 5478 trigram Jaccard 0.24 vs 0.16 and 0.12 for two same-place controls — text alone separates same-edition twins perfectly and cross-edition twins weakly; all 15 transcribed fixture incipits are distinct.

**The gap.** Nothing links the two editions, so a historian gets two citations for one document, two datings that disagree, and no signal that they disagree.

**The proposal.** `scripts/concordance.py` producing a LanceDB table `concordance(df_number int32, sdhk_number int32, method str, confidence float, date_fi str, date_se str, place_fi str, place_se str)` next to `df`: block on (year ±1, folded place) and issuer token, then (a) exact folded 12-token incipit → confidence 1.0 'incipit'; (b) trigram ≥ 0.2 with issuer match → 0.6 'candidate'; (c) hand-curated overrides file. df_get_charter appends 'Also edited as SDHK 15325 (Riksarkivet), dated there 1400-01-10 — the editions disagree on the date'; df_search hits get a `= SDHK n` suffix when confidence 1.0. The router (ecosystem-2) collapses twins. Identifier convention for the family: `archive:fi:df:1086`, `archive:se:sdhk:15325`, declared in the server card (ecosystem-4).

**Why it is once-in-a-lifetime.** Cross-edition identity of medieval charters across the Finnish and Swedish national editions is scholarly infrastructure that does not exist in machine-readable form; producing it as a by-product of two MCP servers is what makes the family more than the sum of its servers.

**How we would know.** Coverage: fraction of the 1,207 Sweden-issued df charters with a confidence-1.0 twin (expect a majority); precision ≥ 0.95 on a 50-pair hand-checked sample for the 1.0 tier; the DF 1086 dating disagreement appears verbatim in one df_get_charter reply; the concordance table ships with its own provenance metadata.

**Risks.** Requires the SDHK data (ra-mcp's table) — a Riksarkivet decision, not a code change; unavailable in this environment beyond three verified pairs. Cross-edition twins are weakly separable by text (0.24 vs 0.16), so the candidate tier will need human review; presenting a twin as 'same document' when the editions differ is a scholarly claim — always say 'also edited as', never merge texts. Argues against 'holds no records' only mildly: the concordance is derived metadata, not records.

**First step.** Ship the fingerprint (fold + first-12-tokens + trigram set) as `models.incipit_fingerprint()` with the fixture tests above, and a `concordance_overrides.jsonl` seeded with the three verified pairs; render the 'Also edited as' line from it in format_charter. No SDHK data needed for the first PR.

**Spike.** scratchpad spike (already run): folds fixture incipits and scores the three verified pairs against controls; extend by exporting `data/concordance` from the three seed pairs and checking df_get_charter(1086) renders the SDHK line.

### Skeptics

**Meticulous code reviewer — premise-checking every cited file, line, library call and spike against the repository and by re-running the spikes and calling ra-mcp** — *not refuted*, confidence 0.70

data-sources.md:80-89 field table has no cross-reference field; the fixture's keys are objectID, df, dating_*_year, issuingplace, issuingplacecountry, language, indexterm, transcript, _geoloc. The three pairs were verified by calling ra-mcp: SDHK 15325 is dated '14000110; omdat. från 13990111' at Åbo with the same incipit as fixture DF 1086 (1399); SDHK 5478 is 13470901 Åbo, Kung Magnus, 'Wy. magnus med gudz nadhom konunger i suerige…' vs DF 526 'Vi Magnus, met Gudhz nampn (ͻ: nadh) konunger Swerikis…'; SDHK 24100 is Karl Knutsson's Raumo privilege of 1442 = README's DF 2457. Recomputed the fingerprint with models.strip_editorial_apparatus + NFKD fold: DF 1086/SDHK 15325 12-token incipit Jaccard = 1.00; DF 526/SDHK 5478 trigram Jaccard on the first 300 folded chars = 0.36 vs 0.16 (DF 1086) and 0.20 (DF 727) for same-place controls; all 15 transcribed fixture incipits are distinct.

*Corrections:* The cited spike file scratchpad/spikes/concordance does not exist (find/grep for jaccard/trigram/incipit found nothing outside pygments); the numbers were re-derived here and differ in magnitude (0.36 vs 0.16/0.20 rather than 0.24 vs 0.16/0.12 — slice-length dependent) but agree in shape: same-edition twins separate perfectly, cross-edition twins weakly. 'Ruotsi 1,207' is at df_tool.py:139, not :132. The 'majority of 1,207 Sweden-issued charters' expectation is untestable without SDHK data.

*Sharpened:* First PR needs no SDHK data: models.incipit_fingerprint(text) → (folded 12-token tuple, trigram set) with the fixture tests above (incipit Jaccard 1.00 for DF 1086, 15 distinct incipits), a concordance_overrides.jsonl seeded with (1086,15325,'1400-01-10 omdat. från 13990111'), (526,5478,'1347-09-01'), (2457,24100,'1442-04-15'), and format_charter rendering 'Also edited as SDHK 15325 (Riksarkivet), dated there 1400-01-10 — the editions disagree on the date'. Make the concordance a second LanceDB table with its own archive.* provenance (ecosystem-4) so df_get_charter looks it up by df_number via a BTree; keep the 'candidate' tier out of tool output until a 50-pair hand check exists.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *refuted*, confidence 0.60

I verified the central pair myself by calling ra-mcp (diplomatics_search_sdhk keyword='Masko' place='Åbo'): SDHK 15325's edition text 'Jak Abram Brodherson riddare, høwitzman pa Abo, helsar idher alla som i Masko sokn ok i Pyke…' dated '14000110; omdat. från 13990111' is DF 1086 in the fixture ('Jak Abram Brodherson, riddare, høwitzman pa Abo…', 1399); DF 526 (1347, Åbo, 'Vi Magnus, met Gudhz nampn…') is in the fixture too. The data-sources.md field list indeed has no cross-reference field, and df_tool.py:139 gives 'Ruotsi 1,207'. But the cited spike does not exist: `grep -rl 'Jaccard|trigram|incipit'` over the scratchpad (excluding site-packages) finds nothing, and there is no scratchpad/spikes/concordance — the 1.00 / 0.24 / 0.16 / 0.12 numbers cannot be checked and must be treated as unmeasured. On fit a strong maintainer says not now and not here: (1) the input the concordance needs (SDHK's table) belongs to ra-mcp and is not available to this repo beyond three hand-verified pairs; (2) asserting that two editions are the same act is a scholarly claim, and this project's stance (data-sources.md:163-164, the tool descriptions) is to cite the archive, not the snapshot, and to hold no records of its own; (3) a `concordance_overrides.jsonl` with three pairs in a 6,876-charter corpus, plus a `models.incipit_fingerprint()` with no consumer in this repo, is a gesture rather than infrastructure; (4) the proposal's own candidate tier (0.24 vs 0.16 on cross-edition twins) needs human review it cannot get here; (5) the stronger key is not text at all — both editions cite FMU (Finlands medeltidsurkunder; df_get_charter's description says the archive page carries the FMU/REA references) — but the harvested export does not carry it, so even the right design starts with a harvest change or a scrape of df.kansallisarkisto.fi, a Kansallisarkisto conversation rather than a code change. The next corpora on the roadmap are voudintilit and tuomiokirjat; a DF↔SDHK concordance is a joint Riksarkivet–Kansallisarkisto dataset that should be published as data (with its own provenance) and consumed by both servers, not built inside this one's models.py.

*Corrections:* The fingerprint spike cited as evidence is not in the scratchpad; its Jaccard figures are unverified. If the idea is ever taken up, the linking key should be the FMU number both editions cite rather than an incipit fingerprint, which the export does not carry today.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel / Dagger+GHA engineer** — *not refuted*, confidence 0.60

The cited spike does not exist: there is no scratchpad/spikes/concordance*, and grep for NFKD/trigram/fingerprint/SDHK across the scratchpad finds only judge-authored files, so the Jaccard figures (1.00 incipit; 0.24 vs 0.16/0.12 trigram) are unverified. I verified the underlying identity claim independently by calling ra-mcp: diplomatics_search_sdhk('Brodherson Masko Pyke') returns SDHK 15325, place Åbo, dated '14000110; omdat. från 13990111 och 14090112*', Edition text 'Jak Abram Brodherson riddare, høwitzman pa Abo, helsar idher alla som i Masko sokn ok i Pyke byggiæ ok boo, thet som konungxdomen ther wti thilhører...'; the fixture's DF 1086 (dating_start_year 1399) reads 'Jak Abram Brodherson, riddare, høwitzman pa Abo, helsar idher alla, som i Masko sokn ok j Pyke byggiæ ok boo, thet som konungxdomen therwti thil hører...'. After punctuation stripping the first 12 tokens are identical (divergence starts at token 16: j/i, 'therwti thil hører' vs 'ther wti thilhører'), so the 12-token exact-incipit tier and the dating disagreement (1399 vs 1400-01-10) are both real. SDHK replies carry an 'Edition:' source-language block, so fingerprints can be computed from ra-mcp output rather than requiring a table dump for the seed pairs. Both DF 526 and DF 1086 are in the 18-row fixture, so the first-step tests are runnable offline. Effort L is honest for the script; the measurable coverage over 1,207 Sweden-issued charters needs SDHK bulk data, which is a Riksarkivet decision as the risks say. One risk understated: SDHK 15902 and 16509 in the same reply open with the same 8-token formula 'Alla the men thetta bref høra ælla see hælssa jak', so a bare 12-token incipit will collide on formulaic openings unless blocking on year/place/issuer is mandatory, which the proposal does specify.

*Corrections:* (1) No concordance spike exists in the scratchpad; the Jaccard numbers must be re-derived (the DF 1086/SDHK 15325 12-token identity is confirmed here by a live ra-mcp call, the trigram figures are not). (2) The exact-incipit tier must require the (year ±1, folded place, issuer) block — formulaic openings ('Alla the men thetta bref høra ælla see hælssa jak ...') recur across unrelated SDHK charters, so incipit-only 1.0 confidence would be wrong. (3) Cross-edition twins diverge within 16 tokens even for the same-edition pair (j/i, word splits), so NFKD fold plus j->i and whitespace-insensitive comparison are required for the 12-token tier to be robust, not optional.

*Sharpened:* First PR, fully offline: models.incipit_fingerprint(text) = strip_editorial_apparatus + NFKD fold + j->i + lowercase + first 12 alphabetic tokens, with fixture tests that all 15 transcribed incipits are distinct and that DF 1086's fingerprint equals the SDHK 15325 edition text captured in this session; concordance_overrides.jsonl seeded with (1086,15325,'1399','1400-01-10'), (526,5478), (2457,24100); format_charter appends 'Also edited as SDHK 15325 (Riksarkivet), dated there 1400-01-10 — the editions disagree on the date' from that file. Second phase (needs SDHK data or a paginated ra-mcp crawl of the ~1,207 Sweden-issued DF charters, feasible at 25/page): scripts/concordance.py blocking on (year ±1, folded place, issuer) with tiers incipit=1.0 / trigram-candidate=0.6 / override, 50-pair hand check for precision ≥0.95, and the concordance table carrying the same archive.* provenance metadata as df (ecosystem-4). Effort L for phase two, S for phase one.

---

## `ecosystem-7` — A shared conformance test kit so every server in the family proves the same search invariants

**survived 3/3, with corrections · effort M · impact 4/5**

**What the code does today.** test_search_invariants.py holds engine-generic properties written against DfSearch and two literal terms (BASE_TERMS = ['Åbo','ecclesia'], line 29): AND ⊆ OR (line 92), fuzzy ⊇ exact on a base form, pages tile without gaps (line 133), stable order, case/accent invariance, page ≤ limit ≤ total, paging past the end keeps the total, phrase ⊆ words, fuzzy+phrase refused (line 184), blank keyword and impossible paging rejected — 11 of its ~20 tests mention nothing df-specific. The remainder pin df's index config (line 42) and filters (line 111). test_retrieval_quality.py guards against vacuous passes ('fixture no longer covers footnote markers'). The second audit ran ten injection payloads through the filters by hand. None of this can run against ra-mcp — and ra-mcp's SDHK visibly fails the first invariant today (194 hits for 'Magnus konunger Raumo' is not AND).

**The gap.** The most valuable tests in the family are trapped in one repo and coupled to one corpus's terms; conformance is asserted by copying prose ('same shape as ra-mcp'), not by running the same tests.

**The proposal.** `archive_mcp_core.testkit`: a `SearchContract` Protocol (`search(keyword, *, limit, offset, match_all, fuzzy) -> SearchResult`), a `Conformance(base_terms: list[str], filters: dict[str, tuple[value, row_predicate]], traps: TrapCoverage(untranscribed, unknown_year, apparatus, phrase, stop_words))` declared by each corpus, and `pytest_generate_tests` that instantiates the 11 invariants + the filter subset/honour tests + the audit's 10 injection payloads + `awkward_queries_do_not_raise` for every declared corpus. MCP-level kit: `assert_tools_never_raise(server, fuzz)`, `assert_error_strings_mark_span(server)`, `assert_descriptions_carry_numbers(server)` (every description contains a digit), `assert_landing_page_lists_tools`. Each corpus's TrapCoverage makes the fixture-coverage guards generic. Published inside core; both repos run `pytest -p archive_mcp_core.testkit`.

**Why it is once-in-a-lifetime.** Conformance becomes a fact a CI badge can state ('archive-mcp conformance 1.0') instead of a sentence in a docstring, and the ra-mcp run turns the family's divergences into a numbered backlog rather than folklore.

**How we would know.** kansallisarkisto passes 100% of the kit with zero df-specific code in the kit; the first ra-mcp/SDHK run reports a concrete failure count (expected ≥2: AND semantics, quote handling), each mapped to an ecosystem-3 grammar row; adding voudintilit requires only a Conformance declaration.

**Risks.** Some invariants are engine assumptions (BM25 tie stability, one ranked set sliced) that Elasticsearch-backed ra-mcp tools will legitimately fail — the kit needs tiers ('envelope', 'semantics', 'engine') so a failure is a classification, not a blocker. Extracting tests without the corpus prose risks losing the measured reasoning in their docstrings; keep the docstrings, they are the spec.

**First step.** Move the 11 generic invariants into `packages/kansallisarkisto-lib/src/ra_mcp_kansallisarkisto_lib/testkit.py` parametrised by a Conformance object, re-express test_search_invariants.py as `df_conformance = Conformance(base_terms=['Åbo','ecclesia'], filters={...})`, and keep the df-specific index-config tests where they are. Same test count, one PR.

**Spike.** Write the Conformance object for df and run the extracted invariants against DfSearch on the fixture (expect identical pass count to today); then run them against a 30-line stub SearchContract that implements OR semantics and confirm exactly 'requiring_all_terms_narrows' fails.

### Skeptics

**Meticulous code reviewer: premise-checking against the repository, the two audits, ra-mcp's source (cloned read-only at /home/user/ai-riksarkivet/ra-mcp, HEAD fe7a6c5) and live experiments** — *not refuted*, confidence 0.85

Checked every cited line in /home/user/kansallisarkisto-mcp/packages/kansallisarkisto-lib/tests/test_search_invariants.py: BASE_TERMS at line 29, AND⊆OR at 92, fuzzy⊇exact at 102, paging tiles at 133, stable order 145, case/accent 153, page≤limit≤total 161, past-the-end 168, phrase⊆words 176, fuzzy+phrase refused 184, blank keyword 199, impossible paging 206; df-specific index config at 42-77 and filters at 111-130. Counted: 23 test functions (4 index-config, 15 fixture invariants, 4 corpus-only); exactly 11 of the 15 fixture invariants mention nothing df-specific beyond the record key. test_retrieval_quality.py:66-71 carries the 'fixture no longer covers footnote markers' guard as claimed; test_awkward_queries_do_not_raise is at line 150. The second audit's ten payloads (docs/development/audit.md lines 25-31) are hand-run only — grep for 'OR 1=1' / 'DROP TABLE' in packages/*/tests finds nothing, so no test pins them. Ran the proposal's own spike: copied the file into the scratchpad with a conftest whose `search` fixture is parametrised over DfSearch and a 6-line OrStub that forces match_all=False; the 11 generic tests × 2 fixtures gave 38 passed, 2 failed, and the two failures are exactly test_requiring_all_terms_narrows[or-stub-Åbo] and [or-stub-ecclesia]. Verified the SDHK claim by calling the live ra-mcp tool diplomatics_search_sdhk('Magnus konunger Raumo'): 5,765 hits, first row a Raumo charter with no Magnus — OR semantics confirmed, but the number 194 is wrong. Read ra-mcp: packages/libs/diplomatics-lib/src/ra_mcp_diplomatics_lib/search_operations.py:25-52 `search_sdhk(keyword, *, limit, offset, author, place, language)` has no match_all or fuzzy, and dataset-lib search.py:170 calls table.search(keyword, query_type='fts') with LanceDB's default OR. ra-mcp has one of the invariants natively (dataset-lib/tests/test_search.py:103 test_pagination_is_stable_and_gap_free) and no shared kit (packages/libs/common/tests holds datasets/dev_server/formatting/http_client/telemetry tests only). test_routes.py:40 test_landing_page_lists_every_registered_tool already exists, so that kit item is a move. No archive_mcp_core package exists anywhere; it is ecosystem-1's hypothetical.

*Corrections:* (1) SDHK today returns 5,765 hits for 'Magnus konunger Raumo', not 194 (live call, 2026-09-10). (2) The AND invariant is not something SDHK 'fails' — ra-mcp's search_sdhk has no match_all parameter at all, so against the proposed SearchContract signature it is inexpressible without an adapter; the kit needs an 'unsupported' classification per contract member, not just tiers. (3) `pytest -p archive_mcp_core.testkit` registers a plugin but pytest collects tests from files, not plugins; the kit must either implement pytest_collect_file/pytest_pycollect_makemodule or ship test modules the consumer re-exports (`from ...testkit.invariants import *`) beside a `conformance` fixture. (4) The tests are coupled to more than terms: the record id key r['df'] (lines 94-95, 136-147, 156-158), the phrase pair 'de ecclesia' (177-178, 188) and get_charter (192-195); Conformance needs id_of(record), a phrase pair and an optional lookup member. (5) first_step drops testkit.py into the lib's runtime package, making pytest an import of a shipped module — lazy-import or an optional extra. (6) assert_landing_page_lists_tools already exists as test_routes.py:40; the audit's ten payloads are the genuinely untested part.

*Sharpened:* Ship `ra_mcp_kansallisarkisto_lib.testkit` (later core) as importable test modules plus a `conformance` fixture: `Conformance(search, id_of, base_terms=['Åbo','ecclesia'], phrase=('de ecclesia', ['de','ecclesia']), filters={...}, supports={'match_all','fuzzy','phrase'})`, with every invariant tagged envelope/semantics/engine and skipped-as-'unsupported' when the contract member is absent. Add the second audit's ten LIKE/quote payloads as a generic `filters_match_injection_as_literals` property (0 hits against a nonzero baseline — the one property the kit adds that no repo has today), and `tools_never_raise` at the MCP layer. Measured on the fixture: DfSearch passes 40/40 parametrisations, an OR stub fails exactly the two AND parametrisations; the first ra-mcp/SDHK run will report AND as 'unsupported' (no match_all) and OR-by-default (5,765 hits) as a semantics finding, giving the grammar backlog a number instead of folklore.

**Maintainer of this exact project — judged for value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in view.** — *not refuted*, confidence 0.80

Checked packages/kansallisarkisto-lib/tests/test_search_invariants.py end to end. The count holds: 11 of the tests are engine-generic (requiring_all_terms_narrows :92, fuzzy_widens_a_base_form :102, pages_tile :133, repeating_same_order :145, case_and_accents :153, page_never_exceeds :161, paging_past_the_end :168, phrase_never_wider :176, fuzzy_on_quoted_phrase_refused :184, blank_keywords :198, impossible_paging :205); the rest pin df's index config (:42-77), df filters (:111-130) and get_charter (:191) and must stay where they are. But the generic ones are less parametrised than the proposal implies: only two tests use BASE_TERMS; the others hard-code 'Åbo' (:136,:147,:162,:171,:210), the co-term 'latina' (:94), the phrase '"de ecclesia"' (:177) and the accent pairs (Åbo/Abo, :152) — so a Conformance object needs base_terms, a co_term, a phrase and accent_pairs, not base_terms alone. Ran the proposal's spike: a pytest plugin that forces match_all=False into lancedb_fts_search (scratchpad/orstub/orstub.py) against the unmodified invariants file gives '2 failed, 28 passed, 6 skipped' — exactly test_requiring_all_terms_narrows[Åbo] and [ecclesia], and note it is the strict 'len(wide) > len(narrow)' assertion at :98 that catches OR-only, since narrow <= wide is vacuously true under OR; the kit must keep that strictness. Verified the ra-mcp claim live: diplomatics_search_sdhk('Magnus konunger Raumo') returns 5,765 hits (not 194) with top hits containing only 'Raumo' — OR semantics confirmed, more strongly than stated. Cause, from AI-Riksarkivet/ra-mcp code search: ra_mcp_dataset_lib/search.py's lancedb_fts_search(db, table_name, keyword, *, limit, offset=0, where=None) builds table.search(keyword, query_type='fts') with no MatchQuery/operator and no match_all/fuzzy, and raises plain ValueError on a blank keyword; diplomatics-lib search_operations.py calls it for SDHK_TABLE. So SDHK is LanceDB-backed through the older spine this repo was ported from (dataset.py:8) — the AND failure is a fixable spine divergence, not an Elasticsearch engine difference, and the proposal's 'engine tier' risk is misattributed for this case. Fit: the lib pyproject has no pytest dependency (packages/kansallisarkisto-lib/pyproject.toml deps: opentelemetry-api, lancedb, pyarrow, pydantic) and the image purges what it can, so '-p archive_mcp_core.testkit' inside the runtime package needs a lazy pytest import or an optional extra, or it violates 'stdio costs nothing'. archive_mcp_core does not exist (it is ecosystem-1's proposal); the first_step, which is in-repo in kansallisarkisto-lib, stands on its own and is what I endorse. Value for this project specifically: dataset.py:5 promises df, voudintilit and tuomiokirjat 'share a single implementation instead of each growing its own copy of the same bugs' — a Conformance declaration per corpus is the test-side half of that promise and is exactly what makes 'the next corpus a week's work'. For tuomiokirjat, SearchResult.total_is_capped (dataset.py:101-104) and the MAX_TOTAL_COUNT=10_000 slice mean paging_past_the_end's offset=10_000 sits on the cap boundary; the kit must read total_is_capped rather than assume an exact total.

*Corrections:* (1) SDHK returns 5,765 hits today for 'Magnus konunger Raumo', not 194 — the direction of the claim is right, the number is stale. (2) ra-mcp's SDHK is not Elasticsearch-backed: it uses ra_mcp_dataset_lib.lancedb_fts_search (LanceDB, plain-string fts query → OR by default, no match_all/fuzzy parameters, plain ValueError on blank keyword); the AND failure is a spine divergence the same fix as this repo's MatchQuery/FullTextOperator branch (dataset.py:44-46 of lancedb_fts_search) closes, not an engine tier. (3) The SearchContract Protocol search(keyword, *, limit, offset, match_all, fuzzy) does not match ra-mcp's search_sdhk(keyword, *, limit, offset, place, language, author) — the Conformance needs a capabilities set {match_all, fuzzy, phrase} so tests skip with a reason instead of erroring on signature. (4) Only 2 of the 11 invariants are parametrised by BASE_TERMS today; the Conformance also needs co_term ('latina'), phrase ('"de ecclesia"') and accent_pairs. (5) The kit cannot add pytest to the lib's runtime dependencies — lazy import or an optional extra.

*Sharpened:* Add packages/kansallisarkisto-lib/src/ra_mcp_kansallisarkisto_lib/testkit.py (pytest imported lazily; exposed via an optional extra `[testkit]`, so the runtime wheel and stdio cost nothing) holding the 11 invariants verbatim with their docstrings, parametrised by a `Conformance(search: Callable[..., SearchResult], id_field: str, base_terms, co_term, phrase, accent_pairs, filters: dict[name, (value, row_predicate)], capabilities: frozenset[{'match_all','fuzzy','phrase'}], tier)` and a `pytest_generate_tests` hook. Re-express test_search_invariants.py's generic half as `df_conformance = Conformance(search=DfSearch.search, id_field='df', base_terms=['Åbo','ecclesia'], co_term='latina', phrase='"de ecclesia"', accent_pairs=[('Åbo','åbo'),('Åbo','ÅBO'),('Åbo','Abo'),('ecclesia','ECCLESIA')], filters={'language': ('latina', lambda r: r['language']=='latina'), ...})` and keep the index-config, get_charter and corpus-only tests where they are. Keep the strict 'OR must be wider' assertion — it is the only line that distinguishes an OR-only engine. Handle SearchResult.total_is_capped so the paging invariants hold on tuomiokirjat at the 10,000 boundary. Acceptance: df passes the same 30 tests with zero df literals in testkit.py; the OR-stub run (scratchpad/orstub) fails exactly the two requiring_all_terms_narrows cases; when VoudintilitSearch lands, its test file is one Conformance declaration plus voudintilit's own traps (zero-padded ids, ingest.py:28). The ra-mcp run is a downstream offer, not a dependency: expected result is requiring_all_terms_narrows failing on OR (5,765 for a three-word query), fuzzy tests skipped for lack of capability, blank-keyword failing at the envelope tier (ValueError, not SearchInputError) — three numbered divergences ra-mcp can close by adopting this repo's spine, which is the concrete case for ecosystem-1.

**Feasibility and honesty of effort against the installed stack (lancedb 0.38, fastmcp 3.4.4, pyarrow 24, pydantic-settings 2.15, pytest 9.1) — verified by reading the code, the ra-mcp checkout at /home/user/ai-riksarkivet/ra-mcp, and running spikes under the scratchpad.** — *not refuted*, confidence 0.85

Ran the spike. Extracted the 11 invariants from packages/kansallisarkisto-lib/tests/test_search_invariants.py (lines 91-210) into /tmp/claude-0/-home-user-kansallisarkisto-mcp/6ff06e2b-109d-555b-be3f-fc58e4c2ea7f/scratchpad/kit/testkit.py parametrised by a Conformance dataclass, with a conftest supplying `subject`/`conf` fixtures over the 18-row fixture. Against DfSearch: 19 passed in 0.27 s (same parametrised count as today's generic subset). Against a 6-line OrStub that forwards with match_all=False: exactly `test_requiring_all_terms_narrows[Åbo]` and `[ecclesia]` fail, 17 pass — the spike's prediction holds to the test name. Checked the ra-mcp evidence live: mcp__ra-mcp__diplomatics_search_sdhk('Magnus konunger Raumo') returns 5,765 hits today (not 194) and the top hit (SDHK 32294) contains only 'Raumo' — OR semantics confirmed, worse than stated. Read ra-mcp's engine: /home/user/ai-riksarkivet/ra-mcp/packages/libs/dataset-lib/src/ra_mcp_dataset_lib/search.py:133-141 — `lancedb_fts_search(db, table_name, keyword, *, limit, offset, where)` has no match_all/fuzzy, and diplomatics-lib search_operations.py:25-56 `search_sdhk(keyword, *, limit, offset, author, place, language)` likewise. So ra-mcp's diplomatics tools are LanceDB, not Elasticsearch, and calling them with match_all= raises TypeError rather than failing an invariant. Checked pytest mechanics: `pytest -p testkit` with the kit on PYTHONPATH loads the plugin and reports 'no tests collected in 0.00s' — `-p` registers hooks, it never collects test functions; `--pyargs testkit` does start collecting (it reached pytest_generate_tests). Audit reference verified: docs/development/audit.md:26 'ran ten payloads through' the filters. Effort M is honest for the lib-level kit: the spike was ~80 lines in under half an hour; the MCP-level assertions (assert_tools_never_raise with fuzz, descriptions-carry-a-digit, landing-page-lists-tools — the last already exists as test_routes.py:40) are another day; 'published inside core' inherits ecosystem-1's cost, which the proposal acknowledges.

*Corrections:* (1) '11 of its ~20 tests mention nothing df-specific' is true of the count, false of the bodies: every one of the 11 hard-codes df literals — `r["df"]` as the id key, 'latina' as the AND partner (line 94), 'de ecclesia' as the phrase (177, 188), 'Åbo' everywhere, and the four case/accent pairs (152). The Conformance object needs at least base_terms, and_partner, phrase, id_field, case_pairs — five fields, not one list. (2) `pytest -p archive_mcp_core.testkit` collects nothing (verified); use `pytest --pyargs archive_mcp_core.testkit` with each repo's conftest providing the `subject`/`conformance` fixtures, or a one-line `from archive_mcp_core.testkit import *` shim module in each repo's tests/. (3) ra-mcp's SDHK is LanceDB via ra_mcp_dataset_lib.lancedb_fts_search, not Elasticsearch — the 'engine' tier will pass there; what breaks is the contract signature (no match_all, no fuzzy, method named search_sdhk not search). The kit needs a `supports: frozenset[str]` capability declaration so an absent kwarg reports 'unsupported', not 'failed', and a callable adapter rather than a method name. (4) The 194-hit figure is stale: 5,765 today. (5) `assert_landing_page_lists_tools` already exists (test_routes.py:40-49); it moves, it is not new.

*Sharpened:* Ship `ra_mcp_kansallisarkisto_lib.testkit` now (no core package needed): `Conformance(base_terms, and_partner, phrase, id_field, case_pairs, supports=frozenset({'match_all','fuzzy','phrase'}), traps=TrapCoverage(...))`, tiers `envelope` (page ≤ limit ≤ total, paging past the end, blank/impossible-paging rejected), `semantics` (AND ⊆ OR, phrase ⊆ words, filters narrow and are honoured), `engine` (fuzzy ⊇ exact on base forms, pages tile, stable order, case/accent folding). Each corpus's tests/conftest.py supplies `subject` and `conformance`; both repos run `pytest --pyargs ra_mcp_kansallisarkisto_lib.testkit`. test_search_invariants.py shrinks to the four index-config tests plus `df_conformance = Conformance(base_terms=['Åbo','ecclesia'], and_partner='latina', phrase='de ecclesia', id_field='df', ...)`, with the measured docstrings carried onto the kit functions. Concrete first ra-mcp run, predicted from its source: `search_sdhk` adapted with id_field='sdhk' reports 2 semantics failures (AND ⊆ OR on both base terms — 5,765 hits for 'Magnus konunger Raumo' today), `fuzzy` and `match_all` as 'unsupported' (not failures), and the whole engine tier passing because it is the same LanceDB spine. Badge text: 'archive-mcp conformance: envelope 100%, semantics N/M, engine 100%'.

---

## `ecosystem-8` — A family error taxonomy, readiness contract and settings convention, extracted from _answer before it is copied a third time

**survived 3/3, with corrections · effort S · impact 3/5**

**What the code does today.** _answer (df_tool.py:31-56) is the family's de-facto error policy: MissingTableError → full message + error.type 'missing_table' (df_tool.py:42), SearchInputError → 'Error: ' + message + 'validation' (df_tool.py:49), anything else → type-only via format_error (formatter.py:113) + the exception class name as error.type. SearchInputError(ValueError) (dataset.py:63) is 'the only error type whose message is safe to show a caller'; MissingTableError(RuntimeError) lives in errors.py:15. The comment on _answer says it exists because 'the two tools had already drifted'. Readiness (tools.py:96) returns (bool, str) for one table; /ready (routes.py:51-55) can only say ready/not-ready for a server that will soon hold three corpora. Settings mix prefixes: ka_lancedb_uri/ka_mcp_transport (settings.py:21-22) with env_prefix='' (settings.py:12), KA_MCP_OTEL_ENABLED (telemetry.py:42), and the OTEL_* SDK variables; ra-mcp presumably uses RA_ — so an operator's runbook cannot be shared.

**The gap.** Error disclosure, span error classes, readiness shape and settings prefixes are conventions that live in one function and one tuple, are documented as 'same as ra-mcp' by assertion, and will drift on the next port.

**The proposal.** In core: `class ArchiveError(Exception): disclose: ClassVar[bool]`; `InputError(disclose=True)`, `NotReadyError(disclose=True)` (missing or unreadable table — the boot probe's EACCES-as-Not-found case, server.py:301), `UpstreamError(disclose=False)`; `answer(tool, produce)` moved to core with a closed `error.type ∈ {validation, not_ready, upstream, internal}` (today's 'missing_table' and raw exception names disappear — the class name stays in the message for 'internal'). Readiness contract: `readiness() -> Readiness(ok: bool, corpora: dict[str, CorpusStatus(ready|missing|unreadable, detail)])`, HTTP 503 only when no corpus is ready, the one-row probe run on every call (the audit's finding 1 is the bug; this is the contract), and the same object rendered in the server card. Settings: `ArchiveSettings(BaseSettings)` in core taking `env_prefix` from the archive code, so `KA_LANCEDB_URI` and `RA_LANCEDB_URI` are the same field, with HOST/PORT/LOG_LEVEL also prefixed and the OTEL_* names left to the SDK.

**Why it is once-in-a-lifetime.** Once error classes, readiness and settings are one contract, an operator who runs ra-mcp can run kansallisarkisto-mcp without reading source, and the router can treat 'not_ready' from any archive identically — which is what makes partial failure a message rather than an outage.

**How we would know.** _answer deleted from df_tool.py; test_telemetry asserts error.type takes only the four values; /ready on a server with df present and voudintilit absent returns 200 with `corpora.voudintilit='missing'`; the settings table in the README and ra-mcp's list the same variable names modulo prefix.

**Risks.** Changing error.type values and the /ready JSON is a (pre-release) contract change; nothing has been released, so now is the moment. Prefixing HOST/PORT breaks the compose file and the Dockerfile ENV — a one-line change each. This overlaps ecosystem-1's first step by design: it is the smallest extraction that establishes core's shape.

**First step.** Add `errors.py` to kansallisarkisto-lib with the three classes, make SearchInputError and MissingTableError subclasses, move _answer there as `answer()`, and switch the four error.type values; update the two tests that assert 'missing_table'. ~80 lines.

**Spike.** Run test_tools.py and test_telemetry.py after the change and print the set of error.type values seen across all spans — it must be a subset of the four.

### Skeptics

**Meticulous code reviewer: premise-checking against the repository, the two audits, ra-mcp's source (cloned read-only at /home/user/ai-riksarkivet/ra-mcp, HEAD fe7a6c5) and live experiments** — *not refuted*, confidence 0.70

Verified: _answer at df_tool.py:31-56 with 'missing_table' at 42 and 'validation' at 49; SearchInputError(ValueError) at dataset.py:63; MissingTableError(RuntimeError) at errors.py:15; readiness() -> tuple[bool,str] at tools.py:96-111; /ready at routes.py:51-56; settings.py env_prefix='' at 12 and ka_lancedb_uri/ka_mcp_transport at 21-22; KA_MCP_OTEL_ENABLED at telemetry.py:42; format_error at formatter.py:113. Ran a spike (scratchpad/spike/errtypes.py, in-memory exporter, FastMCP Client against the registered server) exercising all three branches: the tools/call span carries error.type='handled_error' for the generic branch — NOT the exception class name — because mark_span_error's default (lib telemetry.py:79-96) overwrites the class name that record_span_exception set at line 58; the class name survives only in the span status description ('df_search failed: RuntimeError') and on the spine's 'search df' span (dataset.py:329-330). Missing branch gives 'missing_table', validation gives 'validation' (also pinned by test_telemetry.py:175). server.py is 114 lines, so 'server.py:301' does not exist; the EACCES-as-Not-found probe is server.py:53-76. Cloned ra-mcp: ra_mcp_common/settings.py:18 uses env_prefix='RA_MCP_' (not 'RA_'); RA_MCP_LOG_LEVEL is prefixed there while KA's LOG_LEVEL is not; PORT is unprefixed in both. ra-mcp's /ready (src/ra_mcp_server/server.py:531-535) already returns {'status':'ready','modules':[...]} and 503 only when nothing is mounted — a per-module shape KA's {'table':'df'} diverged from. ra-mcp's SDHK tool (sdhk_tool.py:89-92) returns f'Error: SDHK search failed — {exc!s}', i.e. the family has no shared disclosure policy; KA's _answer is the only one. FastMCP 3.4.4 (uv.lock:411) has no server card: grep for server_card/well-known in site-packages/fastmcp hits only auth providers. Overlap with the audits: readiness running the probe on every call is second-audit finding 1; type-only in the /ready fallback is finding 2; HOST/PORT/LOG_LEVEL unprefixed is first-audit finding 11, carried in second-audit finding 9. The proposal names finding 1 but treats findings 2 and 11 as its own.

*Corrections:* (1) The generic branch sets error.type='handled_error' on the tool span, not the exception class name; the class name lives in the status description and on the spine span via record_span_exception (dataset.py:329-330, lib telemetry.py:58). The closed set should replace 'handled_error'/'missing_table' on the tool span only; keep the class name on the spine span and in exception.type, or trace stores lose the grouping record_span_exception's docstring promises. (2) server.py:301 → server.py:53-76. (3) ra-mcp's prefix is RA_MCP_ (ra_mcp_common/settings.py:18), and KA already mixes KA_LANCEDB_URI with KA_MCP_TRANSPORT/KA_MCP_OTEL_ENABLED — the shared field should be <PREFIX>_LANCEDB_URI under KA_MCP_/RA_MCP_. (4) There is no FastMCP 'server card' in 3.4.4; the rendering surface is assets/index.html. (5) ra-mcp's /ready already has the 'modules' list and 503-only-when-empty semantics; adopt/extend that key rather than inventing 'corpora', and note ra-mcp probes nothing (mounted modules only). (6) ra-mcp's SDHK tool leaks exc verbatim (sdhk_tool.py:92), so 'extracted before it is copied a third time' understates it: it has not been copied a second time either — the extraction is a fix for ra-mcp, which makes effort M once both repos are touched. (7) Per-call probe, type-only /ready fallback and prefixing HOST/PORT/LOG_LEVEL are already audit findings 1, 2 and 11; only the closed error.type set, ArchiveError.disclose, the per-corpus readiness object and the shared prefixed Settings are new.

*Sharpened:* Extract three contracts into the lib (later core), each with a test that pins the value set: (a) `ArchiveError(disclose: ClassVar[bool])` with InputError/NotReadyError/UpstreamError, SearchInputError and MissingTableError as subclasses, and `answer(tool, produce)` emitting error.type ∈ {validation, not_ready, upstream, internal} on the tool span while the spine span keeps the class name; the spike (scratchpad/spike/errtypes.py) is the acceptance test — today it prints {validation, missing_table, handled_error}. (b) `Readiness(ok, modules: dict[str, CorpusStatus])` extending ra-mcp's existing {'modules': [...]} key, 503 only when every corpus is missing/unreadable, the one-row probe per call and type-only detail (closing audit findings 1 and 2 in the same change), rendered on the landing page. (c) `ArchiveSettings(env_prefix=f'{code}_MCP_')` so KA_MCP_LANCEDB_URI and RA_MCP_LANCEDB_URI are one field and LOG_LEVEL/HOST/PORT are prefixed in both (closing audit finding 11); ra-mcp's sdhk_tool.py:92 verbatim-exception leak becomes the first thing the shared `answer()` fixes when ported back.

**Maintainer of this exact project — judged for value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), with the three-corpus future and the historian user in view.** — *not refuted*, confidence 0.70

Citations verified: _answer at df_tool.py:31-56 with 'missing_table' at :42 and 'validation' at :49; format_error at formatter.py:113 (type-only); SearchInputError(ValueError) at dataset.py:63 with the 'only error type whose message is safe' claim at :277; MissingTableError(RuntimeError) at errors.py:15; readiness() at tools.py:96 returning tuple[bool,str]; /ready at routes.py:51-55; settings.py env_prefix='' at :12 and ka_lancedb_uri/ka_mcp_transport at :21-22; KA_MCP_OTEL_ENABLED read via os.getenv in mcp/telemetry.py:39-42. Two evidence errors: 'server.py:301' does not exist (server.py is 114 lines; the EACCES-as-Not-found text is the probe_table_readable docstring, server.py:53-64), and 'update the two tests that assert missing_table' is wrong — grep shows no test asserts error.type=='missing_table'; test_telemetry.py:175 asserts only 'validation', and the literal appears elsewhere only in docs/development/observability.md:96. The drift claim is real and now measurable: git log 5c69f36 'share the tool error handling instead of repeating it' is the commit that created _answer after df_get_charter lost its SearchInputError branch, and in ra-mcp (AI-Riksarkivet/ra-mcp code search) dataset tools return f"Error: JUDA search failed — {exc!s}" (juda_tool.py, doda_tool.py, vigsel_tool.py, fodelse_tool.py) — the verbatim-message disclosure this repo's audit finding 12 fixed — so the family does not share an error policy today, and 'same as ra-mcp' is asserted, not true. Readiness: ra-mcp's /ready (src/ra_mcp_server/server.py) returns {'status':'ready','modules':[...]} or 503 when none loaded — the proposed per-corpus dict with 503-only-when-none matches that shape but is stronger (a real probe per call, which is audit finding 1's fix; finding 2 says the fallback must be type-only, which the contract must carry). It is consistent with 'boots without data': a server with df present and voudintilit missing should take traffic. Settings: ra-mcp's actual convention (ra_mcp_common/settings.py) is env_prefix 'RA_MCP_' with log_level, otel_enabled, data_dir, stage_*, timeout — no host/port (CLI --port) and no RA_LANCEDB_URI; per-dataset URIs are <NAME>_LANCEDB_URI plus RA_MCP_DATA_DIR. So the 'KA_LANCEDB_URI and RA_LANCEDB_URI are the same field modulo prefix' design is built on a wrong guess; the two servers model data location differently (one db with N tables vs N dbs). Within this repo, though, the inconsistency is exactly as audit finding 11 (audit.md:582) records: HOST/PORT/LOG_LEVEL unprefixed while ka_mcp_transport, KA_MCP_OTEL_ENABLED and KA_MCP_OTEL_LOG_BRIDGE already use KA_MCP_; consumers to update are .dagger/serve.go:60-61,140-141, the dockerfile ENV at :73-74, and README.md:147-155. Fit check on the closed error.type set: record_span_exception (lib telemetry.py:58) sets error.type to the exception class name on every span it passes through, and lancedb_fts_search's error counter uses the class name (dataset.py:330 in file numbering) — that is OTel semconv-correct and useful, so the proposal's spike ('the set of error.type values across all spans must be a subset of the four') would fail on the inner 'search df' span and should not be made to pass; the closed set belongs to the tools/call span only. 'upstream' has no producer in this server (LanceDB is local; nothing calls Sisältöhaku at runtime) — a value nothing emits is noise in a dashboard. lib-purity: answer() needs mark_span_error (lib, api-only) and format_error (a plain string function that can move) and nothing from fastmcp, so moving it to lib keeps 'lib without MCP deps'. SearchInputError must keep ValueError in its MRO (dataset.py:66 promises that); class SearchInputError(ArchiveError, ValueError) does. Effort: errors alone is S; errors+readiness+settings touch tests, docs, dagger and the dockerfile — M overall.

*Corrections:* (1) server.py:301 → server.py:53-64 (probe_table_readable docstring). (2) No test asserts error.type=='missing_table'; the change needs one doc edit (observability.md:96) and one new assertion, not two test updates. (3) ra-mcp's prefix is RA_MCP_, not RA_; it has no RA_LANCEDB_URI and no host/port settings — per-dataset <NAME>_LANCEDB_URI and RA_MCP_DATA_DIR instead — so 'same field modulo prefix' cannot be the design; align this repo's own prefix (KA_MCP_) and stop there. (4) ra-mcp's /ready already returns a module list with 503-only-when-none; the proposed readiness is a strengthening of that shape (probe per call, type-only fallback), not a new family contract. (5) The closed error.type set applies to the tools/call span that mark_span_error marks; record_span_exception's class-name error.type on inner spans is correct and must stay, so the spike must filter to tool spans. (6) Drop 'upstream' — nothing here emits it.

*Sharpened:* Do the in-repo extraction now, before the second corpus copies _answer: add ra_mcp_kansallisarkisto_lib/errors.py with `class ArchiveError(Exception): disclose: ClassVar[bool] = False`, `class SearchInputError(ArchiveError, ValueError): disclose = True` (moved from dataset.py:63, MRO kept), `class NotReadyError(ArchiveError, RuntimeError): disclose = True` (MissingTableError becomes it, message unchanged), and `answer(tool, produce, *, logger) -> str` moved from df_tool.py with format_error moved beside it; tools/call error.type becomes closed over {validation, not_ready, internal}, asserted by a test that filters spans to tools/call and leaves record_span_exception's class-name attribute on inner spans alone. Readiness: `Readiness(ok: bool, corpora: dict[str, CorpusStatus(state: ready|missing|unreadable, detail: str)])` where readiness() runs the one-row PROBE_KEYWORD search on every call (audit finding 1), drops the cached facade on failure so a late mount is picked up, and puts only the exception type in `detail` for 'unreadable' (finding 2); /ready returns 200 while any corpus is ready and 503 with the same object otherwise, and the boot log renders the same object — one source of truth for the operator, the probe and the server card. Settings: env_prefix='KA_MCP_' with fields transport, host, port, log_level, lancedb_uri, otel_enabled, otel_log_bridge, retiring the os.getenv reads in mcp/telemetry.py:35-42; KA_LANCEDB_URI kept as an alias for one release (AliasChoices) since the dockerfile, dagger and README all set it; update .dagger/serve.go:60-61,140-141, dockerfile ENV :73-74 and README.md:147-155 in the same PR. Acceptance: _answer gone from df_tool.py; test_telemetry asserts the three-value set on tools/call spans; /ready on df-present/voudintilit-absent returns 200 with corpora.voudintilit.state=='missing' once a second corpus is registered; the README settings table lists only KA_MCP_* and OTEL_* names. Offer the resulting errors.py/readiness shape to ra-mcp as the reference — their dataset tools currently return exception text verbatim — but do not make this repo depend on a shared core for it.

**Feasibility and honesty of effort against the installed stack (lancedb 0.38, fastmcp 3.4.4, pyarrow 24, pydantic-settings 2.15, pytest 9.1) — verified by reading the code, the ra-mcp checkout at /home/user/ai-riksarkivet/ra-mcp, and running spikes under the scratchpad.** — *not refuted*, confidence 0.80

All three parts are buildable with what is installed. Errors: read df_tool.py:31-56, formatter.py:113 (format_error), dataset.py:63 (SearchInputError), errors.py:15 (MissingTableError), lib telemetry.py:39-96. Ran the spike the proposal asks for (scratchpad/kit/errtypes.py) with an InMemorySpanExporter through the FastMCP Client: the tools/call span today carries error.type ∈ {'validation', 'handled_error'} — and a direct engine failure (`lancedb_fts_search(..., where='no_such_col = 1')`) leaves error.type='ValueError' on the `search df` span via dataset.py:104-105 and telemetry.py:58. Readiness: timed the one-row probe the proposal wants on every /ready call — 5.26 ms/call on the fixture (DfSearch.search('probe', limit=1); 'probe' matches nothing so the MAX_TOTAL_COUNT fetch is empty on the real corpus too), so per-call probing is cheap; audit.md:48-70 (finding 1) and :72-82 (finding 2, exception text leaking from /ready) are the two bugs this contract would close together. Settings: pydantic-settings 2.15.0 — verified `class KA(ArchiveSettings): model_config = SettingsConfigDict(env_prefix='KA_')` resolves KA_LANCEDB_URI and prefers KA_PORT=9000 over a bare PORT=1, so the shared-fields-modulo-prefix design works. Read ra-mcp's actual settings: /home/user/ai-riksarkivet/ra-mcp/packages/libs/common/src/ra_mcp_common/settings.py:15-22 uses env_prefix 'RA_MCP_' with fields log_level, otel_enabled, data_dir (Path('/data')) — no lancedb_uri field. Counted the blast radius of a rename: KA_LANCEDB_URI is named in 24 files and read directly by the lib in config.py:52 (`os.environ.get('KA_LANCEDB_URI')`), outside pydantic-settings entirely. Effort S is within 2x for the errors+readiness half (~80 lines plus four route tests); the settings half is not S if it actually converges with ra-mcp.

*Corrections:* (1) 'server.py:301' does not exist — server.py is 114 lines; the EACCES-as-Not-found case is probe_table_readable at server.py:53-76. (2) The generic branch does not leave 'the exception class name as error.type' on the tool span: df_tool.py:54 record_span_exception sets error.type=RuntimeError, then :55 mark_span_error(msg) with its default 'handled_error' overwrites it (verified: the span carries 'handled_error'). Today's tool-span set is {missing_table, validation, handled_error}. (3) 'test_telemetry asserts error.type takes only the four values across all spans' cannot hold without breaking the lib and OTel semconv: the `search df` span and the kansallisarkisto.lancedb.errors counter (dataset.py:104-105) carry the exception class name by design, and record_span_exception (telemetry.py:58) sets it on every span the exception unwinds through. Scope the closed set to the tools/call span, or make record_span_exception write the class to a second attribute. (4) 'update the two tests that assert missing_table' — no test asserts that value (grep over packages/*/tests: only test_telemetry.py:175 asserts 'validation', which survives). What the /ready JSON change does break is test_routes.py:71, 82, 106-107, 122 (four tests expecting {'status':'ready','table':'df'} / 'reason'). (5) ra-mcp's prefix is RA_MCP_, not RA_, and its field is data_dir, not lancedb_uri — 'the same field modulo prefix' requires renaming on one side and touching the lib's own env read at config.py:52 plus 24 files here (Dockerfile:71-74, compose:14-17, serve.go:58-61/138-141, docs). That half is cross-repo M, not S. (6) NotReadyError: MissingTableError is raised from tools.py:91 inside get_search(), which the lib does not own; the class hierarchy can live in the lib but the raise site stays in the mcp package.

*Sharpened:* Do the errors+readiness half now, as one PR of ~150 lines including tests: `ra_mcp_kansallisarkisto_lib/errors.py` with `ArchiveError(disclose: ClassVar[bool])`, `InputError` (SearchInputError becomes it), `NotReadyError` (MissingTableError becomes it; also raised by readiness when the one-row probe fails — closing audit finding 1 and 2 together), and `answer(tool, produce)` moved to the lib beside format_error, with error.type on the tools/call span closed to {validation, not_ready, internal} (drop 'upstream' until an upstream exists) and the exception class kept on the spine span per semconv. `readiness() -> Readiness(ok, corpora: dict[str, CorpusStatus])` runs `DfSearch.search(PROBE_KEYWORD, limit=1)` on every call (5 ms measured) and /ready returns 200 with per-corpus status, 503 only when no corpus is ready, type-only detail on unexpected failures. Defer the settings half into its own change and state the real target: adopt `KA_MCP_` (matching ra-mcp's `RA_MCP_` and today's KA_MCP_TRANSPORT/KA_MCP_OTEL_ENABLED), keep KA_LANCEDB_URI as a documented alias read in config.py:52 for one release, and open the ra-mcp issue for `data_dir` vs `lancedb_uri` rather than claiming convergence.

---

