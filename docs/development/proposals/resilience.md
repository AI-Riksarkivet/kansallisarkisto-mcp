---
icon: lucide/file-search
---

# Resilience and failure modes

Every proposal from this lens in full, followed by the three skeptics' verdicts on it.
Effort and impact are the lens's own estimates; the verdict tally is the skeptics'.

## `resilience-1` — A real per-call deadline: lance's native query timeout, because FastMCP's timeout= is a silent no-op for these tools

**survived 3/3, with corrections · effort M · impact 5/5**

**What the code does today.** dataset.py:326 runs `query.limit(MAX_TOTAL_COUNT).to_list()` with no deadline; df_tool.py:87-218 registers both tools with no `timeout=`. Both handlers are sync and dispatched to anyio's threadpool (df_tool.py module docstring; test_tools.py:107). Measured (scratchpad/spike_timeout.py): a FastMCP tool declared `@mcp.tool(timeout=0.5)` that sleeps 3 s returned `done`, is_error=False, after 3.01 s — the timeout never fired. Cause: fastmcp/tools/function_tool.py:401 wraps the await in `anyio.fail_after`, but anyio's `run_sync_in_worker_thread` (anyio/_backends/_asyncio.py:2671) runs it under `CancelScope(shield=not abandon_on_cancel)` with abandon_on_cancel=False, so the scope is shielded and the deadline is discarded; had it fired it would raise `McpError(-32000)` — a protocol error the project's philosophy forbids. Meanwhile lancedb 0.38's `to_list(timeout=timedelta)` (lancedb/query.py:1132) is a real Rust-side deadline: measured on a 200k-row synthetic table (spike_native_timeout.py), an 8-word fuzzy=2 OR query took 1,154 ms unbounded and with timeout=5/50/200 ms raised `RuntimeError: lance error: Query Execution error: Execution error: Query timeout after N ms` after 229/245/219 ms — i.e. it aborts within ~250 ms of the deadline. Today that RuntimeError would reach the model as `Error: the search failed with an internal RuntimeError` (formatter.py:316), indistinguishable from a broken table. A stdio client that closes the pipe mid-query (spike_stdio_disconnect.py) leaves the server running the query to completion — 5.8 s after stdin closed for a 6 s tool — because anyio worker threads are non-daemon; only a deadline bounds that.

**The gap.** No query can be bounded in time. A model retrying fuzzy=2 on a long keyword against 7.8M tuomiokirjat pages holds a worker thread for as long as lance takes; a wedged S3/NFS read holds it forever; the boot probe (server.py:67) and readiness (tools.py:96-111) can hang the process or the probe on a stalled mount. Adopting FastMCP's own `timeout=` would look like a fix and do nothing.

**The proposal.** In dataset.py add `DEFAULT_QUERY_DEADLINE = timedelta(seconds=10)` (df searches run in ~7 ms; a 32-way hammer on 200k rows had a median of 10 s, so 10 s is a ceiling nobody legitimate hits) and `PROBE_DEADLINE = timedelta(seconds=2)`; give `lancedb_fts_search` a `deadline: timedelta | None = DEFAULT_QUERY_DEADLINE` keyword and pass it as `to_list(timeout=deadline)` (line 326); same for `get_charter`'s `.to_list()` (search_operations.py:193). Add `class SearchBudgetError(SearchInputError)` and, in the `except Exception` at dataset.py:327, re-raise a lance error whose message contains `Query timeout after` as `SearchBudgetError(f"the search exceeded its {deadline.total_seconds():.0f}s budget; narrow it — drop fuzzy, add a language/place/year filter, or use fewer words — and try again")`, so `_answer` (df_tool.py:44) returns that sentence verbatim with `mark_span_error(..., 'budget')`. Expose `KA_QUERY_DEADLINE_SECONDS` in settings.py and thread it through `DfSearch`. `probe_table_readable` (server.py:67) and `readiness()` run with `PROBE_DEADLINE` so a hung mount produces a 503 in 2 s rather than a stuck probe. Record the deadline on the span as `db.query.deadline_ms` and count `kansallisarkisto.lancedb.errors{error.type=budget}` separately from server faults.

**Why it is once-in-a-lifetime.** It converts an unbounded resource into a documented contract the model can act on — 'narrow it' is a real next move for a language model, 'internal RuntimeError' is not — and it is measured rather than assumed: the project would be the first MCP server to document that FastMCP's per-tool timeout is inert for sync tools and why, with the anyio line cited.

**How we would know.** test: a monkeypatched `to_list` that sleeps 3 s with deadline=timedelta(milliseconds=200) returns text starting `Error: the search exceeded its` within 500 ms and never a protocol error; on the 200k synthetic table the fuzzy=2 8-word query with a 1 s deadline aborts in ≤1.3 s (today: 1.15 s unbounded, would be ~40 s at 7.8M rows by linear extrapolation); p99 of `kansallisarkisto.lancedb.query.duration` is provably ≤ deadline + 0.3 s under a 60-call fuzzy=2 hammer; `/ready` answers within 2.5 s when `to_list` is stalled.

**Risks.** lance's timeout granularity is ~220 ms, so sub-second deadlines are approximate; a timed-out query kept the tokio pool busy briefly (a cheap query immediately after took 424 ms vs ~10 ms), so the deadline bounds the caller, not the CPU — pair with resilience-4. The remote (LanceDB Cloud) client path may treat timeout differently; only the local path was measured. Argues against nothing the project states, but a deadline-hit search has no `total_hits`, so the formatter's honest-total promise needs an explicit 'not counted' branch.

**First step.** One PR: `deadline` parameter on `lancedb_fts_search` and `get_charter`, `SearchBudgetError`, the classifier for `Query timeout after`, the `_answer` branch, and two tests (monkeypatched slow `to_list`; real fuzzy query on a 5k-row fixture blown up from df_sample with a 1 ms deadline asserting the sentence).

**Spike.** Already run: scratchpad/spike_timeout.py (FastMCP timeout=0.5 on a 3 s sync tool → 'done' after 3.01 s) and scratchpad/spike_native_timeout.py (lance timeout fires at 229/245/219 ms for 5/50/200 ms deadlines on a 200k-row table built by spike_cost.py). Re-runnable offline with `/tmp/uvenv/bin/uv run --project /home/user/kansallisarkisto-mcp python spike_native_timeout.py`.

### Skeptics

**Resilience and failure modes (resilience)** — *not refuted*, confidence 0.85

Re-ran scratchpad/spike_timeout.py: `@mcp.tool(timeout=0.5)` on a 3 s sync tool returned `done`, is_error=False after 3.01 s — the no-op is real. Mechanism verified in source: fastmcp 3.4.4 function_tool.py:399-416 wraps `_execute` in `anyio.fail_after` and would raise McpError(-32000); `_execute` dispatches sync tools via `call_sync_fn_in_threadpool` → anyio.to_thread.run_sync; anyio 4.15.1 _asyncio.py:2671 runs it under `CancelScope(shield=not abandon_on_cancel)` with the default abandon_on_cancel=False, so the deadline is shielded. FastMCP only rejects the `run_in_thread=False` combination (function_tool.py:321-338), not this one. lancedb 0.38.0 query.py:1132 `to_list(*, timeout: timedelta)` confirmed; re-ran spike_native_timeout.py on an idle box: unbounded 1,318 ms; deadlines 5/50/200 ms raised `RuntimeError: lance error: Query Execution error: Execution error: Query timeout after N ms` after 293/269/270 ms; under 4-way CPU load the same fired at 327/684/1003 ms. The cheap query right after a timed-out one took 611 ms (idle) — the tokio pool keeps burning. spike_stdio_disconnect.py: server exited 5.8 s after stdin closed, tool ran to completion. dataset.py:326 and df_tool.py (no timeout=) as cited. Audits do not cover any of this.

*Corrections:* Line cites: format_error is formatter.py:113-124 (not :316); get_charter's `.to_list()` is search_operations.py:131 (not :193); server.py:67 is right. lance's timeout floor is ~270-300 ms idle and load-dependent (up to ~1 s at 4-way contention), not ~220 ms. `readiness()` (tools.py:96-111) runs no query at all today — second audit finding 1 — so the PROBE_DEADLINE half of the proposal presupposes that fix. The '10 s ceiling nobody legitimate hits' holds only uncontended: under the 32-way hammer legitimate single queries had medians of 8.8-21 s, so a deadline measured from call entry would convert overload into budget errors — start the clock after admission (resilience-4's gate). Whether the Rust-side deadline also bounds a stalled object-store/NFS read was not tested (only CPU-bound queries were); the 'hung mount → 503 in 2 s' outcome is a hypothesis until a stalled-mount fixture proves it.

*Sharpened:* Give `lancedb_fts_search` and `get_charter` a `deadline: timedelta` passed to `to_list(timeout=)`, classify `Query timeout after` into `SearchBudgetError(SearchInputError)` whose sentence tells the model to narrow, and measure the deadline from *admission* (after the concurrency gate) so it is a per-query budget rather than a queue-wait penalty. Pin two facts in tests and prose: (1) FastMCP `timeout=` is inert for threadpool-dispatched sync tools because anyio shields the worker scope (cite _asyncio.py:2671); (2) lance's deadline has a ~300 ms floor and does not stop the Rust work already scheduled (a cheap query right after took 611 ms), so the deadline bounds the caller, not the CPU — which is why resilience-4 is its partner. Add a third test with a monkeypatched stalled `to_list` for /ready, and a TODO to prove the deadline covers I/O stalls with a FUSE or slow-object-store fixture before documenting 'hung mount → 503 in 2 s'.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions), the three-corpus future and the historian user** — *not refuted*, confidence 0.85

Re-ran scratchpad/spike_timeout.py: FastMCP 3.4.4 `@mcp.tool(timeout=0.5)` on a 3 s sync tool returned `done`, is_error=False after 3.01 s — confirmed inert. Cause verified in the libraries: fastmcp/tools/function_tool.py:401-413 wraps `_execute` in `anyio.fail_after` and would raise `McpError(-32000)`; fastmcp/utilities/async_utils.py:31 dispatches via `anyio.to_thread.run_sync`, whose default is `abandon_on_cancel=False` (anyio/to_thread.py:30), and anyio 4.15.1 `_backends/_asyncio.py:2671` runs the thread under `CancelScope(shield=not abandon_on_cancel)`, so the deadline is never delivered. Notably FastMCP itself only documents the no-op for `run_in_thread=False` (function_tool.py:204-205, 321-338) — the worker-thread case is an unreported upstream bug, which makes documenting it here genuinely valuable. Re-ran spike_native_timeout.py on the 200k cost_db: `to_arrow(timeout=)`/`to_list(timeout=)` (lancedb/query.py:1132-1146) raise `RuntimeError: lance error: Query Execution error: Execution error: Query timeout after N ms` — real Rust-side abort. dataset.py:326 and search_operations.py:130 call `.to_list()` with no timeout today; df_tool.py:51-56 would render it as `internal RuntimeError`. Fit: a caller-fixable sentence returned verbatim is exactly the project's error-disclosure rule; a `SearchInputError` subclass reuses the existing `_answer` branch; the lib gains a `timedelta` parameter and no new dependency; stdio pays nothing. This is measured, corpus-scale-relevant (fuzzy=2 on 7.8M pages) and nobody else has written it down.

*Corrections:* Granularity is worse than stated: today's run aborted 296/399/460 ms after 5/50/200 ms deadlines (not 219-245), and a cheap query immediately after a timed-out one took 763 ms (not 424) — so state the deadline as 'wall-clock bound on the caller, ±0.5 s; the tokio pool finishes the aborted work anyway'. `get_charter`'s `.to_list()` is at search_operations.py:130, not :193. FastMCP's ValueError guard covers only `run_in_thread=False`; cite function_tool.py:321-338 when explaining why the worker-thread path is *also* inert.

*Sharpened:* Make the deadline a lib-level contract: `lancedb_fts_search(..., deadline: timedelta | None = DEFAULT_QUERY_DEADLINE)` and `get_charter` pass `to_list(timeout=deadline)`; classify `Query timeout after` into `SearchBudgetError(SearchInputError)` whose message names the three narrowing moves; `_answer` marks it `budget` not `validation`; `format_results` gets an explicit 'not counted' branch. Pin the FastMCP finding with a test that asserts no registered tool declares `timeout=` AND a test that reproduces the inert-timeout behaviour so an upstream fix is noticed (and file it upstream with the anyio line). Give `probe_table_readable` and the (audit-finding-1-fixed) `readiness()` a 2 s `PROBE_DEADLINE` so a stalled mount is a 503 in 2 s. Record `db.query.deadline_ms` on the span and `error.type=budget` on the counter. Measure the default on the 200k table so `DEFAULT_QUERY_DEADLINE` is a written-down number, and re-measure once resilience-2 lands, since that changes what 'over budget' costs.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel SDK 1.44 / anyio 4.15 as installed; every API claim read in the venv source and the spikes re-run offline on the 18-row fixture and the 200k-row scratchpad cost_db** — *not refuted*, confidence 0.90

Re-ran scratchpad/spike_timeout.py: `@mcp.tool(timeout=0.5)` on a 3 s sync tool returned `done`, is_error=False after 3.01 s, thread 'AnyIO worker thread' — FastMCP's timeout is inert for sync tools. Cause verified: fastmcp/tools/function_tool.py:400-413 wraps `_execute` in `anyio.fail_after` then raises McpError(-32000); fastmcp/utilities/async_utils.py:34 dispatches via `anyio.to_thread.run_sync`; anyio/_backends/_asyncio.py:2671 `CancelScope(shield=not abandon_on_cancel)` shields the worker. lancedb/query.py:1132 `to_list(self, *, timeout: Optional[timedelta])` → :1146 `to_arrow(timeout=)` → LanceFtsQueryBuilder.to_arrow (query.py:2088-2091) passes it to `_execute_query`. Re-ran spike_native_timeout.py on cost_db (200k rows): unbounded fuzzy=2 8-word OR = 1,345 ms; timeout 5/50/200 ms → RuntimeError 'Query timeout after N ms' after 270/268/310 ms; `to_list(timeout=)` raises the same. New experiment judge_d2_cold_timeout.py: a cold first query (fresh process, index not yet loaded) with a 5 ms deadline aborted after 20 ms, so index load/planning is inside the deadline, not before it; `table.search().where(...).to_list(timeout=)` (the get_charter shape, search_operations.py:131) accepts the kwarg; a cheap 25-row query with a 1 ms deadline was killed after 4 ms, so sub-300 ms deadlines kill legitimate queries — 10 s is safe. Re-ran spike_stdio_disconnect.py: server exited 5.7 s after stdin closed, tool ran to completion. df_tool.py:44-56 `_answer` structure and dataset.py:326 confirmed. Effort M (~60 lines across dataset.py, search_operations.py, settings.py, df_tool.py + 2 tests) is within reality.

*Corrections:* Line references are wrong: get_charter's `.to_list()` is at search_operations.py:131 (file is 135 lines, there is no :193); `format_error` is formatter.py:113-124 (file is 124 lines, no :316). routes.py:52 `async def ready` calls `readiness()` synchronously on the event loop — a real 2 s probe there must run via `anyio.to_thread.run_sync` or it stalls every concurrent MCP request. The hung-NFS/S3 case is unproven here (only CPU-bound execution was measured); the '/ready answers within 2.5 s when to_list is stalled' outcome should not rely solely on lance's Rust deadline pre-empting a blocked object-store read. The post-timeout cheap query cost 634 ms in my run (proposal: 424 ms) — the 'bounds the caller, not the CPU' caveat is stronger than stated.

*Sharpened:* Add `deadline: timedelta | None` to `lancedb_fts_search` and `DfSearch.get_charter`, passed as `to_list(timeout=deadline)`, default 10 s (`KA_QUERY_DEADLINE_SECONDS`), floor-checked at ≥1 s because lance's granularity (~250-300 ms) makes anything below that kill 4 ms queries; classify 'Query timeout after' into `SearchBudgetError(SearchInputError)` with the 'narrow it' sentence and `error.type=budget`. For the probe, belt-and-braces: run `probe_table_readable`/`readiness()` in a daemon thread joined with `PROBE_DEADLINE=2 s` (bounded even if lance's deadline cannot interrupt a wedged mount), and call it from the async `/ready` route via `anyio.to_thread.run_sync`. Document in df_tool.py's module docstring — with function_tool.py:400 and anyio _asyncio.py:2671 cited — why `@mcp.tool(timeout=)` must not be used here. Test: monkeypatched slow `to_list` + deadline=200 ms returns the budget sentence in <500 ms and never McpError; a 5k-row fixture blow-up with a 1 ms deadline asserts the sentence end-to-end.

---

## `resilience-2` — Two-phase search: rank 10k row ids, fetch only the page — a bytes budget per call that removes the OOM and the GIL serialisation

**survived 3/3, with corrections · effort L · impact 5/5**

**What the code does today.** dataset.py:243-254 documents the pattern: every search materialises up to MAX_TOTAL_COUNT rows of every non-FTS column to count them, then keeps `limit`; it measured 6.1 → 3.3 MB on df by projecting away `searchable_text`. Measured on a 200k-row table with tuomiokirjat-shaped 2.5 KB pages (scratchpad/spike_cost.py, spike_hammer.py): today's `select(all).limit(10000).to_list()` = 256 ms, 22.2 MB of transcript text per call, RSS +260 MB for one call; 8 concurrent calls took 2.0 s wall = exactly 8 × 0.256 s — zero parallel speed-up, because `to_list` → `to_pylist()` (lancedb/query.py:1146) builds 10,000 Python dicts under the GIL; 32 concurrent 8-word OR searches at fuzzy=0 took 12.9 s wall, median per-call 10.1 s, RSS 292 → 2,570 MB. anyio's threadpool admits 40 (anyio/_backends/_asyncio.py:3162 `CapacityLimiter(40)`), so 40 × ~70 MB ≈ 2.8 GB is the reachable peak in a container. Two-phase measured on the same table: `select(['_score']).with_row_id(True).fast_search().limit(10000).to_arrow()` then `table.take_row_ids(page_ids).select(cols).to_list()` = 24 ms total (page fetch 5.6 ms, order preserved), ~160 KB moved instead of 22 MB. lancedb's default `read_consistency_interval=None` (lancedb/db.py:764-768) pins a `LanceTable` handle at the version it opened, and `lancedb_fts_search` opens a fresh handle per call (dataset.py:290), so both phases see one version and row ids stay valid across the two.

**The gap.** The 10k materialisation is the single scaling hazard: per-call bytes and GIL time are proportional to row width, not page size, so tuomiokirjat (2.5 KB pages) is ~10× df's cost per call and 40 concurrent calls can OOM-kill a 2 GB container — which then fails /health and crash-loops under exactly the load that caused it. The project's stated goal 'scales to 7.8M pages without changing shape' is not true of this line.

**The proposal.** Rewrite the execution half of `lancedb_fts_search` (dataset.py:306-343): phase 1 `query.select(['_score']).with_row_id(True).where(where).fast_search().limit(MAX_TOTAL_COUNT).to_arrow(timeout=deadline)` → an Arrow table `ranked`; `total = ranked.num_rows`; `page_ids = ranked.column('_rowid').slice(offset, limit).to_pylist()`; phase 2 `table.take_row_ids(page_ids).select(columns).to_list()` on the same `table` handle, re-attach `_score` from `ranked.column('_score').slice(offset, limit)`. Keep the docstring's argument for one ranked set (BM25 ties) — it still holds, the ranked set is just ids now — and replace the 6.1→3.3 MB paragraph with the new numbers. Add `MAX_PAGE_BYTES = 2 MB` as a formatter-side guard (100 × full transcripts can exceed what a model context should receive) that truncates snippets rather than rows. Record `db.response.bytes` on the span. Pin with a test that phase 1's schema is exactly `{_score, _rowid}` and that a 100-row page never transfers more than `limit` transcripts (monkeypatch `take_row_ids` to count).

**Why it is once-in-a-lifetime.** It makes 'no count API' a solved problem for LanceDB FTS in the open: an honest total plus stable pagination at ~1% of the bytes, measured, with the version-consistency argument written next to it — the pattern every LanceDB-backed archive server will copy.

**How we would know.** On the 200k synthetic table: per-call ≤30 ms (today 256 ms); 32 concurrent searches add <150 MB RSS (today +2.3 GB) and finish in <2 s wall (today 12.9 s); 8 concurrent calls show ≥3× speed-up over serial on 4 CPUs (today 1.0×). On the real df table the 1,451-hit query cited in dataset.py:252 moves <100 KB in phase 1 instead of 3.3 MB.

**Risks.** `take_row_ids` is documented as 'row ids are not stable and are relative to the current version' — safe here only because the handle is pinned per call; a future switch to `read_consistency_interval=0` or a long-lived handle would silently break it, so the test must assert the handle is opened per call. `_rowid` semantics differ on the remote client. The formatter's `_score` attachment is new code on the hot path. This overrides the documented projection reasoning at dataset.py:249-254 — the comment must be rewritten, not left contradicting the code (the second audit's finding 8 shows what stale prose costs here).

**First step.** PR 1: implement phase 1/phase 2 behind a module flag `TWO_PHASE = True`, keep the old path for A/B in a benchmark script under scripts/, and port the invariants tests (test_search_invariants.py:133-175: tiling, order stability, page ≤ limit) unchanged — they are the contract.

**Spike.** Already run: scratchpad/spike_cost.py (builds cost_db: 200k rows in 19 s, 693 MB; A=256 ms/22 MB vs C=24 ms) and scratchpad/spike_hammer.py (32-way: +2.3 GB, median 10.1 s). The take_row_ids page fetch (5.6 ms, order preserved) was confirmed inline. Rerun: `/tmp/uvenv/bin/uv run --project /home/user/kansallisarkisto-mcp python spike_cost.py 200000`.

### Skeptics

**Resilience and failure modes (resilience)** — *not refuted*, confidence 0.80

Verified the pattern at dataset.py:243-254, 290, 306-343; lancedb query.py:1146 `to_list` = `to_arrow().to_pylist()`; anyio CapacityLimiter(40) at _asyncio.py:3162; db.py:764-768 read_consistency_interval default None. Re-ran spike_cost.py 200000 (cost_db present): A today = 288 ms, 22.2 MB transcript payload, 10,000 rows; B to_arrow = 151 ms (so ~half of today's cost is the GIL-bound to_pylist); C two-phase (ids+score to_arrow + 25-row page) = 34 ms; explain plan for a filtered query shows `ScalarIndexQuery ... @year_from_idx(BTree)` under MatchQuery — prefilter is real. Wrote take_check.py: phase-1 schema is exactly ['_score','_rowid']; `LanceTakeQueryBuilder` has `.select()` and `.with_row_id()` (no-arg — `with_row_id(True)` raises TypeError); a 25-row `take_row_ids(...).select(cols).with_row_id().to_list()` took 9.4 ms. But order is NOT preserved: requested ids came back sorted by row id (shuffled request → sorted output), exactly as lancedb's own docstring warns at table.py:1756-1758. Spike D in spike_cost.py itself fails with `TypeError: take_row_ids() got an unexpected keyword argument 'columns'`, so the 'confirmed inline' order claim is not in any runnable spike. Parallelism: today's path x8 = 0.9× speed-up (2.36 s serial vs 2.58 s parallel, +107 MB RSS); phase-1 x8 = 1.5× (0.11 s vs 0.07 s, +7 MB). Hammer re-run idle: 32 concurrent added +1.9 GB RSS.

*Corrections:* `take_row_ids` does not honour request order — phase 2 must add `.with_row_id()` and re-align rows to phase-1's `_rowid` order in Python before attaching `_score` (the lancedb docstring says exactly this). `with_row_id` on the take builder takes no argument. The '≥3× speed-up on 4 CPUs' outcome is unsupported: measured 1.5× for the phase-1 query, because at ~14 ms per call thread overhead dominates; keep the per-call-latency and RSS outcomes, drop or re-measure the speed-up claim. Hammer RSS growth measured +1.9 GB (proposal +2.3 GB) — same order.

*Sharpened:* Two-phase search: phase 1 `select(['_score']).with_row_id(True).where(where).fast_search().limit(MAX_TOTAL_COUNT).to_arrow(timeout=deadline)` gives the honest total and the ranked `_rowid`s at ~160 KB; phase 2 `table.take_row_ids(page_ids).select(columns).with_row_id().to_list()` on the *same* handle, then reorder by a `{rowid: rank}` map and attach `_score` — with a test that feeds shuffled ids and asserts phase-1 order survives, since lance documents no order guarantee. Pin in a test that the handle is opened per call (dataset.py:290) and that the connection has read_consistency_interval None, because both phases must see one version. Rewrite the 6.1→3.3 MB paragraph with the new numbers (288 ms/22 MB → 34 ms/~0.2 MB on the 200k synthetic table). Measurable: per-call ≤40 ms, 32-way hammer adds <150 MB RSS, page bytes ∝ limit not MAX_TOTAL_COUNT.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions), the three-corpus future and the historian user** — *not refuted*, confidence 0.90

Code confirmed: dataset.py:243-254 documents the 10k materialisation and the 6.1→3.3 MB projection argument; :290 opens a fresh handle per call; :326 `to_list()` → lancedb/query.py:1146 `to_arrow().to_pylist()`; lancedb/db.py:764-768 `read_consistency_interval=None` default means the handle never refreshes, so both phases see one version. `take_row_ids` exists on LanceTable (lancedb/table.py:1747, 2660 → `LanceTakeQueryBuilder` with `.select()`/`.with_row_id()`). Re-ran scratchpad/take_check.py on the 200k table: phase-1 schema is exactly `['_score','_rowid']` (10,000 rows), page fetch of 25 rows 10 ms, and 8 concurrent calls: phase-1 RSS 223→228 MB vs today's path 432→691 MB — the memory claim is solid. The invariants tests at test_search_invariants.py:133-175 (tiling, order stability, page ≤ limit) are the right contract to port unchanged. This is the one line that contradicts 'scales to 7.8M pages without changing shape', and the fix keeps the honest total and stable pagination the docstring argues for.

*Corrections:* `take_row_ids` does NOT preserve requested order in lancedb 0.38: measured `order preserved vs requested: False`, and a shuffled request came back sorted by `_rowid` — so phase 2 must call `.with_row_id()` and re-order in Python by the phase-1 id list (cheap for ≤100 rows), and the 'order preserved' claim in the proposal and its planned comment must go. Parallelism is overstated: today's path measured 1.2× (not 1.0×) and phase-1 1.7× (not ≥3×) on this 4-CPU box; keep the bytes/RSS numbers as the headline and drop the speed-up target to 'measured ≥1.5×'. `_score` is not read by formatter.py and only tolerated by test_search_operations.py:144, so re-attaching it is API compatibility, not hot-path necessity.

*Sharpened:* Phase 1: `select(['_score']).with_row_id(True).where(where).fast_search().limit(MAX_TOTAL_COUNT).to_arrow(timeout=deadline)`; phase 2: `table.take_row_ids(page_ids).select(columns).with_row_id().to_list()` then reorder by `_rowid` into phase-1 order and attach `_score`. Add a test that phase-1's schema is exactly `{_score,_rowid}`, that phase 2 is called with exactly `len(page)` ids (counting monkeypatch), and that `db.open_table` is called once per search (the version-pinning invariant; note in the comment that `read_consistency_interval` or a long-lived handle would silently break row ids). Rewrite dataset.py:249-254 with the new numbers. Then ask the more ambitious question the change unlocks: with phase 1 at ~10 ms per 10k ids, measure whether `MAX_TOTAL_COUNT` can rise (50k? 100k?) so `total_is_capped` is rare even on tuomiokirjat — the honest-total promise gets stronger, not just cheaper.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel SDK 1.44 / anyio 4.15 as installed; every API claim read in the venv source and the spikes re-run offline on the 18-row fixture and the 200k-row scratchpad cost_db** — *not refuted*, confidence 0.85

APIs exist in lancedb 0.38: `with_row_id` (query.py:1284, base builder), `fast_search` on LanceFtsQueryBuilder (query.py:2047), `to_arrow(timeout=)` (query.py:2088), `LanceTable.take_row_ids` (table.py:1747 abstract, :2660 impl → `LanceTakeQueryBuilder` at query.py:4339 which has `.select()`/`.with_row_id()`/`.to_list()`), `read_consistency_interval=None` pins the handle (db.py:764-770). Re-ran scratchpad/take_check.py on cost_db: phase-1 schema is exactly ['_score','_rowid'] with 10,000 rows; 25-row take = 9.4 ms; phase-1 ×8 serial 0.12 s vs parallel 0.07 s; today's path ×8 serial 2.29 s vs parallel 2.10 s, RSS 484→567 MB. In judge_d2_gate.py the unguarded 32-way fuzzy=0 hammer through today's `lancedb_fts_search` drove RSS 288→2,412 MB, confirming the ~+2.1-2.3 GB figure. dataset.py:243-254 docstring and :306-343 execution confirmed as described. Effort L is honest.

*Corrections:* `take_row_ids` does NOT preserve request order: table.py:1756-1758 says 'No guarantees are made regarding the order in which results are returned. If you desire an output order that matches the order of the given ids, you will need to add the row id column to the output and align it yourself', and take_check.py measured `order preserved vs requested: False` / `shuffled order preserved: False | sorted? True` — rows come back sorted by _rowid. The spike's 'order preserved' claim is wrong; phase 2 must call `.with_row_id()` and re-align the page by a `{_rowid: row}` dict in ranked order (25-100 rows, negligible). The '≥3× speed-up on 4 CPUs' outcome is not supported: phase-1 ×8 measured 1.7× (today 1.1×) — the FTS scoring is itself serialised in lance's executor; restate as ≥1.5×. The 'version pinning per call' argument also requires the test to assert that both phases use the same `table` object returned by one `open_table` call.

*Sharpened:* Two-phase `lancedb_fts_search`: phase 1 `select(['_score']).with_row_id(True).where(where).fast_search().limit(MAX_TOTAL_COUNT).to_arrow(timeout=deadline)`; phase 2 `table.take_row_ids(page_ids).select(columns).with_row_id().to_list()` on the same handle, re-ordered by the ranked `_rowid` list and `_score` re-attached from the phase-1 slice. Pin with tests: phase-1 schema == {_score,_rowid}; page order equals ranked order for a shuffled fixture (this is the test the lancedb docstring demands); at most `limit` transcripts transferred (counting monkeypatch on take_row_ids); the existing tiling/stability invariants in test_search_invariants.py:133-175 unchanged. Outcomes on cost_db: ≤30 ms per call (today ~256 ms), 32-way hammer <2 s wall and <150 MB RSS growth (today 12.3 s / +2.1 GB), ≥1.5× 8-way parallel speed-up. Rewrite dataset.py:243-254 to the new numbers; the old 6.1→3.3 MB paragraph is then false.

---

## `resilience-3` — A failure taxonomy and a degraded-state machine: classify lance errors, tell the model whether to retry, flip /ready, and fast-fail through a circuit breaker

**survived 3/3, with corrections · effort L · impact 5/5**

**What the code does today.** df_tool.py:51-56 folds every non-input exception into `format_error`, which returns only the type (formatter.py:316); every lance failure is a `RuntimeError`, so the model always reads `Error: the search failed with an internal RuntimeError`. Measured (scratchpad/spike_corrupt.py) on a real fixture table: FTS index files truncated to 0 bytes → `RuntimeError: lance error: Not found: …/_indices/<uuid>/tokens.lance` — byte-for-byte the same message deployment.md:49 documents for the EACCES/uid-1000 case; `_indices/` deleted → same `Not found`; a data fragment truncated to half → `RuntimeError: lance error: LanceError(IO): Generic LocalFileSystem error: failed to fill whole buffer`; `data/` deleted → `Not found: …/data/<fragment>.lance` while `table_names()` still lists `df`; `_versions/` deleted → `ValueError: Table 'df' was not found` while `table_names()` still lists `df` (so `get_search` at tools.py:88-89 passes and the tool reports an internal ValueError rather than the missing-table sentence); the deadline case (resilience-1) → `Query timeout after N ms`. `readiness()` (tools.py:96-111) reported `(True, 'df')` after `_indices/` was deleted because the facade is cached (the second audit's finding 1 covers the caching; it does not cover that even an uncached probe has no way to say *what kind* of broken). `mark_span_error` (df_tool.py:54) tags these `df_search failed: RuntimeError` — one bucket for permanent corruption, transient I/O and deadlines alike.

**The gap.** Neither the model nor an orchestrator can distinguish 'retry' from 'stop, the archive is down' from 'narrow your query'. A model that gets `internal RuntimeError` on a permanently unreadable table will retry with variations — the hammer case — and every retry re-touches the broken index; /ready stays 200; the error metric has one flat class.

**The proposal.** In dataset.py add `class FailureClass(StrEnum): missing, unreadable, corrupt, budget, transient` and `classify_lance_error(exc) -> FailureClass`: `Table '…' was not found` → missing; `Not found:` with `/_indices/` or `/data/` in the path → unreadable (permanent until an operator acts — the text names the two causes: ownership vs half-copied table); `failed to fill whole buffer`, `Invalid`/`Corrupt` → corrupt; `Query timeout after` → budget; everything else transient. Wrap as `class TableFaultError(RuntimeError)` carrying the class and a caller-safe sentence (never the path). In df_tool.py `_answer` add a branch: missing/unreadable/corrupt → `Error: the Diplomatarium Fennicum table on this server is unreadable (…cause…). Retrying will not help; tell the user the archive search is temporarily unavailable.`; budget → the resilience-1 sentence; transient → `Error: a temporary storage error; retry once.` In tools.py add `_state: dict` = {class, since, count} updated on every fault and cleared on success; `readiness()` returns 503 with `{'status':'not ready','class':'unreadable','since':…}` when the last fault is permanent-class, and re-runs the 2 s probe every 30 s (half-open) so a late mount or a fixed chown recovers without a restart. Circuit breaker: after 3 consecutive permanent-class faults, `_answer` returns the sentence without touching lance until the next half-open probe succeeds (fast-fail, ~0 ms instead of a failing index read). Metrics: `kansallisarkisto.lancedb.errors{error.type=<class>}` and a `kansallisarkisto.lancedb.state` gauge (0 ready/1 degraded/2 down). Document the matrix in observability.md: failure → sentence the model reads → /ready body → metric label.

**Why it is once-in-a-lifetime.** It gives the three consumers of a failure — the model, the orchestrator, the dashboard — one shared vocabulary derived from measured error strings, so 'boots without data, a missing table is a message' extends to 'a broken table is the right message', and a model stops hammering a dead server because it was told to.

**How we would know.** Fault-injection tests (resilience-8) assert for each of the five injected states: the exact tool sentence prefix, `/ready` status code and `class`, and the `error.type` label; the breaker makes the 4th call on an unreadable table return in <1 ms without a lance call (assert via a counting monkeypatch); after restoring the table, `/ready` returns 200 within one half-open interval without a restart; the `df_search failed: RuntimeError` span status string no longer appears anywhere.

**Risks.** Classification is by substring on lance's Rust error text, which is not an API — pin each string with a test that builds the real corruption so a lance upgrade that rewords the message fails loudly rather than silently reclassifying. Fast-fail on 3 consecutive faults could mask a flapping mount; the half-open probe bounds that to 30 s. The breaker is process state, so two replicas can disagree — fine, /ready is per replica. Adds a moving part to a design that prizes having none.

**First step.** PR: `classify_lance_error` + `TableFaultError` in dataset.py with five substring tests built from real corrupted fixture tables (each takes ms — spike_corrupt.py shows the recipes), and the `_answer` branch that returns the class-specific sentence. Breaker and /ready state in a second PR.

**Spike.** Already run: scratchpad/spike_corrupt.py — nine injections (truncated index, deleted _indices, half data fragment, deleted data, deleted _versions, overwrite-under-read, readiness-after-corruption, rename swap) with the exact exception class and message for each. Rerun offline in ~2 s.

### Skeptics

**Resilience and failure modes (resilience)** — *not refuted*, confidence 0.85

Re-ran spike_corrupt.py; every injected state reproduced with the class and message shape claimed: truncated FTS files and deleted `_indices/` → `RuntimeError: lance error: Not found: …/_indices/<uuid>/tokens.lance` (byte-for-byte the deployment.md:49 EACCES message shape); half-truncated fragment → `LanceError(IO): Generic LocalFileSystem error: failed to fill whole buffer`; `data/` deleted → `Not found: …/data/<fragment>.lance` with `table_names()` still ['df']; `_versions/` deleted → `ValueError: Table 'df' was not found` with `table_names()` still ['df'] (so tools.py:88-89's listing check passes and the model gets 'internal ValueError'); readiness() stayed (True,'df') after `_indices/` deletion. df_tool.py:51-56 folds everything into format_error (formatter.py:124, type only); dataset.py:330 counts errors with `error.type = type(exc).__name__` — one flat 'RuntimeError' class. The second audit's finding 1 covers only the caching; nothing in either audit classifies lance failures or proposes a breaker.

*Corrections:* Today's span tagging: df_tool.py:55 calls `mark_span_error(f'{tool} failed: RuntimeError')` with the default error_type, so the span's `error.type` ends up 'handled_error' (overwriting the exception name record_span_exception set at lib telemetry.py:58) while the status message carries the class name — the metric at dataset.py:330 is where the one-flat-class problem actually lives. The half-open re-probe in `readiness()` presupposes the second audit's finding 1 (readiness runs no query today). `Not found:` cannot separate EACCES from a genuinely missing file — the classifier must say 'ownership or half-copied table', as the proposal already does.

*Sharpened:* Add `classify_lance_error(exc) -> FailureClass` in dataset.py keyed on the five measured substrings (`was not found`, `Not found:` + `/_indices/`|`/data/`, `failed to fill whole buffer`, `Query timeout after`, else transient), pinned by tests that *build* each corruption from the fixture in milliseconds (the spike recipes) so a lance rewording fails loudly. Use the class as the `error.type` label at dataset.py:330 and in `_answer`, returning a class-specific sentence that tells the model whether to retry, narrow, or stop. Make `/ready` carry `{class, since}` and the readiness probe half-open (re-run the one-row PROBE_KEYWORD query every 30 s after a permanent-class fault). Breaker as a second PR: after 3 consecutive permanent-class faults, `_answer` fast-fails until the half-open probe succeeds; assert with a counting monkeypatch that the 4th call never touches lance. Document the failure→sentence→/ready→metric matrix in observability.md.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions), the three-corpus future and the historian user** — *not refuted*, confidence 0.70

Error strings confirmed from scratchpad/corrupt_out.txt (spike_corrupt.py, re-read): truncated index → `Not found: …/_indices/<uuid>/tokens.lance`; `_indices` deleted → same; half fragment → `LanceError(IO): … failed to fill whole buffer`; `data/` deleted → `Not found: …/data/<fragment>.lance` while `table_names()` still lists `df`; `_versions` deleted → `ValueError: Table 'df' was not found` — which today passes tools.py:307-309 and reaches `_answer` as `internal ValueError`. df_tool.py:51-56 and formatter.py:113-124 confirm all of these collapse to one type-only sentence, and `mark_span_error` gets one flat bucket. Giving the model 'retrying will not help; tell the user the archive is unavailable' is squarely the project's 'text the model can act on' philosophy, and the taxonomy is derived from measured strings with a pin-the-string test — that is the measured-decision style. Where I say no: the circuit breaker and 30 s half-open state machine. Each failing lance call in the spike returned in milliseconds, so fast-fail saves nothing measurable; a model told 'retrying will not help' already stops; and once audit finding 1 is fixed, `/ready` runs the probe on every call, so the orchestrator's probe interval *is* the half-open interval — the `_state` dict is a moving part the design does not need.

*Corrections:* The 'everything else → transient, retry once' default is the risky leg: an unclassified fault (a lancedb upgrade rewording a message, a new I/O error) would be sent back as 'retry once'. Default unknown classes to today's type-only sentence with no retry advice; only classify what has been measured. Drop the breaker and the `_state` machine; with finding 1 fixed, `readiness()` simply classifies the probe's exception and puts `class` in the 503 body.

*Sharpened:* One PR: `FailureClass` + `classify_lance_error` + `TableFaultError` in dataset.py (never carrying the path), five tests that build the real corruption from the recipes in spike_corrupt.py and assert the class, and the `_answer` branch that returns a class-specific sentence (missing/unreadable/corrupt → 'unreadable on this server, retrying will not help'; budget → resilience-1's sentence). `readiness()` (after audit finding 1) reports `{'status':'not ready','class':…}`; `error.type=<class>` on `kansallisarkisto.lancedb.errors`. Document the matrix (failure → sentence → /ready body → metric label) in observability.md and in deployment.md next to the EACCES paragraph, since 'Not found under _indices' is that case too. No breaker, no gauge, no state.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel SDK 1.44 / anyio 4.15 as installed; every API claim read in the venv source and the spikes re-run offline on the 18-row fixture and the 200k-row scratchpad cost_db** — *not refuted*, confidence 0.85

scratchpad/corrupt_out.txt (spike_corrupt.py re-runnable in ~2 s) contains every string claimed: truncated-index and deleted-_indices both → `RuntimeError: lance error: Not found: …/_indices/<uuid>/tokens.lance`; half fragment → `LanceError(IO): Generic LocalFileSystem error: failed to fill whole buffer`; data/ deleted → `Not found: …/data/<fragment>.lance` while table_names lists ['df']; _versions/ deleted → `ValueError: Table 'df' was not found` while table_names lists ['df']; readiness() stayed (True,'df') after _indices deleted because the facade is cached (tools.py:80-93, readiness at :96-111 confirmed). deployment.md:49 documents the EACCES case with the identical `Not found: /data/df.lance/_indices/<uuid>/tokens.lance` text, so the classifier's 'ownership vs half-copied' sentence is grounded even though root cannot reproduce EACCES here (spike section 6). df_tool.py:51-56 folds everything to `format_error` (formatter.py:113-124, type only). The tests-from-real-corruption recipe costs one ingest each (~0.3-0.5 s). Effort L for classifier + sentences + state + breaker + /ready + docs is realistic; PR 1 alone is S-M.

*Corrections:* formatter.py:316 does not exist — `format_error` is formatter.py:113-124. routes.py:52 `async def ready` calls the sync `readiness()` directly on the event loop; today that is cheap (cached facade) but the proposed 2 s half-open probe would freeze all MCP traffic for up to 2 s every 30 s unless it runs through `anyio.to_thread.run_sync` or in a background thread that updates `_state`. The measurable outcome references 'resilience-8', which is not in this batch. 'Everything else transient' will classify unknown future lance wording as retryable — acceptable, but the substring-pinning tests must also assert the fallthrough class so a reworded message fails loudly.

*Sharpened:* `FailureClass` + `classify_lance_error` in dataset.py with the five real-corruption fixtures as tests, and `_answer` sentences per class. Extend the `missing` class to the `ValueError: Table 'df' was not found` case so the _versions-deleted state returns the existing MISSING_TABLE sentence instead of 'internal ValueError' (closes the gap at tools.py:88-89 where `table_names` passes). Put the half-open probe on a background thread (daemon, PROBE_DEADLINE from resilience-1) that writes `_state`; `/ready` reads `_state` only — O(1), never blocks the loop — and returns 503 with `{'status':'not ready','class':…,'since':…}` for permanent classes. Breaker: 3 consecutive permanent-class faults → fast-fail sentence until the probe succeeds. Metrics `errors{error.type=<class>}` and a `lancedb.state` gauge. Document the failure→sentence→/ready→label matrix in observability.md.

---

## `resilience-4` — Admission control: a small in-process search gate with a queue wait, so a hammering model gets 'busy' text instead of taking the server down

**survived 2/3, with corrections · effort S · impact 4/5**

**What the code does today.** tools.py:73-75 explains the threadpool is real concurrency ('64 simultaneous first calls built 16 separate facades'); nothing bounds it below anyio's 40 (anyio/_backends/_asyncio.py:3162). df_tool.py:106 caps `limit` at 100 and :167 caps `fuzzy` at 2, but neither bounds the number of terms, the number of concurrent calls, nor the total in-flight work. Measured (scratchpad/spike_hammer.py, 200k-row table, today's search path): 32 concurrent 8-word OR searches → wall 12.9 s, median latency 10.1 s, RSS +2.3 GB; a single cheap query behind them took 281 ms vs ~10 ms idle. On a 4-CPU host the threadpool's 40 slots are 10× the cores, so admitting 40 heavy searches only buys latency and memory. The HTTP transport has no auth (second audit, finding 11), so on a hosted endpoint this is reachable by anyone.

**The gap.** Under load the server degrades for everyone at once (latency, then OOM), and a well-behaved second user pays for a badly-behaved first. There is no signal — to the model, to /ready, or to a metric — that the server is saturated rather than slow.

**The proposal.** In dataset.py add a module-level `_gate = threading.BoundedSemaphore(SEARCH_CONCURRENCY)` with `SEARCH_CONCURRENCY = max(2, os.cpu_count() or 2)` (override `KA_SEARCH_CONCURRENCY`) and `QUEUE_WAIT = 2.0 s` (`KA_SEARCH_QUEUE_WAIT_SECONDS`); wrap the query execution at dataset.py:323-343 in `if not _gate.acquire(timeout=QUEUE_WAIT): raise SearchOverloadedError(...)` (a `SearchInputError` sibling so `_answer` returns its text): `Error: the server is busy (N searches in flight); wait a few seconds and retry the same query once.` Export an observable gauge `kansallisarkisto.lancedb.inflight` and counter `errors{error.type=overloaded}`. Because the gate lives in the lib, a standalone library user and the boot probe get the same protection; because the semaphore is bounded per process, memory is bounded at SEARCH_CONCURRENCY × per-call cost (≈4 × 70 MB today, ≈4 × 1 MB with resilience-2). `/ready` stays 200 when busy — busy is not broken.

**Why it is once-in-a-lifetime.** Backpressure expressed in the tool's own medium: a sentence a model can obey ('wait, retry once') rather than a 503 it never sees. Combined with resilience-1/2 it makes the worst-case per-process cost a product of three documented constants instead of an open question.

**How we would know.** Under the 32-way hammer, wall time drops from 12.9 s to ≤4 s for admitted calls, at most SEARCH_CONCURRENCY searches run concurrently (assert with a counting monkeypatch inside the gate), RSS growth ≤ SEARCH_CONCURRENCY × single-call cost, rejected calls return the busy sentence within QUEUE_WAIT + 50 ms, and an interleaved cheap query completes in <50 ms instead of 281 ms.

**Risks.** Rejecting a valid query is new behaviour for a server whose philosophy is 'every question answered'; the default is tuned so only pathological concurrency sees it, and stdio (one client) never does. A too-small gate on a many-core host would under-use lance's tokio pool — hence cpu_count, not a fixed 4. Does not replace an upstream rate limit on a public HTTP endpoint; it is the last line, not the first.

**First step.** One PR (~40 lines + 2 tests): the semaphore and `SearchOverloadedError` in dataset.py, the `_answer` branch, a test that fills the gate with a blocked monkeypatched `to_list` from N threads and asserts the (N+1)th call returns the busy sentence in ~QUEUE_WAIT.

**Spike.** Already measured the unguarded baseline in scratchpad/spike_hammer.py; the guarded run is the same script with a `threading.BoundedSemaphore(4)` around `lancedb_fts_search` — a 5-line edit.

### Skeptics

**Resilience and failure modes (resilience)** — *not refuted*, confidence 0.80

tools.py:72-75 ('64 simultaneous first calls built 16 separate facades') confirmed; anyio CapacityLimiter(40) at _asyncio.py:3162 is the default limiter FastMCP's `call_sync_fn_in_threadpool` → anyio.to_thread.run_sync uses; df_tool.py:106 `le=MAX_LIMIT`, :167 `le=2`; no bound on term count or concurrency anywhere. Re-ran spike_hammer.py on an idle 4-CPU box: 32 concurrent 8-word OR fuzzy=0 → wall 23.8 s, median 21.3 s, max 23.7 s, RSS 293→2,229 MB; fuzzy=2 → wall 11.0 s, median 8.8 s, RSS →3,344 MB; a cheap `konung` query right after took 290 ms vs ~10 ms idle. Second audit finding 11 notes HTTP has no auth and 'each search materialises up to 10,000 rows' as a to-settle list item but proposes no gate. take_check.py: today's path x8 threads = 0.9× of serial — the threadpool buys no parallelism for this call, which is the proposal's argument.

*Corrections:* The slow hammer case is fuzzy=0 (8 OR'd common words match every row: wall 23.8 s, median 21.3 s), not fuzzy=2 (11.0 s); the proposal's 12.9 s/10.1 s figures are ~2× better than an idle re-run but the shape and RSS growth (+1.9 GB) hold. Because today's per-call cost is dominated by GIL-bound `to_pylist` (x8 parallel = 0.9× serial), a gate of cpu_count=4 bounds memory (≈4×70 MB) but barely improves latency until resilience-2 lands — order the work 2 then 4, or start the gate at 2.

*Sharpened:* A `threading.BoundedSemaphore(SEARCH_CONCURRENCY)` in dataset.py around the query execution (dataset.py:323-343) with `acquire(timeout=QUEUE_WAIT)`; on timeout raise `SearchOverloadedError(SearchInputError)` whose sentence says 'busy (N in flight); wait a few seconds and retry once', so `_answer` returns it verbatim and `/ready` stays 200. Default SEARCH_CONCURRENCY = max(2, cpu_count) and QUEUE_WAIT = 2 s, both env-overridable; export an in-flight gauge and `errors{error.type=overloaded}`. Start resilience-1's deadline clock *after* acquisition. Test: fill the gate with N blocked monkeypatched `to_list` calls and assert the (N+1)th returns the busy sentence in QUEUE_WAIT±50 ms; measurable on the 200k table: at most SEARCH_CONCURRENCY searches concurrent, RSS growth ≤ SEARCH_CONCURRENCY × single-call cost, and an interleaved cheap query <50 ms.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions), the three-corpus future and the historian user** — *refuted*, confidence 0.60

The evidence is right (anyio `CapacityLimiter(40)` at _asyncio.py:3162; tools.py:290-293; the hammer numbers) but the reason given — per-call memory and GIL serialisation — is exactly what resilience-2 removes: measured today, 8 concurrent phase-1 queries added 5 MB RSS versus 259 MB on the current path. After resilience-1 bounds time and resilience-2 bounds bytes, what remains is CPU fairness (a cheap query behind heavy ones), and nobody has measured that on the new path. A strong maintainer says 'not now, and not for that reason': rejecting a valid query with a 'busy' sentence contradicts 'every question answered' and needs post-fix evidence before it earns a place. Two design faults on top: `os.cpu_count()` ignores cgroup CPU quotas (it returned 4 here; in a quota-limited container it returns the host's count), so the default is wrong in precisely the container deployment it targets; and a process-global `threading.BoundedSemaphore` inside the lib means a standalone library user's own thread pool gets `SearchOverloadedError` from a server-shaped policy — the lib is meant to be usable on its own. Placement, if ever, is the MCP layer.

*Corrections:* Sequence it: land resilience-1 and -2, re-run spike_hammer.py (32-way, plus one interleaved cheap query) on the two-phase path, and only if the cheap query still waits >100 ms add admission control in the MCP package (a FastMCP middleware or inside `_answer`), sized from the measured per-call CPU cost rather than `os.cpu_count()`. If it is added, /ready stays 200 and the sentence is 'wait a few seconds and retry the same query once' — that part of the proposal is well written.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel SDK 1.44 / anyio 4.15 as installed; every API claim read in the venv source and the spikes re-run offline on the 18-row fixture and the 200k-row scratchpad cost_db** — *not refuted*, confidence 0.90

anyio/_backends/_asyncio.py:3162 `CapacityLimiter(40)` confirmed; fastmcp/utilities/async_utils.py:34 uses `anyio.to_thread.run_sync`; df_tool.py module docstring lines 6-10 states the sync-in-threadpool design. Ran judge_d2_gate.py on cost_db (32 threads, 8-word OR, fuzzy=0, through today's `lancedb_fts_search`, plus one cheap query started at 0.3 s): unguarded → wall 12.3 s, median 9.07 s, RSS 288→2,412 MB, cheap query 11.9 s; with `threading.BoundedSemaphore(4)` and `acquire(timeout=2.0)` → wall 2.8-3.0 s, 10-11 admitted, 21-22 rejected within the 2 s wait, RSS growth +260 MB then +55 MB. So the wall-time, RSS and 'rejected within QUEUE_WAIT' outcomes hold. Effort S (~40 lines + 2 tests) is accurate.

*Corrections:* The outcome 'an interleaved cheap query completes in <50 ms instead of 281 ms' is false: a semaphore has no priority, so the cheap query waited 2.4-2.7 s in the queue behind heavy searches (bounded by QUEUE_WAIT + runtime, vs 11.9 s unguarded) — restate as ≤ QUEUE_WAIT + 50 ms. Two-thirds of the 32-way hammer is rejected at gate 4 with today's ~1 s-per-call cost, so order resilience-2 first or expect the busy sentence routinely under moderate load. `SearchOverloadedError` as a `SearchInputError` subclass would be tagged `error.type=validation` by `_answer` (df_tool.py:44-50) — it needs its own except branch. `os.cpu_count()` ignores cgroup CPU quotas in containers; use `os.process_cpu_count()` (3.13+) or `len(os.sched_getaffinity(0))`.

*Sharpened:* Module-level `BoundedSemaphore(SEARCH_CONCURRENCY)` in dataset.py with `SEARCH_CONCURRENCY = max(2, os.process_cpu_count() or 2)` and `QUEUE_WAIT = 2.0 s` (both env-overridable), wrapping only the query execution at dataset.py:323-343; `SearchOverloadedError` with its own `_answer` branch (`error.type=overloaded`) returning 'the server is busy (N in flight); wait a few seconds and retry once'. Record a `lancedb.queue_wait` histogram and an `inflight` gauge. Outcomes to assert: ≤SEARCH_CONCURRENCY concurrent `to_list` calls (counting monkeypatch), rejected calls return in QUEUE_WAIT + 50 ms, 32-way hammer wall ≤4 s, RSS growth ≤ SEARCH_CONCURRENCY × single-call cost, and any admitted cheap query completes in ≤ QUEUE_WAIT + 50 ms. Sequence it after resilience-2 so the gate is a rarely-hit last line rather than the common path.

---

## `resilience-5` — A bulletproof harvest: pin the plan, verify every slice against its own filter, write atomic parts, and make coverage arithmetic honest

**survived 3/3, with corrections · effort XL · impact 4/5**

**What the code does today.** harvest.py:294 replans from live counts on every run, including `--resume`; :289 loads only `done` keys; :304 takes whatever `/api/export-all` returns and :309-313 appends it and marks the slice done with no check that `len(hits)` equals the planned count or that the hits satisfy the slice filter — even though the module docstring (:35-42) itself records that a wrong `uiState` 'fails silently — the filter is ignored and the export returns an unfiltered page that looks perfectly valid'. :324 defines `complete` as 'no shortfalls', :329 computes `coverage_pct` from lines written over the index total. :173-196 retries 4 times with 1+2+4+8 s backoff and treats an HTTP 429/503 (an HTTPError, subclass of URLError) like any other failure — a 1.5 h tuomiokirjat run dies after 15 s of outage. ingest.py has no de-duplication by `object_id`. Measured with a stub service (scratchpad/spike_harvest.py, ceiling scaled to 100): (1) crash between append and checkpoint, then resume → 360 lines, 300 unique (the docstring's known window, now quantified); (2) index moved between runs so one year crossed the ceiling → resumed plan fetched `c=A|y=1600` and `c=B|y=1600` on top of the done `y=1600` → `coverage_pct: 119.15`, `complete: True`, verify exit 1; (3) service ignoring filters → `coverage_pct: 166.67`, `complete: True`; (4) every export 5 short → verify exit 0, `complete: True`, `coverage_pct: 91.67` — a short file with a clean bill of health. data-sources.md documents that `voudintilit` moved 99,031 → 99,125 between harvests, so case (2) is the normal case for a resumed run, not an edge.

**The gap.** Resume is unsafe against the one thing that always happens (the index moving), the report can claim completeness it does not have, and the only defence — `--verify` on objectID uniqueness — is blind to a short or unfiltered slice. For a 7.8M-document, 1.5 h harvest that feeds the largest table, this is the pipeline's weakest link.

**The proposal.** Rewrite `harvest_index` around a pinned plan and verified parts: (a) `plan.json` written once by `plan_slices` with `{index_total, harvested_at, slices:[{key, year, collection, documents}]}`; `--resume` loads it and never replans; `--replan` diffs a fresh plan against the pinned one and fetches only new/changed cells, recording the old ones as superseded. (b) Each slice is written to `parts/<sha1(key)>.jsonl.gz` via temp file + fsync + `os.replace`; the checkpoint entry becomes `{key, documents, bytes, sha256}` and is itself written atomically. The crash window between append and checkpoint disappears: a part exists complete or not at all; `<index>.jsonl.gz` becomes a concatenation step (`--assemble`) or `_open_jsonl`/ingest learns to read `parts/`. (c) Post-conditions before a slice is marked done: `len(hits) == min(slice.documents, EXPORT_CEILING)`; every hit's `alkuvuosi` equals `slice.year` and its collection facet equals `slice.collection` when set (this is exactly the silent-unfiltered-page failure the docstring warns of); objectIDs unique within the slice. A failing slice is retried once, then recorded as `failed` with the reason, never as done. (d) `report.complete = (unique_documents == index_total and not shortfalls and not failed)`, `coverage_pct` from unique objectIDs and capped at 100, with `duplicates` and `short_slices` fields. (e) `_post`: honour `Retry-After` and treat 429/503 as backoff, 8 attempts with jitter capped at 60 s, so a 5-minute outage is survived. (f) ingest: dedupe by `object_id` in `_df_batches` (a `set` of 7.8M ids ≈ 600 MB is too much — use the plan: parts are disjoint by construction, so dedupe only across superseded parts) or `merge_insert('object_id')`. (g) `--verify` checks parts against the checkpoint's sha256 and the plan's counts, so verifying a resumed run is a hash walk, not a re-read of 6.3 GB.

**Why it is once-in-a-lifetime.** The harvest becomes reproducible and provable: a pinned plan is a citable statement of what was asked of the service on a given day, each part carries its own hash and post-condition, and 'complete' means complete. That is what a historian citing a corpus snapshot and the next corpus's week-long port both need.

**How we would know.** The four stub scenarios in spike_harvest.py, ported to tests: (1) crash-then-resume yields exactly 300 unique lines and 300 total; (2) the moved-index resume fetches only the two new cells and reports coverage ≤100 with `superseded: ['y=1600']`; (3) the filter-ignoring service produces zero done slices and `failed: 3`; (4) the short service reports `complete: false, short_slices: 3`. `--verify` on a 6.3 GB tuomiokirjat harvest completes in seconds (hash walk) rather than a full decompress.

**Risks.** A rewrite of the largest script, testable only against a stub here (no network to the service); the per-hit filter check assumes the export returns `alkuvuosi` and the collection attribute on every hit — verify on the July 2026 files before relying on it. Parts change the on-disk layout documented in data-sources.md and the Makefile's `verify-data`. Pinning the plan means a resumed run deliberately ignores documents added since the plan — correct, but it must be said in the report (`plan_age`).

**First step.** PR 1 (a day): post-conditions (c) and honest `complete`/`coverage_pct` (d) in the existing loop, plus the stub-client test harness from spike_harvest.py as `scripts/tests/test_harvest.py`. PR 2: pinned plan and atomic parts. PR 3: retry policy and ingest dedupe.

**Spike.** Already run: scratchpad/spike_harvest.py and spike_harvest4.py drive scripts/harvest.py's real `harvest_index`/`verify` against an in-memory Sisältöhaku stub with the ceiling scaled to 100 — no network. Rerun: `/tmp/uvenv/bin/uv run --project /home/user/kansallisarkisto-mcp python spike_harvest.py`.

### Skeptics

**Resilience and failure modes (resilience)** — *not refuted*, confidence 0.85

harvest.py checked line by line: :294 replans on every run including --resume; :289 loads only `done`; :304 export, :309-313 append then mark done with no count or filter check; :324 `complete = not shortfalls`; :329 coverage from lines written; :173-196 retries 4× with 2**attempt (1+2+4+8 s) catching URLError, of which HTTPError (429/503) is a subclass; ingest.py `_df_batches` has no object_id de-dup. Docstring :35-42 does warn the wrong uiState 'fails silently'. Re-ran spike_harvest.py: (1) crash-then-resume → 360 lines / 300 unique, verify exit 1; (2) index moved → resumed plan fetched `c=A|y=1600` and `c=B|y=1600` on top of done `y=1600`, coverage_pct 119.15, verify 1; (3) filters ignored → coverage 166.67, complete True. Case (4) reproduces via spike_harvest4.py: coverage 91.67, complete True, verify 0 (spike_harvest.py's stub never actually shorts a 60-doc slice — that is a stub bug, not a harvest one). data-sources.md:68-69 documents voudintilit moving 99,031→99,125. Second audit finding 11 already reports the non-atomic checkpoint.json write; nothing in the audits covers replanning-on-resume, unverified slices, or the coverage arithmetic.

*Corrections:* The non-atomic checkpoint write is already the second audit's finding 11 — cite it as covered, the proposal's (b) goes beyond it. The '1.5 h tuomiokirjat run' figure appears nowhere in data-sources.md or harvest.py; use report.json's elapsed_seconds. `_post` also sleeps the final 8 s after the last failed attempt before raising. The per-hit post-condition (c) needs `alkuvuosi` and the collection attribute on every export hit — unverifiable offline, as the proposal itself says.

*Sharpened:* PR 1 (a day, no layout change): post-conditions before a slice is marked done — `len(hits) == min(slice.documents, EXPORT_CEILING)`, every hit's `alkuvuosi == slice.year` and collection facet == slice.collection when set, objectIDs unique in the slice; a failing slice is retried once then recorded `failed`, never done; `complete = unique_documents == index_total and not shortfalls and not failed`; `coverage_pct` from unique objectIDs capped at 100 with `duplicates`/`short_slices` fields; port spike_harvest(4).py's stub into scripts/tests/test_harvest.py so the four scenarios are CI assertions. PR 2: `plan.json` pinned at first run, `--resume` never replans, `--replan` diffs and supersedes; atomic per-slice parts with sha256 and an atomic checkpoint (closing audit finding 11). PR 3: `Retry-After`-aware backoff with jitter capped at 60 s over 8 attempts; ingest dedupes only across superseded parts.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions), the three-corpus future and the historian user** — *not refuted*, confidence 0.80

Every cited line verified: harvest.py:289 loads only `done` keys; :294 replans from live counts even on `--resume`; :304-313 appends and marks done with no check of `len(hits)` against `slice.documents` nor of the filter on each hit, despite the docstring at :35-42 warning the filter can be silently ignored; :324 `complete = not shortfalls`; :329 `coverage_pct` from lines written; :182-189 four retries with 1+2+4+8 s and `URLError` (so HTTPError 429/503 too) treated as any failure. data-sources.md:69 documents voudintilit moving 99,031→99,125 between harvests, so plan drift on resume is the normal case. Re-ran spike_harvest.py and spike_harvest4.py offline: crash-then-resume → 360 lines/300 unique; drift → resumed plan fetched `c=A|y=1600`, `c=B|y=1600` on top of done `y=1600`, `coverage_pct: 119.15`; filters ignored → `complete: True, coverage_pct: 166.67`; each export 5 short → `verify: 0, complete: True, coverage_pct: 91.67`. For the 7.8M-page harvest that feeds the largest table this is the weakest link, and a pinned, hashed plan is what a historian citing a snapshot needs. XL is honest; PR 1 alone is a day and closes the worst of it.

*Corrections:* spike_harvest.py's run 4 as it stands no longer shows the short case (its stub returns all 60 of a 60-doc slice when trimming from the 100 ceiling); spike_harvest4.py is the one that reproduces `91.67 / complete: True` — port that stub. The non-atomic checkpoint write is already audit finding 11; (b) subsumes it, say so rather than re-report. The per-hit `alkuvuosi`/collection check is unverifiable offline (no network); keep it behind the July 2026 file check as the proposal says. The 7.8M-id `set` estimate (~600 MB) is about right, but if dedupe across superseded parts is ever insufficient, 8-byte hashes in a sorted numpy array (~62 MB) do the job.

*Sharpened:* PR 1 (a day): post-conditions before `done` (`len(hits) == min(slice.documents, EXPORT_CEILING)`, per-hit year/collection equality, objectID uniqueness within the slice), `failed` slices recorded not marked done, `complete = unique == index_total and not shortfalls and not failed`, `coverage_pct` from unique objectIDs capped at 100, `duplicates`/`short_slices` fields, and scripts/tests/test_harvest.py built from the stub. PR 2: `plan.json` pinned once with `index_total`, `harvested_at` and a plan sha; `--resume` never replans, `--replan` diffs and supersedes; atomic gzip parts with sha256 in the checkpoint; `report.json` carries `plan_sha`, `plan_age` and superseded keys so a corpus snapshot is citable as 'harvest of <index>, plan <sha> of <date>, N of M documents'. PR 3: `Retry-After`, 429/503 as backoff, 8 jittered attempts capped at 60 s; ingest reads `parts/` and dedupes only across superseded parts. Update data-sources.md's layout and the Makefile's `verify-data` in the same PRs.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel SDK 1.44 / anyio 4.15 as installed; every API claim read in the venv source and the spikes re-run offline on the 18-row fixture and the 200k-row scratchpad cost_db** — *not refuted*, confidence 0.85

harvest.py read: :189-195 catches `urllib.error.URLError` (HTTPError is a subclass) with `delay = 2**attempt` over `retries=4` → 1+2+4+8 = 15 s total before RuntimeError; :294 `plan_slices` runs on every call including resume; :304-313 append + `progress.done.add` with no `len(hits)` or filter check; :324 `complete: not progress.shortfalls`; :329 coverage from `documents_written/total`. Re-ran scratchpad/spike_harvest.py: case 1 → 360 lines / 300 unique, verify exit 1; case 2 → resumed plan fetched `c=A|y=1600`, `c=B|y=1600` on top of done `y=1600`, coverage_pct 119.15, verify 1; case 3 → complete True, coverage 166.67. spike_harvest4.py (the corrected stub) → verify 0, complete True, coverage 91.67, 165 of 180. `merge_insert(on)` exists at lancedb/table.py:1517. Everything proposed is plain Python (tempfile+fsync+os.replace, sha256, Retry-After header parsing) — no API risk. Effort XL and the three-PR split are honest; PR 1 (post-conditions + honest report + stub harness) is genuinely a day.

*Corrections:* spike_harvest.py's own case 4 does not short anything (slices of 60 < `max_results - 5` = 95, so it reports 100%); the 91.67 figure comes from spike_harvest4.py's fixed stub — cite that file. The fixture df_sample.jsonl contains no `alkuvuosi` key (0 occurrences; it carries `dating_start_year`), so the per-hit year/collection post-condition must be keyed per `IndexSpec` and can only be validated against the real July 2026 export files, as the proposal already flags. A `set` of 7.8M objectIDs is ~600 MB in CPython as claimed, but the pinned plan makes parts disjoint by construction, so the cheap dedupe path is the right one.

*Sharpened:* PR 1 (a day, testable offline): in `harvest_index` refuse to mark a slice done unless `len(hits) == min(slice.documents, EXPORT_CEILING)`, every hit's year/collection field (per IndexSpec) matches the slice, and objectIDs are unique within the slice; record failed slices; `complete = unique == index_total and not shortfalls and not failed`; `coverage_pct` from unique ids capped at 100 with `duplicates`/`short_slices`/`failed` fields; port spike_harvest.py + spike_harvest4.py stubs to scripts/tests/test_harvest.py asserting the four scenarios. PR 2: `plan.json` pinned once, `--resume` never replans, `--replan` diffs and supersedes; atomic `parts/<sha1(key)>.jsonl.gz` via tempfile+fsync+os.replace with sha256 in the checkpoint; `--verify` becomes a hash walk. PR 3: `_post` honours Retry-After, treats 429/503 as backoff, 8 jittered attempts capped at 60 s; ingest reads parts and dedupes only across superseded ones. Update data-sources.md and the Makefile `verify-data` target for the parts layout.

---

## `resilience-6` — Prove the swap: pin lance's atomic commit under disk-full, ship a blue/green table swap, and stop unbounded version retention on re-ingest

**survived 3/3, with corrections · effort S · impact 3/5**

**What the code does today.** ingest.py:144 uses `create_table(..., mode='overwrite')` into the live database; scripts/ingest_df.py:36-41 writes straight into the resolved URI (the served `data/` or `/data`) and never calls `cleanup_old_versions` or `compact_files`. deployment.md:70-72 says only 'indexes are baked in at ingest time and the served copy is read-only' — nothing about how a served table is replaced. Measured (scratchpad/spike_diskfull.py, spike_corrupt.py): a re-ingest interrupted by a 6 KB `RLIMIT_FSIZE` (`LanceError(IO): File too large (os error 27)`) left the table at version 7 with all six indexes and `search OK total=7` — lance's manifest commit is atomic, so a half-written ingest never breaks the served version (a property the project relies on and nowhere states or pins); three consecutive overwrites of the 18-row fixture retained 28 versions and all old data files (0.40 MB for one 0.13 MB table) — for tuomiokirjat (~19 GB of text) every re-ingest into a live directory adds another full copy until the disk is full, and a full disk turns the next commit into the failure just measured; a reader looping on a cached connection saw 0 errors across 3 overwrites (old versions keep serving); and renaming a freshly built directory over the old one under a cached connection served the new table immediately (`open_table` per call re-reads the manifest).

**The gap.** The safe deployment sequence exists by accident and is undocumented, and the unsafe one (`make ingest-df` into the served directory, repeatedly) grows disk without bound. Nothing tests either.

**The proposal.** Two tests in test_ingest.py: `test_interrupted_ingest_leaves_the_previous_version_serving` (subprocess with `resource.setrlimit(RLIMIT_FSIZE, 6000)` — the exact recipe from the spike, ~1 s) and `test_directory_swap_by_rename_is_seen_by_a_cached_connection`. In `ingest_df`, after the indexes: `table.cleanup_old_versions(older_than=timedelta(0), delete_unverified=True)` guarded by a new `retain_versions: bool = False` parameter — default off for a *fresh* directory (nothing to clean), documented as unsafe to run against a directory a server is currently serving. Make `scripts/ingest_df.py` build into `<uri>.new`, run the boot-style probe against it, then `os.replace` it over `<uri>` (blue/green), refusing to build into a directory that already holds a table unless `--in-place`. Document in deployment.md: mount the *parent* directory so the swap is a rename inside the mount, and state the disk arithmetic (2× the table during a swap, never 3×).

**Why it is once-in-a-lifetime.** The one operational sequence every archive operator performs — replace the corpus under a running server — becomes a tested, one-line, atomic operation with a stated disk budget, instead of folklore about what lance happens to tolerate.

**How we would know.** Both tests pass in CI in <3 s; three consecutive `make ingest-df` runs leave exactly one table version and on-disk size within 5% of a single fresh ingest (today 28 versions / ~3×); a server serving during the swap logs zero errors and answers the smoke test's `konung` query before and after; `deployment.md` gains the swap section and the second audit's stale `"table"` key example is corrected on the way.

**Risks.** `cleanup_old_versions` against a directory another process is reading can delete a version that reader is on — hence default off and blue/green by rename as the recommended path. `os.replace` of a directory onto a non-empty directory fails on POSIX; the script must rename old → `.old`, new → live, then delete `.old`, and say so. The `KA_LANCEDB_URI=/data` bind-mount story in the Dockerfile (line 72) needs the parent-directory note or the rename crosses a mount boundary and fails.

**First step.** PR: the two tests (they document the properties even before any code changes) and the `cleanup_old_versions` call with the guard; the blue/green script change in a follow-up.

**Spike.** Already run: scratchpad/spike_diskfull.py (RLIMIT_FSIZE 6 KB → `File too large (os error 27)`, version 7 still serving) and spike_corrupt.py sections 7 and 9 (0 reader errors across 3 overwrites, 28 versions retained; rename swap served immediately).

### Skeptics

**Resilience and failure modes (resilience)** — *not refuted*, confidence 0.85

ingest.py:144 `mode='overwrite'` and scripts/ingest_df.py:36-41 (mkdir + connect + ingest into the resolved URI, no cleanup) confirmed; deployment.md:70-72 says only 'indexes are baked in… served copy is read-only'. Re-ran spike_diskfull.py: with RLIMIT_FSIZE 6,000 the second ingest failed `LanceError(IO): File too large (os error 27)` and the table stayed at version 7 with indices [BTree, Bitmap, Bitmap, FTS, BTree, BTree], `search OK total=7`, 28 files on disk. spike_corrupt.py sections 7 and 9: 18 reader searches during three overwrites with 0 errors; 28 versions and 0.40 MB after three overwrites of a ~0.13 MB table; rename-swap served immediately under a cached connection (because dataset.py:290 does open_table per call). lancedb 0.38 table.py:4148 `cleanup_old_versions(older_than=None, *, delete_unverified=False)` exists and delegates to `to_lance()`. Dockerfile line 72 `KA_LANCEDB_URI=/data` confirmed. Neither audit mentions version retention or a swap procedure.

*Corrections:* Spike section 9 does `shutil.rmtree(root)` then `os.rename(new, root)` — there is a window with no table at all, so what was tested is 'a cached connection sees a replaced directory', not an atomic swap; the test must use the old→`.old`, new→live sequence the risks describe. `compact_files` is deprecated in 0.38 (table.py:4183) in favour of `optimize()`; use `cleanup_old_versions(older_than=timedelta(0), delete_unverified=True)` or `optimize(cleanup_older_than=…)`. 'Exactly one table version' is not the right assertion — each ingest commits 7 versions (create + 6 index builds); assert on-disk size within 5% of a fresh ingest instead.

*Sharpened:* Pin two properties as tests: `test_interrupted_ingest_leaves_the_previous_version_serving` (subprocess with RLIMIT_FSIZE=6000, then assert version, six indexes and a successful search) and `test_directory_swap_is_seen_by_a_cached_connection` using the real old→.old, new→live, rm .old sequence. Add `cleanup_old_versions(older_than=timedelta(0), delete_unverified=True)` at the end of `ingest_df` behind `retain_versions=False`, documented as unsafe against a directory another process is serving. Make scripts/ingest_df.py build into `<uri>.new`, run the boot-style one-row probe, then swap, refusing an existing table without `--in-place`. deployment.md: mount the parent directory so the swap is a rename inside one filesystem; disk budget 2× during a swap; fix the stale `"table"` key on the way (second audit finding 9).

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions), the three-corpus future and the historian user** — *not refuted*, confidence 0.85

ingest.py:144 uses `mode='overwrite'` and scripts/ingest_df.py:188-193 writes straight into the resolved URI; deployment.md's 'Building the data' section says nothing about replacing a served table. Reproduced the retention claim: four `ingest_df` runs on the fixture → 7/14/21/28 versions and 0.10→0.40 MB (7 versions per ingest = create_table + 6 indexes), i.e. every re-ingest in place adds a full copy — for a 19 GB tuomiokirjat table that is the disk-full failure spike_diskfull.py then reproduces. Spike outputs re-read: RLIMIT_FSIZE interruption left the previous version serving with all indexes (lance manifest commit is atomic), and a rename swap under a cached connection served immediately because dataset.py:290 / search_operations.py:129 `open_table` per call. Two short tests that pin properties the deployment already depends on, plus a one-line cleanup and a documented swap, is exactly 'operator runs it without reading source'. Dockerfile:72 `KA_LANCEDB_URI=/data` confirmed.

*Corrections:* `Table.cleanup_old_versions` is deprecated since lancedb 0.21 and in this venv raises `ImportError: The lance library is required … pip install pylance` (table.py:2602-2604) — pylance is not a dependency of lancedb 0.38. Use `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` (table.py:2046): measured 28 versions/0.40 MB → 1 version/0.09 MB, 6 indices intact, search still answers 7. The swap unit is the table directory `<uri>/df.lance`, not `<uri>`: build into `<uri>/.staging/df.lance` on the same filesystem, rename live → `df.lance.old`, staging → `df.lance`, delete old — so the `/data` bind mount works unchanged and no 'mount the parent directory' note is needed. `os.replace` onto a non-empty directory failing on POSIX is correct and is why the two-step rename is required.

*Sharpened:* Tests in test_ingest.py: `test_interrupted_ingest_leaves_the_previous_version_serving` (subprocess + RLIMIT_FSIZE 6000) and `test_table_directory_swap_by_rename_is_seen_by_a_cached_connection`, both <3 s. `ingest_df` gains `prune_history: bool = True` that calls `optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` after the indexes, documented as safe only for a directory nobody is serving. `scripts/ingest_df.py` builds into `<uri>/.staging`, runs the boot-style probe against it, then does the two-step rename; refuses to build over an existing `df.lance` unless `--in-place`. deployment.md gains 'Replacing the corpus under a running server' with the disk arithmetic (2× during swap; 1× after; never 3×) and fixes the stale `"table"` key example from audit finding 9 on the way.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel SDK 1.44 / anyio 4.15 as installed; every API claim read in the venv source and the spikes re-run offline on the 18-row fixture and the 200k-row scratchpad cost_db** — *not refuted*, confidence 0.90

Re-ran scratchpad/spike_diskfull.py: second ingest under RLIMIT_FSIZE=6000 failed with `LanceError(IO): File too large (os error 27)`; the table stayed at version 7 with all six indices, search OK total=7 — lance's manifest commit is atomic as claimed. corrupt_out.txt:172-174 and :220: 18 reader searches / 0 errors across 3 overwrites, 28 versions retained, rename swap served immediately under a cached connection. ingest.py:144 `mode='overwrite'` and scripts/ingest_df.py:36-41 (writes into the resolved URI, no cleanup) confirmed; Dockerfile:72 `KA_LANCEDB_URI=/data` confirmed. Ran judge_d2_cleanup.py and judge_d2_optimize.py on the fixture: each ingest adds 7 versions (3 ingests = 21 versions, 0.10→0.30 MB); `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)` (lancedb/table.py:4198) brought it to 1 version / 0.09 MB with all 6 indices intact and search working. Effort S for the two tests + cleanup guard is right; the blue/green script with .old/.new rename dance is S-M.

*Corrections:* `cleanup_old_versions` is the wrong call for this stack: in lancedb 0.38 it is deprecated since 0.21 and the sync implementation (table.py:4174) does `self.to_lance()`, which imports `pylance` — not installed in this venv — so it raises `ImportError: The lance library is required` (measured). Use `table.optimize(cleanup_older_than=timedelta(0), delete_unverified=True)`; note the sync `optimize` returns None, not stats, and it also compacts fragments (fine for a fresh, read-only served copy). The '28 versions after three overwrites' figure is four ingests (7 versions each); three ingests give 21.

*Sharpened:* Tests in test_ingest.py: `test_interrupted_ingest_leaves_the_previous_version_serving` (subprocess + RLIMIT_FSIZE 6000, ~1 s) and `test_directory_swap_by_rename_is_seen_by_a_cached_connection`, plus `test_ingest_into_fresh_directory_leaves_one_version` asserting `len(list_versions()) == 1` after `optimize(cleanup_older_than=timedelta(0), delete_unverified=True)`. In `ingest_df` add `prune: bool = True` calling `optimize(...)` after the indexes, documented as unsafe against a directory another process is serving. `scripts/ingest_df.py`: build into `<uri>.new`, run the boot-style probe, then rename live → `.old`, `.new` → live, delete `.old` (os.replace cannot overwrite a non-empty directory), refusing an existing table without `--in-place`. deployment.md: mount the parent directory so the rename stays inside one filesystem, and the disk budget is 2× during a swap. Outcome: three consecutive `make ingest-df` runs leave exactly 1 version and on-disk size within 5% of a fresh ingest (today ~3×), and the serving server logs zero errors across the swap.

---

## `resilience-7` — Keep the telemetry stack's own failures out of the application's error signal

**survived 3/3, with corrections · effort S · impact 3/5**

**What the code does today.** telemetry.py:116-119 attaches `_MetricsLoggingHandler` to the root logger at WARNING, counting every WARNING+ record into `kansallisarkisto_mcp.log.messages{log.level}` — the metric observability.md:94 describes as the error-rate panel; telemetry.py:140 attaches the OTLP `LoggingHandler` at NOTSET to the same root logger. Measured (scratchpad/spike_otel_loop.py) with `KA_MCP_OTEL_ENABLED=true` and the endpoint pointed at a refusing port: search latency was unaffected (median 6.6 → 7.1 ms, so the audit's bounded-shutdown fix and the batch processors hold), but in 12 s of a perfectly healthy server the `opentelemetry.exporter.otlp.proto.grpc.exporter` logger emitted 6 ERROR and 5 WARNING records versus 0 from the application — every one counted into `log.messages{ERROR}` and every one handed back to the failing `LoggingHandler` for export, which fails and logs again. `BatchSpanProcessor` silently drops beyond its 2,048-span queue (observed `_max_queue_size: 2048`, queue at 352 after 12 s) and nothing surfaces the drop.

**The gap.** With the collector down, the error-rate panel shows ~0.5 errors/s from a healthy server, indistinguishable from real faults, and the log bridge amplifies its own failure. The one time an operator most needs the error metric to be trustworthy — infrastructure trouble — it is not.

**The proposal.** Add a `logging.Filter` that drops records whose `name` starts with `opentelemetry.` and install it on both handlers (`_MetricsLoggingHandler` and `LoggingHandler`); route those records instead to a `kansallisarkisto_mcp.telemetry.export_failures{signal=traces|metrics|logs}` counter via a tiny dedicated handler on the `opentelemetry.exporter` logger, rate-limited to one stderr line per minute per signal. Set `OTEL_EXPORTER_OTLP_TIMEOUT` default to 5 s in `init_telemetry` if unset (the SDK default is 10 s and retries stack). Document in observability.md a 'collector down' row: what you will and will not see (spans dropped past 2,048, metrics delayed, application metrics intact).

**Why it is once-in-a-lifetime.** The observability layer is already unusual in marking text-returned errors on spans; making it immune to its own outage completes the promise that the error metric means application errors and nothing else.

**How we would know.** Re-running spike_otel_loop.py after the change: 0 WARNING+ records reach `_MetricsLoggingHandler` from `opentelemetry.*` in 12 s (today 11); `telemetry.export_failures` increments instead; stderr carries ≤1 exporter line per minute per signal (today ~1 every 1–2 s); a test with the endpoint at 127.0.0.1:1 asserts `log.messages{ERROR}` stays 0 across 50 searches.

**Risks.** Filtering a logger by name prefix hides genuine SDK misconfiguration from the log stream; the rate-limited line and the counter are the replacement, and the filter must not touch the stderr `basicConfig` handler so a laptop user still sees the first failure. Pure MCP-package change; the lib's API-only stance is untouched.

**First step.** One PR: the filter class, two `addFilter` calls, the counter handler, one test using the in-memory metric reader pattern from test_telemetry.py.

**Spike.** Already run: scratchpad/spike_otel_loop.py — counts log records by (logger, level) over 12 s with a refusing collector (11 from opentelemetry.*, 0 from the app; latency unchanged). Offline, ~15 s.

### Skeptics

**Resilience and failure modes (resilience)** — *not refuted*, confidence 0.85

telemetry.py:116-119 attaches `_MetricsLoggingHandler` at WARNING to the root logger and :140 attaches OTel `LoggingHandler(level=NOTSET)` to the same root — confirmed. Re-ran spike_otel_loop.py with the endpoint at 127.0.0.1:1: root handlers [NullHandler, H, _MetricsLoggingHandler, LoggingHandler]; in 12 s of a healthy server: ('opentelemetry','WARNING') 5, ('opentelemetry','ERROR') 5, application WARNING+ = 0. opentelemetry-sdk 1.44.0 `_logs/_internal/__init__.py:664` LoggingHandler.emit has no logger-name guard, so exporter failures do re-enter the failing bridge. grpc/exporter.py:354 default timeout 10 s confirmed. `_shared_internal/__init__.py:198-207` shows the 2,048 queue and the drop path. Not covered by the audits (finding 13 is the shutdown hang only).

*Corrections:* observability.md does not mention `kansallisarkisto_mcp.log.messages` at all — the 'error-rate panel' phrase is telemetry.py:94's comment, so the doc row the proposal wants is currently missing entirely, not at :94. 'Nothing surfaces the drop' is wrong: `_shared_internal/__init__.py:205` logs 'Queue full, dropping spans' at WARNING — which is itself another `opentelemetry.*` record counted into log.messages{WARNING} and re-fed to the failing bridge, so it strengthens rather than weakens the case. Counted 5 ERROR + 5 WARNING, not 6 + 5.

*Sharpened:* Install a `logging.Filter` rejecting `record.name.startswith('opentelemetry.')` on both root-attached handlers so `log.messages{level}` counts application records only; attach a small handler on the `opentelemetry.exporter` and `opentelemetry.sdk` loggers that increments `kansallisarkisto_mcp.telemetry.export_failures{signal}` (derive signal from the logger/message: spans, metrics, logs) and emits at most one stderr line per minute per signal, leaving the basicConfig stderr handler untouched so a laptop user sees the first failure. Default `OTEL_EXPORTER_OTLP_TIMEOUT` to 5 s if unset. Add the missing `log.messages` row and a 'collector down' row to observability.md (spans dropped past 2,048 with the SDK's own 'Queue full' warning now routed to the counter; metrics delayed; application metrics intact). Test with the in-memory metric reader: 50 searches with the endpoint at 127.0.0.1:1 leave log.messages{ERROR} at 0 and export_failures > 0.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions), the three-corpus future and the historian user** — *not refuted*, confidence 0.75

telemetry.py:354-357 attaches `_MetricsLoggingHandler` at WARNING to the root logger and :378 attaches the SDK `LoggingHandler` at NOTSET to the same root logger; the OTLP gRPC exporter logs failures at ERROR/WARNING (opentelemetry/exporter/otlp/proto/grpc/exporter.py:513, 526, 536) and defaults `OTEL_EXPORTER_OTLP_TIMEOUT` to 10 (exporter.py:354). The SDK `LoggingHandler.emit` (sdk/_logs/_internal/__init__.py:666-671) has no name-based recursion guard, so exporter failures are re-fed to the failing log exporter. Re-ran scratchpad/spike_otel_loop.py with the endpoint at 127.0.0.1:1: 12 s of a healthy server produced 2 ERROR records from `opentelemetry.*` and 0 WARNING+ from the application, both counted into `log.messages{ERROR}` — the error panel observability.md describes lies exactly when the collector is down. Small, MCP-package only, no lib change, and it completes a promise the project already makes.

*Corrections:* Magnitude is lower than claimed on this run: 2 exporter ERRORs in 12 s (not 6 ERROR + 5 WARNING), so '~0.5 errors/s' is an upper bound that depends on retry timing; the point (nonzero from a healthy server, amplified through the bridge) stands. Also note the SDK now marks its own `LoggingHandler` deprecated in favour of `opentelemetry-instrumentation-logging` (sdk/_logs/_internal/__init__.py:581-582); the PR should decide whether the log bridge stays on the SDK class or moves, rather than filter a deprecated handler.

*Sharpened:* A `logging.Filter` dropping `name.startswith('opentelemetry.')` on the two OTel handlers only (never on the stderr basicConfig handler), a `kansallisarkisto_mcp.telemetry.export_failures{signal}` counter fed by a tiny handler on the `opentelemetry.exporter` logger with a one-line-per-minute-per-signal stderr rate limit, `OTEL_EXPORTER_OTLP_TIMEOUT` defaulted to 5 s in `init_telemetry` if unset, and a test using test_telemetry.py's in-memory reader that asserts `log.messages{ERROR}` stays 0 across 50 searches with the endpoint at 127.0.0.1:1. Add the 'collector down' row to observability.md: spans drop past the 2,048 queue, metrics delay, application metrics intact.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel SDK 1.44 / anyio 4.15 as installed; every API claim read in the venv source and the spikes re-run offline on the 18-row fixture and the 200k-row scratchpad cost_db** — *not refuted*, confidence 0.80

telemetry.py:116-119 `_MetricsLoggingHandler` at WARNING on the root logger and :140 `LoggingHandler(level=NOTSET)` on the root logger confirmed; observability.md:90-98 documents the error-rate panel. Installed OTel SDK/exporter 1.44.0: the gRPC exporter logs via `getLogger('opentelemetry.exporter.otlp.proto.grpc.exporter')` (exporter.py:122) and defaults `OTEL_EXPORTER_OTLP_TIMEOUT` to 10 (exporter.py:354); `LoggingHandler.emit` (sdk/_logs/_internal/__init__.py:665-672) has no logger-name exclusion, so exporter failure records do re-enter the log bridge as claimed. `BatchSpanProcessor` queue size defaults via `OTEL_BSP_MAX_QUEUE_SIZE` (2048). Re-ran scratchpad/spike_otel_loop.py twice: opentelemetry.* ERROR records in 12 s = 1 and 4, application WARNING+ = 0. The fix is a `logging.Filter` on two handlers plus a counter — trivially feasible; effort S is right.

*Corrections:* The magnitude is unstable and overstated: two re-runs gave 1 and 4 ERROR and 0 WARNING records in 12 s (proposal: 6 ERROR + 5 WARNING, '~0.5 errors/s'); with SDK 1.44 the retry loop is bounded by the export timeout, so it is roughly one ERROR per failed batch export, not a stream. The gap still stands — any nonzero count from a healthy server corrupts the panel — but the 'today 11' baseline in the measurable outcome should read 'nonzero'. A 12 s wall-clock test is flaky; force the export instead.

*Sharpened:* Add `_NotTelemetryLogs(logging.Filter)` dropping `record.name.startswith('opentelemetry.')` and install it on `_MetricsLoggingHandler` and the OTLP `LoggingHandler` (not on the stderr basicConfig handler); attach a dedicated handler to the `opentelemetry.exporter` logger that increments `kansallisarkisto_mcp.telemetry.export_failures{signal}` (signal inferred from the logger name suffix) and prints at most one stderr line per minute per signal; default `OTEL_EXPORTER_OTLP_TIMEOUT` to 5 s in `init_telemetry` when unset. Test with the in-memory metric reader from test_telemetry.py: point the endpoint at 127.0.0.1:1, run 50 searches, call `force_flush` on the tracer provider to trigger the failed export deterministically, assert `log.messages{ERROR}` == 0 and `telemetry.export_failures{signal=traces}` ≥ 1. Add the 'collector down' row to observability.md (spans dropped past 2,048 queued, metrics delayed, application metrics intact).

---

## `resilience-8` — Fault injection as a test discipline: a `faults` fixture family that builds each failure from the 18-row fixture and asserts the documented behaviour at all three surfaces

**survived 3/3, with corrections · effort M · impact 4/5**

**What the code does today.** The suite fakes failures by monkeypatching a method to raise (`test_tools.py:92-104` raises a RuntimeError with a fake lance path; `test_server.py:88-104` substitutes an `Unreadable` class). No test builds a genuinely broken table, a stalled query, a full disk, a refusing collector or a disconnecting stdio client — yet every one is reproducible offline in milliseconds to seconds, as the spikes under the scratchpad show (truncate `_indices/*/*` to 0 bytes; delete `_indices/`, `data/` or `_versions/`; truncate a data fragment to half; `RLIMIT_FSIZE` in a subprocess; endpoint `127.0.0.1:1`; a stdio subprocess whose stdin is closed mid-call). The behaviour under each is currently whatever falls out of `_answer`'s `except Exception` (df_tool.py:51) — the same sentence for all — and nothing pins that /ready, the tool text and the `error.type` label agree for a given fault.

**The gap.** Resilience claims in this repository are prose (deployment.md's ownership paragraph, tools.py's 'never raises' docstrings) verified by hand once; the retrieval-quality suite showed what pinning a class of failure buys, and there is no equivalent for operational failure.

**The proposal.** Add `packages/kansallisarkisto-lib/tests/faults.py` (a conftest plugin) with fixtures parametrised by kind: `broken_table(kind)` for `index-truncated | index-missing | data-truncated | data-missing | versions-missing`, built by ingesting df_sample into tmp_path and applying the recipe; `stalled_query(seconds)` monkeypatching `lancedb.query.LanceQueryBuilder.to_arrow` to sleep before delegating (the slow-filesystem shim, honouring a passed `timeout` so deadline tests are real); `disk_full` running a callable in a subprocess under `RLIMIT_FSIZE`; `refusing_collector` setting the OTLP env to a closed port; `stdio_server(tool)` returning a subprocess with a JSON-RPC helper for the disconnect case. Then one parametrised test per surface: `test_tool_text_per_fault` (exact sentence prefix from resilience-3), `test_ready_per_fault` (status code + class), `test_error_type_per_fault` (span attribute via the in-memory exporter already used in test_telemetry.py), plus `test_deadline_returns_text_not_protocol_error`, `test_gate_rejects_the_nth_call`, `test_interrupted_ingest_keeps_serving`, `test_stdio_disconnect_exits_within_deadline`. Generate observability.md's failure matrix from the parametrisation ids so the table cannot drift from the tests (the same trick the second audit's finding 6 used for the fixture count).

**Why it is once-in-a-lifetime.** It makes every sentence in the deployment and observability pages an executed assertion built from real broken bytes, on the same 18-row fixture, in seconds — the operational counterpart to test_retrieval_quality.py, and the thing that lets a maintainer upgrade lancedb and know within one CI run whether an error string or a crash-safety property changed.

**How we would know.** ≥12 new tests, each <2 s, all offline; CI fails if any of the five lance error strings changes wording; the failure matrix in observability.md is generated, with a test asserting the page matches; coverage of df_tool.py's `_answer` branches and tools.py's readiness paths reaches 100% by fault kind rather than by mock.

**Risks.** Substring-based classification and byte-level corruption recipes are coupled to lance's on-disk layout (`_indices/<uuid>/tokens.lance`, `data/*.lance`, `_versions/*.manifest`); a layout change breaks the fixtures loudly, which is the intent, but someone must then re-derive the recipes. The stdio subprocess test adds ~6 s unless the sleep is shortened to 1 s. Running as root in some CI containers makes the mode-0600 permission case unreproducible (the spike hit this: euid 0 ignores mode bits) — that one stays a documented manual check or uses a uid-switching subprocess.

**First step.** PR: `broken_table` fixture with the five recipes and a single parametrised test asserting today's behaviour verbatim (the exact `RuntimeError`/`ValueError` messages from spike_corrupt.py) — a characterisation test that resilience-3 then updates to the new sentences.

**Spike.** Already run: scratchpad/spike_corrupt.py, spike_diskfull.py, spike_otel_loop.py, spike_stdio_disconnect.py (with stdio_slow_server.py) are the fixtures in prototype form; each ran offline in 1–15 s against the 18-row fixture.

### Skeptics

**Meticulous code reviewer — premise-checking every cited file, line, library call and spike against the repository and by re-running the spikes and calling ra-mcp** — *not refuted*, confidence 0.80

Checked test_tools.py:92-104 (monkeypatched RuntimeError with a fake lance path) and test_server.py:88-104 (Unreadable class) — both as described. grep for rmtree/truncate/RLIMIT/127.0.0.1:1/stdin.close across packages/*/tests finds nothing, so no test builds a genuinely broken table. df_tool.py:51 is the broad except. Re-ran the prototypes: spike_corrupt.py (corrupt_out.txt) yields RuntimeError 'Not found: …/_indices/<uuid>/tokens.lance' for BOTH index-truncated and index-missing, RuntimeError 'failed to fill whole buffer' for data-truncated, RuntimeError 'Not found: …/data/<frag>.lance' for data-missing, and ValueError "Table 'df' was not found" for versions-missing; table_names() still lists 'df' in the data-missing and versions-missing cases; readiness() stays (True,'df') after _indices is deleted (already audit-2 finding 1). spike_diskfull.py ran in 3.0 s (second ingest fails with 'File too large (os error 27)', old table v7 still searchable, 7 hits). spike_stdio_disconnect.py ran in 7.3 s (server exited rc=0 5.8 s after stdin closed — i.e. when the 6 s tool body finished). spike_otel_loop.py ran in 13.8 s (4 opentelemetry ERROR records in 12 s, 0 application warnings). lancedb.query.LanceQueryBuilder.to_arrow(self, *, timeout: Optional[timedelta]) exists and to_list() delegates to it (query.py:1146), so the stalled_query shim is feasible. test_telemetry.py:164-175 already pins error.type == 'validation' for one error-string path; test_routes.py pins /ready 503 for a missing table only.

*Corrections:* (1) 'the same sentence for all' is not quite right: _answer yields 'internal RuntimeError' for four recipes and 'internal ValueError' for versions-missing — two sentences, not one; and index-truncated and index-missing produce the identical lance message, so there are four distinct lance strings, not five. (2) 'the same trick the second audit's finding 6 used for the fixture count' — that is the FIRST audit's finding 6 (audit.md ≈ line 499, '18 charters, seven places say 16'); the second audit's finding 6 is the int32 ingest abort. (3) observability.md has no failure matrix today (only the error.type sentence at lines 94-97), so it would be created, not 'generated instead of hand-maintained'. (4) test_ready_per_fault cannot work as written until audit-2 finding 1 (cached facade) is fixed or the fixture injects the fault before the first readiness() call. (5) The stdio spike demonstrates exit-after-tool-completion, not a deadline; 'exits within deadline' needs the sleep to exceed the deadline to be a real assertion.

*Sharpened:* Add packages/kansallisarkisto-lib/tests/faults.py with broken_table(kind) for five recipes whose expected surfaces are already known from corrupt_out.txt: index-truncated and index-missing → RuntimeError 'Not found … tokens.lance'; data-truncated → RuntimeError 'failed to fill whole buffer'; data-missing → RuntimeError 'Not found … data/*.lance' while table_names() still says ['df']; versions-missing → ValueError "Table 'df' was not found" while table_names() still says ['df']. One parametrised characterisation test pins (tool text prefix, /ready status after facade reset, error.type) per kind; disk_full (RLIMIT_FSIZE subprocess, 3 s) pins 'old version still serves 7 hits'; refusing_collector (endpoint 127.0.0.1:1) pins '≤4 opentelemetry ERROR records per 12 s and zero application warnings'; stdio_server pins exit rc=0 within tool-duration+1 s after stdin closes. Land it after audit-2 finding 1 so readiness actually re-probes, and write the observability.md failure matrix from the parametrisation ids with a test that diffs the page against them.

**Maintainer of kansallisarkisto-mcp: value and fit against the stated philosophy (boots without data; text not exceptions; lib without MCP/SDK deps; stdio costs nothing; measured decisions; supply-chain posture), the three-corpus future and the historian user** — *not refuted*, confidence 0.75

Evidence checked. test_tools.py:92-104 does fake the failure with a monkeypatched RuntimeError and test_server.py:88-104 with an `Unreadable` class — confirmed. df_tool.py:51 `except Exception` collapses every fault into the one format_error sentence (formatter.py:124) — confirmed. I re-read scratchpad/spike_corrupt.py and its output corrupt_out.txt: the five recipes are real and offline: index-truncated → RuntimeError 'Not found: …/_indices/<uuid>/tokens.lance'; index-missing → same class and same 'tokens.lance' text; data-truncated → RuntimeError 'failed to fill whole buffer'; data-missing → RuntimeError 'Not found: …/data/<hash>.lance' while table_names still lists 'df'; versions-missing → ValueError "Table 'df' was not found" while table_names STILL lists 'df' (so tools.py:90 readiness says ready and the tool answers 'internal ValueError' — an actual readiness/tool disagreement nobody pins today). Spike step 8 also shows readiness() (tools.py:96-111) stays True after _indices is deleted post-boot because the DfSearch facade is cached; step 7 shows 18 searches during three overwrite-ingests with 0 errors and step 9 shows a rename-swap under a cached connection serving the new table. Those two positive properties are the operator's upgrade recipe and are asserted nowhere. Deployment.md has no paragraph on overwrite/swap (grep for overwrite|swap|rsync|rename returns nothing), and observability.md has no failure matrix today (grep 'matrix' empty) — the proposal 'generates' a table that does not yet exist. The 'same trick as audit finding 6' is inaccurate: audit.md:498-505 says the count was fixed by hand from `wc -l`, and no test in packages/*/tests reads any .md file. I confirmed lancedb 0.38.0's LanceFtsQueryBuilder overrides to_arrow (`LanceQueryBuilder.to_arrow is LanceFtsQueryBuilder.to_arrow` → False), and that dataset.py:326 calls to_list() with no timeout. stdio.out holds a cold-start measurement, not the disconnect result; spike_diskfull's output is not in the scratchpad (only full_db/df.lance remains), so those two fixtures are prototypes, not measured.

*Corrections:* (1) `stalled_query` must patch `lancedb.query.LanceFtsQueryBuilder.to_arrow` (or `to_list`), not `LanceQueryBuilder.to_arrow` — the FTS builder overrides it and the patch as written would never fire. (2) The 'exact RuntimeError/ValueError messages' contain absolute paths and index UUIDs; a characterisation test must assert on a normalised form (exception class + a path-free stem such as 'Not found' / 'failed to fill whole buffer'). (3) Two of the five kinds (index-truncated, index-missing) are indistinguishable at every surface today — both are RuntimeError 'Not found …tokens.lance'; count them as one class unless resilience-3 separates them. (4) observability.md has no failure matrix to generate from, and no existing test asserts docs content — the doc/test coupling is new machinery, not a repeat of finding 6. (5) Without resilience-3, `test_tool_text_per_fault` asserts the same sentence for four kinds; the ≥12-test count only holds once that proposal lands. (6) The disk-full and stdio-disconnect spikes have no recorded results in the scratchpad; treat them as untested prototypes.

*Sharpened:* Add a `broken_table(kind)` fixture family in packages/kansallisarkisto-lib/tests (ingest df_sample into tmp_path, then apply one of four recipes: fts-index-gone, data-truncated, data-gone, versions-gone) and a `live_table` fixture for the two positive recipes (overwrite-ingest while reading; rename-swap under a cached connection). Pin, per kind, the three surfaces that must agree: (a) DfSearch.search raises a *lancedb* class (RuntimeError/ValueError) whose message is never returned — the tool text starts with 'Error: the search failed with an internal <Class>'; (b) tools.readiness() — which today reports (True,'df') for versions-gone and for any post-boot index loss because get_search() checks table_names, not a query — and make the test *fail* on that disagreement so the fix (a one-row probe in readiness, or a cached-facade invalidation) is forced; (c) the `error.type` attribute on the ops span via test_telemetry's in-memory exporter. Add the two positive properties as the operator contract: a reader never errors during an overwrite-ingest (measured 18/18) and a rename-swap is served without restart. Put `lancedb.__version__` into the parametrisation id so a pin bump that changes an error class shows as a one-line diff. Keep disk-full, refusing-collector and stdio-disconnect out of the first PR until their spikes have recorded results; the `stalled_query` shim patches LanceFtsQueryBuilder.to_list and only earns its place once dataset.py passes a timeout.

**Feasibility and honesty of effort — LanceDB 0.38 / PyArrow 24 / FastMCP 3.4.4 / OTel / Dagger+GHA engineer** — *not refuted*, confidence 0.85

Re-ran the prototype fixtures. scratchpad/spike_corrupt.py output (corrupt_out.txt) gives the real signatures on lancedb 0.38/lance-io 11.0.0: index files truncated to 0 bytes and _indices deleted BOTH raise RuntimeError 'lance error: Not found: .../_indices/<uuid>/tokens.lance'; data fragment truncated -> RuntimeError 'LanceError(IO): Generic LocalFileSystem error: failed to fill whole buffer'; data/ deleted -> RuntimeError 'Not found: .../data/<hash>.lance' (table_names still lists df); _versions deleted -> ValueError "Table 'df' was not found" (table_names still lists df). spike_diskfull.py re-run: RLIMIT_FSIZE 6000 -> second ingest fails RuntimeError 'File too large (os error 27)', the prior table survives (version 7, 18 rows, 6 indices, search OK). spike_stdio_disconnect.py re-run: server exits rc=0 5.8 s after stdin closed, the 6 s query ran to completion on the AnyIO worker thread. All offline, 1-15 s each, so the fixture family is buildable and the test times claimed are real. tools.py:439-446 claims readiness and tools 'agree by construction', but the spike shows readiness() stays (True,'df') after _indices is deleted because the DfSearch facade is cached — a test_ready_per_fault would fail today for every corrupt-table kind, which is the finding the suite would pin. Checked the monkeypatch target: dataset.py:326 calls query.limit(...).to_list(); lancedb/query.py:1132 to_list -> self.to_arrow(), and LanceFtsQueryBuilder overrides to_arrow at query.py:2087 (to_arrow(timeout: Optional[timedelta])), so patching LanceQueryBuilder.to_arrow (query.py:1088) never runs for an FTS search. Effort M is within range for ~12 tests + conftest plugin + a doc generator; existing conftests (lib tests/conftest.py:33-45, mcp tests/conftest.py:22-26) already ingest df_sample into tmp_path, so broken_table is a thin wrapper over them. Root euid caveat confirmed in the spike (euid 0).

*Corrections:* (1) stalled_query must patch lancedb.query.LanceFtsQueryBuilder.to_arrow (query.py:2087) or LanceQueryBuilder.to_list (query.py:1132, not overridden), not LanceQueryBuilder.to_arrow — the FTS builder overrides it and the base-class patch is a silent no-op. (2) observability.md has no failure matrix today (only the error.type sentence at lines 94-96); the matrix is new content to be generated, not an existing table to keep in sync. (3) index-truncated and index-missing yield the identical signature (RuntimeError 'Not found .../tokens.lance'), so the five recipes give four distinguishable strings. (4) The verbatim strings embed absolute paths and a Rust source location with crate version ('lance-io-11.0.0/src/object_store.rs:1143:12'); a verbatim assertion fails on every lance bump for cosmetic reasons — assert exception type plus the stable substring ('Not found', 'failed to fill whole buffer', 'was not found', 'File too large'). (5) test_server.py:96-100 already hand-writes exactly the string the real recipe produces, so the first PR can replace that fake with broken_table('index-missing') rather than add beside it.

*Sharpened:* Ship packages/kansallisarkisto-lib/tests/faults.py as a pytest plugin registered in both conftests: broken_table(kind) for the five recipes (built from the existing df_fixture ingest into tmp_path), stalled_query(seconds) patching LanceFtsQueryBuilder.to_arrow, disk_full via RLIMIT_FSIZE subprocess, refusing_collector on 127.0.0.1:1, stdio_server(tool). One parametrised characterisation test per surface asserting (exception type, stable substring, tool-text prefix, /ready status, error.type) — which will immediately expose that /ready returns 200 for every post-boot corruption because of the cached facade (tools.py:421-436), turning that prose claim into a tracked defect. Emit the failure matrix into observability.md from the parametrisation ids with a test that the page matches. Budget: M (2-4 days), ~14 tests, each <2 s except the stdio one (~6 s, or 1 s with a shorter sleep).

---

